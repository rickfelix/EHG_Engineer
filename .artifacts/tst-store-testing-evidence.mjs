import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';
import { createClient } from '@supabase/supabase-js';
import dotenv from 'dotenv';
dotenv.config();

const SD = 'SD-LEO-INFRA-SUMMARY-COLUMNS-DERIVED-001';
const db = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL || process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

const findings = [
"BLOCKER-1 (FR-3/FR-4, fatal): the proposed predicate derives the summary from a field DOCUMENTED IN ITS OWN WRITER as incapable of carrying it, and is wrong in BOTH directions. lib/uat/result-recorder.js:613 writes control_pack_failures = (controlPackFailures.length > 0 ? controlPackFailures : null) and the comment immediately below (SECURITY finding NEW-4) states verbatim: 'control_pack_failures=null is ambiguous between \"evidence was supplied and every check passed\" and \"no evidence was ever supplied\" -- lib/eva/uat-robustness-gate.js needs to tell those apart'. FALSE-NEGATIVE: a clean 4-of-4 fully-evaluated run has failures=jsonb null, so the FR-3 trigger overwrites control_pack_evaluated to FALSE and uat-robustness-gate.js:141 BLOCKS a legitimately passing run -- the jsonb-null 'fix' the PRD headlines is precisely what creates this. FALSE-POSITIVE: a 1-of-4 partial with a non-empty failures array derives TRUE, which is byte-for-byte the bug QF-20260830-666 fixed (lib/eva/uat-control-pack.js:191 names the defective shape explicitly: 'result-recorder.js prior control_pack_evaluated: Boolean(controlPackEvidence) went true on ANY evidence object regardless of how many controls it actually covered'). FR-3 re-implements that exact bug in SQL. FR-4's claim that the trigger keeps the reader's boolean-keyed logic correct is therefore FALSE.",
"BLOCKER-2 (FR-3, the '23 of 24 drifted' figure is a measurement artifact; the backfill would CORRUPT 23 correct rows): I reproduced the 23/24 count exactly, but the label is wrong. Measured live: all 23 rows carry control_pack_status = {fence_two_sidedness: not_attempted, canary_mutation_control: not_attempted, live_deployment_binding: not_attempted, minimum_assertion_manifest: evaluated} -- 3 of 4 required controls never ran, so control_pack_evaluated=false is CORRECT under QF-20260830-666 ('ALL REQUIRED controls evaluated', Solomon ruling 711cce6d; result-recorder.js:593 comment + :619 writes controlPackStatus.allRequiredEvaluated). These are not drifted rows; they are rows where the PROPOSED NEW predicate disagrees with the EXISTING CORRECT value. FR-3 AC-4's same-migration backfill would overwrite 23 rows of correct history with a weaker predicate. There is in fact NO summary/detail drift in this instance: a single writer sets control_pack_failures, control_pack_evaluated and control_pack_status atomically in one .update() (result-recorder.js:606-622), so they cannot disagree. If a trigger is wanted at all, the canonical source is control_pack_status (no value equal to 'not_attempted'), never control_pack_failures.",
"BLOCKER-3 (FR-3, will not compile as written): control_pack_failures is NOT a column on uat_test_runs. Measured columns: id, run_id, suite_id, sd_id, prd_id, environment, browser, device_type, viewport_width, viewport_height, status, total_tests, passed_tests, failed_tests, skipped_tests, pass_rate, started_at, completed_at, duration_ms, triggered_by, trigger_source, machine_info, test_config, created_at, metadata. An information_schema scan for column_name LIKE '%control%' on this table returns only metadata (jsonb). The PRD's literal predicate is a bare column reference and raises 'column control_pack_failures does not exist' inside the trigger body; it must be NEW.metadata->'control_pack_failures'. Consequence the PRD misses: with no such column there is no WHEN (OLD.col IS DISTINCT FROM NEW.col) clause available, so the trigger must fire on EVERY insert/update of the whole metadata blob. system_architecture.data_flow ('A write to control_pack_failures ... fires its BEFORE trigger') is wrong at the mechanism level.",
"BLOCKER-4 (FR-5, dead by construction / zero-yield): venture_gate_last_verdict does not exist as a column ANYWHERE in the schema -- information_schema.columns scanned for column_name ILIKE '%venture_gate%' OR ILIKE '%last_verdict%' returned ZERO rows. It exists only as a metadata KEY, and only on SD-LEO-INFRA-BIND-OBSERVE-ONLY-001 (value NOT_MET, jsonb type string). It is ABSENT from the one row FR-5's allowlist permits the trigger to touch, SD-LEO-INFRA-VENTURE-DEMAND-DISTRIBUTION-001-E. The trigger is scoped exclusively to the row lacking the source and forbidden from the only row that has it: it derives null on its sole legal target, permanently. TS-3 would PASS (it asserts only non-firing on the 2 unrelated SDs) while the requirement yields nothing -- a test that structurally cannot detect the defect it is paired with.",
"Q1 ANSWER (VERIFIED LIVE -- syntactically and semantically correct, 2 caveats): the predicate (v IS NOT NULL AND jsonb_typeof(v) != 'null') is valid Postgres and correctly trichotomizes. Measured truth table against the live DB: SQL NULL (absent key via ->) => is_not_null=false, jsonb_typeof=NULL, predicate=FALSE; jsonb null literal => is_not_null=true, jsonb_typeof='null', predicate=FALSE; populated array => predicate=TRUE. CAVEAT A: EMPTY array [] => jsonb_typeof='array' => predicate TRUE (may or may not be intended; no TS covers it). CAVEAT B: the jsonb STRING \"null\" => jsonb_typeof='string' => predicate TRUE. The predicate is only correct once the operand is written as NEW.metadata->'control_pack_failures' (see BLOCKER-3); note that -> returns SQL NULL for an absent key, which is what makes the IS NOT NULL half meaningful at all. Verdict: sound SQL aimed at the wrong source field (BLOCKER-1/2).",
"Q2 ANSWER (VERIFIED LIVE -- YES, with 2 caveats): sd_key is a real text NOT NULL column on strategic_directives_v2, so NEW.sd_key is reliably populated inside a BEFORE INSERT OR UPDATE trigger body and an allowlist gate on it is mechanically sound. CAVEAT A: sd_key is NOT the primary key -- id (character varying, NOT NULL) is; if any path ever UPDATEs sd_key itself, gating on NEW.sd_key versus OLD.sd_key becomes a real distinction the PRD does not address. CAVEAT B: scale. The table holds 6,270 rows and the trigger fires on ALL sd_v2 UPDATE traffic, so the allowlist guard must be the FIRST statement with an early RETURN NEW, not a filter applied after computation.",
"MEDIUM-5 (FR-5, trigger-count undercount ~7x): the PRD says 'the >=8 existing triggers on strategic_directives_v2 (alphabetical order)'. Measured live: 57 non-internal triggers, 37 of them BEFORE. Several BEFORE UPDATE triggers mutate metadata, and one (trg_aaa_sync_type_change_reason) is deliberately aaa-prefixed to win alphabetical ordering -- so name-ordering against 37 BEFORE peers is materially harder than the PRD's reasoning assumes. No test scenario asserts ordering against ANY of them; TS-3 tests allowlist scoping only. Separately: uat_test_runs currently has ZERO triggers (measured), so FR-3's would be the first on that table and TS-6's DISABLE TRIGGER scenario has no existing-trigger precedent there.",
"Q3 ANSWER (a convention exists; the PRD names NO test sibling for either migration -- the largest process gap): database/chairman-gated/ holds 160 files with two established conventions. (a) *_dry_run.mjs (11 files, chmod +x) is the newer dominant pattern: readFileSync the REAL UP .sql and DOWN .sql, execute both inside ONE transaction that ALWAYS ROLLBACKs, via createDatabaseClient from lib/supabase-connection.js with isMainModule from lib/utils/is-main-module.js; SAVEPOINT-guarded helpers for expected-rejection assertions (an error otherwise poisons the outer transaction); session_replication_role='replica' to bypass unrelated triggers during fixture setup, re-armed to 'origin' before the real assertions so the trigger under test runs at full strength. Best reference: database/chairman-gated/20260912_venture_channel_publish_ledger_outbound_gate_trigger_dry_run.mjs. (b) *_acceptance.mjs (11 files) for post-apply verification. (c) For staged-not-applied DDL, an ephemeral vanilla-PG test under tests/ddl/ (precedent: tests/ddl/strategic-directives-canonical-writer-choke-ddl.db.test.js, cited by the 20260824 choke migration's own header). PLAN should require a *_dry_run.mjs sibling per migration: FR-5 is explicitly staged-not-applied, so a dry-run is the ONLY way TS-3 can execute at all.",
"Q4 GAP-1 (no empty-array case): control_pack_failures=[] derives TRUE under the proposed predicate (verified live). No TS covers it, and it is the case most likely to flip a GREEN run past uat-robustness-gate.js.",
"Q4 GAP-2 (no key-absent case): 2 of 26 live rows (8e54bfc7, bda551a1) carry NEITHER control_pack_failures NOR control_pack_evaluated -- they predate the control pack. The trigger would stamp control_pack_evaluated:false onto them, asserting 'we evaluated and it did not run' about runs from before the feature existed. FR-3 AC-4 says '23 of 24' and silently omits what happens to these 2. No TS covers 'metadata has no control_pack_failures key at all'.",
"Q4 GAP-3 (no UPDATE-path test): TS-1 and TS-2 are both INSERTs, but the trigger is BEFORE INSERT OR UPDATE and the real production write path is an UPDATE (result-recorder.js completeSession does .update().eq('id', testRunId)). An UPDATE touching an unrelated metadata key must not clobber a correctly-set value. Completely untested.",
"Q4 GAP-4 (no direct-write-conflict test): TS-1 requires evaluated be set 'by the trigger, never by the INSERT statement itself', yet no scenario covers the conflict case -- an INSERT/UPDATE that explicitly supplies control_pack_evaluated:true while failures is jsonb null. A BEFORE trigger that unconditionally overwrites is correct; one that only fills when the key is absent is not. The PRD never states which, and no TS pins it.",
"Q4 GAP-5 (no DOWN/rollback test despite TR-2 mandating it): TR-2 requires every DOWN sibling to capture pre-derivation values before DROP TRIGGER, and FR-3/FR-5 each carry a DOWN acceptance criterion -- yet TS-1 through TS-7 contains ZERO rollback scenario. The dry-run convention runs UP and DOWN in one rolled-back transaction, so this is cheap to add; the omission is conspicuous.",
"Q4 GAP-6 (no lock_timeout assertion): TR-1 and FR-5 AC-3 both mandate SET lock_timeout='3s' per the 20260824 choke precedent (header lines 21-35, which record the live measurement that service_role and postgres have NO lock_timeout configured, so an unguarded CREATE TRIGGER queues indefinitely behind existing traffic rather than failing fast). No TS verifies the header actually contains it. A one-line string assertion inside the dry-run closes this.",
"Q4 GAP-7 (TS-6 is not runnable as specified): it requires ALTER TABLE uat_test_runs DISABLE TRIGGER plus an anon-key write path. DISABLE TRIGGER takes its own ACCESS EXCLUSIVE lock and the PRD's own TR-1 lock guidance is not applied to it; under the dry-run convention it must sit inside the rolled-back transaction. As written, TS-6 reads as a production mutation.",
"Q4 GAP-8 (TS-7 is a grep, not a test): 'no file targets sms_outbound_obligations' is trivially satisfiable and cannot detect the hazard FR-6 names. It should instead assert against pg_trigger that sms_outbound_obligations carries no trigger -- a liveness check, which is exactly the logic TR-3 already argues for elsewhere in this same PRD.",
"Q4 GAP-9 (lint predicate (c) has no test at all): FR-1 defines 3 lint predicates. TS-4 covers (b) jsonb-key, TS-5 covers (a) boolean/GENERATED. Predicate (c), the cross-table-derived status/text heuristic that is 'flagged not auto-failed', has ZERO test scenarios -- so the flagging path can ship dead and nothing would reveal it.",
"FR-6 VERIFIED CORRECT (the strongest requirement in this PRD): checked against live lib/chairman/sms-outbound-worker.js (912 LOC). The claim-serialization idiom is exactly as FR-6 describes (UPDATE ... SET status='sending', claimed_at=now WHERE id=? AND status='owed' AND claimed_at IS NULL). Delivery truth is split by design: the worker stamps 'sent' on a Twilio 201-ACCEPT and NEVER 'delivered'; delivered_at is set only by a signature-valid MessageStatus callback in api/webhooks/twilio-sms.js. Three independent reapers already rewrite status (sent-no-callback -> owed_escalate, sending-crash -> re-arm to owed, stale-void -> canceled), so a receipt-derived BEFORE trigger would race all three. sms_status_staging confirmed 0 rows. Live sms_outbound_obligations: 1277 delivered / 26 sent / 14 canceled / 10 failed. The descope reasoning holds against the code as it actually exists today.",
"POSITIVE: FR-1/FR-2's widening of the lint to 3 predicates is well-reasoned and the corrected 7-column baseline is the right call. FR-2's explicit statement that exhaustive jsonb/cross-table enumeration is not mechanically bounded is honest scoping, not hand-waving. TR-3's pairing of 'zero rows disagree' with a pg_trigger liveness assertion is the correct instinct and should be extended to TS-7 (see GAP-8)."
];

const summary = "FAIL -- prospective PRD review, 4 blockers. The PRD's headline fix (FR-3's jsonb-null predicate correction) is sound SQL pointed at the wrong source field: control_pack_failures is documented IN ITS OWN WRITER (lib/uat/result-recorder.js:613-617) as unable to distinguish 'ran clean' from 'never ran', so the proposed trigger is wrong in BOTH directions -- it would BLOCK a clean 4-of-4 run (failures=jsonb null => evaluated=false) and PASS a 1-of-4 partial (non-empty failures => evaluated=true), the latter being verbatim the bug QF-20260830-666 fixed. The '23 of 24 drifted rows' figure is a measurement artifact: all 23 have 3-of-4 controls not_attempted, so evaluated=false is CORRECT, and FR-3's same-migration backfill would corrupt them. The predicate also does not compile as written (control_pack_failures is not a column on uat_test_runs; only metadata jsonb exists), and FR-5 is dead by construction (venture_gate_last_verdict exists nowhere as a column, and as a metadata key it is absent from the one allowlisted row while present only on a row the allowlist excludes). Q1 verified live: predicate correct, 2 caveats (empty array and jsonb string 'null' both derive true). Q2 verified live: sd_key is text NOT NULL, NEW.sd_key is a sound BEFORE-trigger gate; table has 6270 rows and 57 triggers / 37 BEFORE, not the >=8 the PRD assumes. Q3: a *_dry_run.mjs convention exists (11 files, UP+DOWN in one rolled-back txn) and the PRD names no test sibling for either migration. Q4: 9 concrete test-scenario gaps. FR-6's SMS descope is verified correct against live code and is the strongest requirement in the PRD.";

const results = {
  verdict: 'FAIL',
  confidence: 92,
  summary,
  findings,
  critical_issues: [
    'FR-3 trigger predicate re-implements the QF-20260830-666 bug AND adds a false-negative that would block clean fully-evaluated runs',
    'FR-3 same-migration backfill would overwrite 23 rows of CORRECT control_pack_evaluated history',
    'FR-3 predicate references control_pack_failures as a column; no such column exists on uat_test_runs (only metadata jsonb)',
    'FR-5 derives from venture_gate_last_verdict, which exists nowhere as a column and is absent from the single allowlisted row -- zero-yield by construction'
  ],
  metadata: {
    review_mode: 'prospective-prd-review',
    prd_id: 'PRD-SD-LEO-INFRA-SUMMARY-COLUMNS-DERIVED-001',
    test_scenarios_reviewed: 7,
    test_gaps_found: 9,
    blockers: 4,
    live_measurements: {
      uat_test_runs_rowcount: 26,
      uat_test_runs_triggers: 0,
      control_pack_failures_is_a_column: false,
      rows_where_new_predicate_disagrees: 23,
      rows_with_neither_key: 2,
      green_rows: 1,
      sd_v2_rowcount: 6270,
      sd_v2_triggers_total: 57,
      sd_v2_triggers_before: 37,
      venture_gate_last_verdict_column_exists: false,
      sms_status_staging_rowcount: 0,
      sms_outbound_obligations: { delivered: 1277, sent: 26, canceled: 14, failed: 10 }
    },
    recommended_derivation_source: "control_pack_status (assert no value equals 'not_attempted'), i.e. result-recorder.js buildControlPackStatus().allRequiredEvaluated -- never control_pack_failures",
    test_convention_pointer: 'database/chairman-gated/20260912_venture_channel_publish_ledger_outbound_gate_trigger_dry_run.mjs (*_dry_run.mjs pattern: UP+DOWN in one always-ROLLBACK txn via lib/supabase-connection.js createDatabaseClient)'
  }
};

const resolution = await resolveSubAgentRepo({ sdId: SD, targetApplication: null, subAgentCode: 'TESTING', supabase: db });
applySubAgentRepoVerdict(results, resolution, { skipVerdictAdjust: true });
const stored = await storeSubAgentResults('TESTING', SD, null, results, { phase: 'PLAN' });
console.log('STORED:', JSON.stringify(stored)?.slice(0, 500));
process.exit(0);
