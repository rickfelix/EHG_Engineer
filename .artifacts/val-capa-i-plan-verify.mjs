import dotenv from 'dotenv';
dotenv.config();
import { storeSubAgentResults, getSupabaseClient } from '../lib/sub-agent-executor/index.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';

const SD = 'SD-LEO-INFRA-VENTURE-QUALITY-CAPA-001-I';
const supabase = await getSupabaseClient();
const { data: subAgent } = await supabase.from('leo_sub_agents').select('*').eq('code', 'VALIDATION').maybeSingle();

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence_score: 91,
  execution_time_ms: 0,
  summary: 'PLAN-phase VERIFY of CAPA-001-I at HEAD b41994adeda. FR-1..FR-5 and FR-6 factory-side reader are genuinely implemented, match their CORRECTED FR bodies, follow this codebase conventions (createLogger in 5 of 7 touched lib/eva files, bounded selects clean, dedicated table not a new artifact_type, venture_resources as PRIMARY repo source per TR-4), and are covered by real behavioural tests including the specific negative cases the PRD names. Test suite re-run by me: 616 files passed, 7855 tests passed, 34 skipped; the single failing suite (path-integrity-flags-live-defaults.db.test.js) is PRE-EXISTING on origin/main, untouched by this branch, and fails only on a db-tier environment gate. TWO items must close before LEAD sign-off: (V-1) the FR-6 venture-side deliverable stack-scan.yml was NEVER AUTHORED anywhere in rickfelix/altifyai, so the shipped reader is dead by construction for the only venture it targets; (V-2) eva-logger-required-lint exits 1 on this branch and its CI workflow triggers on exactly the paths this changeset touches, so the PR will fail that check. Plus (V-4) the PRD carries five stale acceptance texts the (correct) implementation deliberately does not meet.',
  critical_issues: [
    'V-1 FR-6 AC-1 UNMET, READER DEAD BY CONSTRUCTION: .github/workflows/stack-scan.yml does not exist in rickfelix/altifyai. Measured three independent ways: (a) the venture_resources local_directory path C:\\Users\\rickf\\Projects\\_EHG\\altifyai\\.github\\workflows\\ contains only ci.yml; (b) gh api repos/rickfelix/altifyai/contents/.github/workflows on origin/main returns 13 workflows (ci, contamination-scan, deploy, and 10 one-shot-*) and NONE is stack-scan.yml; (c) gh api search/code?q=repo:rickfelix/altifyai+filename:stack-scan.yml returns total_count 0, across all 20+ branches. FR-6 has three limbs (a) author stack-scan.yml, (b) resolve the repo via venture_resources, (c) build a new actively-wired reader. Limbs (b) and (c) shipped and are correct; limb (a) did not ship. Consequence: for AltifyAI, readStackScanConclusion (lib/eva/bridge/stack-scan-reader.js:99) will always return {available:false, reason:"no_completed_stack_scan_run"}, so packet.stackScan can never carry a real conclusion and FR-6 AC-5 (the factory records or displays the stack-scan conclusion) has nothing to record. AC-7 (EXEC re-fetches origin/main before deciding what stack-scan.yml adds) also has no evidence of having been performed. This is the zero-yield-by-construction class: the wiring reads as complete while the requirement is dead.',
    'V-2 CI LINT FAILURE INTRODUCED BY THIS CHANGESET: node scripts/lint/eva-logger-required-lint.mjs exits 1 (verified exit code, mode=diff scanned=7) naming lib/eva/chairman-product-review.js and lib/eva/utils/validate-venture-default-capabilities.js. Both are MODIFIED by this changeset and both have ZERO createLogger references (grep -c createLogger: 0 and 0; the other 5 touched lib/eva files have 2 each). The lint is diff-scoped against the merge-base with main and enforces on MODIFIED files, not just new ones, so modifying these two previously-backlogged files pulls them into enforcement. .github/workflows/eva-logger-required-lint.yml triggers on pull_request paths lib/eva/**/*.js (this changeset qualifies) and runs the identical command, so the PR will fail this required check. TR-2 named this exact risk verbatim: "avoids a foreseeable CI failure at EXEC". Remedy is small: import and call createLogger in both, or add a documented // eva-logger-lint-ignore: <reason> pragma.'
  ],
  warnings: [
    { severity: 'MEDIUM', issue: 'V-3 UNBOUNDED EXTERNAL NETWORK CALL INSIDE THE S24 CHOKE-POINT. generateReviewPacket now awaits readStackScanConclusion in a bare Promise.all (lib/eva/chairman-product-review.js:328-332). That call issues fetch() to api.github.com with no AbortSignal and no timeout (grep for AbortSignal|timeout|signal in lib/eva/bridge/stack-scan-reader.js: NONE). The same file already carries an explicit binding condition from SD-LEO-INFRA-COMPETITIVE-BASELINES-RECURRING-001 FR-6 (Solomon condition) at :27-29 with COMPETITIVE_BASELINE_TIMEOUT_MS=5000, written because "the competitive-baseline packet read must never hang the S24 choke-point -- Supabase JS has no default request timeout, so a bare try/catch cannot bound a stall". The new read is a third-party internet call, strictly MORE stall-prone than the Supabase call that budget was written for, and Node undici default headersTimeout is 300s. generateReviewPacket runs under requestProductReview, called from post-build-convergence-gate.js:150 and artifact-persistence-service.js:1012, and per chairman-decision-watcher.js:508 on every blocked _advanceStage tick.', recommendation: 'Wrap readStackScanConclusion in the same Promise.race time budget already used for competitiveBaseline, or pass signal: AbortSignal.timeout(5000) into the fetch. This is the one convention in the touched file that the new code does not follow.' },
    { severity: 'MEDIUM', issue: 'V-4 PRD CARRIES FIVE STALE ACCEPTANCE TEXTS THE IMPLEMENTATION DELIBERATELY DOES NOT MEET. (a) FR-5 AC-1 "GUIDED_TOUR_STOPS has zero entries with artifactType: null after implementation" is unmet and unmeetable: signup and core_action both still carry artifactType: null (chairman-product-review.js:79-80). The implementation instead binds signup via screenId + SIGNUP_SCREEN_NAME_RE and drops core_action binding entirely -- the CORRECT call per the EXEC TESTING finding (no venture-independent structural signal exists for a core-action screen), but AC-1 was never amended. (b) FR-5 AC-3 "shows a real, non-empty artifact reference for all 6 tour stops" is likewise unmet: core_action deliberately renders the honest "Not yet documented" placeholder. (c) Top-level acceptance_criteria[3] repeats the same stale zero-null-tour-stops claim. (d) Top-level acceptance_criteria[4] says FR-6 conclusion is "fetchable via the factory existing workflow-agnostic reader with zero factory-side code changes" -- directly contradicted by FR-6 own corrected body and AC-2, which mandate a NEW reader because the existing one is dead code; the implementation correctly follows the FR body. (e) TS-7 ("resolves the repo from metadata.synthetic_actor.github_repo") and risks[3].mitigation ("EXEC must read metadata.synthetic_actor.github_repo, never repo_url") are both contradicted by FR-6 AC-2 and TR-4, which make venture_resources PRIMARY; the code correctly follows TR-4.', recommendation: 'Amend the PRD before LEAD sign-off so LEAD does not sign against criteria the correct implementation deliberately does not meet. In every one of the five cases the CODE is right and the PRD TEXT is stale -- do not change code to satisfy them.' },
    { severity: 'LOW', issue: 'V-5 JSDOC MISPLACEMENT. In lib/eva/utils/validate-venture-default-capabilities.js the new validateOverrideReason JSDoc block was inserted BETWEEN the pre-existing JSDoc for validateVentureDefaultCapabilities and that function declaration (~:44-64). The file now has two consecutive JSDoc blocks: the first (documenting parsedSprintPlan and opts.defaultCapabilitiesOverride) is orphaned and attaches to nothing, and validateVentureDefaultCapabilities is left undocumented.', recommendation: 'Move validateOverrideReason and its doc block above the validateVentureDefaultCapabilities JSDoc.' },
    { severity: 'LOW', issue: 'V-6 THE THREE NEW SECTIONS ARE DROPPED BY THE HUMAN-READABLE RENDERER. scripts/chairman-product-review-packet.js non---json output enumerates only access, guidedTour and surfacesInventory; reconciliationTable, registryScorecard and stackScan are not printed. They ARE present under --json and ARE persisted into chairman_decisions.brief_data via requestProductReview (briefData spreads the whole packet), so FR-4 AC-3 and FR-5 AC-2 (distinct named top-level keys in generateReviewPacket output) are genuinely SATISFIED and FR-6 AC-5 records limb is satisfied -- but the chairman-facing CLI print does not display them. Cosmetic companion: the PRD names the keys reconciliation_table and registry_scorecard (snake_case); the code uses camelCase, consistent with the packet existing keys guidedTour/surfacesInventory/verdictTable/competitiveBaseline. The code convention choice is right; the AC text does not match literally.', recommendation: 'Add the three sections to the human-readable branch of scripts/chairman-product-review-packet.js, or record explicitly that --json / brief_data is the intended consumption surface.' },
    { severity: 'LOW', issue: 'V-7 THE PACKET RUNS THE CAPABILITY CHECKS REGARDLESS OF THE FLAG. chairman-product-review.js calls precheckCapabilities and readCapabilityOverrides unconditionally, independent of LEO_S24_CAPABILITY_CHECKLIST_REQUIRED, so 7 capability verifications (each hitting the DB) run on every packet generation even with the flag OFF -- and readCapabilityOverrides will log a warn on every call until the migration is applied. Defensible (packet display is advisory, not gating, so it is not a rollout-safety hole), but it is a per-tick cost the flag otherwise avoids.', recommendation: 'No action required for this SD; note it if packet latency becomes a concern.' }
  ],
  recommendations: [
    'CONDITIONAL_PASS. Close V-1 and V-2 before EXEC-TO-PLAN is accepted. V-1 is the substantive one: author stack-scan.yml in rickfelix/altifyai (the local clone is stale -- re-fetch origin/main first, which is AC-7 own instruction), or formally rescope FR-6 to reader-only and move limb (a) to a tracked follow-up so the SD does not close claiming a capability that cannot fire.',
    'V-2 is a small fix with a hard CI consequence: add createLogger to lib/eva/chairman-product-review.js and lib/eva/utils/validate-venture-default-capabilities.js, or a documented lint pragma. Note that chairman-product-review.js currently threads logger = console through to the new readers, which overrides their own module loggers -- adding createLogger there fixes both the lint and that inconsistency.',
    'Amend the five stale PRD texts in V-4 rather than changing code. In all five the shipped behaviour is the better one and was chosen on measured evidence (17 ventures sampled for the screen-1 assumption; venture_resources vs hand-writable metadata for the repo pointer).',
    'The already-known migration-not-applied blocker is unchanged and correctly tracked in the PRD: FR-2 AC-1/AC-2, FR-3 AC-3 live limb, and FR-4 AC-2 cannot be verified against real AltifyAI data until database/migrations/20260913_venture_screen_disposition_reconciliation.sql is applied. I confirmed the migration itself is sound: all three tables carry UNIQUE (venture_id, screen_id) / (venture_id, capability_id), so buildScreenReconciliation onConflict: venture_id,screen_id upsert will not hit Postgres 42P10 at first real run.'
  ],
  metadata: {
    phase: 'PLAN',
    sd_key: SD,
    validation_type: 'PLAN_VERIFICATION_gate4',
    head_sha: 'b41994adeda',
    branch: 'feat/SD-LEO-INFRA-VENTURE-QUALITY-CAPA-001-I',
    test_run_measured_by_me: {
      command: 'npx vitest run tests/unit/eva/',
      test_files: '616 passed, 1 failed, 6 skipped (623)',
      tests: '7855 passed, 34 skipped (7889), 0 test-level failures',
      duration_s: 34.96,
      failing_suite: 'tests/unit/eva/path-integrity-flags-live-defaults.db.test.js',
      failing_suite_is_preexisting: 'CONFIRMED: exists on origin/main (git ls-tree), NOT in git diff --name-only origin/main...HEAD. Fails with DB_TIER_BLOCKED no_designated_target because VITEST_DB_ALLOW_REF is unset -- a runtime environment gate on the db tier, not a code failure, and unrelated to this SD.'
    },
    fr_by_fr_verification: {
      'FR-1': 'MATCHES. legal-doc-producer.js:88 companyDomain = extractBareDomain(company?.website) || extractBareDomain(venture.metadata?.live_url); metadata added to the ventures select at :65; typeof-string guard at :45 covers BOTH call sites. AC-1 covered by 4 real tests incl. company.website precedence and non-string JSONB. AC-3 regression guard is a source-text scan that strips comment lines before counting import/call sites -- valid, not tautological. AC-2/AC-4 need a real prod run (tracked).',
      'FR-2': 'MATCHES. Pure verifier, never a classifier -- checkScreenDispositions returns ok:false on BOTH read errors (fail-closed), names undisposedScreenIds specifically (AC-3), 3 classes enforced, recordScreenDisposition trims and rejects empty reason and forces D2_RETIRE to COMPLETE. Durable table (AC-4). AC-1/AC-2 need the applied migration.',
      'FR-3': 'MATCHES. CAPABILITY_IDS length verified = 7 at runtime (feedback-widget, error-capture-middleware, cost-instrumentation, telemetry-analytics, calm-decision-card, health-uptime-probe, operating-model-grounding). 7 distinct capability:<id> REQUIRED rows (AC-1), override only when !SIGNAL_BACKED_CAPABILITY_IDS.has(id) (AC-2/AC-5), shared validateOverrideReason not a divergent copy (AC-4). Default-OFF flag mirrors the LEO_S21_GROWTH_PLAYBOOK_REQUIRED precedent via a generalised readFeatureFlag. Tests cover both flag states incl. the byte-identical-baseline assertion.',
      'FR-4': 'MATCHES. Dedicated table, no new venture_artifacts.artifact_type -- artifact-type-db-parity.test.js unmodified and passing (AC-4). One row per screen with every named field (AC-1). Included verbatim in the packet as reconciliationTable, not a re-derived summary (AC-3). built_surface_artifact_type is sourced only from a recorded ENTRY_POINT disposition evidence_ref and is null otherwise -- the module says so and never guesses. AC-2 needs live data.',
      'FR-5': 'PARTIALLY MATCHES -- see V-4(a)/(b). The three new packet sections are present as distinct top-level keys (AC-2 satisfied). AC-1 and AC-3 are NOT met and were deliberately superseded by the EXEC TESTING fix (1f80584f7bb); the PRD text was never amended.',
      'FR-6': 'READER MATCHES, WORKFLOW MISSING -- see V-1. venture_resources PRIMARY with .eq(status,active).order(created_at desc).limit(1).maybeSingle() per TR-4 and SEC-2 (AC-2 source clause). Explicit not-configured result, never a throw (AC-3). URL is workflow-file-specific (/actions/workflows/stack-scan.yml/runs?branch=main&status=completed&per_page=1) so it can never fall back to any-recent-successful-run, with a test where a failing stack-scan coexists with a passing unrelated run (AC-6). Real production caller via generateReviewPacket (AC-2 caller clause). AC-1/AC-5/AC-7 unmet.'
    },
    conventions_check: {
      logger_TR2: 'FAIL on 2 of 7 touched lib/eva files -- see V-2. The 5 NEW modules all use createLogger correctly with a module-scoped default and an injectable logger param.',
      bounded_selects_TR3: 'PASS. node scripts/lint/count-truncation-diff-lint.mjs: 0 new needs-review select() sites across 7 changed files. Manually confirmed every new read is bounded: limit(200) dispositions/reconciliation, limit(500) uat_test_results, limit(50) overrides, limit(1).maybeSingle() venture_resources and uat_test_runs, maybeSingle() artifacts and feature flags.',
      supabase_injection: 'PASS. Every new function takes supabase as an injected param; no module-level client is constructed.',
      error_handling: 'PASS with one nuance. The two GATE-facing readers fail CLOSED (checkScreenDispositions returns ok:false on read error; precheckCapabilities degrades a capability to wired:false). The two DISPLAY-facing readers fail SOFT (readScreenReconciliation returns [], readCapabilityOverrides returns an empty Map) -- correct for a packet section that must never block the choke-point, and an empty overrides Map makes capabilities read fail, which is still the safe direction.',
      exports_vs_imports: 'PASS. Verified every symbol chairman-product-review.js imports exists with the right shape: CAPABILITY_IDS, SIGNAL_BACKED_CAPABILITY_IDS, precheckCapabilities exported from stage-23-launch-readiness.js:519-524; readCapabilityOverrides from validate-venture-default-capabilities.js; readScreenReconciliation from screen-reconciliation-builder.js; readStackScanConclusion from bridge/stack-scan-reader.js. No circular import: stage-23-launch-readiness.js imports only launch-mode, external-observation, mode-evidence, validate-venture-default-capabilities, venture-default-capabilities config and logger -- none reaches back to chairman-product-review.js.',
      migration_soundness: 'PASS. All 3 tables carry the UNIQUE constraint the code upserts/looks up on, RLS enabled with the venture-scoped SELECT + service_role FOR ALL policy pair, CHECK constraints on both enum-ish columns.'
    },
    stack_scan_yml_measurement: {
      local_directory: 'C:\\Users\\rickf\\Projects\\_EHG\\altifyai\\.github\\workflows\\ -> only ci.yml present',
      origin_main_workflows: ['ci.yml', 'contamination-scan.yml', 'deploy.yml', 'one-shot-ai-probe.yml', 'one-shot-d1-diagnose-events-handler.yml', 'one-shot-d1-diagnose-usage-panel.yml', 'one-shot-d1-diagnose.yml', 'one-shot-d1-migrate.yml', 'one-shot-d1-provision.yml', 'one-shot-d1-query.yml', 'one-shot-observability-query.yml', 'one-shot-secret-provision.yml', 'one-shot-wrangler-tail.yml'],
      github_code_search_total_count: 0,
      conclusion: 'stack-scan.yml exists nowhere in rickfelix/altifyai -- not on main, not on any branch, not in any open or merged PR.'
    },
    not_relitigated: 'Per the task brief I did not re-open the 3 EXEC-phase SECURITY findings (fixed in 44501dc4901 + 154180bd35f) or the EXEC-phase TESTING guided-tour finding (fixed in 1f80584f7bb). I read the fixes to confirm they are present in the code I validated, and found nothing new in them. V-3 is a DIFFERENT class (availability/stall, not injection) and was not covered by either prior pass.',
    measured_at: new Date().toISOString(),
    measured_by: 'validation sub-agent (Opus 5), executed against the worktree at HEAD b41994adeda -- every claim here re-measured by execution (test run, two lints, gh api, runtime capability count), none taken from prior reports'
  }
};

const resolution = await resolveSubAgentRepo({ subAgentCode: 'VALIDATION', sdId: SD, targetApplication: 'EHG_Engineer', supabase });
applySubAgentRepoVerdict(results, resolution);

const stored = await storeSubAgentResults('VALIDATION', SD, subAgent, results, { phase: 'PLAN', sdKey: SD });
console.log('STORED id=', stored?.id, 'verdict=', stored?.verdict, 'phase=', stored?.phase, 'sd_id=', stored?.sd_id);
