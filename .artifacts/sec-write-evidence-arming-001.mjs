#!/usr/bin/env node
/** SECURITY (Chief Security Architect) EXEC verdict for SD-LEO-FIX-CHAIRMAN-APPROVED-ARMING-001. */
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';
import { getSupabaseClient } from '../lib/sub-agent-executor/supabase-client.js';

const SD_ID = '6b7de42a-00ff-4d09-9941-00de496d2f95';
const SD_KEY = 'SD-LEO-FIX-CHAIRMAN-APPROVED-ARMING-001';

const findings = [
  { id: 'S1-approval-gate-not-weakened-by-diff', severity: 'INFO',
    summary: 'Q1 ANSWERED: the approval-requirement gate is UNCHANGED by this diff and nothing in the diff can be used to bypass it. transitionLifecycleState\u2019s block (registry.js:375-393: newState===enabled && risk_tier in {medium,high} -> read leo_feature_flag_approvals status=approved and require approvals_received >= APPROVAL_REQUIREMENTS[tier].enable) is byte-identical before/after; the diff only un-discards the already-accepted reason/actorId/actorType options and appends a logAudit() call AFTER the flag UPDATE. requestApproval/approveTransition are not touched at all (git diff main...HEAD shows zero changed lines in either). The new exported mutator markRolledOut() writes ONLY {rolled_out_at, updated_at} and cannot set is_enabled or lifecycle_state, so it is not an enabling path. Fail direction of the approved-approval lookup is safe: .single() discards its error, so any read fault yields approval=null and the function THROWS (fail-closed).' },
  { id: 'S2-approval-row-is-a-permanently-reusable-re-arm-token', severity: 'MEDIUM',
    summary: 'NEW, not previously documented anywhere in the repo. transitionLifecycleState never CONSUMES, expires, or invalidates the approval row it reads, and leo_feature_flag_approvals has no used_at/consumed_at/expires_at column (live column list: id, flag_key, transition_type, required_approvals, approvals_received, approver_ids, requester_id, status, created_at, resolved_at). The lookup is unbounded by recency. This SD created the first such row for a governance-enforcing flag: id=74e7f6de-349a-44f7-8cec-377d81235d6a, flag_key=STAGE_GATE_PREDICATE_ARMED, transition_type=enable, status=approved, 2/2. It is now a PERMANENT token: any future disable -> re-enable of STAGE_GATE_PREDICATE_ARMED via the governed path will satisfy the 2-approval gate off the 2026-09-12 chairman decision forever, with zero new chairman involvement, while producing an audit row indistinguishable from a freshly-approved one. Blast radius is bounded by S3 (the gate is already bypassable), so this is audit-integrity / false-assurance rather than privilege escalation. Fix: mark the approval consumed inside transitionLifecycleState after a successful UPDATE and filter the lookup on it.' },
  { id: 'S3-approval-gate-is-advisory-on-one-path-preexisting-and-documented', severity: 'MEDIUM',
    summary: 'PRE-EXISTING, already measured and documented in this repo at scripts/enroll-migration-tier-gate-flag.mjs:16-32, which states verbatim: "transitionLifecycleState(key,enabled) -> BLOCKED, requires 2 approval(s) / registry.updateFlag(key,{isEnabled:true}) -> SUCCEEDS, zero approvals / raw service-role .update({is_enabled:true}) -> SUCCEEDS". Re-confirmed against current source: updateFlag (registry.js:232-256) sets is_enabled AND co-sets lifecycle_state via computeCoupledLifecycleState(), satisfying chk_flag_lifecycle_enabled_consistency, with NO approval check anywhere. No RLS policy, trigger or CHECK enforces the approval. NOT introduced by this diff and NOT a regression. It matters HERE because the apply script header and the PR narrative present the requestApproval -> approveTransition x2 -> transitionLifecycleState ceremony as "the two approvals a HIGH-risk-tier flag registry gate requires" -- a printed discriminator, not an enforced gate. Recommend the follow-up the enroll script itself proposes: a BEFORE UPDATE trigger on leo_feature_flags refusing is_enabled false->true for risk_tier=high without a matching unconsumed approved row. That single trigger closes S2 and S3 together.' },
  { id: 'S4-citation-is-unverified-free-text-spoofable', severity: 'MEDIUM',
    summary: 'Q2 ANSWERED: NO verification exists. changed_by, requester_id and approver_ids[] are plain TEXT with no FK, no format check, no existence check, and no status check against chairman_decisions / chairman_ratifications. registry.js performs zero validation on any of them; the string chairman_decision:<uuid> is a naming convention only. Any holder of SUPABASE_SERVICE_ROLE_KEY (i.e. every LEO script) can write an arbitrary citation naming a nonexistent, pending, or rejected decision and it will read as an approved chairman citation downstream. The repo already ships machinery for exactly this class of claim -- lib/audits/approval-artifact-resolver.js implements a two-origin provenance contract -- and the registry does not use it. TRUTHFULNESS OF THIS INSTANCE VERIFIED LIVE (good faith confirmed, control still absent): chairman_decisions 6cb60a30-93d4-49a3-83b4-8e1ba1d49dd2 exists with status=approved, decision=approve; chairman_ratifications b75ddfff-ea06-495d-a507-b887f1623eed exists, ratified_at 2026-09-12T16:05:21Z, quote "A", source "sms:2a2fac91... (verified chairman number)". Fix: resolve the prefix against the named table and assert status before accepting it as an approver identity.' },
  { id: 'S5-two-approvals-both-derive-from-one-utterance-and-requester-self-approved', severity: 'MEDIUM',
    summary: 'Separation-of-duties: the 2-of-2 requirement was satisfied by one principal. Live row 74e7f6de shows requester_id === approver_ids[0] === chairman_decision:6cb60a30... -- the requester approved its own request, because approveTransition only rejects a duplicate approverId (approval.approver_ids.includes(approverId)) and never compares against requester_id. The second approver is chairman_ratification:b75ddfff..., which the ratification row itself records as the ratification OF decision packet 6cb60a30 (quote "A"), i.e. both approvals trace to a single chairman SMS reply. The apply script discloses this honestly in its own header ("both artifacts of the SAME single chairman SMS reply A"), but that disclosure lives in a one-off script, NOT in any queryable column: the durable approval row presents two distinct approver_ids that read as two independent approvals to every downstream reader. Whether one chairman utterance SHOULD satisfy a 2-approval gate is a governance question for LEAD, not a code defect -- but the mechanism, not the narrative, is what future readers see. Fix (code): reject approverId === requester_id in approveTransition.' },
  { id: 'S6-logAudit-conditional-environment-no-injection-no-integrity-drift', severity: 'INFO',
    summary: 'Q3 ANSWERED: no injection, no data-integrity issue. The insert is supabase-js .from(...).insert(obj) -- a PostgREST JSON body, parameterized end to end; no SQL string is assembled anywhere in registry.js and none of flag_key/action/changed_by/new_state is interpolated into a statement. Omitting the environment key does not drift the stored value: leo_feature_flag_audit (base table, database/migrations/20260201_leo_feature_flags_fix.sql:125-134) declares environment TEXT with NO NOT NULL and NO DEFAULT, so "key absent" and "key = NULL" store the identical NULL. Both root-cause claims in the commit message are CONFIRMED by live self-cleaning probes against the real relation: (A) insert naming environment:null -> REJECTED "cannot insert into column environment of view leo_feature_flag_audit_log"; (C) action:"created" -> REJECTED "violates check constraint chk_action_type"; (D) action:"transition" with no environment -> ACCEPTED. Zero probe rows left behind (verified count 0).' },
  { id: 'S7-the-new-environment-branch-is-a-guaranteed-reject-path', severity: 'LOW',
    summary: 'LATENT TRAP introduced by the fix. if (environment) { insertPayload.environment = environment; } reads as "we pass environment through when supplied", but the view rejects the column on ANY insert that names it, value-independent -- measured: probe (B) with environment:"production" -> REJECTED, same error as the null case. So the branch is unreachable-by-design today (setPolicy was changed to stop passing it and no other caller does) and guaranteed to fail the audit write the moment a future caller supplies one. Combined with S8 that failure is a console.warn and the mutation still succeeds. Recommend deleting the environment parameter from logAudit() entirely, or routing environment-bearing writes to the base relation leo_feature_flag_audit, rather than leaving a branch whose only possible outcome is a swallowed failure.' },
  { id: 'S8-audit-write-failure-is-non-fatal-console-warn-only', severity: 'LOW',
    summary: 'The fix converts silent total failure into console.warn, which is a genuine improvement, but the mutation still commits with NO audit record and no durable signal. For a table whose entire purpose is the governance audit trail -- and whose defect class here was literally "failing silently since inception, never inspected" -- a console.warn in a CI/agent log is not a detection mechanism. Recommend either throwing after the mutation (loud), or routing the failure to a durable channel (feedback/harness_backlog) so the next occurrence of this class is discoverable by query rather than by someone scrolling a transcript.' },
  { id: 'S9-backfill-idempotency-fails-open-on-the-duplicate-case', severity: 'MEDIUM',
    summary: 'Q4 ANSWERED, part 1 -- SCOPING IS CORRECT, IDEMPOTENCY IS NOT. Scoping: ENTRIES is a hardcoded 3-element literal (STAGE_GATE_PREDICATE_ARMED, LEO_HIGH_CONSEQUENCE_GATES_ENABLED, HIGH_CONSEQUENCE_STAGE_CUTOVER_ENABLED); every write is an INSERT with flag_key taken from that literal; there is no UPDATE, no DELETE, no wildcard and no .in() on a write. Scoping verdict: PASS. Idempotency: the guard is const { data: existing } = await supabase...eq(flag_key).eq(changed_by, DECISION_CITATION).maybeSingle() -- the error is DESTRUCTURED AWAY. Measured live: maybeSingle() against a flag_key with >1 matching row returns data=null AND error PGRST116 "JSON object requested, multiple (or no) rows returned". The script therefore treats "two citation rows already exist" (and any transient read fault) as ABSENCE and inserts a THIRD. This is the null-from-an-errored-SELECT trap sitting inside an idempotency guard for an audit table. Partial mitigation only: the final readback asserts data.length === 3 and reports FAIL, i.e. duplicates are detected AFTER they are written, never prevented. Fix: capture and throw on error, or use .select(id).limit(1) and test error explicitly.' },
  { id: 'S10-backfill-backdates-created_at-provenance-not-queryable', severity: 'MEDIUM',
    summary: 'Q4 ANSWERED, part 2 -- AUDIT-INTEGRITY. The backfill overrides the DB default with a hand-picked EVENT_AT = 2026-09-12T21:29:10.500000+00:00 so the row interleaves with the genuine trigger-written rows, even though it will actually be written days later. created_at is client-settable (measured: probe (E) inserted created_at 2020-01-01 -> ACCEPTED). The only marker that the row is a backfill lives INSIDE the new_state JSONB (backfilled_by / backfill_reason / cited_decision), not in any indexed or top-level column -- so every ordering query, every "latest row" read, and every "was this cited at the time" check sees a row that claims contemporaneity it does not have. Recommend leaving created_at to the DB default (now()) and carrying the reconstructed event time as an explicit new_state.event_at, so the row is honest about being a reconstruction.' },
  { id: 'S11-audit-log-is-fully-mutable-by-service-role-no-immutability-trigger', severity: 'MEDIUM',
    summary: 'MEASURED, not inferred: I inserted probe rows into leo_feature_flag_audit_log AND DELETED them with the service-role client, successfully, leaving zero residue. The governance audit trail is therefore append-and-amend: arbitrary changed_by (S4), arbitrary created_at (S10), and arbitrary DELETE. It is not tamper-evident. This repo already has the pattern for the stronger property -- database/chairman-gated/20260907_feedback_immutability_trigger.sql -- and this table, which is the one being cited as the durable record of a chairman-approved governance-gate arming, does not have it. Not introduced by this diff; surfaced because this SD is what makes the table load-bearing. Recommend a chairman-gated immutability trigger (no UPDATE, no DELETE, created_at forced to now()) as the follow-up that makes the citation worth citing.' },
  { id: 'S12-client-clock-timestamps-produced-a-causally-impossible-ordering', severity: 'LOW',
    summary: 'Live anomaly, measured on the row this SD created: leo_feature_flag_approvals 74e7f6de has resolved_at = 2026-09-12T21:29:08.316Z but created_at = 2026-09-12T21:29:09.600Z -- RESOLVED 1.28s BEFORE IT WAS REQUESTED. Cause: resolved_at is a client-side new Date().toISOString() (approveTransition, registry.js:493) while created_at is the DB DEFAULT now(); the two clocks are skewed. Same pattern on the flags: rolled_out_at=21:29:08.512 (client) precedes its own audit row created_at=21:29:09.976 (DB). markRolledOut(), NEW in this diff, extends the pattern with two more client-clock writes (rolled_out_at, updated_at). Harmless operationally; corrosive to an audit trail whose value is a defensible chronology, and it silently invalidates any reasoning that orders these tables against each other. Recommend DB-side now() for all audit/approval timestamps.' },
  { id: 'S13-no-test-covers-the-high-risk-approval-block', severity: 'LOW',
    summary: 'COVERAGE GAP on the exact control this SD depends on. tests/unit/feature-flags/registry-audit-logging.test.js:81 deliberately sets risk_tier:low with the comment "avoid the approval-requirement branch for this unit test". There is consequently NO test anywhere asserting the negative case -- that transitionLifecycleState(highRiskFlag, enabled) THROWS when the approval row is missing, unapproved, or short of required_approvals. The 22 tests in the two new files all pass (vitest run, 2 files / 22 tests). For an SD whose stated purpose is arming a HIGH-risk flag through the approval gate, one negative-path test on that branch is the cheapest durable protection against a future refactor silently removing it.' },
  { id: 'S14-citation-rows-not-yet-present-in-the-live-audit-trail', severity: 'MEDIUM',
    summary: 'STATE OBSERVATION, EXEC completeness rather than a code defect. As of this review the backfill has NOT been run: a live read of leo_feature_flag_audit_log for all three flag_keys returns 25 rows, ALL with changed_by=NULL (DB-trigger-written), and ZERO rows carrying DECISION_CITATION. The live apply at 2026-09-12T21:29:09-10Z predates the logAudit fix, so the arming of a HIGH-risk governance-enforcing flag currently has NO attributable actor in the audit trail at all. The SD central promise is therefore unfulfilled in the live DB until the backfill executes. Flag state itself is correct: STAGE_GATE_PREDICATE_ARMED is_enabled=true lifecycle_state=enabled risk_tier=high; the two graduated flags carry rolled_out_at=2026-09-12T21:29:08.512Z (both are risk_tier=medium despite the HIGH_CONSEQUENCE name prefix -- no approval gate applied to their graduation, and markRolledOut has none, which is acceptable since rolled_out_at only suppresses a governance-review recommendation and grants no capability).' },
  { id: 'S15-no-injection-no-secrets-no-new-access-surface', severity: 'INFO',
    summary: 'Q5 SWEEP, clean. SQL/command injection: none -- every DB call in the diff is PostgREST .insert()/.update()/.eq() with values in the JSON body; no execSync/spawn/child_process anywhere in the diff; no template-literal SQL. Secrets: zero hits for api_key|secret|password|token|Bearer|sk-|service_role_key on added lines beyond the standard process.env.SUPABASE_SERVICE_ROLE_KEY client construction, which matches every other script in scripts/one-off/. Access control: no new table, no migration, no RLS policy change, no new anon/authenticated-reachable path -- all three scripts are service-role, locally-invoked one-offs. lib/governance/stage-gate-predicate.js is unchanged and retains its documented fail-safe direction (a read fault on STAGE_GATE_PREDICATE_ARMED resolves armed=false, i.e. shadow mode, never a spurious BLOCK). lib/sd-creation/source-adapters/qf.js composeEscalatedTitle() is a pure length bound, verified arithmetically safe: 440-char slice + 51-char marker = 491 < varchar(500), and the marker U+2026 counts as 1 character (Postgres varchar counts characters, not bytes). Sole nit: String.slice(0,440) can in principle split a surrogate pair and emit a lone surrogate that Postgres rejects -- same failure class the fix exists to prevent, negligible in practice for QF titles.' },
];

const warnings = [
  'S2 (MEDIUM): the approval row this SD created (74e7f6de) is never consumed and never expires -- it is a permanent re-arm token for STAGE_GATE_PREDICATE_ARMED that will make every future governed re-enable look freshly chairman-approved.',
  'S9 (MEDIUM): the backfill idempotency guard swallows its error and treats PGRST116 (multiple rows) as absence -- measured live. It will insert a duplicate citation row on the very case it is meant to prevent, and only detect it afterward in the readback.',
  'S14 (MEDIUM): zero citation-carrying audit rows exist in the live DB right now; all 25 rows for the three flags have changed_by=NULL. The SD promise is unfulfilled until scripts/one-off/sd-leo-fix-chairman-approved-arming-001-backfill-audit-citation.mjs actually runs.',
  'S3/S4/S5 (MEDIUM, largely pre-existing): the "2 chairman approvals" ceremony is enforced on exactly one code path, accepts unverified free-text identities, permits requester self-approval, and was satisfied here by two artifacts of a single SMS reply. Accurate as narrative, not as an enforced control.',
];

const recommendations = [
  'CONDITIONAL_PASS. The diff introduces NO privilege escalation, NO injection, and NO weakening of the approval gate; it is net-positive for audit integrity, converting a since-inception total silent failure of leo_feature_flag_audit_log writes into working, attributed rows (both root causes re-confirmed live by self-cleaning probe). Conditions are about the durability of the record this SD is creating, not about the code being unsafe to merge.',
  'BEFORE EXEC CLOSES (cheap, in-scope): (1) run the backfill so S14 stops being true, and fix its guard first per S9 -- capture the error, or use .select(id).limit(1); (2) drop the hardcoded backdated created_at per S10 and carry the event time in new_state; (3) add the negative test per S13 asserting transitionLifecycleState throws on a high-risk flag with no approved row.',
  'FOLLOW-UP SD (out of scope here, but this is the fix that makes the ceremony real): a BEFORE UPDATE trigger on leo_feature_flags refusing is_enabled false->true for risk_tier=high without a matching unconsumed approved row, plus consumption of the approval inside transitionLifecycleState. That single change closes S2 and S3 together and turns the documented "advisory on one path" label into an enforced gate. scripts/enroll-migration-tier-gate-flag.mjs:32 already proposes exactly this and calls it "worth filing".',
  'FOLLOW-UP SD: a chairman-gated immutability trigger on leo_feature_flag_audit_log (per S11) -- no UPDATE, no DELETE, created_at forced to now() -- modelled on database/chairman-gated/20260907_feedback_immutability_trigger.sql. Without it the citation this SD is going to such lengths to record is deletable by any service-role holder, which I demonstrated.',
  'FOLLOW-UP (small): reject approverId === requester_id in approveTransition (S5); delete the unreachable environment branch in logAudit (S7); move audit/approval timestamps to DB-side now() (S12); consider resolving chairman_decision:/chairman_ratification: prefixes against their tables before accepting them as approver identities (S4).',
];

async function main() {
  const supabase = await getSupabaseClient();
  const resolution = await resolveSubAgentRepo({
    sdId: SD_ID, targetApplication: 'EHG_Engineer', subAgentCode: 'SECURITY',
    fallback: 'EHG_Engineer', supabase,
  });

  let results = {
    verdict: 'CONDITIONAL_PASS',
    confidence: 92,
    findings, warnings, recommendations,
    conditions: [
      { action: 'Fix the backfill idempotency guard (S9): the error from .maybeSingle() is destructured away, so PGRST116 (>1 matching row) reads as absence and inserts a duplicate citation row. Measured live.', priority: 'high', blocking: false },
      { action: 'Run the backfill (S14): zero citation-carrying rows exist live; all 25 audit rows for the three flags have changed_by=NULL, so the armed HIGH-risk governance flag currently has no attributable actor.', priority: 'high', blocking: false },
      { action: 'Stop backdating created_at in the backfill (S10) and carry the reconstructed event time in new_state instead; created_at is client-settable (measured) and the backfill marker is only visible inside the JSONB.', priority: 'medium', blocking: false },
      { action: 'Add a negative test for the HIGH-risk approval branch (S13): the new test deliberately uses risk_tier=low to avoid it, so nothing asserts transitionLifecycleState throws when approvals are missing.', priority: 'medium', blocking: false },
      { action: 'File the follow-up: consume the approval row in transitionLifecycleState and add a BEFORE UPDATE trigger refusing is_enabled false->true for risk_tier=high without an unconsumed approved row (S2+S3).', priority: 'medium', blocking: false },
    ],
    justification: 'CONDITIONAL_PASS: no privilege escalation, no injection and no weakening of the approval gate in the diff, which is net-positive for audit integrity; conditions cover a measured fail-open in the new backfill idempotency guard, an unrun backfill leaving the live citation absent, a backdated audit created_at, a missing negative test on the HIGH-risk approval branch, and the permanently-reusable approval token this SD created.',
    summary: 'CONDITIONAL_PASS. Q1: the approval gate is byte-identical before/after -- the diff only un-discards reason/actorId/actorType and appends a logAudit call; markRolledOut writes only rolled_out_at and cannot enable anything; the approved-approval lookup fails closed. Nothing in the diff bypasses the gate. Q2: the citation is unverified free text -- no FK, no existence check, no status check -- so any service-role holder can forge one; this instance is truthful (decision 6cb60a30 status=approved; ratification b75ddfff quote "A" from a verified chairman number, both read live). Q3: no injection (PostgREST parameterized, no SQL assembly, no child_process) and no integrity drift (base column is nullable with no default, so omitted === NULL); both root-cause claims re-confirmed by self-cleaning live probes, zero residue. Q4: scoping is correct (3 hardcoded keys, insert-only, no UPDATE/DELETE/wildcard) but idempotency fails open -- the .maybeSingle() error is destructured away and PGRST116 on >1 row returns data=null, so the duplicate case it guards against produces a third row; it also backdates created_at. Q5: no secrets, no new access surface, stage-gate predicate unchanged and still fail-safe-to-shadow. Three findings worth carrying beyond the diff: the approval row is a permanently-reusable re-arm token (never consumed, no expiry); the approval gate is advisory on ONE path (updateFlag and raw UPDATE both enable a high-risk flag with zero approvals -- pre-existing and already documented at enroll-migration-tier-gate-flag.mjs:16-32); and leo_feature_flag_audit_log has no immutability trigger, demonstrated by inserting and then deleting rows with the service-role client. Live state: 0 citation rows exist yet -- the backfill has not run.',
    metadata: {
      gate: 'EXEC - SECURITY review of PR #8814',
      scope: 'governance feature-flag arming + audit-trail defect fix; service-role one-off scripts, no user-facing surface, no migration, no RLS change',
      approval_gate_check: 'PASS - gate unchanged by diff, fails closed on read fault; but advisory on one path (pre-existing, documented) and its approval row is never consumed',
      citation_verification_check: 'FAIL (control absent) - changed_by/requester_id/approver_ids are unverified free text; this instance verified truthful by direct read of chairman_decisions + chairman_ratifications',
      injection_check: 'PASS - PostgREST parameterized inserts only; no SQL assembly, no child_process, no interpolation in the diff',
      audit_integrity_check: 'CONDITIONAL - arbitrary changed_by, client-settable created_at, and DELETE all succeed under service_role (demonstrated); no immutability trigger',
      backfill_scoping_check: 'PASS - 3 hardcoded flag_keys, insert-only, no UPDATE/DELETE/wildcard',
      backfill_idempotency_check: 'FAIL - maybeSingle() error destructured away; PGRST116 on multiple rows reads as absence and inserts a duplicate (measured)',
      secret_exposure_check: 'PASS - zero secret-pattern hits on added lines beyond standard SUPABASE_SERVICE_ROLE_KEY client construction',
      probe_method: 'live service-role reads of leo_feature_flag_audit_log / leo_feature_flag_approvals / leo_feature_flags / chairman_decisions / chairman_ratifications; five self-cleaning INSERT probes against the real view proving the environment-column rejection, the chk_action_type rejection, the admissible action, and client-settable created_at (0 residual rows); a maybeSingle() multi-row probe reproducing the backfill guard fail-open; vitest run of both new test files (22 passed)',
      files_examined: [
        'lib/feature-flags/registry.js',
        'lib/sd-creation/source-adapters/qf.js',
        'lib/governance/stage-gate-predicate.js',
        'scripts/one-off/qf-20260912-959-arm-stage-gate-and-graduate.mjs',
        'scripts/one-off/sd-leo-fix-chairman-approved-arming-001-backfill-audit-citation.mjs',
        'scripts/one-off/record-mechanism-verifications-chairman-approved-arming-001.mjs',
        'scripts/enroll-migration-tier-gate-flag.mjs',
        'lib/audits/approval-artifact-resolver.js',
        'database/migrations/20260201_leo_feature_flags_fix.sql',
        'tests/unit/feature-flags/registry-audit-logging.test.js',
      ],
      pr: 'https://github.com/rickfelix/EHG_Engineer/pull/8814',
      model: 'Opus 5',
      model_id: 'claude-opus-5[1m]',
      invoked_at: new Date().toISOString(),
    },
    detailed_analysis: {
      sd_key: SD_KEY,
      worktree: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer/.worktrees/SD-LEO-FIX-CHAIRMAN-APPROVED-ARMING-001',
      branch: 'feat/SD-LEO-FIX-CHAIRMAN-APPROVED-ARMING-001',
      blocking_findings: 0,
      medium_findings: 7,
      low_findings: 4,
      info_findings: 4,
    },
    phase: 'EXEC',
  };

  results = applySubAgentRepoVerdict(results, resolution);

  const stored = await storeSubAgentResults('SECURITY', SD_ID,
    { name: 'Chief Security Architect (security-agent)' }, results,
    { sdKey: SD_KEY, phase: 'EXEC' });

  console.log('VERDICT WRITTEN:');
  console.log('  ID:', stored.id);
  console.log('  verdict:', stored.verdict, '@ confidence', stored.confidence);
  console.log('  phase:', stored.phase);
  console.log('  repo_path:', stored.metadata?.repo_path);
  console.log('  repo_resolved:', stored.metadata?.repo_resolved);
  console.log('  executed_from_cwd:', stored.metadata?.executed_from_cwd);
}
main().catch(e => { console.error('FAILED:', e.message); console.error(e.stack); process.exit(1); });
