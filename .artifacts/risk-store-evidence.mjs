// Canonical path: resolveSubAgentRepo -> applySubAgentRepoVerdict -> storeSubAgentResults.
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';

const SD_ID = 'ef96ac1a-69f1-4f57-8ba5-fcec84ad66d5';
const PHASE = 'LEAD';

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 88,
  critical_issues: [
    'R1 CORRECTNESS (impact CRITICAL / likelihood HIGH-if-implemented-literally): Bucket A action as written ("REVOKE anon + authenticated") is a NO-OP. Live proacl read 2026-08-15 shows ALL SIX Bucket A functions carry the PUBLIC grant (=X/postgres) in addition to explicit anon/authenticated grants: PUBLIC+anon+auth+svc. anon and authenticated INHERIT the PUBLIC pseudo-role, so revoking only the two named roles leaves EXECUTE reachable. The predecessor migration 20260728_revoke_public_execute_role_flag_rpcs.sql got this right (REVOKE ... FROM PUBLIC, anon, authenticated) and its header records the same measured ACL shape. The SD prose dropped PUBLIC. Same defect applies to the 7 Bucket B functions that carry PUBLIC: approve_chairman_decision, claim_sd, fn_is_service_role, fn_list_chairman_webauthn_credentials, fn_user_has_venture_access, reject_chairman_decision, upsert_operator_cash_burn.',
    'R2 RECURRENCE FIX DOES NOT WORK AS SPECIFIED (impact HIGH / likelihood HIGH): the SD prescribes ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC. Live pg_default_acl read shows Supabase has ALREADY installed an explicit default ACL for (role=postgres, schema=public, objtype=f): postgres=X/postgres, anon=X/postgres, authenticated=X/postgres, service_role=X/postgres. That entry is ADDITIVE to the built-in PUBLIC=X default. Revoking only PUBLIC from the default therefore removes the built-in grant and leaves four explicit grants intact, so every future function still lands anon-executable. The stated fix closes the smaller of the two paths and reads as closure. Correct statement: ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC, anon.',
    'R3 DESIGNATED VERIFIER IS BLIND TO THE CHANGE (impact HIGH / likelihood CERTAIN): success_criteria #2 names scripts/audit-rpc-execute-grants.mjs as the Bucket B post-migration check. That script only ever evaluates has_function_privilege for the authenticated role (lines 13/82/109/115) and never evaluates anon. Bucket B revokes anon and preserves authenticated. The script therefore measures the one axis the migration deliberately does NOT change and cannot observe the axis it does change; it will report green whether the anon revoke was correct, a no-op, or catastrophic.',
  ],
  warnings: [
    { severity: 'HIGH', issue: 'R4 SCAN DIMENSION 1 IS NARROWER THAN CLAIMED (impact HIGH / likelihood CONFIRMED). The SD states dimension 1 covered ehg/src + ehg/supabase/functions + server/. It did NOT cover EHG_Engineer/scripts/, EHG_Engineer/lib/, or EHG_Engineer/supabase/functions/ (13 edge functions). Confirmed miss: fn_stage_artifact_precondition is classified Bucket A "called only by SECDEF fn", but scripts/harness/s20-fixture.mjs:219 calls it as a DIRECT .rpc(). The classification is factually wrong as stated. It survives only because that script builds a service_role client (s20-fixture.mjs:100) - safety by coincidence, not by the reasoning recorded. The same unscanned tree holds direct .rpc() callers for Bucket B claim_sd (lib/claim-guard.mjs:784/797, scripts/worker-checkin.cjs:388, scripts/qf-start.js:99, lib/session-conflict-checker.mjs:319, lib/sd-creation/source-adapters/qf.js:148), export_blueprint_review (4 sites in lib/eva/bridge/), can_auto_advance, delete_venture, approve_chairman_decision.' },
    { severity: 'MEDIUM', issue: 'R5 A SIXTH CALLER SURFACE EXISTS THAT NO REPO SCAN CAN SEE (impact HIGH / likelihood MEDIUM). cron.job rows live in the live database, not the repo. database/migrations/20260410_friday_notification_cron.sql states in its own header that it MUST be executed in the Supabase Dashboard SQL Editor because pg_cron is not reachable over the pooler, so the repo is provably NOT the source of truth for what is scheduled. Any Dashboard-created cron job or pg_net webhook calling one of the 42 is invisible to all five declared dimensions. The two committed cron/pg_net jobs were checked and both use service_role (safe), but committed jobs are a subset of live jobs by construction.' },
    { severity: 'MEDIUM', issue: 'R6 TRIAGE LIST IS 18 DAYS STALE AND ALREADY LEAKING (impact MEDIUM / likelihood CONFIRMED). Live scan finds 27 anon-executable SECURITY DEFINER functions in public; only 25 of them are in the three buckets. Two are in NO bucket: fn_anon_ingress_prior_hour_count (postgres=X,anon=X,authenticated=X,service_role=X) and log_sd_mutation_audit (PUBLIC+anon+auth+svc). Both post-date the 2026-07-28 triage. The buckets are a point-in-time snapshot with no freshness stamp; the population they describe grows while the SD waits on the chairman gate.' },
    { severity: 'MEDIUM', issue: 'R7 BUCKET B IS 63 PERCENT NO-OP; SCOPE IS OVERSTATED (impact LOW security / MEDIUM judgement). 17 of 27 Bucket B functions ALREADY have anon_x=false (shape auth+svc): advance_venture_stage, advance_venture_to_stage, bootstrap_venture_workflow, can_auto_advance, create_eva_conversation, delete_venture, eva_circuit_allows_request, export_blueprint_review, kill_venture, log_stage_advance_override, park_venture_decision, record_eva_failure, record_eva_success, rescan_stage_20, reset_eva_circuit, set_global_auto_proceed, set_stage_override. These are exactly the functions restored by 20260606_restore_rpc_execute_grants.sql and 20260609_grant_execute_rescan_stage_20.sql. Only 10 of 27 Bucket B functions need any change. Presenting 42 verdicts to the chairman overstates the residual exposure being authorised by roughly 40 percent.' },
    { severity: 'MEDIUM', issue: 'R8 NO PAIRED ROLLBACK IS REQUIRED BY ANY SUCCESS CRITERION (impact HIGH / likelihood MEDIUM). The repo convention is unambiguous and the SD own cited predecessor followed it: commit 13d02e18d81 shipped 20260728_revoke_public_execute_role_flag_rpcs.sql TOGETHER WITH its _DOWN.sql, whose body restores the exact measured pre-apply ACL and carries the warning that it is for use only to unbreak a caller the migration verification missed. 29 _rollback.sql files plus a _STAGED_DOWN convention corroborate. None of the six success_criteria mention a rollback artifact. Because apply is a one-shot chairman ceremony an EXEC agent cannot re-enter, absence of a pre-authored DOWN means recovery requires composing new permission SQL under incident pressure and obtaining a second 3-factor approval.' },
    { severity: 'MEDIUM', issue: 'R9 BUCKET A REVERSES A DELIBERATE PRIOR SAFETY DECISION WITHOUT CITING IT (impact MEDIUM / likelihood MEDIUM). fn_user_has_company_access was EXPLICITLY allowlisted by 20260603_03_revoke_secdef_execute_from_anon_authenticated.sql as an explicit auth primitive kept for defense-in-depth, described there as a sibling helper not currently in a policy. This SD moves it to Bucket A (revoke anon AND authenticated). It is safe TODAY (zero policies reference it) but scripts/lint/rls-anon-tenant-predicate-lint.mjs:207 actively instructs policy authors to bind tenant predicates to fn_user_has_venture_access() / fn_user_has_company_access(). RLS helpers execute as the QUERYING role, so the repo own lint steers future authors into the pattern that detonates this revoke, and the failure surfaces as a broken policy rather than a failed migration.' },
    { severity: 'MEDIUM', issue: 'R10 BUCKET B/C BOUNDARY RESTS ON A POLICY-ROLE DISTINCTION WITH A 412-STATEMENT BLIND SPOT (impact HIGH / likelihood LOW-MEDIUM). The SD reasoning cites roles={anon} policies such as feedback.venture_user_insert_feedback. In Postgres, CREATE POLICY without a TO clause defaults to TO PUBLIC, which INCLUDES anon. Static count across database/migrations: 1757 CREATE POLICY statements, 412 (23 percent) have no TO clause. If the triage matched roles={anon} and did not treat roles={public} as anon-facing, roughly 23 percent of the policy corpus is unexamined for exactly the Bucket B versus C decision. A miss is not a silent zero-row result: EXECUTE denied inside a policy predicate raises 42501 and fails the whole query for anon.' },
    { severity: 'LOW', issue: 'R11 CEREMONY QUEUE COLLISION (impact LOW / likelihood MEDIUM). At least two other chairman-gated permission migrations are already staged and unapplied: 20260718_switchon_rls_narrow_authenticated_read_STAGED.sql and 20260728_anon_authenticated_audience_and_narrowing_STAGED.sql (the latter has NO paired DOWN). Ordering and interaction with this SD migration are undeclared; a bundled ceremony would make attribution of any resulting break ambiguous.' },
  ],
  recommendations: [
    'M1 (blocks EXEC authoring) Rewrite the bucket actions as explicit SQL in the SD/PRD rather than prose. Bucket A: REVOKE EXECUTE ON FUNCTION <sig> FROM PUBLIC, anon, authenticated. Bucket B: REVOKE EXECUTE ON FUNCTION <sig> FROM PUBLIC, anon; followed by GRANT EXECUTE ON FUNCTION <sig> TO authenticated (defensive re-assertion, verified non-destructive because all 27 already hold an explicit authenticated grant). Verified-safe fact worth recording: ALL 42 functions carry an explicit service_role grant, so revoking PUBLIC cannot strand any service_role caller - this removes the largest theoretical blast radius (the entire LEO harness runs on service_role) as a live concern rather than an assumed one.',
    'M2 (blocks EXEC authoring) Correct the ALTER DEFAULT PRIVILEGES statement to REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC, anon (keeping authenticated and service_role). Scope FOR ROLE postgres IN SCHEMA public - verified correct target, since all 139 public SECDEF functions are owned by postgres with no second creating role to miss.',
    'M3 (blocks the ceremony) Replace the success_criteria #2 verifier. audit-rpc-execute-grants.mjs cannot see anon. Either extend it to assert anon EXECUTE is false for Buckets A and B and unchanged for Bucket C, or add a purpose-built check. Keep the existing authenticated assertion as the no-collateral-damage half. A verifier that cannot observe its subject must not be cited as the criterion.',
    'M4 (unblocks the stated KNOWN GAP - already executed by this assessment) Live grant-state verification is NOT actually blocked. The pooler password is stale (28P01 password authentication failed for user postgres; host aws-1-us-east-1.pooler.supabase.com is correct and reachable - an earlier ENOTFOUND was an aws-0 versus aws-1 region typo, not an outage). The catalog is fully readable now over REST with the service-role key via rpc exec_sql with parameter name sql_text (not sql). The full pre-apply ACL baseline for all 42 functions was captured this session and is embedded in metadata.live_acl_baseline. Re-capture immediately before the ceremony and diff after; this satisfies success_criteria #1/#2/#3 without waiting on a credential rotation.',
    'M5 (blocks the ceremony) Author a paired _DOWN.sql alongside the forward migration, matching 20260728_revoke_public_execute_role_flag_rpcs_DOWN.sql: restore the exact measured pre-apply ACL per function and carry the same explicit warning that applying it reinstates the exposure. Use metadata.live_acl_baseline captured here as the restore target so the DOWN is grounded in a measurement rather than an assumption.',
    'M6 Re-run the triage scan against live pg_proc at PLAN rather than the 2026-07-28 snapshot, and stamp the result with its measurement time. Fold in fn_anon_ingress_prior_hour_count and log_sd_mutation_audit or record an explicit exclusion reason for each. Bring the corrected "10 of 27 Bucket B actually change" number to the chairman so the authorisation is given against the real residual, per the SD own stated intent that the ask go up against a verified list.',
    'M7 Extend the caller scan to the three unscanned trees (EHG_Engineer/scripts, EHG_Engineer/lib, EHG_Engineer/supabase/functions) and record, per function, the file:line AND the client factory used - the evidence standard the predecessor migration set for itself. Sweep completed for the 42 during this assessment: every Engineer-side caller found builds a service_role client, and the two edge-function callers (ehg telegram-chairman-bot build-queue-flow.ts calling claim_sd, and fn_is_chairman) both use SUPABASE_SERVICE_ROLE_KEY (index.ts:63). No anon-key caller was found for any Bucket A or Bucket B function.',
    'M8 Point the companion CI standing check at the hazard that will actually exist after M2 lands. Measured: of 118 function-creating migrations from Jun-Aug 2026, only 17 carry an explicit GRANT EXECUTE and 101 rely on the default. Once anon is removed from the default ACL, the prevailing authoring style silently produces functions the app cannot call, failing at runtime as HTTP 403 rather than at CI. A check that only demands REVOKE FROM PUBLIC or a recorded reason nags about a hazard M2 has already closed while missing the one M2 creates. It should also require any new SECURITY DEFINER function to declare its intended callers (service_role only / authenticated / anon) and carry the matching explicit GRANT.',
    'M9 Confirmed-safe, no action needed (recorded so it is not re-litigated): the chairman gate cannot be bypassed by tier misclassification. scripts/lib/migration-tier-classifier.mjs FORBIDDEN_TOPLEVEL (line 44) includes both GRANT and REVOKE, and every ALTER DEFAULT PRIVILEGES statement necessarily contains one of them, so this migration classifies TIER-2 and routes to the 3-factor ceremony. Also confirmed: all 42 functions have exactly ONE overload each, so the overload-safety trap is currently inert - still author signature-based REVOKEs for future-proofing.',
    'M10 Sequence this ceremony explicitly against the two already-staged permission migrations (20260718_switchon..., 20260728_anon_authenticated_audience...) so a post-apply break is attributable to one change. Do not bundle.',
  ],
  detailed_analysis: [
    'RISK ASSESSMENT - SD-LEO-INFRA-CLOSE-REMAINING-SECURITY-001 (LEAD phase, 2026-08-15).',
    '',
    'METHOD. Live catalog ground truth obtained despite the reported credential block (see M4): per-function proacl plus has_function_privilege for anon/authenticated/service_role across all 42 bucketed functions, a full public-schema SECDEF census, pg_default_acl, and proowner. Static analysis: caller sweep across EHG_Engineer scripts/lib/server/src/api and its 13 edge functions, the ehg app src and its 60 edge functions, 18 sibling venture repos, 1428 migrations, 1757 CREATE POLICY statements, the migration tier classifier, and the git history of the predecessor migration.',
    '',
    'HEADLINE. The SD triage judgement is sound and, where testable, live-confirmed - notably Bucket C fn_relay_insert_sms_candidate, which reads anon plus service_role with authenticated ABSENT, the precise signature of an external webhook integration that the SD predicted from reasoning alone. The defects are not in the triage; they are in the three places the SD converts triage into action: the REVOKE statement omits PUBLIC (R1), the ALTER DEFAULT PRIVILEGES statement omits anon (R2), and the designated verifier cannot see anon at all (R3). All three share one shape - the artifact reads as closure while leaving the anon path open.',
    '',
    'Q1 - BLAST RADIUS OF ONE WRONG CLASSIFICATION. Asymmetric by bucket, and smaller than feared once measured. Bucket A (which revokes authenticated) is the only genuinely dangerous bucket; a wrong verdict there produces HTTP 403 for every logged-in user of that path. Bucket B keeps authenticated, so a wrong verdict costs only anon reachability. The decisive measured fact is that ALL 42 functions carry an explicit service_role grant, so no revoke in this SD can strand the service_role callers that run the entire LEO harness - claim_sd alone is called from five harness call sites plus the chairman Telegram bot. Worst case is therefore bounded well below a fleet halt. The genuine residual blast radius is concentrated in RLS policy helpers: fn_user_has_venture_access (17 policies), fn_is_service_role (7) and is_chairman_role (6) sit in Bucket B, and a policy helper is invoked as the querying role. Denying EXECUTE inside a policy predicate raises 42501 and fails the entire query rather than returning zero rows, so a miss there is loud, immediate and total for the affected table and role. Empirical base rate rather than speculation: the June 2026 sweep of this exact class missed at least 20 of roughly 112 functions (18 percent), and detection ran through production 403s over six days (20260603_03, then 20260606_restore of 18 functions, then 20260606 get_gate_decision_status, then 20260609 rescan_stage_20 - the last found by a chairman clicking a Stage 20 button). The SD risk register rates this likelihood low; the only prior run of this procedure against this database says otherwise.',
    '',
    'Q2 - CALLER-SURFACE COVERAGE. The five dimensions are strong on DB-internal reachability (policies, function bodies, triggers, views) and structurally incomplete on caller-side reachability. Three categories the scan cannot see, one of which produced a confirmed misclassification. (a) EHG_Engineer own trees - scripts/, lib/ and supabase/functions/ (13 edge functions) were outside the declared scope, and fn_stage_artifact_precondition, labelled called only by SECDEF fn, has a direct .rpc() at scripts/harness/s20-fixture.mjs:219. (b) cron.job and pg_net rows, which live in the database and are documented as Dashboard-created because pg_cron is unreachable over the pooler - the repo is provably not their source of truth. (c) Anything outside every checked-out repo, which the SD itself correctly identifies as unfalsifiable and correctly converts into positive evidence to EXCLUDE for Bucket C. Sweep results after extending coverage: zero anon-key callers for any Bucket A or B function; all Engineer-side callers use service_role; both edge-function callers use SUPABASE_SERVICE_ROLE_KEY. Coverage is therefore repairable and currently clean once repaired - but the five dimensions claim should be restated as five DB-side dimensions plus a partial caller-side dimension.',
    '',
    'Q3 - ROLLBACK. Required, both by repo convention and by this SD own cited predecessor: commit 13d02e18d81 shipped the forward migration and its _DOWN.sql together, the DOWN restoring the exact measured pre-apply ACL with an explicit warning that it reinstates the exposure. 29 _rollback.sql files and a _STAGED_DOWN convention corroborate. No success criterion in this SD requires one. This matters more here than for an ordinary migration because apply happens once, inside a chairman ceremony an EXEC agent cannot re-enter - without a pre-authored DOWN, recovery means composing new permission SQL during a live incident and obtaining a second 3-factor approval. Note also that the recently staged 20260728_anon_authenticated_audience_and_narrowing_STAGED.sql ships with no DOWN, so the convention is already eroding.',
    '',
    'Q4 - ALTER DEFAULT PRIVILEGES. Two distinct risks, both live-measured. FIRST, the stated statement does not achieve the goal: Supabase already maintains pg_default_acl for (postgres, public, functions) equal to postgres=X, anon=X, authenticated=X, service_role=X, additive to the built-in PUBLIC=X. Revoking only PUBLIC from the default leaves four explicit grants and every future function still anon-callable - a silent non-fix that reads as closure (R2/M2). SECOND, the corrected statement changes the contract under which the repo currently authors functions: 101 of 118 function-creating migrations from Jun-Aug 2026 carry no explicit GRANT EXECUTE and rely entirely on the default. Existing functions are unaffected because defaults bind at creation, so there is no collision with any already-authored migration and no uncommitted in-flight migration exists. The forward-looking hazard is real and has a live precedent: fn_relay_insert_sms_candidate is anon-only by design, so the next legitimately anon-facing integration created after this lands will be born without anon EXECUTE and fail as a production webhook error rather than a CI failure. The change is worth making, but must be paired with M8 or it converts a closed security gap into an open reliability gap.',
    '',
    'Q5 - SCORING. See metadata.domain_scores. Overall HIGH, driven by integration_risk 8 and security_risk 7. HIGH requires a documented mitigation plan rather than a block; M1, M2, M3 and M5 are the blocking subset.',
    '',
    'VERDICT: CONDITIONAL_PASS. The strategic intent, the bucket reasoning and the chairman-gate posture are all sound and should proceed to PLAN. The SD must not go to the chairman ceremony in its current form: as written the migration would be a no-op on Bucket A, the recurrence fix would not prevent recurrence, and the named verifier would report green either way. All three are cheap to correct at PLAN, and the credential gap that motivated the unverified caveat is not real - the full pre-apply baseline is captured in this row.',
  ].join('\n'),
  metadata: {
    assessment_type: 'BMAD_6_domain_risk',
    overall_risk: 'HIGH',
    domain_scores: {
      technical_complexity: 6,
      security_risk: 7,
      performance_risk: 1,
      integration_risk: 8,
      data_migration_risk: 5,
      ui_ux_risk: 4,
    },
    live_verification: {
      performed: true,
      measured_at: new Date().toISOString(),
      method: 'rpc exec_sql with sql_text parameter, over REST using SUPABASE_SERVICE_ROLE_KEY',
      pooler_status: '28P01 password authentication failed for user postgres at aws-1-us-east-1.pooler.supabase.com - stale SUPABASE_DB_PASSWORD (14 chars); host correct and reachable; NOT a network or region fault',
      supersedes_sd_risk_4: 'SD risk #4 (live grant-state verification blocked) is RESOLVED by this row; the baseline below IS the independent re-confirmation LEAD was said to lack',
      public_secdef_census: { total_secdef_public: 139, anon_executable: 27, authenticated_executable: 43, all_owned_by: 'postgres' },
      untriaged_anon_exposed: ['fn_anon_ingress_prior_hour_count', 'log_sd_mutation_audit'],
      bucket_b_already_compliant_count: 17,
      bucket_b_actually_needs_change_count: 10,
      all_42_single_overload: true,
      all_42_have_explicit_service_role_grant: true,
      pg_default_acl_public_functions: 'postgres=X/postgres, anon=X/postgres, authenticated=X/postgres, service_role=X/postgres (FOR ROLE postgres IN SCHEMA public, objtype=f) - additive to the built-in PUBLIC=X',
    },
    live_acl_baseline: {
      note: 'Pre-apply ACL shapes measured 2026-08-15. Use as the restore target for the paired _DOWN migration (M5) and as the pre/post diff basis for success_criteria #1/#2/#3.',
      A_all_six: 'PUBLIC+anon+auth+svc (anon=true, auth=true, svc=true): fn_enforce_stage_advancement_artifact_gate, fn_quick_fixes_validate_target_application, fn_stage_artifact_precondition, fn_user_has_company_access, fn_verify_and_consume_stepup_token, fn_write_kill_audit_trail',
      B_public_anon_auth_svc: ['approve_chairman_decision', 'claim_sd', 'fn_is_service_role', 'fn_list_chairman_webauthn_credentials', 'fn_user_has_venture_access', 'reject_chairman_decision', 'upsert_operator_cash_burn'],
      B_anon_auth_svc_no_public: ['check_feedback_duplicate', 'get_gate_decision_status', 'is_chairman_role'],
      B_already_anon_false_no_change_needed: ['advance_venture_stage', 'advance_venture_to_stage', 'bootstrap_venture_workflow', 'can_auto_advance', 'create_eva_conversation', 'delete_venture', 'eva_circuit_allows_request', 'export_blueprint_review', 'kill_venture', 'log_stage_advance_override', 'park_venture_decision', 'record_eva_failure', 'record_eva_success', 'rescan_stage_20', 'reset_eva_circuit', 'set_global_auto_proceed', 'set_stage_override'],
      C_unchanged_targets: 'check_feedback_rate_limit PUBLIC+anon+auth+svc; fn_advance_pipeline_stage anon+auth+svc; fn_is_chairman PUBLIC+anon+auth+svc; fn_relay_insert_sms_candidate anon+svc with authenticated ABSENT (external-integration signature CONFIRMED); is_leo_admin PUBLIC+anon+auth+svc; lhe_pending_migration_applied PUBLIC+anon+auth+svc; record_venture_error anon+auth+svc; set_session_working_context PUBLIC+anon+auth+svc; venture_exists_and_active anon+auth+svc',
    },
    prior_attempt_base_rate: {
      forward: 'database/migrations/20260603_03_revoke_secdef_execute_from_anon_authenticated.sql (~112 functions, dynamic allowlist of 6)',
      breakage_and_repairs: ['20260606_restore_rpc_execute_grants.sql (18 functions)', '20260606_grant_execute_get_gate_decision_status.sql (1)', '20260609_grant_execute_rescan_stage_20.sql (1, found 6 days later via a chairman UI button)'],
      observed_miss_rate: 'at least 20 of ~112 (about 18 percent); detection latency up to 6 days, all via production 403 / 42501',
    },
    blocking_mitigations: ['M1', 'M2', 'M3', 'M5'],
    verified_safe_no_action: [
      'tier classifier routes GRANT/REVOKE to TIER-2 chairman ceremony (FORBIDDEN_TOPLEVEL line 44)',
      'all 42 functions are single-overload',
      'all 42 hold an explicit service_role grant - no service_role collateral risk',
      'no uncommitted or in-flight migration collides with the ALTER DEFAULT PRIVILEGES change',
      'zero anon-key callers found for any Bucket A or B function across ehg, EHG_Engineer, 60 ehg edge functions, 13 Engineer edge functions and 18 sibling repos',
    ],
  },
  execution_time_ms: 0,
};

const resolution = await resolveSubAgentRepo({
  sdId: SD_ID,
  subAgentCode: 'RISK',
});
applySubAgentRepoVerdict(results, resolution);
const stored = await storeSubAgentResults('RISK', SD_ID, { name: 'Risk Assessment Sub-Agent' }, results, { phase: PHASE });
console.log('STORED:', JSON.stringify(stored)?.slice(0, 300));
console.log('repo_path         :', results.metadata.repo_path);
console.log('executed_from_cwd :', results.metadata.executed_from_cwd);
console.log('verdict           :', results.verdict, '| confidence:', results.confidence);
