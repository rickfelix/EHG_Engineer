// SD-LEO-INFRA-CLOSE-REMAINING-SECURITY-001 — DATABASE sub-agent evidence writer (PLAN phase).
// Canonical path: resolveSubAgentRepo -> applySubAgentRepoVerdict -> storeSubAgentResults.
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';

const SD_ID = 'ef96ac1a-69f1-4f57-8ba5-fcec84ad66d5';
const PHASE = 'PLAN';

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 90,
  summary:
    'Authored the corrected migration design and RE-MEASURED the true live residual via the exec_sql REST path ' +
    '(pooler credential-broken). LIVE CENSUS 2026-08-15: 139 SECURITY DEFINER functions in public; exactly 27 are ' +
    'anon-EXECUTE-able today. That 27 reconciles with ZERO remainder: Bucket A 6/6 still exposed, Bucket B 10/27 still ' +
    'exposed (17 already closed by the June-2026 sweep + SD-MAN-FIX-SECURITY-GUARD-PACK-001), Bucket C 9/9 still exposed ' +
    '(deliberate), plus 2 newly-triaged unbucketed functions. C4 CONFIRMED: the chairman ask is for 16 functions genuinely ' +
    'changing state, not 42 — but with a DIFFERENT COMPOSITION than C4 assumed, because three placements moved. ' +
    'THREE DEFECTS FOUND BEYOND C1-C9: (1) fn_write_kill_audit_trail must move Bucket A -> Bucket B: it has a SECURITY ' +
    'INVOKER caller, fn_chairman_decide(uuid,text,text,text,boolean) (prosecdef=false, anon+authenticated executable), ' +
    'which does PERFORM fn_write_kill_audit_trail(...). Under invoker mode the inner call runs AS THE CALLING ROLE, so ' +
    'revoking authenticated breaks fn_chairman_decide. Bucket A premise "called only by SECDEF fn" is FALSE for this one ' +
    'function, and unlike C6 fn_stage_artifact_precondition the VERDICT changes, not just the reasoning. It is the only ' +
    'invoker-caller instance across all 17 candidates (whole-catalog scan). (2) C8 CONFIRMED with exactly one instance: ' +
    'is_chairman_role() must move Bucket B -> Bucket C, referenced by public.archetype_benchmarks::archetype_benchmarks_admin ' +
    '[ALL] roles={public} — anon has table SELECT and RLS is enabled, so by the SD own Bucket C rule it is excluded. ' +
    '(3) THE ADP RECURRENCE FIX IS UNPROVEN AND MAY BE A NO-OP ON THE PUBLIC AXIS: pg_default_acl for (postgres, public, ' +
    'functions) ALREADY omits PUBLIC, yet log_sd_mutation_audit — newest function in public (max oid 2939648), whose ' +
    'migration 20260802_sd_mutation_audit_trigger.sql contains ZERO GRANT statements — landed with =X/postgres (PUBLIC) ' +
    'AND anon. Clean natural experiment: the built-in acldefault() PUBLIC grant is ADDITIVE to, not replaced by, the ' +
    'pg_default_acl row (corroborated in schema governance, whose ADP row omits PUBLIC and postgres yet 14/15 of its ' +
    'functions carry both). So ALTER DEFAULT PRIVILEGES ... REVOKE ... FROM PUBLIC would produce a row shaped identically ' +
    'to what already exists. It cannot be settled read-only (exec_sql blocks DDL), so the migration MUST carry an ' +
    'in-transaction empirical self-test and the CI lint must be the LOAD-BEARING control, not a companion. There are also ' +
    'TWO creating-role ADP rows (postgres AND supabase_admin); the supabase_admin one is not reachable from a postgres ' +
    'migration. C5 partially FALSIFIED: get_daily_briefing/get_okr_metrics/get_portfolio_summary are prosecdef=FALSE ' +
    '(SECURITY INVOKER), not SECDEF — out of scope, not bucket entries. The other two C5 names triage cleanly: ' +
    'fn_anon_ingress_prior_hour_count -> Bucket C (it backs feedback::anon_feedback_ingress_bounds roles={public}, it IS ' +
    'the anon ingress bound), log_sd_mutation_audit -> Bucket A (trigger fn on strategic_directives_v2, 0 policies, 0 callers). ' +
    'C1 CONFIRMED and quantified: 14 of the 16 targets carry an explicit PUBLIC aclitem, so omitting PUBLIC from the REVOKE ' +
    'is a no-op for 14/16. C7 baseline capture PROVEN: emits 126 replayable EXECUTE grants across the 27 functions ' +
    '(PUBLIC 19 / anon 27 / authenticated 26 / postgres 27 / service_role 27). MIGRATION NOT APPLIED — @approved-by stays ' +
    'blank per the SD non-delegable chairman gate.',
  findings: [
    { id: 'true-residual-27', severity: 'info', note: 'LIVE (exec_sql REST, service_role, 2026-08-15): 139 SECURITY DEFINER fns in public, 27 anon-executable. Reconciles exactly: BucketA 6 + BucketB-live 10 + BucketC 9 + 2 new = 27, zero unaccounted. 19 of the 27 carry an explicit PUBLIC (=X/postgres) aclitem. Bucket B members already closed (17): advance_venture_stage, advance_venture_to_stage, bootstrap_venture_workflow, can_auto_advance, create_eva_conversation, delete_venture, eva_circuit_allows_request, export_blueprint_review, kill_venture, log_stage_advance_override, park_venture_decision, record_eva_failure, record_eva_success, rescan_stage_20, reset_eva_circuit, set_global_auto_proceed, set_stage_override.' },
    { id: 'defect-invoker-caller-fn_write_kill_audit_trail', severity: 'critical', note: 'NEW, beyond C1-C9. fn_write_kill_audit_trail(uuid,integer,text,uuid,text,uuid) is Bucket A but has a SECURITY INVOKER caller: fn_chairman_decide(uuid,text,text,text,boolean), prosecdef=false, anon_exec=true, auth_exec=true, body does PERFORM fn_write_kill_audit_trail(...). Invoker mode => inner call executes as the CALLING role => REVOKE ... FROM authenticated breaks fn_chairman_decide with "permission denied for function". VERDICT: move to Bucket B (REVOKE FROM PUBLIC, anon; KEEP authenticated). Whole-catalog invoker scan found this is the ONLY such instance among all 17 candidates. Secondary flag: fn_chairman_decide itself is anon-executable (SECURITY INVOKER, so RLS still applies) — recommend a separate follow-up, not in this SD scope.' },
    { id: 'c8-confirmed-is_chairman_role', severity: 'high', note: 'C8 CONFIRMED, exactly one instance across all 1590 policies. is_chairman_role() (Bucket B) is referenced by public.archetype_benchmarks::archetype_benchmarks_admin [ALL] roles={public} qual=is_chairman_role(). archetype_benchmarks has relrowsecurity=true and anon holds table-level SELECT; its only other policy is archetype_benchmarks_select [SELECT] roles={public} USING(true). roles={public} includes anon, so by the SD own Bucket C criterion ("referenced by an anon/PUBLIC-facing policy") is_chairman_role moves Bucket B -> Bucket C. Scan denominator: 1572 policies in schema public, 227 with roles={public}, 72 with roles={anon}. NOTE: the first regex instrument returned a FALSE ZERO (backslash mangling through JS->SQL->regex); re-run with a validated word-boundary matcher after confirming the known-positive fn_is_chairman=29.' },
    { id: 'c2-adp-unproven-possible-noop', severity: 'critical', note: 'C2 mechanism is real but the FIX AS SCOPED IS UNPROVEN AND MAY BE A NO-OP ON THE PUBLIC AXIS. Live pg_default_acl has TWO rows for (public, functions): (postgres) = postgres=X|anon=X|authenticated=X|service_role=X and (supabase_admin) = same shape. NEITHER carries a PUBLIC (=X) entry. Yet log_sd_mutation_audit — max oid 2939648 in public, migration database/chairman-gated/20260802_sd_mutation_audit_trigger.sql contains ZERO GRANT statements — landed with ACL "=X/postgres | postgres=X | anon=X | authenticated=X | service_role=X". A migration with no GRANT at all is a clean natural experiment: the built-in acldefault() PUBLIC=EXECUTE is ADDITIVE to, not replaced by, the pg_default_acl row. Corroborated in schema governance (ADP row = authenticated=X|service_role=X, omitting BOTH PUBLIC and postgres, yet 14/15 governance fns carry both =X and postgres=X). Consequence: ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC would yield a row shaped like the one already present, which demonstrably does not suppress PUBLIC. The FROM anon half should work (removes a positive grant genuinely in the stored row). Cannot be settled read-only: exec_sql rejects DDL so no probe function can be created. MANDATORY DESIGN RESPONSE: (a) the migration carries an in-transaction empirical self-test (CREATE a throwaway fn, read its proacl, assert no PUBLIC and no anon, DROP, RAISE EXCEPTION on failure); (b) the CI lint becomes the LOAD-BEARING control, not a companion; (c) document that the supabase_admin ADP row is unreachable from a postgres-run migration (requires membership in supabase_admin).' },
    { id: 'c5-partially-falsified', severity: 'high', note: 'C5 named 5 unbucketed functions. THREE ARE NOT SECURITY DEFINER: get_daily_briefing(), get_okr_metrics(), get_portfolio_summary() all have prosecdef=FALSE. They are anon-executable but SECURITY INVOKER (run as caller, RLS applies) — outside this SD SECDEF-EXECUTE scope. Do NOT bucket them; recommend a separate anon-read-surface review. The other two triage cleanly and ARE SECDEF: fn_anon_ingress_prior_hour_count(text) -> BUCKET C (backs public.feedback::anon_feedback_ingress_bounds [INSERT] roles={public}; it IS the anon ingress hourly bound, invoked as anon — revoking breaks anon feedback ingest; note it already has NO PUBLIC grant, its migration revoked that, so its anon grant is load-bearing by design); log_sd_mutation_audit() -> BUCKET A (returns trigger, attached to strategic_directives_v2::trg_sd_mutation_audit, 0 policy refs, 0 function-body callers, 0 app .rpc() sites).' },
    { id: 'c6-reconfirmed', severity: 'medium', note: 'C6 both placements re-confirmed with corrected reasoning. fn_stage_artifact_precondition(uuid,integer): 4 SECDEF callers (advance_venture_stage, advance_venture_to_stage, rescan_stage_20, fn_enforce_stage_advancement_artifact_gate) PLUS the direct .rpc() at scripts/harness/s20-fixture.mjs:219 — verified that file builds its client from SUPABASE_SERVICE_ROLE_KEY (lines 100-102), so service_role retains EXECUTE and the revoke is safe BY THAT MECHANISM, not because "called only by SECDEF fn". Record the real reason. fn_user_has_company_access(uuid): 1 SECDEF caller (fn_user_has_venture_access), 0 policies, 0 app .rpc() — safe today, but the revoke REVERSES the explicit 20260603_03 allowlist decision AND collides with scripts/lint/rls-anon-tenant-predicate-lint.mjs which names fn_user_has_company_access() as a recommended tenant-binding auth primitive in its unbound_tenant_predicate violation message. Must go to the chairman as a flagged reversal, not a bucket line item.' },
    { id: 'c1-confirmed-quantified', severity: 'high', note: 'C1 CONFIRMED and quantified. 14 of the 16 targets carry an explicit PUBLIC aclitem (=X/postgres): all 6 Bucket A (fn_enforce_stage_advancement_artifact_gate, fn_quick_fixes_validate_target_application, fn_stage_artifact_precondition, fn_user_has_company_access, fn_verify_and_consume_stepup_token, log_sd_mutation_audit) and 8 of 10 Bucket B (approve_chairman_decision, claim_sd, fn_is_service_role, fn_list_chairman_webauthn_credentials, fn_user_has_venture_access, reject_chairman_decision, upsert_operator_cash_burn, fn_write_kill_audit_trail). The 2 without PUBLIC: check_feedback_duplicate, get_gate_decision_status. Omitting PUBLIC from the FROM list makes the REVOKE a no-op for 14/16.' },
    { id: 'c7-baseline-proven', severity: 'info', note: 'C7 baseline capture designed and PROVEN to produce replayable restore statements: 126 EXECUTE grants across the 27 currently-anon-exposed SECDEF functions (grantee distribution PUBLIC 19 / anon 27 / authenticated 26 / postgres 27 / service_role 27). TWO implementation constraints found: (1) exec_sql keyword guard rejects any sql_text containing GRANT or REVOKE even inside a string literal in a SELECT, so the _DOWN generator MUST assemble statements client-side from raw aclitem rows; (2) oid::regprocedure::text DROPS the schema qualifier when public is in search_path — build signatures as public.<proname>(<pg_get_function_identity_arguments>) explicitly.' },
    { id: 'c3-verifier-extension-design', severity: 'high', note: 'EXTEND scripts/audit-rpc-execute-grants.mjs in place (116 lines; today it asserts ONLY has_function_privilege(authenticated,...) so it reports green whether this SD fix worked or not). Add sibling manifest scripts/audit-rpc-execute-grants-buckets.json keyed by exact public.<name>(<identity args>) with a REQUIRED reason per entry. Assertions: A1 Bucket A => NOT anon AND NOT authenticated AND no PUBLIC aclitem; A2 Bucket B => NOT anon AND no PUBLIC aclitem AND IS authenticated (this reuse is why extending beats a new file — it preserves the script original CREATE OR REPLACE grant-drift purpose); A3 Bucket C => ACL byte-identical to the recorded baseline; A4 COMPLETENESS GATE — every anon-executable SECDEF function in public must appear in the manifest, else FAIL "unbucketed SECDEF function" (this makes C5 floor-not-ceiling mechanical). ALSO FIX the silent-fallback defect: EHG_APP_SRC default resolves to .worktrees/ehg/src from a worktree, which does not exist, silently degrading to a STALE 24-name list missing 7 real Bucket B members (check_feedback_duplicate, claim_sd, fn_is_service_role, fn_list_chairman_webauthn_credentials, fn_user_has_venture_access, is_chairman_role, upsert_operator_cash_burn). Resolve from applications.local_path or explicit env and EXIT 1 when absent rather than falling back — a silent stale fallback is a green light over an unmeasured tier. Add an exec_sql REST fallback so the guard runs while the pooler credential is broken.' },
    { id: 'ci-lint-design', severity: 'high', note: 'Standing check, matching the house convention exactly (verified against 40+ existing lints): scripts/lint/secdef-execute-revoke-lint.mjs + scripts/lint/secdef-execute-revoke-allowlist.json (entries REQUIRE non-empty reason), npm script lint:secdef-execute-revoke, workflow .github/workflows/secdef-execute-revoke-lint.yml with a paths filter on database/**/*.sql plus the lint and allowlist files. Parses each migration for CREATE [OR REPLACE] FUNCTION <name>(...) carrying SECURITY DEFINER, and requires in the SAME file a REVOKE ... ON FUNCTION <same name> ... FROM <list containing PUBLIC>; PUBLIC missing from the FROM list is a violation even when anon/authenticated are present (that is the C1 no-op class, caught at authoring time). Exit 1 on violation. Support --diff (changed files, PR speed) and --all, matching schema-reference-lint.mjs. Export the pure scan function for a sibling .test.js (venture-artifacts-write-lint.test.js precedent) and guard the entrypoint with lib/utils/is-main-module.js. KNOWN BLIND SPOT, state it in the header: this is a TEXT lint over migration files, so it cannot see functions created outside migrations (Supabase dashboard SQL editor) — a different observability axis. That gap is covered by verifier assertion A4, which measures the LIVE catalog. Two enforcement sites on two axes: file-text pre-merge, live-catalog post-apply. Neither alone is sufficient.' },
    { id: 'sibling-migration-ordering', severity: 'medium', note: 'database/chairman-gated/20260815_venture_user_feedback_ownership_rpc.sql (SD-LEO-FIX-CLOSE-ANON-VENTURE-001) is NOT YET APPLIED — live measurement shows both functions it revokes, venture_exists_and_active(uuid) and check_feedback_rate_limit(uuid), are STILL anon-executable. Both are Bucket C here. Consequence for the design: the Bucket C invariant must be expressed as "this migration did not change Bucket C", asserted by comparing pre/post state WITHIN this migration own transaction — NOT against a file-time baseline, which would break depending on which chairman ceremony lands first. That formulation is order-independent.' },
    { id: 'template-provenance', severity: 'info', note: 'REVOKE/GRANT ordering taken verbatim from the two proven siblings. 20260611_guard_pack_secdef_fns.sql lines 798-814: a block of REVOKE EXECUTE ... FROM PUBLIC, anon (8 statements) FOLLOWED BY a block of GRANT EXECUTE ... TO service_role, authenticated (8 statements) — revokes first, grants restated after, so a FROM PUBLIC revoke can never strip an intended authenticated grant. 20260815_venture_user_feedback_ownership_rpc.sql line 328 and 357-358 add the Bucket-A shape: REVOKE ... FROM PUBLIC, anon, authenticated, and its TR-2 comment records the same reasoning C2 derives independently. That file also demonstrates the chairman-gated header convention: an explicit "AWAITING CHAIRMAN REVIEW — no @approved-by stamp exists for this file yet, deliberately" line.' },
  ],
  metadata: {
    design_informed: 'NO DESIGN sub-agent row exists for this SD (queried sub_agent_execution_results for sd_id ef96ac1a-69f1-4f57-8ba5-fcec84ad66d5: only STORIES/PLAN, EXPLORE/LEAD, RISK/LEAD, VALIDATION/LEAD x2). This DATABASE analysis ran INDEPENDENTLY, informed instead by the LEAD-phase VALIDATION (0332fe88-4e8d-4362-9c41-07c3fc96ac86) and RISK (e252eef9-9f7a-4180-81bb-d00f8ea470a6) corrections C1-C8 and the EXPLORE finding C9, all of which were re-verified live rather than trusted.',
    measurement_path: "supabase.rpc('exec_sql', { sql_text }) via REST with service-role key (pooler credential-broken). Guard notes: sql_text must be trimmed (leading newline => 'only allows SELECT/WITH'); has_function_privilege('PUBLIC',...) errors with 'role PUBLIC does not exist' — detect PUBLIC via an aclitem matching '=%X%'; any sql_text containing GRANT/REVOKE is rejected even inside a SELECT string literal.",
    measured_at: '2026-08-15',
    secdef_fns_in_public: 139,
    anon_executable_secdef_live: 27,
    with_explicit_public_grant: 19,
    chairman_ask_true_count: 16,
    bucket_a_final: ['fn_enforce_stage_advancement_artifact_gate()', 'fn_quick_fixes_validate_target_application()', 'fn_stage_artifact_precondition(uuid,integer)', 'fn_user_has_company_access(uuid)', 'fn_verify_and_consume_stepup_token(uuid,uuid)', 'log_sd_mutation_audit()'],
    bucket_b_final: ['approve_chairman_decision(uuid,text,text,approval_type_enum,uuid)', 'check_feedback_duplicate(uuid,text)', 'claim_sd(text,text,text,boolean,integer)', 'fn_is_service_role()', 'fn_list_chairman_webauthn_credentials()', 'fn_user_has_venture_access(uuid)', 'fn_write_kill_audit_trail(uuid,integer,text,uuid,text,uuid)', 'get_gate_decision_status(uuid,integer)', 'reject_chairman_decision(uuid,text,text,uuid)', 'upsert_operator_cash_burn(numeric,numeric,numeric,numeric)'],
    bucket_c_final: ['check_feedback_rate_limit(uuid)', 'fn_advance_pipeline_stage(uuid,text,text,uuid,uuid)', 'fn_anon_ingress_prior_hour_count(text)', 'fn_is_chairman()', 'fn_relay_insert_sms_candidate(text,text,text,text,text)', 'is_chairman_role()', 'is_leo_admin()', 'lhe_pending_migration_applied()', 'record_venture_error(uuid,text,text,jsonb)', 'set_session_working_context(text,jsonb)', 'venture_exists_and_active(uuid)'],
    bucket_moves_from_original: ['fn_write_kill_audit_trail: A -> B (SECURITY INVOKER caller fn_chairman_decide needs the authenticated grant)', 'is_chairman_role: B -> C (C8: archetype_benchmarks_admin policy roles={public})', 'log_sd_mutation_audit: unbucketed -> A (trigger fn, 0 policies, 0 callers)', 'fn_anon_ingress_prior_hour_count: unbucketed -> C (backs anon_feedback_ingress_bounds roles={public})', 'get_daily_briefing / get_okr_metrics / get_portfolio_summary: NOT SECDEF, out of scope entirely'],
    adp_rows_live: { 'postgres/public/functions': 'postgres=X|anon=X|authenticated=X|service_role=X (no PUBLIC entry)', 'supabase_admin/public/functions': 'postgres=X|anon=X|authenticated=X|service_role=X (no PUBLIC entry, unreachable from a postgres migration)' },
    adp_effectiveness: 'UNPROVEN — possibly a no-op on the PUBLIC axis. Requires an in-migration empirical self-test (create/inspect/drop a throwaway function).',
    rollback_baseline_stmts: 126,
    migration_applied: false,
    approved_by_stamp: 'DELIBERATELY BLANK — chairman-gated, permission changes non-delegable',
    proposed_files: ['database/chairman-gated/20260816_close_remaining_secdef_execute_exposure.sql', 'database/chairman-gated/20260816_close_remaining_secdef_execute_exposure_DOWN.sql', 'scripts/lint/secdef-execute-revoke-lint.mjs', 'scripts/lint/secdef-execute-revoke-allowlist.json', '.github/workflows/secdef-execute-revoke-lint.yml', 'scripts/audit-rpc-execute-grants-buckets.json (manifest, extends existing verifier)'],
  },
  execution_time_ms: 900000,
};

const resolution = await resolveSubAgentRepo({
  sdId: SD_ID,
  subAgentCode: 'DATABASE',
  targetApplication: 'EHG_Engineer',
});
applySubAgentRepoVerdict(results, resolution);

const stored = await storeSubAgentResults('DATABASE', SD_ID, { name: 'Principal Database Architect' }, results, { phase: PHASE });
console.log('STORED_VERDICT=' + results.verdict);
console.log('STORED_ROW_ID=' + (stored?.id || stored?.data?.id || JSON.stringify(stored)));
console.log('STORED_PHASE=' + (stored?.phase || 'n/a'));
console.log('STORED_SD_ID=' + (stored?.sd_id || 'n/a'));
console.log('REPO_PATH=' + results.metadata.repo_path);
console.log('EXECUTED_FROM_CWD=' + results.metadata.executed_from_cwd);
console.log('REPO_RESOLVED=' + results.metadata.repo_resolved);
