import dotenv from 'dotenv'; dotenv.config();
import { createClient } from '@supabase/supabase-js';
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';

const SD_UUID = '3f128d5c-8168-4415-86cc-ab5da4663d11';
const SD_KEY = 'SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D';
const supabase = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

const { data: sa } = await supabase.from('leo_sub_agents').select('*').eq('code', 'DATABASE').maybeSingle();

const findings = [
  { id: 'DB-D1', severity: 'info',
    title: 'NO MIGRATION NEEDED and NO COLUMN MISSING - verified column by column against the 466-line migration',
    detail: "Every field each FR of child D writes or reads is declared in database/migrations/20260906_michael_tables.sql with the correct type, nullability and CHECK. michael_feeder_runs: feeder/et_date/attempt/venue/status/counts/log_md/started_at/finished_at/model_used/tokens_in/tokens_out all present (venue CHECK IN task_scheduler,gha,seat at line 156 ADMITS 'seat'; status CHECK IN ok,degraded,failed,skipped,imported covers all four FR-1(g) values; counts JSONB NOT NULL CHECK jsonb_typeof='object'). michael_calendar_day: all 11 FR-3 columns present. michael_gmail_triage_items: class/action_intent/action_taken_at/needs_you/needs_you_reason/borderline/rule_key/verified_by/summary/last_message_id/reopened_at all present. michael_todoist_snapshot: effort_grade/est_minutes/role_tag/proposed_date/proposed_action/chosen_action/rule_key/mutations_applied (array CHECK line 248)/moved_back_at all present. michael_gmail_labels: label_id/name/class/keep_in_inbox/summarize/last_seen_in_gmail_at all present. michael_staged_items: kind/payload present, and kind carries NO CHECK constraint so FR-6's new kinds task_route and tasks_cleanup need no child-B change. michael_rules (read-only for D): domain/rule_key/rule_json/status/auto_apply/auto_apply_verb/provenance all present, verb CHECK admits both label and reschedule. Child D ships zero DDL and none is required." },
  { id: 'DB-D2', severity: 'critical',
    title: "FR-9's seat feeder_runs upsert target (et_date, feeder) matches NO unique index - 42P10, zero rows written",
    detail: "FR-9 specifies the seat run row as an upsert 'for (feeder seat-classify, et_date)'. The only unique index is michael_feeder_runs_date_feeder_attempt_uniq on (et_date, feeder, attempt). PostgREST forwards on_conflict verbatim and Postgres raises 42P10 ('no unique or exclusion constraint matching the ON CONFLICT specification') writing ZERO rows. In-repo precedent with a real outage: lib/eva/stage-handlers/s11.js:344 - 'Using venture_id alone threw Postgres 42P10 and wrote 0 rows, which - combined with the swallowed error below - left every new venture without a GVOS profile and blanked Stage 17 (RCA 2026-05-22)'. The compounding half applies here too: writeRows REFUSES rather than throws, so an unchecked w.ok in classify-apply reproduces the silent failure. FIX (no DDL): pin attempt and use the full key - t.upsert(seatRow, { onConflict: 'et_date,feeder,attempt' }) with attempt:1 explicit, and always branch on w.ok." },
  { id: 'DB-D3', severity: 'high',
    title: "23505 IS what PostgREST surfaces, but writeRows does NOT let the caller see it - FR-1(e) is unimplementable as written",
    detail: "MEASURED from the installed client, not folklore: node_modules/@supabase/postgrest-js 2.103.0 documents the upsert failure body inline (~line 4255) as code 23505 / 'duplicate key value violates unique constraint' / HTTP 409. The repo branches on error.code==='23505' at ~30 sites including two genuine unique-index races (lib/eva/qa/stitch-vision-qa.js:363, lib/eva/stage-execution-worker.js:4585) and the closest analogue to FR-1(e), lib/eva/chairman-product-review.js:330 (mints the next attempt_number explicitly because defaulting to 1 'would 23505-collide'). BUT lib/michael/db.mjs:70 collapses the PostgrestError to a string: error.code is consulted ONLY when error.message is empty, and a 23505 always has a message. The caller receives refusal:'WRITE_FAILED' and a message string with NO code field. FIX: additive one-line passthrough `code: error.code ?? null, details: error.details ?? null` in both WRITE_FAILED branches - backward compatible, verified none of the 11 writeRows call sites reads a `code` key." },
  { id: 'DB-D3b', severity: 'high',
    title: 'Exact single-flight + attempt max+1 + 23505 retry shape, with four design corrections to FR-1',
    detail: "Proposed: one bounded read (.eq(et_date).eq(feeder).order(attempt desc), select 'attempt,status,started_at,finished_at') serves all three predicates - already_ok (any row status='ok'), in_flight (started_at set AND finished_at null AND started_at >= now-10min), and attempt=max+1. Then INSERT (not upsert) and `if (isUniqueViolation(res)) continue` for one retry, where isUniqueViolation checks res.code==='23505' OR /duplicate key value|already exists/i on the message (belt-and-braces, mirroring stitch-vision-qa.js:363). FOUR CORRECTIONS: (1) INSERT not upsert - an upsert on the full key would MERGE into the concurrent fire's row and defeat single-flight entirely; the unique index must arbitrate. (2) The 10-minute fence must be an ISO STRING, never a Date object - the QF-20260906-599 not_before-as-object defect one field over degrades the fence to a no-op and every fire reads as free. (3) status is NOT NULL with a CHECK and there is NO 'running' member, so the start row must claim 'degraded' (honest: a killed run reads as retry-eligible); claiming 'ok' would make the next fire inert with already_ok. (4) Exhausting the retry is INERT, not failed - returning failed writes a false failure into the quiet tick's failed-run count. Precedent in-tree: scripts/michael/retention.mjs:83-90 already does the max+1 read (no retry) and writes started_at+finished_at in one insert, so retention can never trip D's single-flight fence." },
  { id: 'DB-D4', severity: 'high',
    title: "PostgREST cannot express ON CONFLICT DO UPDATE ... WHERE - three 'never overwrite' acceptance criteria cannot be upserts",
    detail: "FR-4 ('does not overwrite a row whose action_taken_at is set'), FR-7 ('never overwriting a row whose chosen_action or moved_back_at is set') and FR-9 ('without touching rows whose action_taken_at or chosen_action is set') all depend on a conditional DO UPDATE, which has no PostgREST representation; a plain upsert overwrites unconditionally. TWO SHAPES cover every case. (A) Feeder path where rows may not exist: .upsert(rows, { onConflict:'et_date,thread_id', ignoreDuplicates:true }) to create-if-absent (VERIFIED present: postgrest-js 2.103.0 line 4266 emits Prefer: resolution=ignore-duplicates = ON CONFLICT DO NOTHING), then a separate .update({...}).eq(et_date).eq(thread_id).is('action_taken_at', null). (B) Recorder path where rows already exist queued: a plain .update({...}).eq(...).is('chosen_action', null).is('moved_back_at', null). The .is(col,null) filter makes the guard a SERVER-SIDE predicate rather than a read-then-write race." },
  { id: 'DB-D5', severity: 'high',
    title: 'Bulk upsert writes NULL into columns absent from SOME rows - the chairman label config is the exposed case',
    detail: "READ from the installed client at postgrest-js 2.103.0 line 4269: for an array payload the client sets the `columns` query param to the UNION of every row's keys, and by default (defaultToNull:true, no Prefer: missing=default) a row lacking a key in that union is written NULL. On the merge path that union is also the DO UPDATE SET list, so an existing value is overwritten with NULL. Highest-consequence instance: FR-4's label reconcile - if some label rows carry keep_in_inbox and others do not, the chairman's keep_in_inbox/summarize configuration is silently reset on the rows that omit them. TWO RULES for every bulk write in D: (1) build every row object in a batch with an IDENTICAL key set; (2) NEVER include a column the writer does not own (keep_in_inbox/summarize/class on labels; action_taken_at/verified_by on items; chosen_action/moved_back_at on tasks) - a column absent from ALL rows is absent from the columns list and is therefore preserved." },
  { id: 'DB-D6', severity: 'medium',
    title: 'michael_staged_items has no natural key, so FR-6 stages duplicates on every 15-minute fire',
    detail: "The only index is the partial (kind) WHERE dispositioned_at IS NULL; there is NO unique constraint, so there is no upsert target and insert is the only shape (as autonomy-read.mjs:117 and capture.mjs:24 do). FR-6 stages unrouted items AND the cleanup list on every run, and tasks-classifier fires every 15 minutes inside a 45-minute window - up to three duplicate stage rows per item per day, growing the seat's disposition queue with noise. FIX in code, no DDL: before inserting, read open rows of that kind through the partial index (.eq('kind','task_route').is('dispositioned_at',null), bounded), hash each payload with the existing canonicalJson + sha256Hex helpers already exported from lib/michael/db.mjs, and insert only unseen hashes. Store the hash inside payload (payload.dedupe_sha256) so the check is a string compare." },
  { id: 'DB-D7', severity: 'medium',
    title: 'michael_calendar_day natural key omits calendar_id - the two calendars collide on a shared invitation',
    detail: "FR-3 upserts events from 'primary' and MICHAEL_EXELON_CALENDAR_ID in ONE run, keyed (et_date, event_id). Google Calendar propagates the same event id to a guest's copy of an invitation, so an event present on both calendars collides on the key and the second upsert overwrites the first row's calendar_id - last-write-wins, attribution lost, NO error raised. This is NOT a missing column and must NOT become child-B DDL. FIX in code: dedupe in memory before the upsert preferring 'primary', and record counts.cross_calendar_collisions so the frequency is visible. If collisions prove routine and attribution matters, that is a future child-B key change, never a D change." },
  { id: 'DB-D8', severity: 'medium',
    title: "Child B's DO $verify$ v_columns gap - same class as child C's key_fingerprint, ROUTE TO B, no schema change",
    detail: "The verify block pins 19 column/type triples. It DOES cover every natural-key column D depends on TRANSITIVELY, because all 14 index assertions are present and an index cannot exist without its columns (date_feeder_attempt_uniq proves feeder+attempt, labels_label_id_uniq proves label_id, date_thread/date_task prove thread_id/task_id, the two partial rule_key indexes prove rule_key). UNASSERTED by both v_columns and any index, yet relied on by D: feeder_runs venue/counts/log_md/started_at/finished_at/model_used/tokens_in/tokens_out; calendar_day title/starts_at/ends_at/all_day/response_status/coded_marker/optional/overlap_group; triage_items action_intent/action_taken_at/needs_you/needs_you_reason/borderline/verified_by/summary/last_message_id; todoist_snapshot est_minutes/role_tag/proposed_date/proposed_action/chosen_action/mutations_applied; gmail_labels name/class/keep_in_inbox/summarize/last_seen_in_gmail_at; staged_items payload; rules domain/rule_json/status/auto_apply/auto_apply_verb. BLAST RADIUS IS BOUNDED: the assertions only matter when a wrong-shaped table pre-exists and CREATE TABLE IF NOT EXISTS no-ops, and I MEASURED all eleven tables absent live, so the first apply creates them from this exact DDL. Recommend B add the seven whose absence would break D SILENTLY rather than loudly: feeder_runs.venue, .counts, .model_used, .tokens_in, .tokens_out, triage_items.action_taken_at, todoist_snapshot.mutations_applied." },
  { id: 'DB-D9', severity: 'info',
    title: 'No new index is needed - reasoning per read with the size bound stated',
    detail: "Today's runs per feeder (et_date+feeder equality, order by attempt): (et_date, feeder) is a LEADING PREFIX of michael_feeder_runs_date_feeder_attempt_uniq - index scan, single-digit rows; the migration comment at line 168 says et_date leads deliberately for this. Yearly growth ~5 feeders x 4 attempts x 365 = 7k rows. Quiet-tick failed-run count (et_date + status='failed'): index scan on leading et_date then filter. Queue reads (et_date + class IS NULL / effort_grade IS NULL): et_date leads both daily uniques. THE ONE UNCOVERED READ is FR-5's borderline resurfacing, which looks up prior rows by thread_id ACROSS et_dates - thread_id is the trailing column so the index cannot serve it - but it is a seq scan over a table bounded by 30-day retention at ~400 rows/day = ~12k rows, single-digit ms. DO NOT add an index: it would be premature AND would require child-B DDL." },
  { id: 'DB-D13', severity: 'medium',
    title: 'The 500-row literal read cap has only 20% headroom on the gmail day, with no truncation signal',
    detail: "readRows hard-codes .limit(500) (lib/michael/db.mjs:50) with a comment naming count-truncation-diff-lint as the reader of that literal, so every D read through readRows is compliant BY CONSTRUCTION and callers write no .select( chain of their own. Any NEW direct .select() in scripts/michael/*.mjs that bypasses readRows must carry its own literal limit or count:'exact' or it BLOCKS the PR. HEADROOM: FR-4 runs two queries at maxResults 200, so up to 400 triage rows land per ET date against a 500 cap with NO truncation signal (readRows returns rows silently). Two cheap mitigations: narrow the guard read to .eq('et_date',d).in('thread_id',batchIds).not('action_taken_at','is',null) instead of reading the whole day; and in queue-read.mjs emit counts.truncated = (rows.length === 500)." },
  { id: 'DB-D10', severity: 'info',
    title: 'A head-count probe cannot distinguish ABSENT from EMPTY - measured during this review',
    detail: "MEASURED: sb.from(t).select('*', { count:'exact', head:true }) on an ABSENT table returns error:null, count:null - it reads as PRESENT. My first probe of all eleven michael_* tables reported 'PRESENT rows=null' for every one; a body read (.select('*').limit(1)) correctly returned PGRST205 for all eleven, with strategic_directives_v2 as a positive control (count=6149) and michael_nonexistent_xyz as a negative control (identical null signature). Already known and handled in-tree: scripts/michael-quiet-tick.mjs:83 comments 'A null count with no error is the missing-relation signature on a client that does not throw'. CONSEQUENCE FOR D: readRows does a BODY read and is safe, so FR-1(c)'s tables_absent pre-flight is correct as designed - but queue-read.mjs must go through readRows, never a head count, or its tables_absent branch will never fire." },
  { id: 'DB-D11', severity: 'info',
    title: 'michael_rules must never be upserted through PostgREST (partial unique index) - D reads only, recorded as a boundary marker',
    detail: "michael_rules_active_domain_key_uniq is PARTIAL: (domain, rule_key) WHERE status='active'. Postgres requires the predicate to be repeated in the ON CONFLICT clause and PostgREST's on_conflict parameter CANNOT express it, so the attempt raises 42P10. That is why scripts/michael/rule-encode.mjs:146-150 uses a guarded UPDATE followed by an INSERT rather than an upsert. Child D reads michael_rules only - CONFIRMED: FR-7's todoist-brief reads active auto_apply rows and writes only michael_todoist_snapshot, and rules-match is three PURE functions over rule_json (matchGmailRule, matchKeywordBuckets, roleTagFor) with no client and no write. The two auto_apply CHECKs (verb+since not null; provenance->verifier->>subject_hash not null) are INSERT/UPDATE-time constraints invisible to a reader. Recorded so a later PR does not 'simplify' rule-encode into an upsert. One matcher note: rule_json is JSONB NULL, so a rule row with a null rule_json must return null from the matcher, never a vacuous matched:true." },
];

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 92,
  summary: "NO MIGRATION IS NEEDED AND NO COLUMN IS MISSING. Verified column by column against all 466 lines of database/migrations/20260906_michael_tables.sql: every field each FR of child D writes or reads exists with the correct type, nullability and CHECK, and all five upsert conflict targets the PRD names ('et_date,event_id', 'et_date,thread_id', 'et_date,task_id', 'label_id', 'et_date,feeder,attempt') are backed by a matching unique index. Specifically confirmed: the feeder_runs venue CHECK ADMITS 'seat' (line 156); model_used/tokens_in/tokens_out occur at exactly three places in the whole file (lines 162-164), all inside michael_feeder_runs; counts has the jsonb object CHECK and mutations_applied the jsonb array CHECK; staged_items.kind carries NO CHECK so FR-6's new kinds need no B change; the michael_rules verb CHECK admits both of FR-7's verbs and todoist-brief needs no write to that table. Child D ships zero DDL and none is required. CONDITIONAL, not PASS, because FIVE code-shape defects in the PRD's own write design would fail at the first live run after the chairman applies the migration, all fixable inside D with no schema change: (DB-D2 CRITICAL) FR-9's seat run-row conflict target (et_date, feeder) matches no unique index and raises 42P10 writing zero rows - the exact defect that blanked Stage 17 per s11.js:344; (DB-D3 HIGH) 23505 IS what PostgREST surfaces, measured from the installed postgrest-js 2.103.0 and confirmed by ~30 in-repo call sites, but writeRows collapses the PostgrestError to a string so the caller NEVER sees the code, making FR-1(e)'s retry unimplementable through the shared seam without a message regex; (DB-D4 HIGH) PostgREST cannot express a conditional DO UPDATE, so the three 'never overwrite a row whose action_taken_at/chosen_action/moved_back_at is set' criteria cannot be upserts; (DB-D5 HIGH) bulk upsert builds its column list as the UNION of all row keys and NULLs the gaps, which would silently reset the chairman's keep_in_inbox/summarize label config; (DB-D6 MEDIUM) staged_items has no natural key so FR-6 duplicates its staging on every 15-minute fire. Also measured live: all eleven michael_* tables are ABSENT (PGRST205) against dedlbzhpgkmetvhbkyzq, with a positive and a negative control, which bounds the child-B verify-block column gap (DB-D8, routed to B, same class as child C's key_fingerprint) to latent. No new index is needed anywhere (DB-D9). No DDL was executed and no michael_* row was written.",
  critical_issues: [
    "DB-D2: FR-9's seat michael_feeder_runs upsert target (et_date, feeder) matches NO unique index - Postgres raises 42P10 and writes ZERO rows, and because writeRows refuses rather than throws, an unchecked w.ok makes it silent. Use onConflict 'et_date,feeder,attempt' with attempt:1 explicit, and branch on w.ok. Precedent: lib/eva/stage-handlers/s11.js:344 (blanked Stage 17 for every new venture, RCA 2026-05-22).",
  ],
  warnings: [
    "DB-D3: FR-1(e)'s '23505 retry' cannot be implemented through lib/michael/db.mjs as it stands. 23505 IS the code PostgREST surfaces (verified in postgrest-js 2.103.0's own upsert docstring and ~30 in-repo call sites), but writeRows:70 uses error.code ONLY when error.message is empty, and a 23505 always has a message. Add `code: error.code ?? null` to both WRITE_FAILED branches (backward compatible - no existing caller reads a code key) or restate the FR in terms of the message predicate.",
    "DB-D4: The three 'never overwrite a row whose X is set' acceptance criteria (FR-4 action_taken_at, FR-7 chosen_action/moved_back_at, FR-9 both) cannot be expressed as an upsert - PostgREST has no ON CONFLICT DO UPDATE ... WHERE. Use upsert with ignoreDuplicates:true to create-if-absent, then a separate .update(...).is('action_taken_at', null) so the guard is a server-side predicate rather than a read-then-write race.",
    "DB-D5: A bulk upsert sets the `columns` param to the UNION of every row's keys and NULLs the gaps (postgrest-js 2.103.0 line 4269, defaultToNull:true). Heterogeneous row objects would silently reset the chairman's keep_in_inbox/summarize on the label rows that omit them. Build identical key sets per batch and never include a column the writer does not own.",
    "DB-D6: michael_staged_items has NO unique constraint, so FR-6's staging is insert-only and duplicates on every 15-minute fire - up to three rows per item per day into the seat's disposition queue. Dedupe by hashing payload with the existing canonicalJson + sha256Hex helpers before insert.",
    "DB-D7: michael_calendar_day's natural key (et_date, event_id) omits calendar_id, and FR-3 writes from two calendars in one run. A shared invitation carries the same event id on both, so the second upsert overwrites the first row's calendar_id silently. Dedupe in memory preferring 'primary' and count the collisions - do NOT change the key (that would be child-B DDL).",
    "DB-D8 (route to child B): the DO $verify$ v_columns array leaves feeder_runs venue/counts/model_used/tokens_in/tokens_out, triage_items action_taken_at and todoist_snapshot mutations_applied unasserted - the same gap class already recorded for child C's key_fingerprint. Bounded to latent because all eleven tables are absent live. Child D must NOT close this with DDL.",
    "DB-D13: the 500-row literal read cap has ~20% headroom on the gmail day (2 queries x maxResults 200 = up to 400 rows/ET date) with no truncation signal. Narrow the guard read with .in('thread_id', batchIds) and emit counts.truncated when rows.length === 500.",
  ],
  recommendations: [
    "PROCEED with child D as scoped, with no migration and no DDL. State FR-10's no-DDL claim as MEASURED: every column is declared, all five conflict targets have a backing unique index, and the only schema-adjacent finding (DB-D8) is a verify-block hardening owned by child B.",
    "PLAN: fold DB-D2 (onConflict 'et_date,feeder,attempt'), DB-D4 (upsert-ignoreDuplicates then guarded update) and DB-D5 (uniform key sets; never write a column you do not own) into the FR/TR text before EXEC. These are three one-line-per-site changes that each fail loudly-or-silently at first live run if left.",
    "EXEC PR 1: land DB-D3's `code`/`details` passthrough in lib/michael/db.mjs writeRows alongside the harness, and implement the retry with isUniqueViolation checking BOTH res.code === '23505' and the message regex (the stitch-vision-qa.js:363 belt-and-braces shape).",
    "EXEC PR 1: the single-flight start row must INSERT (never upsert - an upsert merges into the concurrent fire's row and defeats single-flight), must claim status 'degraded' (there is no 'running' member in the CHECK and 'ok' would make the next fire inert), must compare the 10-minute fence as an ISO STRING (a Date object degrades the fence to a no-op, the QF-20260906-599 class), and must return INERT rather than failed when the retry is exhausted (a false failure pollutes the quiet tick's failed-run count).",
    "EXEC PR 3 and PR 5: add the DB-D7 in-memory calendar dedupe with counts.cross_calendar_collisions, and the DB-D6 payload-hash dedupe before staging.",
    "Route DB-D8 to child B as a verify-block hardening (add the seven silent-failure columns to v_columns). Child D must not edit database/migrations/20260906_michael_tables.sql - FR-10's `git diff --stat` criterion is the right guard and should stay verbatim.",
    "Do NOT add any index. The natural keys cover every hot read (et_date leads both the feeder-runs and the daily-table uniques), and FR-5's only uncovered read is a seq scan over ~12k retention-bounded rows.",
    "queue-read.mjs must read through readRows, never a head count - a count:'exact' head request on an absent table returns error:null and count:null, which reads as PRESENT (measured this session; already handled in the quiet tick at scripts/michael-quiet-tick.mjs:83).",
  ],
  metrics: {
    migration_lines_reviewed: 466,
    tables_in_migration: 11,
    tables_child_d_writes: 6,
    tables_child_d_reads_only: 1,
    columns_verified: 58,
    columns_missing: 0,
    migrations_required: 0,
    ddl_statements_required: 0,
    upsert_conflict_targets_verified: 5,
    upsert_conflict_targets_backed_by_unique_index: 5,
    upsert_conflict_targets_specified_wrong_in_prd: 1,
    indexes_required: 0,
    check_constraints_verified: 12,
    live_tables_present: 0,
    live_tables_absent: 11,
    critical_findings: 1,
    high_findings: 4,
    medium_findings: 4,
    routed_to_child_b: 1,
  },
  findings,
  metadata: {
    sd_key: SD_KEY,
    sd_uuid: SD_UUID,
    prd_id: 'PRD-SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D',
    phase: 'PLAN',
    branch: 'feat/SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D',
    head_commit: 'daf75d6dfd76de465642187810c460ac767f4680',
    scope_assessment: 'CONFIRMED_NO_SCHEMA_CHANGE',
    report_path: 'C:/Users/rickf/AppData/Local/Temp/claude/C--Users-rickf-Projects--EHG-EHG-Engineer/85c82b18-0984-4948-bd86-1992cdf5170d/scratchpad/database-plan-d-report.md',
    verification_method: 'Full read of the 466-line migration; read of lib/michael/db.mjs, lib/michael/rules.mjs, scripts/michael/retention.mjs, scripts/michael-quiet-tick.mjs and all 11 writeRows call sites; read of the installed @supabase/postgrest-js 2.103.0 upsert implementation and docstring for the 23505 surface, the columns-union NULL behaviour and ignoreDuplicates; repo grep for 23505 and 42P10 precedents; live PostgREST presence probe of all 11 michael_* tables against dedlbzhpgkmetvhbkyzq with a positive control (strategic_directives_v2) and a negative control (michael_nonexistent_xyz).',
    live_measurement: 'All 11 michael_* tables ABSENT (PGRST205) as of 2026-09-06. No DDL executed; no michael_* row written (they do not exist).',
    prospective_caveat: 'EXEC has not authored any of the eight PRs yet, so this reviews the PRD design against the migration source, not a shipped diff. The no-DDL verdict rests on the measured column-by-column match plus the five conflict targets, which is stronger than the PRD merely declaring no DDL. The five code-shape conditions are prospective and must be re-verified by TESTING at EXEC.',
  },
  validation_mode: 'prospective',
  phase: 'PLAN',
};

const resolution = await resolveSubAgentRepo({
  sdId: SD_UUID,
  targetApplication: 'EHG_Engineer',
  subAgentCode: 'DATABASE',
  probeExistsRelative: 'package.json',
  supabase,
});
console.log('repo resolution =>', JSON.stringify(resolution));
applySubAgentRepoVerdict(results, resolution);
console.log('verdict after repo verdict =>', results.verdict, '| confidence', results.confidence);

const stored = await storeSubAgentResults('DATABASE', SD_UUID, sa, results, { phase: 'PLAN', sdKey: SD_KEY });
console.log('STORED =>', JSON.stringify(stored)?.slice(0, 1200));
