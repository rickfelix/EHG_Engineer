const { spawnSync } = require('child_process');
const title = "The worker check-in QF self-claim loop emits no per-candidate trace, so a silently refused claim (tryClaim !ok) leaves no error string anywhere: QF-20260911-117 was first-eligible in two seats' candidate lists yet skipped twice with nothing to read after the fact - log verdict, work-class reason, moot result and tryClaim ok/error per candidate";
const description = [
 "Sourced from the coordinator (lane row ce4f2829, corr a707217e, 00:31Z) closing the QF-117 measurement, accepted for Adam's belt. MEASURED (coordinator, audit_log priority_shadow rows + Golf's status reply b1d44766): Alpha-3's EXPLICIT claim of QF-20260911-117 succeeded at 00:13Z (claim_sd accepted) while two free seats' check-in auto-claim (Golf 23:55Z, Alpha 00:02Z) skipped it although 117 was first-eligible in both candidate lists; tryClaim(117) returned !ok silently and the loop continued to a stale-verify medium each time. Adam's own measurements the same night: getQfPickerVerdict says 117 eligible:true; the candidate window (worker-checkin.cjs :714-735, cap 100) held 59 rows with 117 at rank 53; QF_SEVERITY_RANK puts high above medium. Five misses in one evening, mechanism named but unattributable because nothing is written.",
 "MECHANISM (read at the write site by the coordinator, cited): scripts/worker-checkin.cjs :756-800, the QF self-claim path, emits NO per-candidate trace - the SD belt has belt_ranked_claimable and belt_ineligibility_breakdown in the check-in JSON and the audit_log priority_shadow row, the QF path has nothing; claim_sd's refusal is returned to the caller and dropped. So neither the worker (from its own output) nor the coordinator (from the DB after the fact) can answer 'why not 117'.",
 "FIX SHAPE (tier 1, additive logging only): for each QF candidate the loop touches, append { qf_id, severity, picker_verdict, work_class_reason, withheld_reason, tryClaim: { ok, error_code, error_message } } to (a) the check-in JSON under qf_candidates_trace and (b) the audit_log priority_shadow row's payload; cap at the first 10 candidates to bound size; never change ordering or claim behaviour. TESTS: fixture loop where the first candidate's tryClaim returns an error string -> the trace carries that string and the second candidate's ok:true; JSON shape asserted; existing directed-assignment and severity-sort tests unchanged.",
 "Source: coordinator finding, Adam 49eabb23 mint (CONST-002). Related: QF-20260911-004 (reservation fold, the cure for the message-vs-row half), QF-20260911-285 (hint reader columns), the 09-06 dedicated-seat recurrence (board item b469ba9c, folded into 004 tonight)."
].join(' ');
const r = spawnSync('node', ['scripts/create-quick-fix.js', '--title', title, '--type', 'bug', '--severity', 'medium', '--description', description, '--target-application', 'ehg_engineer', '--created-by', 'adam-49eabb23'], { encoding: 'utf8', env: process.env });
const out = (r.stdout||'') + (r.stderr||''); const m = out.match(/QF-\d{8}-\d{3}/); console.log('KEY', m ? m[0] : 'NONE');
console.log(out.split('\n').filter(l => /Status:|routing_tier|error|Error|refus/.test(l) && !/injected env/.test(l)).slice(0,4).join('\n'));
if (m) { const b = spawnSync('node', ['C:/Users/rickf/Projects/_EHG/EHG_Engineer/.artifacts/adam-bind-one-49eabb23.mjs', m[0], m[0] + " (Tier 1, medium): worker check-in QF self-claim loop has no per-candidate trace; silent tryClaim refusals are unattributable (coordinator finding ce4f2829, the 117 five-miss night)"], { encoding: 'utf8', env: process.env, cwd: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer' }); console.log((b.stdout||'').split('\n').filter(l=>/BOUND/.test(l)).join('\n') || ('BIND FAIL ' + (b.stderr||'').slice(0,200))); }
