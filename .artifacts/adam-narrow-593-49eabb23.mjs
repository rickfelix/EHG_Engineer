import 'dotenv/config';
import { createClient } from '@supabase/supabase-js';
import { createOrUpsertNode } from '../lib/adam/task-ledger.js';
const s = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
const key = 'QF-20260911-593';
const title = "parent-completion reads the NEWEST PLAN-TO-LEAD row instead of the newest ACCEPTED one, so a worker's manual retries that fail a gate bury the auto-accepted row and orchestrator completion stops at 'manual remediation' (DURABILITY-AUDIT-001, 3 blocked rows after the 23:53Z accepted row)";
const { data: cur } = await s.from('quick_fixes').select('description').eq('id', key).single();
const description = "NARROWED 2026-09-12 01:3xZ (Adam 49eabb23): the CHILD_SCOPE_COVERAGE boilerplate-names half of this ticket is now OWNED BY QF-20260911-793 (Alpha's filing, high, tier 2, claimed and in execution by Bravo 45924f8d since 01:10Z; Alpha RCA d42c260b numbers live there). This ticket keeps ONLY the second reader defect, which 793 does not cover. DEFECT: lib/fleet/parent-completion.mjs ~93-97 selects PLAN-TO-LEAD handoff rows .order('created_at', desc).limit(1) with no status filter, while its own header (~14-28) says it should skip straight to completion when the latest PLAN-TO-LEAD is already 'accepted'. SPECIMEN: SD-LEO-ORCH-CAPA-DURABILITY-AUDIT-001 auto-accepted PLAN-TO-LEAD at 23:53:49Z (ORCHESTRATOR_ALL_CHILDREN_TERMINAL); three manual retries 00:18-00:20Z failed CHILD_SCOPE_COVERAGE and became the newest rows, so the completion path read 'blocked' and stopped at manual remediation until the coordinator approved a one-gate bypass. FIX SHAPE (tier 1): read the newest ACCEPTED PLAN-TO-LEAD (status='accepted', order created_at desc, limit 1) and only if none exists fall back to the newest row for the blocked-retrigger logic; keep the sdKey echo check. TESTS: history [accepted, blocked, blocked, blocked] -> proceeds to completion without a new PLAN-TO-LEAD; history [blocked] only -> retrigger path unchanged; history [] -> unchanged.\n\n--- original text (superseded by the narrowing above; gate half now QF-20260911-793) ---\n" + String(cur.description);
const { error } = await s.from('quick_fixes').update({ title, description }).eq('id', key);
console.log(error ? 'ERR ' + error.message : 'NARROWED ' + key);
const n = await createOrUpsertNode(s, { source_kind: 'sourced_sd', source_ref: key, tier: 'child', parent_id: '45ee754a-59ab-46dd-81da-9f39f4b3c282', title: key + " (Tier 1, NARROWED): parent-completion reads newest PLAN-TO-LEAD row, not newest ACCEPTED — gate half moved to QF-20260911-793 (Bravo executing)" });
console.log('BOARD', n && n.id);
