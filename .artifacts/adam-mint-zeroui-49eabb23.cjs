const { spawnSync } = require('child_process');
const title = "The TESTING sub-agent's zero-UI-by-diff exemption fails closed whenever the diff range cannot be resolved, so a genuinely zero-UI SD falls through to the mandatory E2E flow and is BLOCKED: three times in one session (SESSION-COORDINATION-INSERT-001, ADAM-DURABLE-DUTY-001, FOUR-GATES-RETURNED-001) - make the zero-UI detection robust and route to a unit/integration-only evidence path";
const description = [
 "Sourced from Alpha's completion flag 0911a58f (SD-LEO-FIX-FOUR-GATES-RETURNED-001, 23:41Z) relayed by the coordinator (004a01c8, 587cd31f), accepted for Adam's belt. Flag text, verbatim: 'The generic TESTING sub-agent's E2E-mandatory path blocked unconditionally on this zero-UI-surface SD for a THIRD time this session (after SD-LEO-FIX-SESSION-COORDINATION-INSERT-001 and SD-LEO-FIX-ADAM-DURABLE-DUTY-001). A durable pattern now measured three times independently -- worth a dedicated fix to auto-detect zero-UI-surface diffs and route to a unit/integration-only evidence path.' MEASURED 01:0xZ: sub_agent_execution_results has TESTING rows BLOCKED at 21:26Z (a874f8c4) and 23:33Z (d78cef4e) tonight with empty summary and no sd_key in metadata (the same evidence-row emptiness class as QF-20260911-644).",
 "MECHANISM (read at the write site): lib/sub-agents/testing/diff-range.js header lines 5-6 state the failure mode in the code's own words - when the diff range cannot be derived, 'the zero-UI-by-diff gate then fails closed and the run falls through to the full E2E flow, which BLOCKs a genuinely zero-UI, already-merged SD that has no way to be honestly' tested by E2E; lib/sub-agents/testing/index.js :56 (QF-20260902-796: a non-exempt-by-type SD can still be zero-UI by measured diff), :685 and :845 (exempt-by-type OR by measured zero-UI diff runs the scoped non-UI path). The exemption exists; its input (a resolvable diff range) is what fails on squash-merged or already-merged branches (the 0-CHECKS=TIMING / squash-merged follow-up class), so the fail-closed default turns a safe exemption into a hard block three times in one day.",
 "FIX SHAPE (tier 2): (a) when diff-range resolution fails, fall back to a second measurement before failing closed: diff the PR's merged file list (findMergedPrFileList already exists in scripts/modules/handoff/executors/lead-final-approval/chairman-apply-state.js) or the SD's metadata.files_to_modify against the UI-surface globs; only if BOTH are unavailable fail closed; (b) when the fallback says zero-UI, route to the scoped unit/integration evidence path exactly as :845 does for the by-type exemption and stamp metadata.zero_ui_source = 'merged_pr_file_list' | 'files_to_modify'; (c) write a non-empty summary on the BLOCKED row naming which measurement failed (today's rows have none); (d) tests: fixture SD with an unresolvable diff range but a merged PR file list of *.mjs only -> scoped non-UI path; same with one *.tsx file -> full E2E; both unavailable -> fail closed with the reason in summary.",
 "Source: worker completion flag via the coordinator; Adam 49eabb23 mint (CONST-002). Related: QF-20260902-796 (introduced the by-diff exemption), the squash-merged-branch follow-up memory (0-CHECKS=TIMING), QF-20260911-644 (empty evidence summaries)."
].join(' ');
const r = spawnSync('node', ['scripts/create-quick-fix.js', '--title', title, '--type', 'bug', '--severity', 'medium', '--description', description, '--target-application', 'ehg_engineer', '--created-by', 'adam-49eabb23'], { encoding: 'utf8', env: process.env });
const out = (r.stdout||'') + (r.stderr||''); const m = out.match(/QF-\d{8}-\d{3}/); console.log('KEY', m ? m[0] : 'NONE');
console.log(out.split('\n').filter(l => /Status:|routing_tier|error|Error|refus/.test(l) && !/injected env/.test(l)).slice(0,4).join('\n'));
if (m) { const b = spawnSync('node', ['C:/Users/rickf/Projects/_EHG/EHG_Engineer/.artifacts/adam-bind-one-49eabb23.mjs', m[0], m[0] + " (Tier 2, medium): TESTING zero-UI-by-diff exemption fails closed on unresolvable diff ranges and blocks zero-UI SDs via mandatory E2E (3x on 09-11, flag 0911a58f)"], { encoding: 'utf8', env: process.env, cwd: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer' }); console.log((b.stdout||'').split('\n').filter(l=>/BOUND/.test(l)).join('\n') || ('BIND FAIL ' + (b.stderr||'').slice(0,200))); }
