import { createSupabaseServiceClient } from '../lib/supabase-client.js';
const supabase = createSupabaseServiceClient();
const key = 'SD-LEO-FIX-PRE-COMMIT-SECRET-001';
const description = `Bug (measured at .husky/pre-commit:274 on origin/main 3a16c427, 2026-09-11): the Stage 1 secret scan builds STAGED_CONTENT from \`git diff --cached --diff-filter=ACM -U0\`, which compares the index against HEAD only. On a merge commit (.git/MERGE_HEAD present, e.g. merging origin/main into a long-diverged feature branch per the documented ship path) every line the other parent brings in reads as ADDED relative to the branch tip, so main's own pre-existing content is scanned as if it were entering the tree. The wider the divergence, the more of main's history the scanner re-reads as new. Witnessed 2026-09-08 (coordinator advisory 0b217d91, Golf-4 signal 05aba085): the hook flagged two asserted-redacted fixture lines already on origin/main (tests/unit/complete-quick-fix/merged-reconcile-verification.test.js:435 and tests/unit/hooks/notification-permission-wait.test.js:163) and blocked Bravo's PR #8490 merge commit. The documented escape hatch (LEO_ALLOW ticket-reason prefix + --no-verify) is classifier-denied for worker seats, and ratification f0b5a482 forecloses a bypass grant, so the guard itself must be fixed. The hook's own comment at :285-287 already states the intent: block secrets ENTERING the tree, never pre-existing ones. Expected: on a merge commit the hook scans only lines new relative to BOTH parents, so the merge is accepted; non-merge commits keep the current basis. Escalated from QF-20260911-880 (Adam ruling d67d7094: a git-hook edit must run under the full gate set, not as a QF).`;
const scope = `IN SCOPE: (1) .husky/pre-commit Stage 1 secret detection: when .git/MERGE_HEAD exists, restrict the scanned added-line set to lines added relative to BOTH parents (intersection of the added lines from \`git diff --cached HEAD\` and \`git diff --cached MERGE_HEAD\`), keeping the existing HEAD basis for non-merge commits and preserving all existing exclusions (hook self-exclusion, .githooks, apa-calibration-fixtures base64 allowlist, added-lines-only filter). (2) A regression test under tests/unit/hooks/ that builds a throwaway repo, merges a branch carrying a redacted fixture line matching a secret pattern, and asserts the merge commit is not blocked while a genuinely new secret still is. (3) No change to SECRET_PATTERNS, no bypass flags, no edits to hooks.Stop or permissions.allow. OUT OF SCOPE: other hook stages; the LEO_ALLOW escape hatch; classifier policy. If the classifier denies the .husky/pre-commit write from the SD worktree, the worker signals stuck with the verbatim denial and the ready patch, and holds.`;
const success_metrics = [
  { metric: 'Merge-commit false positives eliminated', target: 'A merge bringing in a pre-existing redacted fixture line is NOT blocked by Stage 1 (regression test green)' },
  { metric: 'Genuine secrets still blocked', target: 'A newly added line matching SECRET_PATTERNS on the same merge commit IS blocked (negative-control test green)' },
  { metric: 'Non-merge behaviour unchanged', target: 'Existing hook unit tests under tests/unit/hooks green; no change to SECRET_PATTERNS' },
  { metric: 'Zero regressions', target: '0 existing tests broken (scoped unit run green)' },
];
const key_changes = [
  { change: 'Stage 1 secret scan: on merge commits (MERGE_HEAD present) intersect the added-line sets from HEAD and MERGE_HEAD so only lines new to both parents are scanned', impact: 'Merging origin/main into a diverged branch no longer re-reads main\'s own history as new secrets; ship path unblocked without any bypass' },
  { change: 'Regression test: throwaway-repo fixture merges a branch carrying a redacted fixture line and asserts no block; negative control asserts a real new secret still blocks', impact: 'Pins the two-parent basis so the bug cannot silently return' },
  { change: 'Hook comment at :285-287 updated to name the two-parent basis on merge commits', impact: 'Intent and implementation agree in the file' },
];
const { error } = await supabase.from('strategic_directives_v2').update({ description, scope, success_metrics, key_changes }).eq('sd_key', key);
console.log(error ? 'ERR ' + error.message : 'OK enriched ' + key);
