import dotenv from 'dotenv';
dotenv.config({ path: '.env' });
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';

const SD_ID = 'fbbf9a6d-e079-4c22-9189-88336aae9a16';

const critical_issues = [
  "FR-5 CRITICAL (NEW): the derivation INVERTS a deliberate governance clearance. Live: SD-LEO-INFRA-VENTURE-DEMAND-DISTRIBUTION-001-E has metadata.fence_status_2026_08_17.state='CLEARED' (cleared_by 0d37100a, cleared_at 2026-08-17T15:39:26Z, Adam advisory 6ca01194, with an explicit evidence_correction stating 'Clears stand') while metadata.venture_gate_last_verdict='NOT_MET'. FR-5's trigger derives state FROM that verdict, so its FIRST fire overwrites a coordinator's evidenced clearance with a 46-day-stale NOT_MET. The PRD frames FR-5 as a benign sync and nowhere states that applying it reverses a governance decision on a deferred SD.",
  "FR-5 CRITICAL (NEW): no value mapping is specified, and no AC or test scenario pins the derived VALUE. The two vocabularies are disjoint across all live data -- venture_gate_last_verdict in {NOT_MET} (2 rows), fence_status_2026_08_17.state in {CLEARED} (3 rows). system_architecture says only 'recomputing the state sub-key from venture_gate_last_verdict via a two-level jsonb_set' and never defines the function. TS-3 tests only allowlist SCOPING; TS-3b tests only key PRESENCE. The single thing that determines whether this chairman-gated trigger is correct -- what value it writes -- is entirely unspecified and untested.",
  "FR-3 HIGH (NEW, regression introduced by the correction itself): the BLOCKER-1/2/3 fix was applied to functional_requirements / acceptance_criteria / test_scenarios ONLY. Three sections EXEC builds from still specify the REJECTED design verbatim: (a) system_architecture.components[0].responsibility = 'BEFORE trigger deriving ... control_pack_evaluated from control_pack_failures with the jsonb-null fix, plus a same-migration backfill' -- reinstates BOTH BLOCKER-1 and BLOCKER-2; (b) implementation_approach.phases[1] = 'Ship the uat_test_runs pilot trigger + backfill', contradicting FR-3 AC-5 'NO backfill ships'; (c) integration_operationalization.consumers[0].interaction = 'now guaranteed in sync with control_pack_failures by FR-3 trigger' -- wrong source. An implementer reading system_architecture would build exactly the trigger BLOCKER-1 rejected plus the backfill BLOCKER-2 rejected.",
  "FR-3 HIGH (NEW): the canonical writer emits THREE value shapes, not two, and the third is unmentioned and untested anywhere in the PRD. lib/eva/uat-control-pack.js:221-224 sets status[control] to 'evaluated', or a template string 'waived: <reason>' (free text), or 'not_attempted'. The PRD's predicate ('a value other than not_attempted') happens to handle waivers correctly, but live data contains ONLY {not_attempted, evaluated}, so an implementer would plausibly write an equality test against 'evaluated'. That inverts waiver semantics against the app layer (app: waived -> allRequiredEvaluated TRUE; trigger: waived -> FALSE), manufacturing the exact summary/detail drift this SD exists to eliminate. No test scenario covers a waived control."
];

const findings = [
  "CONCEDED -- BLOCKER-4 was MY measurement error, not stale data. Direct query of strategic_directives_v2 where sd_key='SD-LEO-INFRA-VENTURE-DEMAND-DISTRIBUTION-001-E': metadata.venture_gate_last_verdict IS PRESENT, value 'NOT_MET', checked_at 2026-07-28T23:44:24.752Z, alongside 3 sibling venture_gate_* keys, 49 metadata keys total. The PRD's correction is factually right and my 'zero-yield by construction' claim was wrong. FR-5 is NOT dead by construction.",
  "VERIFIED FIXED -- BLOCKER-1: FR-3's predicate is now sourced from control_pack_status, never control_pack_failures, and is semantically faithful to the app layer on the observed value shapes. lib/eva/uat-control-pack.js:226 computes missing = CONTROL_PACK_CONTROLS.filter(c => status[c]==='not_attempted'); allRequiredEvaluated = missing.length===0. The PRD's 'all 4 present and != not_attempted' matches this for 'evaluated' and for the waived shape. The false-negative (fully evaluated, zero failures, control_pack_failures = jsonb null) now correctly derives TRUE via TS-2b.",
  "VERIFIED FIXED -- BLOCKER-2: the 'no backfill needed' claim is TRUE. Queried all 26 uat_test_runs rows; 24 carry control_pack_status/failures/evaluated. Recomputed the corrected predicate against every row: 0 disagreements with the stored control_pack_evaluated. All 24 are cpe=false and derive false. A backfill under the OLD failures-based predicate would have flipped 23 correct rows to true. Removing it was right.",
  "VERIFIED FIXED -- BLOCKER-3: confirmed control_pack_evaluated / control_pack_failures / control_pack_status are metadata jsonb keys, NOT columns. uat_test_runs top-level columns are: id, run_id, suite_id, sd_id, prd_id, environment, browser, device_type, viewport_width, viewport_height, status, total_tests, passed_tests, failed_tests, skipped_tests, pass_rate, started_at, completed_at, duration_ms, triggered_by, trigger_source, machine_info, test_config, created_at, metadata. FR-3 now states this correctly (acceptance_criteria[0] and system_architecture still use the bare 'uat_test_runs.control_pack_evaluated' form, cosmetic).",
  "MEDIUM (NEW) -- the PRD's claim that the trigger CANNOT be scoped is false, and it is load-bearing for the HOT table. FR-3 and system_architecture both assert 'having no distinct column to scope a WHEN clause to, fires on every INSERT/UPDATE'. Postgres supports BEFORE UPDATE OF metadata and WHEN (OLD.metadata IS DISTINCT FROM NEW.metadata); the latter is established in-repo precedent (database/migrations/20260412_deferred_sd_audit_trigger.sql:29, 20260529_create_venture_stages_unified.sql:230, 20260608_venture_stages_audit_add_gate_label.sql:81). Low impact on cold uat_test_runs, but the same false justification is carried into FR-5's design on strategic_directives_v2 -- a hot table with 57 existing triggers -- where firing on every UPDATE of every column is a real overhead and ordering concern.",
  "MEDIUM (NEW) -- the trigger does NOT 'mirror' allRequiredEvaluated on the partial-key path, contrary to FR-3's stated equivalence. App: missing filters status[c]==='not_attempted', so an ABSENT required key is NOT counted missing -> allRequiredEvaluated returns TRUE (fails open). PRD trigger: requires all 4 keys PRESENT -> FALSE (fails closed). The trigger is the safer of the two, but the divergence is real, unstated, and lands on exactly the shape the trigger exists to guard (FR-3's own stated purpose: 'a future writer bypassing that atomic path'). TS-2d covers control_pack_status entirely absent; NO scenario covers control_pack_status present-but-partial.",
  "MEDIUM (NEW) -- internal contradiction inside the corrected PRD. FR-5 acceptance_criteria[2] still requires the migration be 'named to fire correctly relative to the >=8 existing triggers on strategic_directives_v2', while TR-5 explicitly labels '>=8' the SD's uncorrected placeholder and gives the measured figure as 57 triggers / 37 BEFORE. The correction updated TR-5 but not the AC that consumes it.",
  "MEDIUM (NEW) -- TS-3's premise is inaccurate. It describes '3 SD rows all carrying a fence_status_2026_08_17 key' as symmetric. Verified live (full 6270-row paginated scan): 3 rows carry fence_status_2026_08_17, but only 2 carry venture_gate_last_verdict -- SD-FDBK-ENH-EHG-OPERATING-COMPANY-001-A has the fence key and NO derivation source. Separately, SD-LEO-INFRA-BIND-OBSERVE-ONLY-001 (completed) carries the byte-identical CLEARED/NOT_MET mismatch, so the SD ships a trigger that 'fixes' one row while knowingly leaving identical drift on another, with no recorded rationale beyond its completed status.",
  "LOW (NEW) -- TS cross-reference rot from the renumbering: integration_operationalization.data_contracts[0].validation says 'TS-2 pins the jsonb-null edge case', but after the correction TS-2 is the 3-of-4-not_attempted scenario and TS-2b is the jsonb-null one.",
  "LOW (NEW) -- backfill-by-side-effect on legacy rows. 2 of 26 uat_test_runs rows (8e54bfc7 created 2026-08-30, bda551a1 created 2026-02-20) carry NO control_pack_* keys at all, predating the feature. Per TS-2d the trigger writes control_pack_evaluated=false whenever control_pack_status is absent, so any future UPDATE to those rows silently adds the key -- asserting 'control pack was not evaluated' for runs that predate the control pack. Incremental, but in tension with AC-5's 'NO backfill ships'.",
  "CONTEXT -- FR-3's TRUE branch has zero production precedent. Across all 24 instrumented rows the only status values ever written are 'not_attempted' and 'evaluated', with 3 of 4 required controls 'not_attempted' in 100% of rows; control_pack_evaluated is false on 24/24, true on 0. TS-1 and TS-2b are therefore synthetic-only. This strengthens the no-backfill conclusion but means the derive-true path ships unexercised by real data.",
  "VERIFIED -- cited precedent files all exist: database/chairman-gated/20260912_venture_channel_publish_ledger_outbound_gate_trigger_dry_run.mjs (TR-4 convention) and database/chairman-gated/20260824_strategic_directives_canonical_writer_choke.sql (TR-1 lock_timeout, documented at lines 21-32). result-recorder.js line citations check out: 593 QF comment, 597 buildControlPackStatus call, 614 control_pack_failures, 619 control_pack_evaluated=allRequiredEvaluated, 620 control_pack_status -- all inside ONE atomic .update(), confirming FR-3's 'no live drift today' reasoning.",
  "VERIFIED -- FR-4 is sound. lib/eva/uat-robustness-gate.js:141 keys on the falsy check of run.metadata.control_pack_evaluated and reads control_pack_status at :145 for the missing-detail message; leaving it unmodified is correct. Note the trigger makes control_pack_evaluated always PRESENT, retiring the 'entirely absent' state that tests/unit/eva/uat-robustness-gate.test.js:153 pins as distinct. Gate outcome is unchanged (absent and false are both falsy), so this is a note, not a blocker."
];

const results = {
  verdict: 'FAIL',
  confidence: 90,
  critical_issues,
  warnings: [],
  recommendations: [
    "FR-5 BLOCKING: state plainly in the PRD and the migration header that applying this trigger REVERSES the 2026-08-17 clearance on SD-LEO-INFRA-VENTURE-DEMAND-DISTRIBUTION-001-E (CLEARED -> derived-from-NOT_MET). This must be part of the chairman decision packet, not a side effect discovered at apply time.",
    "FR-5 BLOCKING: specify the explicit value mapping from venture_gate_last_verdict to fence_status_2026_08_17.state (the vocabularies are disjoint: NOT_MET vs CLEARED), and add an acceptance criterion plus a test scenario that assert the DERIVED VALUE, not just scoping and key presence.",
    "FR-3 BLOCKING: propagate the BLOCKER-1/2/3 correction into system_architecture.components[0], implementation_approach.phases[1], and integration_operationalization.consumers[0] -- all three still specify control_pack_failures and/or a backfill.",
    "FR-3 BLOCKING: add a test scenario for a waived control (status value 'waived: <reason>') deriving control_pack_evaluated=true, and state the three-value vocabulary explicitly in the data contract so no implementer writes an equality test against 'evaluated'.",
    "Recommended: add a TS for control_pack_status present-but-partial (documenting the intentional fail-closed divergence from allRequiredEvaluated); scope the trigger with WHEN (OLD.metadata IS DISTINCT FROM NEW.metadata) per in-repo precedent and drop the 'cannot be scoped' claim; reconcile FR-5 AC-3's '>=8 triggers' with TR-5's measured 57/37; fix TS-3's 3-carrier premise and the TS-2/TS-2b cross-reference."
  ],
  summary: "FAIL -- 4 remaining blockers, all of them NEW (2 genuine design gaps in FR-5, 2 introduced or left behind by the correction itself). Conceding first: my prior BLOCKER-4 was a measurement error. venture_gate_last_verdict IS present on SD-LEO-INFRA-VENTURE-DEMAND-DISTRIBUTION-001-E with value NOT_MET; FR-5 is not zero-yield and the PRD's correction of me is right. BLOCKER-1, 2 and 3 are genuinely fixed and I verified each against live data: the predicate now derives from control_pack_status and is faithful to lib/eva/uat-control-pack.js on observed shapes; the 'no backfill' claim is true (0 of 24 rows disagree with the corrected predicate); control_pack_* are correctly described as metadata keys. What re-verifying BLOCKER-4 surfaced instead is worse than the claim it disproved. The allowlisted row reads fence_status_2026_08_17.state='CLEARED' while venture_gate_last_verdict='NOT_MET', so FR-5's trigger does not sync a summary to its detail -- its first fire REVERSES an evidenced coordinator clearance (cleared_by 0d37100a, Adam advisory 6ca01194, with an evidence_correction explicitly recording 'Clears stand'). Compounding that, the PRD never defines the mapping between the two disjoint vocabularies, and neither an acceptance criterion nor any test scenario asserts the value the trigger would write -- TS-3 covers allowlist scoping and TS-3b covers key presence, so the only thing that makes this chairman-gated trigger correct or catastrophic is unspecified and unverified. Separately, the FR-3 correction was applied only to functional_requirements, acceptance_criteria and test_scenarios: system_architecture.components[0] still reads 'deriving control_pack_evaluated from control_pack_failures ... plus a same-migration backfill', implementation_approach.phases[1] still says 'trigger + backfill', and integration_operationalization still claims sync with control_pack_failures -- so the sections EXEC actually builds from reinstate BLOCKER-1 and BLOCKER-2 verbatim. Finally, the canonical writer emits three status shapes ('evaluated', a 'waived: <reason>' template string, and 'not_attempted'), but live data shows only two, no test covers a waiver, and an implementer writing the obvious equality test against 'evaluated' would invert waiver semantics against the app layer -- manufacturing the exact drift class this SD exists to close. The expanded test_scenarios are a real improvement on FR-3's INSERT/UPDATE, rollback and lint paths; they remain gapped on FR-5's derived value, waivers, and partial control_pack_status.",
  metadata: {
    phase: 'PLAN',
    prd_id: 'PRD-SD-LEO-INFRA-SUMMARY-COLUMNS-DERIVED-001',
    prd_updated_at: '2026-09-13T01:43:44.731971',
    review_type: 'adversarial re-review after prior FAIL 51dde124',
    prior_evidence_row: '51dde124-d736-4ace-a9eb-90264145515a',
    supersedes_row: '1d78482f-bc8c-486f-b49d-5a10b9773718 (same FAIL verdict; rewritten because repo_path resolved null without targetApplication and the anti-snowball strip removed metadata.findings)',
    blockers: 4,
    prior_blockers_resolved: ['BLOCKER-1', 'BLOCKER-2', 'BLOCKER-3'],
    prior_blockers_retracted: ['BLOCKER-4 -- own measurement error, conceded after direct re-query'],
    detailed_findings: findings,
    live_verification: {
      uat_test_runs_rows: 26,
      rows_with_control_pack_keys: 24,
      predicate_disagreements: 0,
      control_pack_evaluated_true_count: 0,
      distinct_status_values_observed: ['not_attempted', 'evaluated'],
      writer_emits_third_shape: 'waived: <reason> (lib/eva/uat-control-pack.js:223)',
      sd_rows_scanned: 6270,
      fence_status_carriers: 3,
      venture_gate_last_verdict_carriers: 2,
      allowlisted_row_state: 'CLEARED',
      allowlisted_row_verdict: 'NOT_MET'
    }
  }
};

const resolution = await resolveSubAgentRepo({ sdId: SD_ID, targetApplication: 'EHG_Engineer', subAgentCode: 'TESTING' });
applySubAgentRepoVerdict(results, resolution);

const stored = await storeSubAgentResults('TESTING', SD_ID, { name: 'TESTING' }, results, { phase: 'PLAN' });
console.log('STORED:', JSON.stringify(stored)?.slice(0, 800));
