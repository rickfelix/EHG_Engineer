import dotenv from 'dotenv'; dotenv.config({ path: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer/.env' });
import { createClient } from '@supabase/supabase-js';
const s = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
const key = 'QF-20260911-018';
const { data: cur, error: e0 } = await s.from('quick_fixes').select('description').eq('id', key).single();
if (e0) { console.log('ERR read ' + e0.message); process.exit(1); }
const fold = ' UPDATE 2026-09-12 03:1xZ (fold from coordinator reply b65cdb67, VERIFIED at the write sites by Adam): the one-row lever proposed above ("raise grace on this one row") is NOT durable and is withdrawn. lib/machinery-class/armed-registration.js L110-132 upserts expected_interval_seconds and grace_multiplier=ARMED_GRACE_MULTIPLIER (const 2, L29) on EVERY sweep run, and the sweep stamp closure scripts/cron/drive-report-hourly-sweep.mjs L215-219 rewrites grace_multiplier=2 on every stamp, so any hand edit dies at the next delivered run. The workflowCron gap-subtraction (FR-1, L97-103) does NOT cover this case either: the workflow declares cron 0 * * * * (no declared gap), so largestDeclaredGapSeconds is the nominal hour and the subtraction is zero - the gaps here are RUNNER delivery jitter, not declared schedule gaps (measured deliveries 09-11/12: 04:07, 09:27, 14:11, 18:19, 21:37, 00:01Z; gaps 2h24m-5h20m; no 01:00, 02:00 or 03:00Z run as of 03:1xZ). DURABLE FIX LOCATION, added to (a)-(d): (e) the sweep register call (drive-report-hourly-sweep.mjs L118, HOURLY_EXPECTED_INTERVAL_SECONDS L78) and registerArmedMachinery must carry the calibration - either a per-row graceMultiplier option honoured by both the upsert and the stamp closure, or an expectedIntervalSeconds set from the measured delivery cadence (~10800 today) with the ceiling in (a) - and the stamp closure must stop re-asserting grace_multiplier=2 (it exists to override the column default 3; move that default into the registration payload instead). Test: registering with graceMultiplier 5 then stamping leaves the row at 5.';
const description = cur.description + fold;
const { data, error } = await s.from('quick_fixes').update({ description }).eq('id', key).select('id,status');
console.log(error ? 'ERR ' + error.message : ('FOLDED ' + data[0].id + ' ' + data[0].status + ' len=' + description.length));
