const { spawnSync } = require('child_process');
const title = "The Adam board has bind-at-mint but no close-at-source: an open adam_task_ledger child stays open after its QF or SD reaches a terminal status, so the open count the chairman reads drifted 17 percent high tonight (12 done-at-source residue rows of 70 open by 02:28Z) - make the QF status writer and the SD LEAD-FINAL acceptance close the sourced board child";
const description = [
 "Sourced from Solomon's board check #58 (lane row 5aacdba4, corr ac8fd880, 02:28Z; propose-only) and accepted for Adam's belt. MEASURED by Solomon (paged full scan n=6,195; each cited key re-read at conclusion time): done-at-source residue on adam_task_ledger read 3 at 14:4xZ, 4 at 20:28Z, 8 at 23:28Z, 12 at 02:28Z of 70 open rows - Michael-002 parent+child, QF-881, TROUBLESHOOTING-001, QF-964, QF-140, QF-382, QF-275, RATIFICATION-ENCODE-VERIFICATION-001, QF-110, QF-466, QF-117; TRUE-ACTIONABLE was 58, so the P1 open number the chairman reads was 17% high. Adam re-measured and closed the residue by hand at 02:5xZ (see the closure stamps on the rows).",
 "MECHANISM: bind-at-mint works (lib/adam/task-ledger.js createOrUpsertNode is called from every Adam mint; all 9 of tonight's mints have children), but nothing marks a source_kind='sourced_sd' child done when its source_ref reaches a terminal status: lib/quick-fix/status-writer.cjs setQuickFixStatus (the single canonical QF status writer) and the SD LEAD-FINAL acceptance path write only their own tables. The board therefore only ever grows unless a seat closes rows by hand, and every census, stall probe and the chairman's weekly open count read the residue as live work.",
 "FIX SHAPE (tier 2): (a) a small board closer lib/adam/task-ledger.js closeSourcedChild(supabase, sourceRef, terminalStatus) that marks open children with that source_ref done and stamps blocker 'CLOSED AT SOURCE: <ref> <status> <ts>'; (b) call it from setQuickFixStatus when the new status is completed or cancelled, and from the LEAD-FINAL acceptance writer (and the SD cancel path) when an SD reaches completed/cancelled; (c) a residue gauge in the Adam quiet tick: count of open sourced children whose source is terminal, expected 0, printed as informational; (d) idempotent, never touches manual (finding-class) rows. TESTS: completing a fixture QF with a bound child closes the child; a second completion is a no-op; a manual child with the same text is untouched.",
 "Source: Solomon finding; Adam 49eabb23 mint (CONST-002). Distinct from QF-20260911-983 (escalated QF rows never auto-cancelled) though the same family; related: feedback_bind_every_adam_mint_to_the_board_at_mint_time."
].join(' ');
const r = spawnSync('node', ['scripts/create-quick-fix.js', '--title', title, '--type', 'bug', '--severity', 'low', '--description', description, '--target-application', 'ehg_engineer', '--created-by', 'adam-49eabb23'], { encoding: 'utf8', env: process.env });
const out = (r.stdout||'') + (r.stderr||''); const m = out.match(/QF-\d{8}-\d{3}/); console.log('KEY', m ? m[0] : 'NONE');
console.log(out.split('\n').filter(l => /Status:|routing_tier|error|Error|refus/.test(l) && !/injected env/.test(l)).slice(0,4).join('\n'));
if (m) { const b = spawnSync('node', ['C:/Users/rickf/Projects/_EHG/EHG_Engineer/.artifacts/adam-bind-one-49eabb23.mjs', m[0], m[0] + " (Tier 2, low): Adam board has bind-at-mint but no close-at-source; 12/70 open rows were done at source by 02:28Z (Solomon board check #58)"], { encoding: 'utf8', env: process.env, cwd: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer' }); console.log((b.stdout||'').split('\n').filter(l=>/BOUND/.test(l)).join('\n') || ('BIND FAIL ' + (b.stderr||'').slice(0,200))); }
