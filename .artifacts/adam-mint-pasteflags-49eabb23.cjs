const { spawnSync } = require('child_process');
const title = "record-account-capacity.mjs can only ledger a /usage paste against the ACTIVE account at the current instant (no --account / --pasted-at), so a paste relayed after a login rotation or recorded late cannot be ledgered truthfully: two chairman pastes on 2026-09-11 were lost to the ledger and the bandwidth forecast read INSUFFICIENT DATA";
const description = [
 "MEASURED 2026-09-12 02:0xZ at the Adam bandwidth-forecast tick: node scripts/account-usage-paste-projection.mjs printed 'Account 7bc04ab1 - Session / Week all-models / Week Fable: INSUFFICIENT DATA -- 1 paste on file (need 2+)'. account_usage_pastes holds one Code Street Labs row (id 1, pasted 2026-09-02T14:26Z) and Deep Soul rows up to 2026-09-11T09:50Z. On 2026-09-11 the chairman pasted /usage at the Adam terminal twice: Deep Soul Sessions at ~22:15Z (session 69%, week all-models 54%, week Fable 95%, session reset 00:40Z, week reset Sep 18 08:00Z) and, after /login, Code Street Labs at ~22:22Z (0 / 0 / 0, week reset Sep 18 19:00Z). Neither reached the ledger: the seat recorded them in its own file and memory but did not run the recorder at paste time (Adam miss, owned).",
 "MECHANISM (read at the write site): scripts/record-account-capacity.mjs header lines 5-22 - 'Records a chairman-pasted /usage dashboard reading against the CURRENTLY ACTIVE account'; flags are --weekly-all-models-pct, --weekly-fable-pct, --weekly-reset, --session-pct, --session-reset, --promo-note only; lib/fleet/account-usage-paste-writer.cjs ~66-68 sets account_uuid8 from the resolved active identity and pasted_at from now. So (a) the Deep Soul paste cannot be recorded at all once the login has rotated to Code Street Labs, and (b) recording the 22:22Z Code Street Labs reading now would stamp a 0% reading at 02:0xZ while the sampler reads 76%, fabricating a burn-rate point. The honest outcome tonight is INSUFFICIENT DATA and a silent duty marker; the forecast duty is blind for a whole rotation night by construction.",
 "FIX SHAPE (tier 1, additive flags + writer params): (a) --pasted-at <ISO> (must be in the past, within 7 days, else refuse) so a paste relayed from the chairman's message timestamp is ledgered at the true instant; (b) --account <uuid8|org-name> to ledger a paste for a non-active registry account (the identity map in account-usage-reader.cjs already knows the three), refusing unknown ids; (c) the writer's readback compares against the supplied values; (d) the Adam contract note: run the recorder AT paste time, once per paste, before anything else. TESTS: --pasted-at in the future or >7d old refused; --account deepsoul writes uuid8 ca1de6e4 while the active login is 7bc04ab1; default behaviour unchanged. Acceptance readback: after backfilling tonight's two pastes with true timestamps, the projection reports numbers for both accounts.",
 "Source: Adam 49eabb23 (CONST-002 propose-only) from the QF-20260905-121 bandwidth-forecast duty; chairman-facing capacity instrument (the forecast duty went dark two nights running per the tick prompt). Related: SD-LEO-INFRA-USAGE-PASTE-LEDGER-001 (the ledger), QF-20260911-082 (sampler label defect: the same rotation night)."
].join(' ');
const r = spawnSync('node', ['scripts/create-quick-fix.js', '--title', title, '--type', 'bug', '--severity', 'low', '--description', description, '--target-application', 'ehg_engineer', '--created-by', 'adam-49eabb23'], { encoding: 'utf8', env: process.env });
const out = (r.stdout||'') + (r.stderr||''); const m = out.match(/QF-\d{8}-\d{3}/); console.log('KEY', m ? m[0] : 'NONE');
console.log(out.split('\n').filter(l => /Status:|routing_tier|error|Error|refus/.test(l) && !/injected env/.test(l)).slice(0,4).join('\n'));
if (m) { const b = spawnSync('node', ['C:/Users/rickf/Projects/_EHG/EHG_Engineer/.artifacts/adam-bind-one-49eabb23.mjs', m[0], m[0] + " (Tier 1, low): record-account-capacity lacks --pasted-at/--account, so late or rotated-account /usage pastes cannot be ledgered; forecast blind on rotation nights"], { encoding: 'utf8', env: process.env, cwd: 'C:/Users/rickf/Projects/_EHG/EHG_Engineer' }); console.log((b.stdout||'').split('\n').filter(l=>/BOUND/.test(l)).join('\n') || ('BIND FAIL ' + (b.stderr||'').slice(0,200))); }
