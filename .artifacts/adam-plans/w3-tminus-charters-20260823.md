# W3 T-MINUS CHARTERS — Five instrument-repair packages (documents only; minting awaits chairman approval)
**Assembled 2026-08-23 under the chairman's GO. Every claim below is evidence-referenced to the Solomon-verified reconciliation report; every charter binds to the five-step proof chain: (1) preserved pre-fix failure evidence → (2) falsifiable negative test written FIRST → (3) bounded change → (4) positive regression proof, surrounding suite green → (5) exact commit SHA recorded in the ledger + traversal manifest.**
Sequencing: **P1 strictly first** (every other package's proof rides its evidence layer). P2/P3 next (ceremony-heavy). P4/P5 parallel after P1. Anti-smoothing check passed per charter: each repairs the measuring instrument; none improves AltifyAI's chances.

---

## P1 — EVIDENCE LAYER (run identity, attempt history, outcome semantics)
**Objective**: make every W3 gate evaluation a durable, attributable, immutable-once-final record.
**Pre-fix evidence (preserved)**: eva_stage_gate_results 1,796 rows — resolved_outcome populated 0 (column has existed, never once written); zero gate_type='kill' rows in table history; upsert on (venture,stage,gate_type) with 0/1,796 timestamp drift = re-evaluation unobservable by construction; correlationId minted per run (eva-orchestrator.js:128) but dropped at persistence (in gate_criteria of only 34/1,796 rows; never reaches venture_artifacts); feedback.gate_run_id populated 36/33,566.
**FRs**: (1) run_id + attempt_number columns on eva_stage_gate_results; INSERT-per-attempt for traversal writes; real unique constraint (run_id, stage_number, gate_type, attempt_number) with live duplicate-rejection proof. (2) Attempt lifecycle: row created resolved_outcome=NULL; ONE atomic NULL→final update carrying evidence+evaluator in the same statement; finalize-immutability trigger; interrupted attempts stay NULL-visible; recovery = attempt n+1. (3) resolved_outcome writer for the canonical 7-term enum; JS/SQL verb-set alignment. (4) Decision linkage: chairman_decisions.context + venture_artifacts.metadata stamp the same run_id. (5) Dead-reader fix: launch-workflow trio (index.js:44,96,136) selects nonexistent reasoning/score, swallows 42703, computes readiness over zero gates — fix columns + propagate errors. (6) Reader census of eva_stage_gate_results consumers (the ONE open input on the existing-table-vs-side-table provisional). (7) Quarantine view excluding the 930 legacy venture_id-NULL rows.
**Negative tests first**: duplicate attempt insert must REJECT; post-final update must REJECT; launch-readiness query on missing columns must FAIL LOUD (not []).
**Ceremony items**: the run_id/attempt DDL + immutability trigger (additive + trigger — chairman ceremony).

## P2 — GATE SSOT (one stage-set truth, all graders derived)
**Objective**: one canonical kill/promotion stage set with every live representation derived from it.
**Pre-fix evidence**: 7 live mutually-inconsistent representations + stale CLI (reconciliation §4 inventory): stage-gates.js hardcodes KILL={3,5,13,24}/PROMOTION={17,18,23} — the confirmed 23/24 swap on the live validateStageGate path at AltifyAI's launch boundary; gate-outcome-bridge omits the S23 kill so outcome recording never fires there; chairman_dashboard_config.hard_gate_stages "retired 2026-05-12" yet live-consumed (stage-work-sync.js:28-39); the staged 20260722 SSOT migration replaces the drifted SQL arrays that omitted promotion stages 10/16/19/25 (forensically exploited: reconciliation §3).
**FRs**: (1) 23/24 swap fix in stage-gates.js. (2) Apply the staged 20260722 gate-type SSOT migration (its own header requires the pre-deploy census + forensic-query artifact attached — both now exist as reconciliation §3/§4). (3) Publish the canonical post-migration stage/gate map; venture_stages via the governance API = the one source. (4) Derive-or-retire every inventory row: gate-bars, stage-advance-worker, gate-outcome-bridge (+S23 kill), the dashboard-config row (re-bless as derived view or remove reader — never retired-but-consumed), the stale CLI. (5) Advancement-writer census refresh (before/after) + close/fence the 2 live ungoverned bypasses + total forward-direction enforcement (directional guard: no ungoverned backward writes; regression machinery stays deferred by design). (6) Replay the historical unauthorized-advance fixtures parameterized over the 25-row class (NEVER pinned to a constant — both query definitions published in reconciliation §3).
**Negative tests first**: a transition leaving an SSOT promotion stage without an approved decision at (venture, stage, run/attempt) must BLOCK; a backward write outside the governed path must BLOCK.
**Ceremony items**: the staged 20260722 migration (chairman GO per its own header); directional-guard trigger.

## P3 — PATH INTEGRITY (no silent bypass survives)
**Objective**: the chairman-decision advance path runs every check; continuation is loud, never silent.
**Pre-fix evidence**: the chairman-decision path + approveHandoff bypass exit-gates, thesis-kill, and gate-debt (prior packet §4 structural finding); gate-debt satisfies on ANY approved decision at (venture,stage) — no decision-value filter, no attempt linkage → a one-time yes is a perpetual pass (artifact-persistence-service.js:549-558); override marking simultaneously over-marks (290 rows carry override keys, 257 on machine-PASSED rows) and under-marks (manual-INSERT path leaves no trace); JS/SQL enforcement verb sets disagree; the external-publication evaluator FAILS OPEN on error (stage-execution-worker.js:2901-2903, `productReviewApproved = true // fail open` — verified verbatim).
**FRs**: (1) Wire checkExitGates + thesis-kill + gate-debt into the chairman-decision path — a chairman yes on failed criteria records `override` loudly (run/attempt-scoped), never bypasses. (2) Gate-debt keyed to run/attempt (kills the perpetual-pass). (3) Align the JS and SQL decision-verb allow-lists. (4) Flip the external-publication evaluator to fail-closed. (5) `skip` disabled for W3 evaluations.
**Negative tests first**: chairman-approve at a stage with failed machine criteria must produce resolved_outcome='override' with passed=false (never machine_pass, never silent advance); evaluator error on the publication check must BLOCK.

## P4 — DISPOSITION & RAILS (recovery machinery honest and bounded)
**Objective**: the thin defect-disposition loop over existing primitives, with real rails.
**Pre-fix evidence**: venture_token_budgets=0 rows, venture_phase_budgets=0 rows (first real sub-agent invocation throws NO_BUDGET_RECORD — a manufactured defect); skipRCA:true hardcoded (rca-orchestrator.js:464); the handoff RCA gate queries the wrong table (can false-pass perfect); error-triggered-sub-agent-invoker exists with exactly the ratified breakers (3/pattern/30min, 10/SD, 1h cooldown — lib/error-triggered-sub-agent-invoker.js:45-63) and zero production callers; QF eligibility: 3 conditions enforced, 3 partial, 3 convention-only (reconciliation §7 matrix).
**FRs**: (1) Seed both budget tables (seeding values proposed inside the charter for chairman visibility; fail-closed stays). (2) Repair the RCA gate's wrong-table read. (3) Govern skipRCA (flag or explicit param — never hardcoded true). (4) Disposition-loop wiring: detect→classify→diagnose→retry(≤2)→revise→re-evaluate→verify→record preventive learning→escalate; every eligible NATURAL defect enters; none manufactured; O3=NOT_EXERCISED if no eligible natural defect occurs. (5) QF eligibility preflight: the completion-time diff-path scan (blocks QFs touching migrations/gate-criteria/policy/hook/history/secrets/publish paths) — conditions #8/#9 become code-enforced BEFORE autonomous recovery enables; honest labels on the convention-only remainder. (6) The invoker enters DESIGN only (chairman provisional §5.5): no wiring authorization here; wiring is its own later decision after budget+eligibility+breaker verification.
**Negative tests first**: sub-agent invocation with seeded-then-exhausted budget must halt fail-closed (and with seeded-adequate budget must NOT halt); a QF diff touching a gate-criteria file must be REFUSED autonomous execution.

## P5 — CARGO INSTRUMENTS (AltifyAI's kill criteria measurable)
**Objective**: the ratified K1–K3 kill criteria produce real evaluation evidence.
**Pre-fix evidence**: thesis-kill gauges resolve undefined/NaN — inputs unwired; armed conceptually, unmeasurable in fact (a kill gate that cannot fail).
**FRs**: (1) Wire measurable sources for K1–K3 with source provenance on every reading. (2) Fail closed on missing/NaN inputs → cannot_evaluate (venture holds, factory finding mints — never a pass, never a kill). (3) Kill-validity precondition implemented reading canonical stage metadata (reconciliation §3 rule). (4) Demonstrate one full evaluation producing evidence rows on synthetic-then-real inputs.
**Negative tests first**: NaN input must yield cannot_evaluate (not pass, not kill, not crash); a kill verdict without frozen criteria + evidence refs must be REJECTED as invalid.

---

## Out of T-minus, pre-registered (unchanged)
Stage-regression machinery (reserved capability, designed later); full writer-consolidation beyond the census/fencing above (learnable-during); CEO/VP wrapper revival (`-B` SD, chairman-fenced); D7 ephemeral tokens (pending Solomon's protections determination); cost-governor enforcement (findings-driven post-T1); venture_stage_work append-only trigger (early-during; until it lands the manifest cites no rows from it).

## The next chairman decision (single)
**Approve minting P1–P5 as five SDs** (with their two staged ceremony migrations queued for a sitting). On that approval: minting proceeds through the canonical path, implementation runs through normal LEO gates, and T1 activation remains a separate final GO once P1–P5 land with green proof chains.
