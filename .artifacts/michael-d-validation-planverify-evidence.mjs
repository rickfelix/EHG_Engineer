import dotenv from 'dotenv'; dotenv.config();
import { createClient } from '@supabase/supabase-js';
import { storeSubAgentResults } from '../lib/sub-agent-executor/results-storage.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../lib/sub-agents/resolve-repo.js';

const SD_UUID = '3f128d5c-8168-4415-86cc-ab5da4663d11';
const SD_KEY = 'SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D';
const HEAD = '68c2b9481c8e784b462a401ab3c8f29543c95641';
const supabase = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

const { data: sa } = await supabase.from('leo_sub_agents').select('*').eq('code', 'VALIDATION').maybeSingle();

const frTable = [
  { fr: 'FR-1', verdict: 'IMPLEMENTED', tests: 'lib/michael/feeder.test.js 27 + rules-match.test.js 10 + scripts/michael-quiet-tick.test.js 11',
    note: 'runFeeder at lib/michael/feeder.mjs:157 covers (a)-(j). Amendments honoured: inclusive inWindow:56-61; etDateOverride:158; FEEDERS frozen registry:26-36 with FEEDER_UNKNOWN:161; start row status skipped + counts.phase started:140; INSERT-not-upsert attempt max+1 with 23505 retry:127-149; isUniqueViolation:69; per-feeder 20-min ISO-string fence:74-77,114-115; tables_absent:true:180; three-way exitCodeFor:80-84; stderrLogger default:97; gracefulExit defined once:87; assembleReadiness with READY/DEADLINE constants:242. DEVIATION (a): the tick keeps a one-arg inWindow wrapper defaulting to WINDOW_ET (scripts/michael-quiet-tick.mjs:39-41) instead of passing WINDOW_ET at its two call sites; the harness predicate is window-required and there is no third copy, so the intent holds. Amendment (l) measured exactly: 11 quiet-tick tests pass.' },
  { fr: 'FR-2', verdict: 'IMPLEMENTED', tests: 'google-clients.test.js 6 + constants.test.js 5 + gmail-client.test.js',
    note: 'listCalendarEvents singleEvents/orderBy startTime/maxResults 250 at lib/michael/google-clients.mjs:47; listDriveFiles:73; readDriveFileText parents-check then alt=media:89-93 returning FILE_OUTSIDE_CONFIGURED_FOLDER:91 (SECURITY F-10). gmail-client extended additively with listThreads:57, getThreadMeta format=metadata four headers META_HEADERS:14,87, listLabels:111. constants.mjs registry:9-17 with the 6grHWpvVM8QXrj5W default and ceiling 60. DEVIATION: readDriveFileText gained a required folderId argument (needed by the amendment); every caller passes it (tasks-classifier.mjs:184).' },
  { fr: 'FR-3', verdict: 'IMPLEMENTED', tests: 'scripts/michael/calendar-read.test.js 17',
    note: 'runCalendarRead at scripts/michael/calendar-read.mjs:197. assertHostVenue first regardless of injected auth:203. today +/- 1 ET day on primary and MICHAEL_EXELON_CALENDAR_ID:212-219. PERSISTED_KEYS:31 matches the migration columns. classifyDay:163-181 covers Recovery/Interpersonal/Deep/Shallow, coded override and the Tuesday-office rule. dedupeAcrossCalendars preferring primary + counts.cross_calendar_collisions:123-134. Rows only under --apply:233. DEVIATION (PLAN-accepted): the GHA venue refusal is a pre-read refusal with exit 2 and no run row (:203), not the failed run row AC-2/TS-11 word; the amendment itself says "mapping the coded throw to a refusal".' },
  { fr: 'FR-4', verdict: 'IMPLEMENTED', tests: 'scripts/michael/gmail-triage.test.js 19',
    note: 'runGmailTriage at scripts/michael/gmail-triage.mjs:138. Label reconcile writes exactly LABEL_KEYS:45,178. Missing configured label degrades and skips that class:171-173,292 via ruleUsable:111. inboxQueries fresh-minus-keep + older_than sweep:48-51 bounded at THREADS_MAX_RESULTS 200. intentFor gates on auto_apply true AND verb in (label, archive):58-63. Two-step guarded write: upsert ignoreDuplicates:229 then update .is(action_taken_at,null):238. DEVIATION: the update payload is ITEM_UPDATE_KEYS (5 keys) because FR-5 requires borderline in the same patch; AC-4 names only four. The update also carries extra hardening guards .is(verified_by,null) and .or(class.is.null,rule_key.not.is.null) — stricter than promised.' },
  { fr: 'FR-5', verdict: 'IMPLEMENTED', tests: 'gmail-triage.test.js ceiling/idempotency/borderline cases (TS-5, TS-6)',
    note: '--apply/--modify split with MODIFY_REQUIRES_APPLY:143; registrar registers --apply without --modify (measured in the dry-run). Per-ET-date ceiling: budgetFor:81-86 plus a live-ledger recheck loop:260-270, counts.ceiling_hit:270, and feeder.mjs:113 makes every later fire of the date inert with reason ceiling_hit. Record-then-act:251,284. Borderline resurfacing itemRow:93-104. modify_failed continues; failed only when every attempted modify failed:291. DEVIATION (improvement): the budget is the row LEDGER with prior runs threads_modified as a floor, not the plain sum of prior counts the PRD names — documented at :72-77. AC-6 (MODIFY_NOT_LANDED before PR 4b) is obsolete now 4b has landed; no leftover reference remains.' },
  { fr: 'FR-6', verdict: 'IMPLEMENTED', tests: 'tasks-classifier.test.js 14 + retention.test.js 8 (TS-9, TS-19)',
    note: 'runTasksClassifier at scripts/michael/tasks-classifier.mjs:141, assertHostVenue first:147. stale >36h:168; cleanup_pending:173-181; four-bucket routing routeItem:85-95; unrouted staged kind task_route:241; Todoist dedupe over all pages with both labels and the 7-day floor:210-222; creates with both labels:234; cleanup STAGED not written to Drive:245-249. Bounded payload keys ROUTE_KEYS/CLEANUP_KEYS:40,61 with the 200-char title:99. Open-row dedupe through the partial index:127-132. retention.mjs:34 empties dispositioned task_route payloads past the 30-day cutoff (payload is JSONB NOT NULL so {} is the realisation of "null out"). DEVIATION: the cleanup marker is recorded by the first skipped run rather than only by the last ok run (unreachable as written); documented at :169-172. tasks_cleanup payloads are deliberately never emptied because they are the bridged-item ledger the re-creation guard reads (retention.mjs:30-33).' },
  { fr: 'FR-7', verdict: 'IMPLEMENTED', tests: 'todoist-brief.test.js 13 + tests/unit/cron/michael-todoist-brief-wiring.test.js 5 (TS-10, TS-12)',
    note: 'runTodoistBrief at scripts/michael/todoist-brief.mjs:127. Exclusions by name (and sub-projects) excludedProjectIds:87-95; EHG project by id into counts.ehg_pointer:164; roleTagFor:172; parseEstMinutes/gradeFor S<30 M<90 L:45-64; ungraded null. Guarded snapshot write upsert ignoreDuplicates:218 then update .is(chosen_action,null).is(moved_back_at,null):223. Mutations only for auto_apply label/reschedule:178, deduped inside mutations_applied:238, redacted to content_sha256:105. Workflow .github/workflows/michael-todoist-brief-cron.yml carries both cron lines with the trailing ET comment, permissions contents read, concurrency + cancel-in-progress, timeout 10, node 22 + npm cache, npm ci --ignore-scripts, no cd, TODOIST_API_TOKEN last. Repo-wide grep for MICHAEL_ENCRYPTION_KEY / GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET across .github/workflows returns nothing. DEVIATION (cosmetic): the Todoist client is lazy-created inside run() rather than at the private main.' },
  { fr: 'FR-8', verdict: 'IMPLEMENTED', tests: 'tests/unit/fleet/setup-michael-host-tasks.test.js 10 (TS-13) + measured --dry-run',
    note: 'scripts/setup-michael-host-tasks.mjs. MEASURED --dry-run on this host: three colon-free names (EHG Michael tasks-classifier / calendar-read / gmail-triage), three CRLF wrappers with cd /d <repo root> + call node <script>, three /Create lines carrying /SC MINUTE /MO 15 /ST 00:00 /F and NEITHER /RU NOR /NP, TR action wscript.exe //B run-hidden.vbs <wrapper>, gmail-triage registered --apply with no --modify, registers nothing, exit 0. Alarm builders reused by name:58-59; TASK_NAME_ILLEGAL_CHARS validated inside the build path:75; main(argv, deps) exported:146; non-win32 exit 2:156-158; unknown-flag refusal:153; --with-modify promotion path:83. Wrappers gitignored (git check-ignore matches .gitignore:502). PR #8379 body carries the PowerShell transcript of the unelevated /Create SUCCESS and the /RU /NP denial, satisfying AC-3/AC-6.' },
  { fr: 'FR-9', verdict: 'IMPLEMENTED_WITH_DEVIATION', tests: 'queue-read.test.js 6 + classify-apply.test.js 13 (TS-14, TS-18)',
    note: 'queue-read at scripts/michael/queue-read.mjs:71 reads through readRows only:80-81, emits items_truncated/tasks_truncated at the 500 bound:88 and takes an exact count only afterwards:41-46,89-90, and returns empty lists at exit 0 when both tables are absent:83. classify-apply at scripts/michael/classify-apply.mjs:150 refuses PROVENANCE_MISSING:95, PRODUCER_UNKNOWN:96, HASH_MISMATCH over the whole envelope minus content_hash:55-59,98, MODEL_NOT_ALLOWED:99, PRODUCED_AT_INVALID:102, ET_DATE_MISMATCH:103, PATH_NOT_ALLOWED:115-119,155; writes by natural key behind .is(class,null).is(action_taken_at,null):191 and .is(effort_grade,null).is(chosen_action,null).is(moved_back_at,null):199; never a summary. role-contract.md section 8 carries the overnight tick paragraph and CLAUDE_MICHAEL.md was regenerated (commit a148450b632). DEVIATIONS: (1) the seat run row does NOT go through runFeeder — it is one accumulating row upserted at attempt 1 on the three-column target (:211), PLAN-accepted; (2) --headers is an explicit flag rather than --json implying the header re-fetch, PLAN-accepted; (3) ITEM_WRITABLE includes action_intent (:46), which the PLAN amendment says never and FR-9 AC-4 says must be refused FIELD_NOT_WRITABLE — see conditions.' },
  { fr: 'FR-10', verdict: 'IMPLEMENTED', tests: 'measured across the branch',
    note: 'MEASURED: no commit tagged SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D touches any path under database/ (git log --grep --name-only over all 40 child-D commits). database/migrations/20260906_michael_tables.sql is child B (commits 06a3161bf8a, 7e63718da1f). Every column each FR reads or writes is declared there and all five upsert conflict targets are backed by a unique index (michael_feeder_runs_date_feeder_attempt_uniq, michael_calendar_day_date_event_uniq, michael_gmail_triage_items_date_thread_uniq, michael_todoist_snapshot_date_task_uniq, michael_gmail_labels_label_id_uniq); the michael_staged_items partial index michael_staged_items_kind_open_idx backs FR-6 dedupe read. Eleven PRs delivered (8362-8385) plus the closing PR 8392. TR-9 holds: every test file is .test.js under lib/michael/, scripts/michael/, tests/unit/fleet/ or tests/unit/cron/, none needs a live michael_* table.' },
];

const findings = [
  { id: 'VAL-D1', severity: 'high',
    title: 'GATE 4 sub-agent coverage: no GITHUB row exists for this SD, and this child ships a GitHub Actions workflow',
    detail: "sub_agent_execution_results for sd_id 3f128d5c-8168-4415-86cc-ab5da4663d11 holds 14 rows: LEAD VALIDATION + Explore; PLAN STORIES, RISK, DATABASE, SECURITY, TESTING, DESIGN; EXEC SECURITY, UAT, TESTING x2; a phase-null REGRESSION; PLAN_VERIFICATION RETRO. GITHUB is MANDATORY for every SD under the GATE 4 coverage rule and is materially in scope here: FR-7 ships .github/workflows/michael-todoist-brief-cron.yml and TR-7 is a workflow-wiring requirement (both-DST cron pair, exact secret set, no Google or encryption token). The forked wiring test tests/unit/cron/michael-todoist-brief-wiring.test.js (5 tests) covers the file's content, but no sub-agent has verified the pipeline itself. REMEDY: invoke the GITHUB sub-agent for phase PLAN_VERIFICATION before EXEC-TO-PLAN's successor handoff." },
  { id: 'VAL-D2', severity: 'high',
    title: "classify-apply's item allow-list includes action_intent, which the PLAN amendment forbids and FR-9 AC-4 says must be refused",
    detail: "The PRD's FR-9 PLAN amendment reads: 'items may set only class, needs_you, needs_you_reason, borderline, verified_by ... never action_intent, action_taken_at, summary, reopened_at, chosen_action, mutations_applied or moved_back_at', and AC-4 reads: 'A verdict file carrying action_intent or action_taken_at on an item ... is refused FIELD_NOT_WRITABLE and writes nothing.' SHIPPED: scripts/michael/classify-apply.mjs:46 ITEM_WRITABLE = ['class','needs_you','needs_you_reason','borderline','verified_by','action_intent'], and scripts/michael/classify-apply.test.js:89 PINS that six-key list while :133 asserts action_intent:'archive' is written through to the row. action_taken_at IS correctly refused (test :93). The FR-9 BODY contradicts its own amendment by listing action_intent in the item shape, so this is a PRD-internal conflict the implementation resolved in favour of the body. MATERIALITY: action_intent is the field gmail-triage's --modify loop reads to decide which Gmail threads to mutate (gmail-triage.mjs:251), so a verdict file can inject a Gmail mutation. Bounded by: INTENT_RE limits the value to archive or label:<id> (classify-apply.mjs:51); the item update is guarded .is(class,null); the envelope is producer-pinned, hash-checked, model-pinned, path-restricted and under 3h old; and the registrar deliberately registers gmail-triage WITHOUT --modify, so nothing executes in the shadow phase. SECURITY reviewed the allow-list at EXEC (row 15af7960) and narrowed it, removing 'unarchive' in PR 8392, but retained archive and label. REMEDY: either record a PLAN amendment accepting action_intent on the seat allow-list with the SECURITY rationale, or drop the key and re-pin the test to the five-key list. Do not leave the PRD and the code disagreeing." },
  { id: 'VAL-D3', severity: 'medium',
    title: 'DOCMON was never invoked although a documentation edit is an FR-9 deliverable',
    detail: "FR-9 makes docs/protocol/michael/role-contract.md section 8 a deliverable and AC-3 makes the CLAUDE_MICHAEL_* regeneration conditional on DB seeding; both landed (commits 8216c616fb8, a148450b632, plus claude-generation-manifest.json). The DOCMON conditional trigger (documentation, docs, README, guide) is met and no DOCMON row exists. Lower severity than VAL-D1 because the content was verified by hand here: the section 8 paragraph names queue-read --json --headers, the Sonnet-classify / Opus-re-judge / ten-percent-sample loop, sub-agent stopping, the runner-produced envelope with producer, run_id, produced_at, model_used, tokens and contentHashFor, and the one accumulating seat venue run row." },
  { id: 'VAL-D4', severity: 'medium',
    title: 'TS-15 measured: two of four feeders exit 2 CONSTANT_MISSING rather than inert exit 0, because constants resolve before the window gate',
    detail: "MEASURED on this host at 2026-09-06 20:30 EDT, well outside every window, all four scripts with --json: gmail-triage -> {ok:true, action:'inert', reason:'outside_et_window'} exit 0; todoist-brief -> same, exit 0; calendar-read -> {ok:false, refusal:'CONSTANT_MISSING', variable:'MICHAEL_EXELON_CALENDAR_ID'} exit 2; tasks-classifier -> {ok:false, refusal:'CONSTANT_MISSING', variable:'MICHAEL_TASKS_DRIVE_FOLDER_ID'} exit 2. Each printed exactly one JSON object. CAUSE, not a bug in itself: resolveConstant runs before runFeeder (calendar-read.mjs:204, tasks-classifier.mjs:148), so the window gate is never reached when the host .env lacks the constant, and FR-2 specifies exactly this named refusal. CONSEQUENCE: the registrar registers these two as every-15-minute host tasks and its preflight does not check the constants (scripts/setup-michael-host-tasks.mjs has no resolveConstant call), so on an unprovisioned host they refuse 96 times a day writing NO run row — invisible to the quiet-tick QUIET_TICK_FEEDER_FAILED gauge, visible only as LastTaskResult 2 in Task Scheduler. REMEDY (either): add the three MICHAEL_* constants to the registrar's preflight so registration refuses on an unprovisioned host, or move the constant resolution inside run() so an off-window fire stays inert. No writes and no API calls occur either way." },
  { id: 'VAL-D5', severity: 'low',
    title: 'A gmail-triage retry can reset a seat-written class on a rule-matched row whose rule declares no class',
    detail: "gmail-triage.mjs:238 guards its update with .is(action_taken_at,null).is(verified_by,null).or('class.is.null,rule_key.not.is.null'), and the patch always carries all five ITEM_UPDATE_KEYS including class. classify-apply.mjs:191 writes only rows with class IS NULL. A thread matched by a gmail rule that sets an action but no class therefore lands rule_key set / class null; the seat classifies it (class set, rule_key still set, verified_by null unless Opus disagreed); a later gmail-triage fire inside the overlapping 04:30-05:30 window passes the or-guard on rule_key.not.is.null and rewrites class back to null. EFFECT is a re-queue on the next seat tick, not a wrong Gmail action or data loss, and it needs a rule with an action and no class. Recording it so a future rule import does not make it routine." },
  { id: 'VAL-D6', severity: 'info',
    title: 'No integration conflict with any sibling child; every sibling-owned file was extended additively',
    detail: "Child D touched five sibling-owned files and each change is additive or contract-preserving. (1) lib/michael/gmail-client.mjs (child C): modifyThread's signature and return shape are unchanged and the only deletions are the inline auth/factory lines refactored into gmailFor()/failure(); child B's scripts/michael/gmail-act.mjs resolves the module lazily and requires only typeof mod.modifyThread === 'function' (gmail-act.mjs:33), which still holds; the default export gained keys and lost none. (2) lib/michael/db.mjs (child B): the only change is `code` and `details` added to the two WRITE_FAILED returns; no existing call site reads either. (3) scripts/michael-quiet-tick.mjs (child A): hhmmToMinutes/inWindow/BRIEF_DEADLINE_ET moved to feeder.mjs and re-imported, with hhmmToMinutes re-exported and inWindow kept as a one-arg wrapper, so the tick's public surface is unchanged and its 11 tests pass. (4) scripts/michael/retention.mjs (child B): michael_staged_items moved out of NEVER_TOUCHED into an empty_payload target exactly as FR-6's amendment mandates; michael_todoist_snapshot stays in NEVER_TOUCHED. (5) docs/protocol/michael/role-contract.md + CLAUDE_MICHAEL.md (child A): a new paragraph in section 8, no existing section rewritten. Provenance parity with child B's rule-encode holds: both pin a producer constant, require run_id, pin the model family, bound produced_at age and hash the canonical JSON (rule-encode.mjs:32,73-80 vs classify-apply.mjs:36,95-102). TR-6 verified by grep: no chairman_handoff write exists anywhere in lib/michael/, scripts/michael/ or the registrar, and fleet-class threads are recorded class 'fleet' only (gmail-triage.mjs:40,220)." },
  { id: 'VAL-D7', severity: 'info',
    title: 'Evaluated commit is one commit behind origin/main; PR 8392 has since landed',
    detail: "Evaluated at 68c2b9481c8, the commit named in the assignment. origin/main is now 25e92767c7f, which merges PR 8392 (child D PR 9): it removes 'unarchive' from classify-apply's INTENT_RE (verified: origin/main:scripts/michael/classify-apply.mjs:52 reads /^(archive|label:[A-Za-z0-9_-]{1,64})$/) and gitignores .artifacts/michael-seat/. Both changes are strictly narrowing and neither alters any finding above. The EXEC TESTING row at 2026-09-07T00:12:44 already carries evaluated sha 9be8d42c396, that PR's commit." },
];

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 93,
  summary: "GATE 4 fidelity check of SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D at 68c2b9481c8. NO FR IS ABSENT: all ten have shipped, tested implementations, and every one of the eight PLAN amendments the assignment named is honoured in code — no /RU /NP on the registrar (measured in a live --dry-run: three /Create lines with /SC MINUTE /MO 15 /ST 00:00 /F and neither flag), start row written status 'skipped' with counts.phase 'started' (feeder.mjs:140), inclusive [start,end] window (feeder.mjs:56-61), --apply writes rows while --modify executes intents with MODIFY_REQUIRES_APPLY (gmail-triage.mjs:143) and the registrar omitting --modify, a per-ET-date ceiling with ceiling_hit made inert for every later fire of the date (gmail-triage.mjs:81-86,270 + feeder.mjs:113), and bounded staged prose with retention null-out (tasks-classifier.mjs:99 200-char title + retention.mjs:34). MEASURED: 368/368 unit tests pass across 22 files covering lib/michael, scripts/michael, the quiet-tick regression, the registrar and the workflow wiring; 153 of those tests are child D's own 14 files. NO INTEGRATION CONFLICT with any sibling: child C's modifyThread contract, child B's db.mjs writeRows and retention, and child A's quiet-tick public surface are all preserved (VAL-D6, with file:line). FR-10 verified by measurement: no commit tagged this SD touches any path under database/, and every column and all five upsert conflict targets are declared in child B's migration. CONDITIONAL rather than PASS on two counts. (VAL-D1, blocking) GITHUB is a GATE 4 mandatory sub-agent and has no row for this SD, while FR-7 ships a GitHub Actions workflow and TR-7 is a workflow-wiring requirement — the pipeline itself has been verified by nobody. (VAL-D2, blocking) classify-apply's item allow-list includes action_intent (classify-apply.mjs:46, pinned by its own test at :89 and exercised at :133), which the FR-9 PLAN amendment explicitly forbids and AC-4 says must be refused FIELD_NOT_WRITABLE; the FR-9 body contradicts its own amendment, and the implementation followed the body. It is materially the field gmail-triage's --modify loop reads, though bounded by INTENT_RE, the class-null write guard, full envelope provenance, and the shadow-phase registration that ships without --modify. Three lower findings: DOCMON never ran although a docs edit is an FR-9 deliverable (VAL-D3); two of four feeders exit 2 CONSTANT_MISSING rather than inert exit 0 on an unprovisioned host because the constants resolve before the window gate, and the registrar's preflight does not check them (VAL-D4, measured); and a gmail-triage retry can reset a seat-written class on a rule-matched row whose rule declares no class, self-healing on the next tick (VAL-D5). Six PRD acceptance-criteria wordings deviate from shipped behaviour; all six were accepted at PLAN or are superseded by the amendments, and each is named with its file:line in the per-FR table.",
  critical_issues: [],
  warnings: [
    "VAL-D1 (blocking for PLAN-TO-LEAD): no GITHUB sub-agent row exists for this SD. GITHUB is MANDATORY under GATE 4 coverage and this child ships .github/workflows/michael-todoist-brief-cron.yml with TR-7 as a workflow-wiring requirement. Invoke GITHUB for phase PLAN_VERIFICATION.",
    "VAL-D2 (blocking for PLAN-TO-LEAD): classify-apply.mjs:46 admits action_intent on the seat item allow-list; the FR-9 PLAN amendment says 'never action_intent' and AC-4 says such a file is refused FIELD_NOT_WRITABLE. Deliberate, tested (classify-apply.test.js:89,133) and SECURITY-reviewed at EXEC, but the PRD and the code disagree. Record the amendment or drop the key.",
    "VAL-D3: DOCMON never ran although FR-9 makes a role-contract edit a deliverable.",
    "VAL-D4 (measured): calendar-read and tasks-classifier exit 2 CONSTANT_MISSING outside their windows on a host without MICHAEL_EXELON_CALENDAR_ID / MICHAEL_TASKS_DRIVE_FOLDER_ID, because resolveConstant precedes runFeeder; TS-15 expects inert exit 0. The registrar registers them anyway with no constants preflight, so an unprovisioned host refuses 96 times a day writing no run row.",
    "VAL-D5: a gmail-triage retry can null a seat-written class on a rule-matched row whose rule declares no class (gmail-triage.mjs:238 vs classify-apply.mjs:191); self-healing on the next seat tick.",
  ],
  recommendations: [
    "Invoke the GITHUB sub-agent for PLAN_VERIFICATION before the PLAN-TO-LEAD handoff (closes VAL-D1).",
    "Resolve the FR-9 action_intent conflict in the PRD: either amend metadata.plan_amendments to accept action_intent on the seat allow-list with the SECURITY rationale, or remove it from ITEM_WRITABLE and re-pin classify-apply.test.js:89 to the five-key list (closes VAL-D2).",
    "Invoke DOCMON for the role-contract section 8 paragraph and the CLAUDE_MICHAEL.md regeneration (closes VAL-D3).",
    "Add the three MICHAEL_* constants to the registrar's preflight, or move constant resolution inside run(), so an off-window fire on an unprovisioned host is inert rather than a silent exit 2 (closes VAL-D4).",
    "Re-run the smoke on the chairman's provisioned host before go-live; only gmail-triage and todoist-brief could be smoked here.",
  ],
  detailed_analysis: { fr_fidelity: frTable, findings },
  metadata: {
    sd_key: SD_KEY,
    sd_uuid: SD_UUID,
    prd_id: 'PRD-SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D',
    phase: 'PLAN_VERIFICATION',
    branch: 'feat/SD-LEO-ORCH-MICHAEL-ROLE-FORMALIZATION-002-D',
    evaluated_commit_sha: HEAD,
    head_commit: HEAD,
    origin_main_at_review: '25e92767c7f1576ffd87167eb7140bc5d8d28909',
    session_id: '45211d51-02f2-4f3e-91cc-0a0524c003fe',
    gate: 'GATE_4_PLAN_VERIFICATION',
    frs_absent: [],
    integration_conflicts: [],
    subagent_coverage: {
      present: ['VALIDATION(LEAD)', 'Explore(LEAD)', 'STORIES', 'RISK', 'DATABASE', 'SECURITY(PLAN,EXEC)', 'TESTING(PLAN,EXEC)', 'DESIGN', 'UAT(EXEC)', 'REGRESSION', 'RETRO'],
      missing_mandatory: ['GITHUB'],
      missing_conditional: ['DOCMON'],
    },
    verification_method: "Full read of the PRD from product_requirements_v2 (10 FRs, 11 TRs, 19 TSs, metadata.plan_amendments) and of every shipped module: lib/michael/{feeder,rules-match,google-clients,gmail-client,constants,db}.mjs, scripts/michael/{calendar-read,gmail-triage,tasks-classifier,todoist-brief,queue-read,classify-apply,retention}.mjs, scripts/setup-michael-host-tasks.mjs, scripts/michael-quiet-tick.mjs, .github/workflows/michael-todoist-brief-cron.yml, docs/protocol/michael/role-contract.md section 8, database/migrations/20260906_michael_tables.sql. Measured: npx vitest run --project unit over lib/michael, scripts/michael, the quiet-tick test, the registrar test and the wiring test (368/368 across 22 files); scripts/michael-quiet-tick.test.js alone (11/11, amendment (l)'s floor); the four feeders invoked for real with --json outside every window; node scripts/setup-michael-host-tasks.mjs --dry-run; git log --grep over all child-D commits with --name-only for the database/ check; git diff of each sibling-owned file against the pre-D base daf75d6dfd7 for deletions; gh pr view 8379 and 8392; a sub_agent_execution_results census by SD uuid.",
    live_measurement: "368/368 unit tests green at 68c2b9481c8. Real-script smoke at 2026-09-06 20:30 EDT: gmail-triage and todoist-brief inert outside_et_window exit 0 with exactly one JSON object each; calendar-read and tasks-classifier CONSTANT_MISSING exit 2 (host .env lacks MICHAEL_EXELON_CALENDAR_ID and MICHAEL_TASKS_DRIVE_FOLDER_ID). Registrar --dry-run printed three wrappers and three /Create lines with no /RU and no /NP and registered nothing. No michael_* row was written and no Google or Todoist API call was made.",
    scope_assessment: 'NO_SCOPE_CREEP_DETECTED',
  },
  validation_mode: 'retrospective',
  phase: 'PLAN_VERIFICATION',
};

const resolution = await resolveSubAgentRepo({
  sdId: SD_UUID,
  targetApplication: 'EHG_Engineer',
  subAgentCode: 'VALIDATION',
  probeExistsRelative: 'package.json',
  supabase,
});
console.log('repo resolution =>', JSON.stringify(resolution));
applySubAgentRepoVerdict(results, resolution);
console.log('verdict after repo verdict =>', results.verdict, '| confidence', results.confidence);

const stored = await storeSubAgentResults('VALIDATION', SD_UUID, sa, results, { phase: 'PLAN_VERIFICATION', sdKey: SD_KEY });
console.log('STORED =>', JSON.stringify(stored)?.slice(0, 1200));
