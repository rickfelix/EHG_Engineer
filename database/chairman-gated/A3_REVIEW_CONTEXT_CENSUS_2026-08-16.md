# Adam Ceremony Census — Staged/Chairman-Gated DDL vs Live Production DB

**Date:** 2026-08-16 · **Mode:** READ-ONLY (SELECT / catalog only; no DDL, no writes)
**Connection:** `SUPABASE_POOLER_URL` → `aws-1-us-east-1.pooler.supabase.com:5432/postgres`, `current_user=postgres`, PostgreSQL 17.4 — **connection succeeded**
**Probe scripts:** `.artifacts/adam-ceremony-census-probe.mjs` (main), `.artifacts/adam-ceremony-census-probe2.mjs` (sub-objects), `.artifacts/adam-ceremony-census-risk.mjs` (blast radius)
**Raw output:** `.artifacts/adam-ceremony-census-raw.json`, `.artifacts/adam-ceremony-census-raw2.json`

**Scope:** 39 files — 15 in `database/chairman-gated/`, 23 `database/migrations/*_STAGED.sql`, plus `20260616_adam_delegated_apply_ledger.sql`. Skipped per instruction: `*_DOWN.sql`, `*_ROLLBACK.sql`, `README.md`, `*acceptance.mjs`.

**Headline:** **23 APPLIED · 15 NOT APPLIED · 1 SUPERSEDED (must NOT be applied) · 0 PARTIAL · 0 UNCERTAIN.**
Every file header claiming "STAGED, NOT APPLIED" was treated as a lead only; **6 of them are in fact live** (see notes) — the header prose is the least reliable field in this corpus.

---

## A. `database/chairman-gated/`

| File | Key objects | Live evidence (probe result) | Verdict | `@approved-by`? | Notes |
|---|---|---|---|---|---|
| `20260802_sd_mutation_audit_trigger.sql` | `FUNCTION log_sd_mutation_audit()`; `TRIGGER trg_sd_mutation_audit` on `strategic_directives_v2` | `fn:log_sd_mutation_audit()` → `{"sig":"log_sd_mutation_audit()","prosecdef":true,...}`; `trg:…trg_sd_mutation_audit` → `"CREATE TRIGGER trg_sd_mutation_audit AFTER UPDATE ON public.strategic_directives_v2 FOR EACH ROW WHEN (((old.status)::text IS DISTINCT FROM (new.status)::text) OR (old.current_phase …) OR (old.claiming_session_id …)) EXECUTE FUNCTION log_sd_mutation_audit()"` | **APPLIED** | yes — `codestreetlabs@gmail.com` | Body carries the `sd_claim_released`/`sd_claim_acquired` branch. `audit_log` exists. |
| `20260803_bound_anon_ingress_source_type_qualifier.sql` | `ALTER POLICY anon_feedback_ingress_bounds` → inline correlated `count(*) < 50` | `pol:public.feedback.anon_feedback_ingress_bounds` → `check_expr` = `… AND (fn_anon_ingress_prior_hour_count((source_type)::text) < CASE (source_type)::text WHEN 'auto_capture' THEN 250 WHEN 'manual_feedback' THEN 200 ELSE 50 END)` | **SUPERSEDED — DO NOT APPLY** | yes — `codestreetlabs@gmail.com` | Live policy is the *successor's* form (see `20260804_ingress_bound_definer_basis`). Applying this file **regresses** the bound: it swaps the SECURITY DEFINER basis back to an inline subquery (which collapses to the inserting role's visible rows — the exact defect 20260804 fixed) and flattens the 250/200/50 ladder to a single 50. |
| `20260803_chairman_decide_null_safe_and_type_honest.sql` | `FUNCTION fn_chairman_decision_value(text,text)`; `CREATE OR REPLACE fn_chairman_decide(uuid,text,text,text,boolean)` | `fn:fn_chairman_decision_value(text,text)` → live, `IMMUTABLE`, body contains `distribution_block`/`gate_failure_escalation`/`gate_override`; `fn:fn_chairman_decide(…)` → body contains `LEFT JOIN`=true, `fn_chairman_decision_value`=true, `v_is_kill_gate`=true, `v_has_venture`=true | **APPLIED** | yes — `codestreetlabs@gmail.com` | Header says "STAGED, NOT APPLIED" — **header is wrong**, the fix is live. |
| `20260803_chairman_queue_truthful_render.sql` | `CREATE OR REPLACE VIEW chairman_all_decision_signals` (adds HELD/unpark_trigger rendering) | `view:chairman_all_decision_signals` → `'HELD until'`=false, `'unpark_trigger'`=false, `'trigger NOT RECORDED'`=false, `'hold'`=false. Live source-4 branch reads `'critical'::text AS priority`, `cd.created_at AS decided_at`, `NULL::uuid AS decided_by`, title `concat('Stage ', cd.lifecycle_stage, ' Chairman Approval')` | **NOT APPLIED** | **no** | **RIVAL of `20260803_chairman_source4_rework.sql`** — both `CREATE OR REPLACE` the same view; last one applied wins. Chairman must pick exactly one. View currently returns 1039 rows. |
| `20260803_chairman_source4_rework.sql` | `CREATE OR REPLACE VIEW chairman_all_decision_signals WITH (security_invoker = on)` (adds `decided_by_user_id`, conditional `decided_at`, CASE priority) | Same probe: live def lacks `decided_by_user_id`; live has `cd.created_at AS decided_at` unconditionally and hardcoded `'critical'::text AS priority`. (`reloptions:["security_invoker=on"]` is already set on the live view — that alone is **not** evidence this file ran.) | **NOT APPLIED** | **no** (its own header states this) | **RIVAL of the file above.** |
| `20260803_current_wave_must_carry_items.sql` | `FUNCTION assert_current_wave_carries_items()`; `CREATE CONSTRAINT TRIGGER trg_current_wave_carries_items` on `roadmap_waves` DEFERRABLE INITIALLY DEFERRED | `fn:assert_current_wave_carries_items()` → `[] EMPTY`; `trg:public.roadmap_waves.trg_current_wave_carries_items` → `[] EMPTY` | **NOT APPLIED** | **no** | `roadmap_waves` + `roadmap_wave_items` both exist. **Apply-time hazard measured** — see Risk note R1. |
| `20260803_session_coordination_scope_anon_reads.sql` | `DROP POLICY service_role_full_access`; `REVOKE SELECT … FROM anon`; `FORCE ROW LEVEL SECURITY` | `pol:…service_role_full_access` → **still present** `{"polpermissive":true,"polcmd":"r","roles":null,"using_expr":"true"}`; `grants:session_coordination` → `anon: REFERENCES,SELECT,TRIGGER`; `rls:` → `relforcerowsecurity:false` | **NOT APPLIED** (0 of 3) | **no** | The live policy is `USING (true)` with `roles=null` (PUBLIC) — an unrestricted read over 3363 rows. |
| `20260804_ai_quality_tuning_symmetric_guards.sql` | `CREATE OR REPLACE VIEW v_ai_quality_tuning_recommendations` (symmetric sample floor 10, NULL-unless-fired `suggested_threshold`) | `view:v_ai_quality_tuning_recommendations` live def is the **old asymmetric** version: `WHEN total < 5 THEN 'INSUFFICIENT DATA: Need more assessments (minimum 5)'`, `pass_rate < 50 AND total >= 5 → DECREASE`, `pass_rate > 90 … AND total >= 10 → INCREASE`, and `suggested_threshold … ELSE pass_threshold` (never NULL). No `INEFFECTIVE CHANGE` arm. | **NOT APPLIED** | **no** | File's post-conditions (`DO $$ … RAISE EXCEPTION`) will run against live data at apply time. |
| `20260804_ingress_bound_definer_basis.sql` | `FUNCTION fn_anon_ingress_prior_hour_count(text)` SECURITY DEFINER; `DROP+CREATE POLICY anon_feedback_ingress_bounds` RESTRICTIVE; `GRANT EXECUTE … TO anon, authenticated` | `fn:…` → `{"prosecdef":true,"provolatile":"s"}`, `SET search_path TO 'public','pg_temp'`, body matches byte-for-byte; `fngrants:` → `anon_exec:true, auth_exec:true`; policy `polpermissive:false` (RESTRICTIVE), `polcmd:"a"`, check_expr uses the function + 250/200/50 CASE | **APPLIED** | yes — `codestreetlabs@gmail.com` | Header says "STAGED, NOT APPLIED. DO NOT RUN THIS FILE." — **header is wrong**, it is live. Supersedes the 20260803 sibling. |
| `20260804_session_coordination_revoke_authenticated_writes.sql` | `REVOKE TRUNCATE, DELETE, INSERT, UPDATE ON session_coordination FROM authenticated` | `grants:session_coordination` → `authenticated: REFERENCES,SELECT,TRIGGER` — none of the four write privileges present | **APPLIED** (end-state satisfied) | yes — `codestreetlabs@gmail.com` | Caveat stated plainly: a REVOKE's end-state is indistinguishable from "never granted". I can assert the target posture holds, not that this file ran. Re-running is a no-op either way. |
| `20260807_belt_capacity_verdicts.sql` | `CREATE TABLE belt_capacity_verdicts` + CHECK; index; `ENABLE RLS`; `REVOKE ALL FROM anon, authenticated` | `rel:` → `"belt_capacity_verdicts"`; `idx:` → `CREATE INDEX … USING btree (recorded_at DESC)`; `rls:` → `relrowsecurity:true`; `grants:` → **service_role only** | **APPLIED** | yes — `rickfelix2000@gmail.com` | Header's "Until this table is APPLIED, scoreLeg4 has zero production callers" no longer holds — it is applied. |
| `20260812_venture_ingest_key_binding.sql` | `TABLE venture_ingest_keys`; 5 functions (`fn_venture_ingest_prior_hour_count`, `_verify_venture_ingest_secret`, `fn_provision_venture_ingest_key`, `fn_submit_venture_feedback`, `fn_submit_venture_error`); widen `feedback_feedback_type_check` (+`venture_feedback`) and `feedback_source_type_check` (+`venture_worker`) | `rel:public.venture_ingest_keys` → `{"rel":null}`; all 5 `fn:` probes → `[] EMPTY`; `check:feedback_accepts` → `{"type_has_venture_feedback":false,"src_has_venture_worker":false}`. Live type check = `sentry_error, user_bug, user_feature_request, user_usability, user_other, venture_error` | **NOT APPLIED** (0 of 8) | **no** | Both constraint widenings are strictly **additive** (no value removed) — safe. `venture_error` is already accepted by a prior migration. **Blocks `20260815_…`** (that file's RPC calls `_verify_venture_ingest_secret`, created here). |
| `20260812_venture_operating_burn_tenant_predicate.sql` | `DROP+CREATE POLICY venture_operating_burn_auth_read` with `fn_user_has_venture_access`; `REVOKE SELECT … FROM anon` | `pol:…venture_operating_burn_auth_read` → **live but `using_expr: "true"`** (the cross-tenant `USING(true)` leak this file fixes), `roles:["authenticated"]`; `grants:venture_operating_burn` → `anon: REFERENCES,SELECT,TRIGGER` still granted | **NOT APPLIED** | **unfilled** — literal placeholder `<unfilled -- set by the chairman's own apply session…>` | `fn_user_has_venture_access(uuid)` exists. **Blast radius today = zero**: `venture_operating_burn` holds **0 rows** (`risk:burn_rows_by_company_null` → `{"total":0}`). Cheapest fix in the bundle. Sibling policy `venture_operating_burn_service` is live and must not be dropped. |
| `20260813_revoke_telegram_bot_insert_feedback.sql` | `DROP POLICY telegram_bot_insert_feedback ON feedback` | `pol:public.feedback.telegram_bot_insert_feedback` → **still present**: `{"polpermissive":true,"polcmd":"a","roles":["anon"],"check_expr":"((source_type)::text = 'telegram'::text)"}` | **NOT APPLIED** | **no** | **Zero live traffic**: 0 telegram-source feedback rows in the last 30 days (`risk:telegram_insert_volume_30d` → `{"n":0,"latest":null}`). Low-risk drop. Note `telegram_bot_select_feedback` is a *separate* live policy this file does not touch. |
| `20260815_venture_user_feedback_ownership_rpc.sql` | `FUNCTION fn_submit_venture_user_feedback(uuid,text,text,text,text)`; `DROP POLICY venture_user_insert_feedback`; `REVOKE EXECUTE ON venture_exists_and_active(uuid)` and `check_feedback_rate_limit(uuid)` FROM anon/authenticated | `fn:fn_submit_venture_user_feedback(…)` → `[] EMPTY`; `pol:…venture_user_insert_feedback` → **still present** `{"roles":["anon"],"check_expr":"((feedback_type)::text ~~ 'user_%') AND (venture_id IS NOT NULL) AND venture_exists_and_active(venture_id) AND (NOT check_feedback_rate_limit(venture_id))"}`; `fngrants:venture_exists_and_active` → `anon_exec:true`; `fngrants:check_feedback_rate_limit` → `anon_exec:true` | **NOT APPLIED** (0 of 4) | **no** | **Highest-risk item in the bundle.** Drops a live anon INSERT policy with real traffic — 17 `user_%` feedback rows in the last 30 days, latest 2026-08-04 (`risk:venture_user_insert_volume_30d`). **Hard-depends on `20260812_…`.** Its own header's line "APPLIED as of 2026-08-15 — confirmed live: venture_ingest_keys and its four functions **absent**" is a statement that the *sibling* is absent — confirmed correct by probe. |

---

## B. `database/migrations/*_STAGED.sql` (+ the delegated-apply ledger)

| File | Key objects | Live evidence (probe result) | Verdict | `@approved-by`? | Notes |
|---|---|---|---|---|---|
| `20260712_drop_venture_user_select_feedback_STAGED.sql` | `DROP POLICY venture_user_select_feedback ON feedback` | `pol:public.feedback.venture_user_select_feedback` → `[] EMPTY`. Live `pols:public.feedback` = `anon_feedback_ingress_bounds, delete_feedback_policy, insert_feedback_policy, select_feedback_policy, telegram_bot_insert_feedback, telegram_bot_select_feedback, update_feedback_policy, venture_user_insert_feedback` | **APPLIED** (end-state satisfied) | yes — `codestreetlabs@gmail.com` | Same DROP-vs-never-existed caveat as above; re-running is a no-op. |
| `20260712_feedback_authenticated_select_caller_venture_STAGED.sql` | `DROP+CREATE POLICY select_feedback_policy` | `pol:…select_feedback_policy` → `{"roles":["authenticated"],"polcmd":"r","using_expr":"(((feedback_type)::text ~~ 'user_%'::text) AND (venture_id IS NOT NULL) AND fn_user_has_venture_access(venture_id))"}` — matches the file exactly | **APPLIED** | **no** | Header says "STAGED — NOT APPLIED BY THIS SD" — **header is wrong**, it is live. |
| `20260716_model_capability_reference_STAGED.sql` | `TABLE model_capability_reference`; RLS; `POLICY model_capability_reference_service_write` | `rel:` → live; `rlspol:` → `relrowsecurity:true, policies:{model_capability_reference_service_write}`; `col:…trusted_for_routing` → `boolean` | **APPLIED** | placeholder — `PENDING — chairman-gated apply ceremony required` | Header's PENDING claim is stale; the table is live. |
| `20260717_fable_suitability_map_STAGED.sql` | `TABLE fable_suitability_map`; 2 indexes; `VIEW v_fable_suitability_map_current`; `REVOKE ALL … FROM anon, authenticated`; `POLICY fable_suitability_chairman_select` | `rel:` → live; `view:` → `reloptions:["security_invoker=on"]`, `SELECT DISTINCT ON (region_key, repo) … ORDER BY region_key, repo, score_version DESC`; `grants:v_fable_suitability_map_current` → **service_role only**; `pol:` → `using_expr:"fn_is_chairman()"`; `idx:idx_fable_suitability_cluster` → live | **APPLIED** | **no** | Header's "typed CEREMONY_PENDING (42P01) flips to live" — already flipped. |
| `20260717_governance_probe_registry_STAGED.sql` | `TABLE governance_probe_registry`; RLS (deliberately no policies); 2 seed rows | `rel:` → live; `rlspol:` → `relrowsecurity:true, policies:null` (matches "service-role only, no anon/authenticated policies"); `rows:` → **both seeds present**: `governance_situation_capture_liveness` (system), `adam_self_adherence_ledger_fresh` (adam) | **APPLIED** | placeholder — `PENDING …` | Seeds included, so nothing outstanding. |
| `20260717_governed_change_proposals_STAGED.sql` | `TABLE governed_change_proposals`; `idx_gcp_class_status`, `idx_gcp_created`; `POLICY gcp_chairman_select` | `rel:` → live; both `idx:` → live; `pol:` → `using_expr:"fn_is_chairman()"` | **APPLIED** | **no** | |
| `20260717_sms_decision_class_whitelist_STAGED.sql` | `TABLE sms_decision_class_whitelist`; RLS; `POLICY …_chairman_select`; `REVOKE INSERT,UPDATE,DELETE FROM anon, authenticated, service_role` | `rel:` → live; `pol:` → `using_expr:"fn_is_chairman()"`; `grants:` → anon/authenticated/service_role each hold only `REFERENCES,SELECT,TRIGGER,TRUNCATE` — **no INSERT/UPDATE/DELETE** | **APPLIED** | **no** | Table holds **0 rows** — that is expected; this file seeds nothing. Its seed sibling (`20260721_…`) is unapplied. |
| `20260717_sms_spend_envelope_STAGED.sql` | `TABLE sms_approved_spend_ledger` + index + 2 policies; 4 columns on `chairman_decisions`; `FUNCTION debit_sms_daily_spend`; rewrite `sms_inbound_log_outcome_check` | `rel:` → live; `rlspol:` → `{sms_approved_spend_ledger_chairman_select, sms_approved_spend_ledger_service_all}`; `idx_sms_approved_spend_ledger_day` → live; `col:chairman_decisions.{amount_usd,undo_deadline,undone_at,consumed_at}` → all 4 present; `fn:debit_sms_daily_spend(uuid,numeric,numeric,numeric)` → live `SECURITY DEFINER`, `search_path public,pg_temp`; `con:sms_inbound_log_outcome_check` → `CHECK (outcome = ANY (ARRAY['answered','expired','no_match','invalid_signature','rate_limited','ambiguous','suspended','undone']))` — **exactly** the file's 8-value list | **APPLIED** (all 4 groups) | **no** | Also live: `chairman_decisions_amount_usd_check`. |
| `20260718_chairman_switchon_policy_STAGED.sql` | `TABLE chairman_switchon_policy`; RLS; `POLICY …_chairman_select`; `REVOKE INSERT,UPDATE,DELETE` | `rel:` → live; `pol:` → `using_expr:"fn_is_chairman()"`; `grants:` → no INSERT/UPDATE/DELETE for anon/authenticated/service_role | **APPLIED** (DDL) | **no** | Table holds **0 rows**. The file itself states the NEVER-AUTO seed rows are "applied by the ceremony, NOT by this migration" — so seeding is a separate, still-outstanding ceremony action. |
| `20260718_model_capability_reference_binding_columns_STAGED.sql` | `ADD COLUMN bound_at, binding_id`; `FUNCTION model_capability_reference_enforce_binding_provenance()`; `TRIGGER trg_…_binding_provenance` | `col:…bound_at` → `timestamp with time zone`; `col:…binding_id` → `uuid`; `fn:` → live, body contains the `TG_OP = 'INSERT' OR OLD.trusted_for_routing IS DISTINCT FROM TRUE` guard; `trg:` → `CREATE TRIGGER … BEFORE INSERT OR UPDATE ON public.model_capability_reference FOR EACH ROW EXECUTE FUNCTION …` | **APPLIED** | **no** (by design) | Depends on `20260716_…` (applied). |
| `20260718_sms_outbound_obligations_STAGED.sql` | `TABLE sms_outbound_obligations`; 2 indexes; RLS; `POLICY …_service_all` | `rel:` → live; `rlspol:` → `{sms_outbound_obligations_service_all}`; `idx_sms_outbound_obligations_claimable` → live | **APPLIED** | **no** | |
| `20260718_switchon_rls_narrow_authenticated_read_STAGED.sql` | `DROP+CREATE POLICY switchon_auto_actions_authenticated_read` and `switchon_decision_audit_authenticated_read`, both `USING (fn_is_chairman())` | Both policies live but **`using_expr:"true"`**, `roles:["authenticated"]` — the unrestricted form this file replaces | **NOT APPLIED** | **no** | `fn_is_chairman()` exists. Should precede `20260728_anon_authenticated_audience…` — see note there. |
| `20260719_coordinator_succession_STAGED.sql` | `TABLE coordinator_role_history`, `TABLE coordinator_follow_ons`; 3 indexes; 2 RLS policies | Both `rel:` → live; `rlspol:` → `{coordinator_role_history_service_write}` / `{coordinator_follow_ons_service_write}`; `idx_coord_role_history_open`, `idx_coord_follow_ons_open` → live | **APPLIED** | **no** | Header's "merged-but-unapplied — applying it activates history + follow-ons" is stale; already active. |
| `20260719_forecast_ledger_STAGED.sql` | `TABLE forecast_ledger`; 2 indexes; `POLICY forecast_ledger_service_all`; `FUNCTION forecast_ledger_seal_guard()`; `TRIGGER forecast_ledger_seal` | `rel:` → live; `rlspol:` → `{forecast_ledger_service_all}`; `idx_forecast_ledger_question_class` → live; `fn:` → live with the sealed-field IS DISTINCT FROM chain; `trg:` → `BEFORE UPDATE … EXECUTE FUNCTION forecast_ledger_seal_guard()` | **APPLIED** | **no** | |
| `20260720_fleet_desired_slots_STAGED.sql` | `TABLE fleet_desired_slots`; index; RLS policy | `rel:` → live; `rlspol:` → `{fleet_desired_slots_service_role_all}`; `idx_fleet_desired_slots_enabled` → live | **APPLIED** | **no** | Header's "merged-but-unapplied … stderr canary" is stale. |
| `20260720_role_drain_sets_STAGED.sql` | `TABLE role_drain_sets`; index; RLS policy | `rel:` → live; `rlspol:` → `{role_drain_sets_service_role_all}`; `idx_role_drain_sets_role_active` → live | **APPLIED** | yes — `codestreetlabs@gmail.com` | Header's "merged-but-unapplied" is stale. |
| `20260721_chairman_decisions_channel_STAGED.sql` | `ALTER TABLE chairman_decisions ADD COLUMN channel TEXT CHECK (channel IN ('sms','console'))` | `col:chairman_decisions.channel` → `[] EMPTY`; `con:chairman_decisions.all_checks` → no `chairman_decisions_channel_check` (live checks: `amount_usd`, `consequence_level`, `decision`, …) | **NOT APPLIED** | **no** | Purely additive nullable column — lowest risk in the bundle. |
| `20260721_sms_whitelist_seed_stage_gate_class_STAGED.sql` | `INSERT INTO sms_decision_class_whitelist ('blocking stage-gate approval', …) ON CONFLICT DO NOTHING` | `seed:sms_whitelist_stage_gate` → `[] EMPTY`; `rows:sms_decision_class_whitelist_count` → `{"n":0}` | **NOT APPLIED** | **no** | Data-only (no DDL). Target table is live, so it can run standalone. Must run **as table owner (postgres)** — INSERT is revoked from service_role. Idempotent. |
| `20260728_adherence_ledger_check_class_STAGED.sql` | `ADD COLUMN adam_adherence_ledger.check_class`; CHECK constraint; partial index | `col:…check_class` → `[] EMPTY`; `con:…_check_class_check` → `[] EMPTY`; `idx:idx_adam_adherence_ledger_check_class` → `[] EMPTY` | **NOT APPLIED** (0 of 3) | **no** | `adam_adherence_ledger` exists. Additive nullable column; existing rows stay NULL = "unclassified". Low risk. |
| `20260728_anon_authenticated_audience_and_narrowing_STAGED.sql` | `DROP+CREATE POLICY research_intel_ref_read` (`is_current = true OR service_role`); 3 `COMMENT ON TABLE` with an `Audience:` line | `pol:…research_intel_ref_read` → live but **old form** `((auth.role() = 'authenticated'::text) OR (auth.role() = 'service_role'::text))`, `roles:null` (PUBLIC); `comment:research_intelligence_reference` / `switchon_auto_actions` / `switchon_decision_audit` → **none contain `Audience:`** | **NOT APPLIED** (0 of 4) | **no** | **Ordering dependency:** its `switchon_*` comments assert *"Narrowed to fn_is_chairman() by …20260718_switchon_rls_narrow_authenticated_read_STAGED.sql"*. Applying this **before** that file writes a comment claiming a narrowing that is not live — a self-inflicted stale-narration defect. Apply `20260718_switchon…` first. |
| `20260728_chairman_override_dedup_STAGED.sql` | `DROP INDEX`; `CREATE UNIQUE INDEX idx_feedback_chairman_override_dedup` partial on `(category, source_type, (metadata->>'override_key'))` | `idx:idx_feedback_chairman_override_dedup` → `[] EMPTY` | **NOT APPLIED** | **no** | UNIQUE index — will fail if duplicate `(category, source_type, override_key)` rows already exist within the partial predicate (`category='chairman_override' AND source_type='auto_capture'`). Not probed for duplicates; worth a pre-check at apply time. |
| `20260802_chairman_queue_age_escalation_STAGED.sql` | `CREATE OR REPLACE VIEW chairman_pending_decisions`; `ALTER VIEW … SET (security_invoker = on)` | `view:chairman_pending_decisions` → `reloptions:["security_invoker=on"]`; def contains `'72:00:00'`=true, `effective_rank`=true, `age_escalated`=true, `priority_rank`=true; live def carries `CASE effective_rank WHEN 1 THEN 'critical' …` and `effective_rank < priority_rank AS age_escalated` | **APPLIED** | **no** | Header's "until it is applied the two surfaces disagree" is stale — applied. Reads `chairman_unified_decisions` (exists). |
| `20260807_trend_eyes_receipt_service_role_only_STAGED.sql` | `DROP+CREATE POLICY trend_eyes_receipt_service_role_only` RESTRICTIVE FOR SELECT TO anon, authenticated | `pol:…` → `{"polpermissive":false,"polcmd":"r","roles":["authenticated","anon"],"using_expr":"(dimension IS DISTINCT FROM 'trend_eyes_sweep_receipt'::text)"}` — matches exactly | **APPLIED** | yes — `rickfelix2000@gmail.com` | Header's "APPLIED 2026-08-07 — historical record" is **confirmed accurate**. The only file whose header self-corrected. |
| `20260616_adam_delegated_apply_ledger.sql` | `TABLE adam_delegated_apply_ledger`; 2 indexes; 2 RLS policies | `rel:` → live; `rlspol:` → `{adam_delegated_apply_ledger_read, adam_delegated_apply_ledger_service_write}`; `idx_adam_delegated_apply_ledger_created` → live | **APPLIED** | yes — `codestreetlabs@gmail.com` | Chairman-bootstrapped as documented. |

---

## UNAPPLIED BUNDLE (proposed ceremony order)

15 files. Ordered by dependency first, then ascending risk. **`20260803_bound_anon_ingress_source_type_qualifier.sql` is deliberately excluded — it is superseded and applying it would regress a live security bound.**

### Group 1 — additive, no live consumer, no dependencies (apply first, cheapest)

1. **`20260721_chairman_decisions_channel_STAGED.sql`** — adds nullable `chairman_decisions.channel` + CHECK ('sms','console'). *Risk: none.* Pure `ADD COLUMN IF NOT EXISTS`; reuses existing table RLS.
2. **`20260728_adherence_ledger_check_class_STAGED.sql`** — adds nullable `adam_adherence_ledger.check_class` + CHECK + partial index. *Risk: none.* Existing rows stay NULL ("unclassified").
3. **`20260721_sms_whitelist_seed_stage_gate_class_STAGED.sql`** — seeds one row into `sms_decision_class_whitelist` (currently 0 rows). *Risk: low, but must run **as table owner `postgres`** — INSERT is revoked from service_role. `ON CONFLICT DO NOTHING` makes it idempotent.*
4. **`20260728_chairman_override_dedup_STAGED.sql`** — creates a UNIQUE partial index on `feedback`. *Risk: **apply-time failure** if duplicate `(category, source_type, metadata->>'override_key')` rows already exist under `category='chairman_override' AND source_type='auto_capture'`. Run a duplicate-count SELECT immediately before.*
5. **`20260812_venture_operating_burn_tenant_predicate.sql`** — replaces `USING(true)` with `fn_user_has_venture_access(venture_id)`; revokes anon SELECT. *Risk: **zero blast radius today** — `venture_operating_burn` holds 0 rows. Do not drop the sibling `venture_operating_burn_service` policy. `@approved-by` is an **unfilled placeholder** — the chairman must fill it with his own git `user.email` before apply.*

### Group 2 — ordered pair (narrowing must precede its own documentation)

6. **`20260718_switchon_rls_narrow_authenticated_read_STAGED.sql`** — narrows two `switchon_*` authenticated-read policies from `USING(true)` to `fn_is_chairman()`. *Risk: any non-chairman authenticated reader of `switchon_auto_actions` / `switchon_decision_audit` loses access. That is the intended tightening.*
7. **`20260728_anon_authenticated_audience_and_narrowing_STAGED.sql`** — replaces `research_intel_ref_read` with a real row predicate (`is_current = true OR service_role`) and writes three `Audience:` table comments. **Must come after #6** — its comments assert the #6 narrowing is live. *Risk: authenticated readers lose access to superseded (`is_current=false`) revisions.*

### Group 3 — chairman must choose exactly ONE (mutually exclusive rivals)

8. **`20260803_chairman_queue_truthful_render.sql`** *(option A)* — rewrites `chairman_all_decision_signals` source-4 to render ratified holds (`HELD until: <unpark_trigger>` / `trigger NOT RECORDED`) and a `held` status.
   **or**
   **`20260803_chairman_source4_rework.sql`** *(option B)* — rewrites the same branch to fix `decided_at`/`decided_by` honesty (`decided_by_user_id`, conditional `decided_at`) with `security_invoker = on`.
   *Risk: both are `CREATE OR REPLACE` on the same view (1039 live rows) — applying both means last-writer-wins and silently discards the other's fix. Neither carries an `@approved-by`. If both behaviours are wanted, they need to be merged into one file **before** the ceremony, not applied in sequence.*

### Group 4 — behaviour-changing view / trigger

9. **`20260804_ai_quality_tuning_symmetric_guards.sql`** — replaces `v_ai_quality_tuning_recommendations` with a symmetric sample floor (10 for both arms) and a NULL-unless-fired `suggested_threshold`. *Risk: the file's in-transaction `DO $$ … RAISE EXCEPTION` post-conditions run against live `ai_quality_assessments` data; a violation rolls the whole apply back (fail-safe, but expect it to be the failure you see).*
10. **`20260803_current_wave_must_carry_items.sql`** — adds a DEFERRABLE constraint trigger rejecting an approved+`now` wave with zero items. **⚠ R1 — measured hazard:** `roadmap_waves` currently holds a row in exactly that forbidden state — `id=512c7478-c5df-4a5c-be00-49aa4d101e5c, sequence_rank=0, status='approved', time_horizon='now', items=0`. `CREATE CONSTRAINT TRIGGER` does **not** validate existing rows, so the apply itself will succeed — but the **next** `UPDATE OF status, time_horizon` on that wave will fail at COMMIT. *Either populate/re-horizon that wave in the same ceremony, or apply knowing that row is now frozen.* (Three other approved+now waves hold 5, 19 and 17 items and are unaffected.)

### Group 5 — session_coordination read scoping

11. **`20260803_session_coordination_scope_anon_reads.sql`** — drops the `USING(true)` PUBLIC policy `service_role_full_access`, revokes anon SELECT, forces RLS. *Risk: this removes the **only** policy on `session_coordination` (3363 rows). After apply the table has RLS enabled, FORCE on, and **zero policies** — every non-`service_role` principal reads zero rows. `service_role` is unaffected (BYPASSRLS). Confirm no anon/authenticated client reads this table before applying; the file's own header mandates a two-sided post-apply probe (anon denied AND service_role full count).*

### Group 6 — the anon-feedback-ingress trilogy (strict order; highest risk)

12. **`20260812_venture_ingest_key_binding.sql`** — creates `venture_ingest_keys` + 5 functions and widens two `feedback` CHECK constraints (**additive only**: `+venture_feedback`, `+venture_worker`). **Must be first of the three.** *Risk: low on its own (nothing depends on it yet); it is the enabler. Carries **no** `@approved-by` — its header explicitly reserves that for the chairman's apply session and warns that finding it applied without one is itself the defect to report.*
13. **`20260813_revoke_telegram_bot_insert_feedback.sql`** — drops the `telegram_bot_insert_feedback` anon INSERT policy. *Risk: **low** — 0 telegram-source feedback rows in the last 30 days. Does not touch the separate live `telegram_bot_select_feedback` policy.*
14. **`20260815_venture_user_feedback_ownership_rpc.sql`** — creates `fn_submit_venture_user_feedback`, **drops the live anon INSERT policy `venture_user_insert_feedback`**, and revokes anon EXECUTE on `venture_exists_and_active` / `check_feedback_rate_limit`. **Hard-depends on #12** (its body calls `_verify_venture_ingest_secret`). *Risk: **highest in the bundle** — this drops an INSERT policy that live clients use: 17 `user_%` feedback rows in the last 30 days (latest 2026-08-04). Any client still POSTing directly to `feedback` breaks the moment this lands; they must be cut over to the RPC (which requires a per-venture ingest secret provisioned via `fn_provision_venture_ingest_key`, i.e. #12 must not only be applied but **used** to mint keys). Do not apply #14 until client migration is confirmed.*

---

## Cross-cutting observations

- **File headers are unreliable as state.** Six files whose headers read "STAGED, NOT APPLIED" / "DO NOT RUN THIS FILE" / "merged-but-unapplied" are in fact **live**: `20260803_chairman_decide_null_safe_and_type_honest`, `20260804_ingress_bound_definer_basis`, `20260712_feedback_authenticated_select_caller_venture_STAGED`, `20260719_coordinator_succession_STAGED`, `20260720_fleet_desired_slots_STAGED`, `20260720_role_drain_sets_STAGED` (plus `20260716`/`20260717_governance_probe_registry` whose `@approved-by` still reads `PENDING`). Only `20260807_trend_eyes_receipt_service_role_only_STAGED` has a header that correctly self-corrected to APPLIED. **The ceremony should apply from this census, not from header prose.**
- **`@approved-by` coverage across the 15-file unapplied bundle: 0 filled, 1 unfilled placeholder, 14 absent.** Every file in the ceremony bundle needs an attestation line added at apply time.
- **Two REVOKE/DROP verdicts are end-state assertions, not execution proof** — `20260804_session_coordination_revoke_authenticated_writes` and `20260712_drop_venture_user_select_feedback_STAGED`. The target posture holds; whether *this file* produced it is not decidable from the catalog. Both are no-ops if re-run.
- **No file references a table or function that does not exist.** All referenced dependencies were probed present: `roadmap_waves`, `roadmap_wave_items`, `switchon_auto_actions`, `switchon_decision_audit`, `research_intelligence_reference`, `ai_quality_assessments`, `sms_inbound_log`, `chairman_unified_decisions`, `session_coordination`, `venture_operating_burn`, `codebase_health_snapshots`, `audit_log`, `adam_adherence_ledger`, and functions `fn_is_chairman()`, `fn_user_has_venture_access(uuid)`, `venture_exists_and_active(uuid)`, `check_feedback_rate_limit(uuid)`.
