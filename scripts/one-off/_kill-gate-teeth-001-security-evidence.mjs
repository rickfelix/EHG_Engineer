import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../../lib/sub-agent-executor/results-storage.js';
import { getSupabaseClient } from '../../lib/sub-agent-executor/supabase-client.js';
import { isMainModule } from '../../lib/utils/is-main-module.js';

const SD_KEY = 'SD-LEO-INFRA-KILL-GATE-TEETH-001';

async function main() {
  const supabase = await getSupabaseClient();
  const resolution = await resolveSubAgentRepo({ sdId: SD_KEY, targetApplication: 'EHG_Engineer', subAgentCode: 'SECURITY', supabase });

  let results = {
    verdict: 'FAIL',
    confidence_score: 90,
    findings: [
      'BLOCKING / HIGH -- DEFAULT-PRIVILEGE ANON EXPOSURE ON BOTH NEW TABLES (measured live on this project 2026-08-29 via pg_default_acl, direct pooler connection as current_user=postgres). pg_default_acl row (defaclrole=postgres, schema=public, objtype=r) is {postgres=arwdDxtm/postgres,anon=arwdDxtm/postgres,authenticated=arwdDxtm/postgres,service_role=arwdDxtm/postgres}. Every table CREATEd by postgres (this house\'s migration apply identity) in schema public is BORN with INSERT/SELECT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER/MAINTAIN already granted to anon and authenticated BY NAME. `REVOKE ALL ON kill_gate_sealed_predictions FROM PUBLIC` (line 162) removes a grant that was never there -- PUBLIC does not appear in that ACL -- and leaves anon=arwdDxtm fully intact. The migration also deliberately does NOT enable RLS. Net effect on apply: kill_gate_sealed_predictions and kill_gate_teeth_proof_records land with full anon CRUD at the Postgres privilege layer and NO RLS backstop. Corroborated on existing tables: chairman_decisions and system_events both carry relacl anon=arwdDxtm today (they are protected only because relrowsecurity=true). Population census: 834 of 869 public tables have RLS ON; all 35 RLS-OFF tables are anon-SELECTable, and 760 of 869 are anon-DELETEable. These two tables would join the 35-table exposed minority -- and they are the tamper-evidence registry, not throwaway snapshots.',
      'BLOCKING / HIGH -- SAME DEFACL DEFECT ON THE SECURITY DEFINER FUNCTION. pg_default_acl (postgres, public, objtype=f) is {postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}. `REVOKE ALL ON FUNCTION kill_gate_teeth_discharged_predictions() FROM PUBLIC` (line 188) is again a no-op against a by-name anon grant. The function is SECURITY DEFINER and therefore executes with the OWNER\'s (postgres) privileges regardless of caller grants -- so on apply, anon gains a working read of every discharged sealed prediction including the discharged_content jsonb payload, via PostgREST RPC. This is the exact recurrence engine this repo already documented in database/chairman-gated/20260816_defacl_anon_auth_axis.sql ("a REVOKE ... FROM PUBLIC-only ADP removes a grant that was never there and leaves the anon/authenticated defaults fully intact ... which is the recurrence engine behind repeated SECDEF exposure findings on this database"). That closing migration is STAGED, NOT APPLIED -- the exposure is live today, so this migration cannot rely on it.',
      'THE BLINDNESS PROOF GUARDS THE WRONG HALF. The privilege-based blindness mechanism against kill_gate_traversal_ro is genuinely correct and the integration test (tests/integration/kill-gate-teeth/sealed-predictions-blindness.test.js) would pass -- kill_gate_traversal_ro is NOT in the default ACL and gains nothing from it, so the 42501 on the base table is real. But the test asserts blindness against the one principal that is not reachable from the internet, while anon (the browser-distributed API-key role) is granted full CRUD on the same table by the same migration. The proof is honest about what it measures and silent about the principal that actually matters. This is this codebase\'s own WRONG-HALF=UMBRELLA / HONEST-BY-CONSTR class.',
      'THE RLS REJECTION REASONING IS CORRECT BUT OVER-GENERALIZED. The migration header (lines 20-30) correctly argues that RLS is a no-op against service_role\'s BYPASSRLS and therefore cannot deliver traversal-side blindness. That is true. But it then drops RLS entirely, discarding the protection RLS provides against anon/authenticated -- a different principal that BYPASSRLS does not apply to. Privilege-blindness and RLS are orthogonal defenses against different principals; adopting one is not a reason to omit the other.',
      'VERIFIED SAFE -- SECURITY DEFINER search_path. `SET search_path = pg_catalog, public` (line 177) is present and explicit on the function definition, so the search_path is fixed at execution and is NOT caller-controllable. pg_catalog is first and contains no object that could shadow kill_gate_sealed_predictions; the target table lives in public, the only other entry. No search_path-injection / object-shadowing surface. The classic SECURITY DEFINER vulnerability class is correctly closed here.',
      'VERIFIED SAFE -- no dynamic SQL in the function. The body is a single static `SELECT * FROM kill_gate_sealed_predictions WHERE discharged_at IS NOT NULL;` in LANGUAGE sql. Zero parameters, zero string concatenation, zero EXECUTE/format(), zero untrusted input reaching the parser. No injection surface exists in the function.',
      'VERIFIED CORRECT -- `GRANT kill_gate_traversal_ro TO postgres, service_role` (line 159) is a ROLE MEMBERSHIP grant, and the PRD claim about it is accurate. Postgres semantics: GRANT <role> TO <member> makes postgres/service_role members of kill_gate_traversal_ro, allowing them to SET ROLE to it and (if INHERIT) to inherit its privileges -- which are exactly: EXECUTE on kill_gate_teeth_discharged_predictions() and nothing else. Privileges do not flow the other direction: kill_gate_traversal_ro does not become a member of postgres or service_role and gains none of their privileges. Net privilege delta for postgres/service_role is nil (they can already read the base table directly), and the grant is a DE-escalation vector (it lets a high-privilege session drop into the restricted identity), not an escalation. The NOINHERIT attribute on kill_gate_traversal_ro governs roles it is a member OF, which is none -- harmless but not load-bearing.',
      'CONSTRAINTS VERIFIED CORRECTLY RESTRICTIVE (as far as they go). sealed_hash `~ \'^[0-9a-f]{64}$\'` is fully anchored and, with Postgres newline-sensitive matching OFF by default, `$` matches only end-of-string -- no embedded-newline bypass. sealer_identity IN (solomon, chairman) and expected_verdict IS NULL OR IN (fired,hold,pass) are correctly enumerated. The discharge-pair symmetry `(discharged_at IS NULL) = (discharged_content IS NULL)` is NULL-safe by construction (IS NULL always yields a non-NULL boolean, so the CHECK can never evaluate to NULL and be trivially satisfied) and correctly forbids both half-discharged shapes.',
      'VERIFIED -- READ-ONLY DEPENDENCY CLAIM HOLDS. `git diff --name-only main...feat/SD-LEO-INFRA-KILL-GATE-TEETH-001` returns exactly 11 files: 1 migration, 3 lib/eva/kill-gate-teeth/* modules, 4 scripts/one-off/* artifacts, 3 tests. lib/eva/lifecycle/thesis-kill-gate.js is NOT among them (its last touching commit is db89b443961, a different SD), and no other pre-existing kill-gate file is modified. firing-verification.js imports getThesisKillFlag as a pure read. The claim is true.',
      'VERIFIED -- NO SQL INJECTION SURFACE IN THE JS. Every database access in firing-fence.js and firing-verification.js goes through the parameterized supabase-js builder (.from/.select/.eq/.in/.order/.insert/.rpc/.maybeSingle). There is no raw SQL string, no .sql(), no template literal interpolated into a query expression, and no schema/column name derived from input.',
      'VERIFIED -- NO SECRET OR CREDENTIAL HANDLING. Neither firing-fence.js nor firing-verification.js reads process.env, constructs a client, or touches a key; the SupabaseClient is injected by the caller. No hardcoded credentials, tokens, connection strings, or hashes-of-secrets appear in either file.',
      'VERIFIED -- FIRING FENCE IS GENUINELY FAIL-CLOSED ON EVERY PATH. Traced all exits of evaluateFiringFence: (1) venture read wrapped in try/catch with `if (error) throw error` inside the try, so both thrown exceptions and returned PostgREST errors land in the catch -> armed:false/VENTURE_READ_FAILED; (2) venture-1 regex match -> armed:false; (3) attestation read identically wrapped -> armed:false/NO_UNATTENDED_PROOF; (4) no valid attestor found (uses optional chaining `row?.payload?.sealed_by`, cannot throw on malformed rows) -> armed:false; (5) the single armed:true return is reachable only after an affirmative attestation row is found. Even a null/undefined `supabase` argument throws a TypeError inside the first try and is caught. There is no code path where an exception, a null result, or a malformed row yields armed:true.',
    ],
    warnings: [
      'MEDIUM -- SEALING IS NOT ACTUALLY TAMPER-EVIDENT: NO HASH/CONTENT BINDING. The migration never verifies that discharged_content hashes to sealed_hash. A discharger can reveal arbitrary content that does not correspond to the committed hash and the DB accepts the row; the CHECK constraints only assert that sealed_hash is 64 hex characters, not that it is the hash OF anything. For a scheme whose entire value proposition is "hash lodged before the run, content revealed after", this is the core invariant and it is unenforced. Recommend a CHECK or BEFORE-trigger asserting encode(digest(discharged_content::text, \'sha256\'), \'hex\') = sealed_hash (jsonb::text is canonically normalized by Postgres, so the serialization is stable) -- noting digest() lives in the extensions schema on Supabase and must be schema-qualified. If enforcement is deferred, it must be a named, disclosed residual in the same style as the migration\'s existing residual-gap comment, not an unstated omission.',
      'MEDIUM -- SEALED COLUMNS ARE MUTABLE AFTER SEALING. Nothing prevents UPDATE of sealed_hash, sealer_identity, or sealed_at on an existing row. Any principal with UPDATE (service_role today; anon too, per the blocking finding above) can rewrite sealed_hash at discharge time to match whatever content it wants to reveal. This defeats the sealing property completely and independently of the grant defect, and it also silently defeats the hash-binding check recommended above if that check is added without immutability. Recommend a BEFORE UPDATE trigger rejecting changes to (sealed_hash, sealer_identity, sealed_at) and to discharged_at/discharged_content once non-NULL (discharge should be write-once).',
      'MEDIUM -- THE UNATTENDED ATTESTATION IS SELF-ASSERTED, NOT AUTHENTICATED. firing-fence.js arms on a system_events row with event_type=PROBE_S0_S5_UNATTENDED_ATTESTATION whose `payload.sealed_by` is a plain string compared against [\'solomon\',\'chairman\']. Nothing authenticates the writer: any principal that can INSERT into system_events -- which includes the shared service_role key every fleet worker holds -- can write {"sealed_by":"solomon"} and arm the fence. The module header states the row is "written by a non-fleet party", but no mechanism enforces that; the fleet can self-attest its own unattendedness. This is the GUARD-PROSE=UNVERIFIED / readers-not-writer class. Recommend binding the attestation to something the fleet cannot mint (a signature over a chairman-held key, a chairman_decisions row with an approval workflow, or an INSERT-restricted surface) before this fence is relied on to gate a live probe.',
      'MEDIUM -- VENTURE-1 HARD EXCLUSION IS PINNED TO A MUTABLE NAME. VENTURE_ONE_NAME_RE = /^apexniche$/i matches ventures.name exactly and case-insensitively, with no trimming and no UUID fallback. A rename, a trailing space, or "Apex Niche" bypasses the exclusion the module describes as UNCONDITIONAL; ventures.name is writable by anyone with UPDATE on ventures. The comment calls this "belt AND braces", but both braces are one mutable text field. Recommend pinning the exclusion to the venture UUID (stable, immutable) with the name match retained only as a secondary net.',
      'LOW -- ABSENT VENTURE ROW IS NOT DISTINGUISHED FROM NULL NAME. `.maybeSingle()` returns data=null with no error when the venture row does not exist, so ventureName stays null and the venture-1 check is skipped without any signal. This is not a fail-open (a non-existent venture cannot be ApexNiche, and arming still requires an affirmative attestation), but the fence cannot tell "venture missing" from "venture exists, name unreadable", and the latter arguably deserves the VENTURE_READ_FAILED refusal it currently gets only on a thrown error.',
      'LOW -- POSTGREST .in() FILTER-LIST ROBUSTNESS (not a live vulnerability). crossCheckDecisionRouting builds `thesis_kill_tier_b:${id}` from system_events payload criterionIds and passes the array to .in(). supabase-js quotes list values containing , ( ) but does not escape embedded double quotes, so a criterionId containing both could in principle perturb the filter list. Not exploitable today: criterionIds originate from thesis-kill-gate.js\'s own internal criteria, never from user input, and the query is a read used only as a secondary cross-check. Noted for defense-in-depth; a shape assertion on criterionId would close it.',
      'LOW -- SEARCH_PATH HARDENING (stylistic). The modern PostgreSQL-recommended form for SECURITY DEFINER is `SET search_path = \'\'` with every object reference fully schema-qualified (public.kill_gate_sealed_predictions). The shipped `SET search_path = pg_catalog, public` is safe as analyzed above, but the stricter form removes reliance on the reader having to reason about what could occupy `public`.',
      'LOW -- kill_gate_teeth_proof_records.gate_type is `text NOT NULL` with no CHECK, while every sibling enumerated column (observed_verdict, observed_source, flag_mode) carries one. firing-verification.js only ever writes \'kill\' or \'none\'. A CHECK would keep the column honest against a future writer.',
    ],
    recommendations: [
      'REQUIRED BEFORE APPLY -- add to the migration, immediately after each CREATE TABLE, the house-canonical posture already documented and used in database/migrations/20260809_venture_demand_verdicts.sql lines 137-154 (which names this exact hazard verbatim: "Supabase\'s ALTER DEFAULT PRIVILEGES grants anon/authenticated on new public tables"): (a) REVOKE ALL ON public.<table> FROM anon, authenticated, PUBLIC; (b) GRANT ALL ON public.<table> TO service_role; (c) ALTER TABLE public.<table> ENABLE ROW LEVEL SECURITY; (d) a service_role-only FOR ALL policy. Apply to BOTH kill_gate_sealed_predictions and kill_gate_teeth_proof_records. Note the ordering rationale from that precedent: RLS alone is safety-by-coincidence because the grant survives, so do both, not either.',
      'REQUIRED BEFORE APPLY -- REVOKE ALL ON FUNCTION public.kill_gate_teeth_discharged_predictions() FROM anon, authenticated, PUBLIC; then GRANT EXECUTE ... TO kill_gate_traversal_ro (and service_role if a service-role call site is intended -- firing-verification.js readDischargedPredictions calls it via .rpc() under the shared service-role client, so service_role EXECUTE is functionally required). Naming anon and authenticated explicitly is mandatory; FROM PUBLIC alone provably does not reach the by-name default grants on this database.',
      'REQUIRED BEFORE APPLY -- add a post-apply verification DO block, in the style of the same venture_demand_verdicts precedent, that asserts the achieved end state rather than the statements issued: for each new table assert has_table_privilege(\'anon\', \'public.<table>\', \'SELECT\') IS FALSE and relrowsecurity IS TRUE, and assert has_function_privilege(\'anon\', \'public.kill_gate_teeth_discharged_predictions()\', \'EXECUTE\') IS FALSE. Without this the migration cannot distinguish "revoked" from "revoked something that was never granted", which is precisely the failure mode that produced this finding.',
      'EXTEND THE BLINDNESS TEST TO THE PRINCIPAL THAT MATTERS. tests/integration/kill-gate-teeth/sealed-predictions-blindness.test.js should add a third assertion: SET ROLE anon (and authenticated) and assert 42501 on the base table AND on the RPC. As written the suite would have stayed green through this entire exposure, which makes it a proof about kill_gate_traversal_ro rather than a proof about readability.',
      'CLOSE THE SEALING INVARIANTS: add hash/content binding at discharge and write-once immutability on the sealed columns (see warnings 1-2), or record both as explicitly disclosed residual gaps alongside the migration\'s existing residual-blindness-wiring gap comment. A registry that is neither hash-bound nor immutable does not deliver tamper-evidence regardless of who can read it.',
      'BEFORE ANY LIVE PROBE IS ARMED: replace or reinforce the self-asserted payload.sealed_by attestation with a surface the fleet cannot mint, and pin the venture-1 exclusion to a UUID. Both are cheap now and become load-bearing the moment the chairman-gated probe is authorized.',
      'RE-RUN SECURITY after remediation. The FAIL here is confined to grant posture and sealing invariants -- the SECURITY DEFINER hygiene, the role-membership semantics, the fail-closed control flow, the injection surface, and the read-only-dependency claim all pass on inspection and would not need re-examination beyond a diff review.',
    ],
    summary: 'FAIL -- 2 blocking findings. The migration\'s privilege-based blindness design against kill_gate_traversal_ro is sound (SECURITY DEFINER search_path correctly pinned, static SQL body with no injection surface, role-membership grant correctly non-escalating as the PRD claims, CHECK constraints correctly restrictive and NULL-safe), and the JS modules are clean (fully parameterized queries, no credential handling, genuinely fail-closed on every traced path). But REVOKE ALL ... FROM PUBLIC is provably insufficient on THIS database: live measurement of pg_default_acl (postgres/public) shows anon and authenticated hold arwdDxtm on tables and EXECUTE on functions BY NAME, not via PUBLIC. On apply, both new tables would land with full anon CRUD and RLS disabled, and the SECURITY DEFINER function would be anon-executable -- exposing the sealed-prediction registry to the browser-distributed anon key while the shipped blindness test, which only exercises kill_gate_traversal_ro, stays green. Secondary: the seal is neither hash-bound to its revealed content nor immutable after sealing, and the firing fence arms on a self-asserted system_events string any fleet worker can write. Remediation is small and has an in-repo precedent (20260809_venture_demand_verdicts.sql lines 137-154). Migration is not yet applied, so this is caught before exposure.',
    detailed_analysis: {
      sd_key: SD_KEY,
      phase: 'EXEC_TO_PLAN',
      commit: 'dbc0ad0af9e',
      pr: 'https://github.com/rickfelix/EHG_Engineer/pull/7657',
      files_reviewed: [
        'database/migrations/20260829_kill_gate_sealed_predictions.sql',
        'lib/eva/kill-gate-teeth/firing-fence.js',
        'lib/eva/kill-gate-teeth/firing-verification.js',
        'tests/integration/kill-gate-teeth/sealed-predictions-blindness.test.js',
      ],
      live_measurements: {
        instrument: 'direct pg connection via SUPABASE_POOLER_URL, current_user=postgres, read-only SELECTs against pg_default_acl / pg_class / has_table_privilege',
        measured_at: '2026-08-29',
        pg_default_acl_postgres_public_tables: '{postgres=arwdDxtm/postgres,anon=arwdDxtm/postgres,authenticated=arwdDxtm/postgres,service_role=arwdDxtm/postgres}',
        pg_default_acl_postgres_public_functions: '{postgres=X/postgres,anon=X/postgres,authenticated=X/postgres,service_role=X/postgres}',
        public_tables_rls_on: 834,
        public_tables_rls_off: 35,
        public_tables_rls_off_all_anon_readable: true,
        public_tables_anon_select: 774,
        public_tables_anon_delete: 760,
        corroborating_existing_tables: {
          system_events: 'relacl includes anon=arwdDxtm, protected only by relrowsecurity=true',
          chairman_decisions: 'relacl includes anon=arwdDxtm, protected only by relrowsecurity=true',
          venture_demand_verdicts: 'relacl has NO anon/authenticated entry and rls=true -- the house-correct precedent',
        },
        closing_migrations_status: 'database/chairman-gated/20260816_defacl_anon_auth_axis.sql and 20260819_anon_truncate_default_privileges.sql are STAGED, NOT APPLIED -- the default-ACL exposure is live today and cannot be assumed away',
      },
      questions_answered: {
        secdef_search_path: 'SAFE -- SET search_path = pg_catalog, public is explicit and fixes the path at execution; not caller-controllable; no shadowing surface',
        secdef_dynamic_sql: 'SAFE -- static single-statement LANGUAGE sql body, zero parameters, zero interpolation',
        grant_role_semantics: 'PRD CLAIM VERIFIED -- membership grant only; privileges flow member-ward (postgres/service_role gain kill_gate_traversal_ro\'s zero base-table privileges), never role-ward; no escalation',
        revoke_from_public_sufficiency: 'INSUFFICIENT -- measured by-name anon/authenticated default grants are untouched by FROM PUBLIC; this is the blocking finding',
        check_constraint_bypass: 'Constraints as written are correctly restrictive and non-bypassable for what they assert; the real gap is what they do NOT assert -- no hash/content binding at discharge and no immutability on sealed columns',
        thesis_kill_gate_unmodified: 'CONFIRMED -- 11 files in the branch diff, thesis-kill-gate.js not among them',
        js_injection_surface: 'NONE -- fully parameterized supabase-js builder throughout',
        js_secrets: 'NONE -- no env reads, no client construction, no credentials',
        fail_closed_completeness: 'CONFIRMED -- all five exits traced; armed:true reachable only via affirmative attestation; no exception, null, or malformed-row path yields armed:true',
      },
    },
    phase: 'EXEC_TO_PLAN',
  };

  results = applySubAgentRepoVerdict(results, resolution);
  const stored = await storeSubAgentResults('SECURITY', SD_KEY, { name: 'SECURITY' }, results, { sdKey: SD_KEY, phase: 'EXEC_TO_PLAN', source: 'manual' });
  console.log('SECURITY EVIDENCE WRITTEN:', stored.id, stored.verdict, stored.confidence);
  process.exit(0);
}

if (isMainModule(import.meta.url)) {
  main().catch((e) => { console.error('FAILED:', e.message, e.stack); process.exit(1); });
}
