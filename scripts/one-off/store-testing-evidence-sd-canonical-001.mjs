// SD-LEO-INFRA-STRATEGIC-DIRECTIVES-CANONICAL-001 -- TESTING sub-agent evidence (PLAN phase).
// Testing-strategy validation BEFORE EXEC begins (no implementation exists yet to run tests against).
// Reviewed: PRD-SD-LEO-INFRA-STRATEGIC-DIRECTIVES-CANONICAL-001 (8 FRs, 24 test_scenarios, 4 TRs,
// 5 top-level acceptance_criteria, implementation_approach) + the writer-inventory evidence file
// (database/evidence/...-writer-inventory.md, 6 sections incl. the 35-BEFORE-ROW trigger firing order).
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../../lib/sub-agent-executor/results-storage.js';

const SD_ID = '0f589709-f317-4d79-ab3a-22a6b8a2faaf';
const PHASE = 'PLAN';

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 88,
  summary: [
    'TESTING STRATEGY REVIEW, PLAN PHASE. The strategy is fundamentally sound, unusually well-reasoned, and BUILDABLE --',
    'the scratch-schema methodology is correct, TR-1/TR-2/TR-3 are internally consistent, and a directly-applicable',
    'in-repo harness precedent already exists (tests/ddl/*.db.test.js, 11 files / 4292 lines, ephemeral Postgres + pg client).',
    'CONDITIONAL, not PASS, for ONE structural reason: the PLAN phase patched the FUNCTIONAL REQUIREMENTS but did NOT patch',
    'the two EXEC-FACING fields that EXEC actually builds from -- test_scenarios and implementation_approach. Every one of',
    'the ~8 PLAN-phase amendments landed as an FR-level acceptance_criterion and ZERO landed as a test_scenario.',
    'That asymmetry is not cosmetic: the PRD top-level acceptance_criteria[0] defines the executed-test set as literally',
    '"TS-1 through TS-21, TS-23, and TS-24" -- i.e. it enumerates ONLY the TS array. Under a literal reading, an EXEC that',
    'runs all 24 TS and none of the FR ACs SATISFIES the PRD stated acceptance criterion while implementing NONE of the',
    'PLAN-phase amendments -- and would actively implement the bug FR-6 just removed, because TS-11 still demands it.',
    'TWO BLOCKING CONTRADICTIONS (T1, T2): TS-11 still asserts "SUCCEEDS without a stamp -- cascade exemption via',
    'pg_trigger_depth()>1", the exact behavior FR-6 REVERSED and whose acceptance criterion #2 explicitly calls an',
    'INVERSION; and TS-18 + implementation_approach step 6 still wire SDRepository.updateStatus(), which FR-4/FR-5',
    'removed as dead-by-unreachability. Both are stale-text defects pointing EXEC at the pre-amendment design.',
    'ONE GENUINELY NEW COVERAGE HOLE (T3, highest value): the zzz_ companion trigger has NO true-positive test, and is',
    'ZERO-YIELD BY CONSTRUCTION against the current 35-trigger estate. Evidence Section 1c measures exactly ONE existing',
    'trigger that mutates a protected column (status_auto_transition), and FR-1 makes it self-stamp -- so every case zzz_',
    'could catch is either already caught by aaa_ or is the now-allowed self-stamped one. Consequence: a zzz_ implemented',
    'with an inverted condition, wrong column list, or always-true registry check would pass ALL 24 test scenarios.',
    'zzz_ would be dead-by-construction while reading as fully wired. Its only named true-positive ("any UNAWARE future',
    'sibling trigger #36") appears once, in FR-2 PROSE, with zero verification instrument.',
    'TWO PROSE-ONLY GUARANTEES WITH NO INSTRUMENT (T4, T7): FR-3 could-not-check fail-CLOSED behavior (registry function',
    'missing/malformed/throwing) is stated in prose, has no AC among FR-3 own 8, and no TS. Concurrency is entirely',
    'unmeasured -- the string "concurren" appears exactly once in the whole PRD and only about rollback predicates.',
    'ONE UNSATISFIABLE-BY-CONSTRUCTION CRITERION (T6): FR-2 AC#6 requires a CI test asserting aaa_/zzz_ sort positions on',
    'LIVE pg_trigger -- but TR-1 forbids applying the migration during EXEC, so those triggers do not exist live in the',
    'phase meant to satisfy it. Needs a pre-apply/post-apply split, not a rewrite.',
    'CORRECTING ONE PREMISE IN THE REFERRING BRIEF: aaa_ and zzz_ CANNOT race each other. They are BEFORE ROW triggers in',
    'the same statement, same transaction, same row -- they run sequentially in one trigger chain. The real concurrency',
    'question is different and is stated in T7.',
    'NONE of the above is a scope objection and none asks EXEC to write test files at PLAN. All findings are edits to the',
    'PRD test_scenarios / implementation_approach fields, estimated at ~6 TS rewrites plus ~6 TS additions.',
  ].join(' '),
  findings: [
    {
      id: 'T1-TS11-still-asserts-the-depth-exemption-FR6-dropped-BLOCKING',
      severity: 'critical',
      note: [
        'BLOCKING CONTRADICTION between test_scenarios and functional_requirements. TS-11 reads, verbatim and unamended:',
        'scenario "Simulated cascade write at trigger depth>1", expected "SUCCEEDS without a stamp -- cascade exemption via',
        'pg_trigger_depth()>1". FR-6 REVERSED exactly this. FR-6 title is "Cascade class: no blanket depth exemption", its',
        'acceptance criterion #2 requires the unstamped depth>1 write to be REJECTED and says so explicitly: "this is an',
        'INVERSION of the original acceptance criterion (which asserted the opposite: that depth>1 alone was sufficient to',
        'pass). The original criterion, if left as-written, would assert the bug this reversal fixes." FR-6 AC#3 additionally',
        'requires the migration to contain NO pg_trigger_depth() reference at all.',
        'FR-6 correctly diagnosed the inversion -- but the inversion was applied to FR-6 own AC list and NOT to TS-11, which',
        'is where EXEC reads its test list from. The PRD now instructs EXEC to do both things at once.',
        'WHY THIS IS BLOCKING RATHER THAN COSMETIC: PRD acceptance_criteria[0] requires "TS-1 through TS-21 ... each pass via',
        'an executed test run". TS-11 is inside that range. To make TS-11 pass as written, EXEC must implement the',
        'pg_trigger_depth()>1 exemption -- which simultaneously fails FR-6 AC#2 and AC#3. There is no implementation that',
        'satisfies both. EXEC hits a contradiction it cannot resolve without escalating, or -- worse -- resolves it silently',
        'in favor of the TS, because the TS is the concrete executable artifact and the FR AC is prose.',
        'THIRD OCCURRENCE, SAME STALE PREMISE: implementation_approach step 5 also still reads "...and TS-11',
        '(pg_trigger_depth()>1 cascade exemption, FR-6: the harness nested trigger-call-depth simulation proves the',
        'exemption)". So the harness design step ALSO instructs EXEC to build depth-simulation specifically to PROVE an',
        'exemption that no longer exists.',
        'REQUIRED FIX (PRD edit, not EXEC work): rewrite TS-11 to expected="REJECTED with SQLSTATE SDCW1, identically to any',
        'other unstamped protected-column write -- proving no depth-based exemption exists", and rewrite',
        'implementation_approach step 5 to match. Recommend ALSO adding the FR-6 AC#3 static check as its own scenario',
        '(assert the migration SQL text contains zero pg_trigger_depth references) -- absence checks need their own',
        'instrument, since no behavioral test can prove a code path is absent rather than merely unreached.',
        'FIXTURE-VALIDITY NOTE ON THE REWRITTEN TS-11: with the exemption dropped, an unstamped depth-1 write and an unstamped',
        'depth>1 write are rejected by the SAME code path. So a TS-11 that does not ACTUALLY reach depth>1 proves nothing',
        'about depth and is indistinguishable from a duplicate of TS-1. The rewritten scenario must assert it genuinely',
        'reached depth>1 (e.g. the cascade fixture records pg_trigger_depth() into a probe table, and the test asserts that',
        'recorded value is >1) -- otherwise it is a test that cannot observe its own subject.',
      ].join(' '),
    },
    {
      id: 'T2-TS18-and-impl-approach-still-wire-SDRepository-updateStatus-dead-method-BLOCKING',
      severity: 'critical',
      note: [
        'BLOCKING STALE REFERENCE, and it manufactures precisely the FALSE COVERAGE that risk-agent F9 warned about.',
        'TS-18 scenario text still reads "(2) PLAN-TO-EXEC, which handoff.js routes through SDRepository.js updateStatus()",',
        'and its expected text still reads "(2) is stamped by SDRepository.js own UPDATE payload". implementation_approach',
        'step 6 still reads "wire the stamp column into SDRepository.js updateStatus() and both RPC bodies".',
        'FR-4 and FR-5 both removed this. FR-4 title is now "handoff.js real write surface (13 own-UPDATE sites + 2 RPCs)',
        'sets the stamp -- NOT SDRepository.js". FR-5 AC#8 confirms updateStatus() dead-by-unreachability via three',
        'independent checks (repo-wide git grep, an sdRepo.<method> call-site census returning only getById/verifyExists, and',
        'a direct class read) and states it is "REMOVED from FR-4 EXEC stamp-wiring targets entirely -- not deprioritized,',
        'not stamped as a defensive measure".',
        'FR-4 AC#1 already carries the corrected wording: "the SDRepository.js-ADJACENT direct .update() path',
        '(plan-to-exec/state-transitions.js -- NOT SDRepository.js itself, which is dead)". So the correction exists in the',
        'FR and was simply never propagated to TS-18 or to the implementation step.',
        'WHY THIS IS WORSE THAN A WASTED HOUR: F9 named the mechanism exactly -- stamping a dead-by-unreachability method',
        '"manufactures FALSE COVERAGE. A test that stamps it passes while proving nothing about any real write path, and a',
        'reviewer counting stamped writers would count it." TS-18 as written would produce a GREEN test whose green means',
        'nothing, and it would consume the TS-18 slot that is supposed to prove the real plan-to-exec path works.',
        'REQUIRED FIX: rewrite TS-18 leg (2) to target scripts/modules/handoff/executors/plan-to-exec/state-transitions.js',
        'forward branch (the real writer), and rewrite implementation_approach step 6 to drop updateStatus() and reference',
        'the 15 enumerated wiring points instead.',
        'NO CONFLICT WITH FR-8 -- RECORDED SO IT IS NOT "FIXED" BY MISTAKE: TS-20 legitimately still names SDRepository.js,',
        'because FR-8 AC#2 wants the repaired ADVISORY SCANNER to detect that file write site. A static lint correctly flags',
        'a textual write pattern regardless of runtime reachability. TS-20 is CORRECT as written; only TS-18 and',
        'implementation_approach step 6 are stale. Do not let a bulk find-and-replace of SDRepository.js break TS-20.',
      ].join(' '),
    },
    {
      id: 'T3-zzz-companion-trigger-has-no-true-positive-test-and-is-zero-yield-by-construction',
      severity: 'critical',
      note: [
        'HIGHEST-VALUE FINDING, AND GENUINELY NEW -- not covered by any existing FR AC or TS, and not raised by the',
        'database-agent or either risk-agent pass. The zzz_ companion trigger is the single largest PLAN-phase addition to',
        'this SD design, and as currently specified NOTHING IN THE TEST PLAN CAN DISTINGUISH A WORKING zzz_ FROM A BROKEN ONE.',
        'MEASURE FIRST. What does zzz_ catch that aaa_ does not? By construction, exactly the protected-column mutations',
        'introduced by trigger positions 2..35 -- the ones that happen after aaa_ has already run at position 1. The evidence',
        'file Section 1c measures that set precisely: "triggers on strategic_directives_v2 that MUTATE a protected column',
        '(1)" -- exactly ONE, status_auto_transition at BEFORE ROW position 6. And FR-1 amends that one function to',
        'SELF-STAMP, which zzz_ then correctly ALLOWS (that is FR-2 AC#4 whole point).',
        'THEREFORE: against the current live estate, zzz_ has ZERO yield. Every write it sees is either already rejected by',
        'aaa_ at position 1, or is the self-stamped auto_transition_status case it is required to permit. There is no',
        'existing write shape in the entire codebase that zzz_ rejects and aaa_ does not.',
        'THAT IS NOT A DESIGN DEFECT -- FR-2 is explicit and correct that zzz_ value is PROSPECTIVE ("does not generalize to',
        'any FUTURE sibling trigger #36", "still catching any UNAWARE future sibling"). Defense-in-depth against a future',
        'author is a legitimate reason to build it. The DEFECT IS IN THE TEST PLAN, which contains no way to observe it.',
        'THE CONCRETE FAILURE MODE: suppose EXEC implements zzz_ with an inverted boolean, a column list that omits',
        'completion_date, a registry-validity call that always returns true, or simply attaches it as BEFORE INSERT instead of',
        'BEFORE UPDATE. Walk all 24 test scenarios: TS-1..TS-3 are caught by aaa_ at position 1. TS-4..TS-7, TS-10, TS-14..',
        'TS-17, TS-24 are expected-SUCCEED and a no-op zzz_ lets them succeed. TS-21 invalid-stamp is caught by aaa_. FR-2',
        'AC#4 and AC#5 are both expected-SUCCEED, so a no-op zzz_ passes them too. FR-2 AC#6 asserts only the trigger NAME',
        'sort order, which a completely broken zzz_ body still satisfies. EVERY SINGLE CHECK PASSES with zzz_ inert.',
        'This is the dead-by-construction-while-reading-as-wired class, and it is being introduced by the very amendment that',
        'was added to close a hole. The one instrument that would catch it does not exist.',
        'REQUIRED NEW TEST SCENARIO (call it TS-25, "zzz_ true-positive / synthetic unaware sibling"): in the scratch schema,',
        'install a synthetic BEFORE ROW UPDATE trigger whose name sorts strictly BETWEEN aaa_ and zzz_ (e.g.',
        'mmm_test_unaware_mutator) whose body assigns NEW.status without touching the stamp column -- deliberately simulating',
        'a future trigger #36 written by an author unaware of the convention. Then issue an UPDATE that touches ONLY',
        'unprotected columns and carries NO stamp. aaa_ at position 1 must PASS it (no protected column changed yet); the',
        'synthetic mutator then changes NEW.status; zzz_ at last position must REJECT with SQLSTATE SDCW1.',
        'ASSERT BOTH HALVES, NOT JUST THE REJECTION: (i) the write is rejected, AND (ii) with the synthetic mutator REMOVED',
        'the identical UPDATE SUCCEEDS. Half (ii) is what proves the rejection was caused by the injected mutation rather',
        'than by some unrelated property of the fixture -- without it, a zzz_ that rejects everything also passes.',
        'COST IS NEAR ZERO. implementation_approach step 5 ALREADY requires the harness to "install renamed/instrumented',
        'sibling triggers". This scenario is that exact capability pointed at zzz_ instead of at aaa_ ordering. No new',
        'harness capability is needed -- only the scenario.',
        'RECOMMEND ALSO adding the mirror case as TS-26: the same synthetic mutator, but self-stamping with a registry-valid',
        'identity, must SUCCEED. That pins the FR-1 self-stamp contract as the documented escape hatch for future trigger',
        'authors, and proves zzz_ discriminates on stamp validity rather than simply rejecting all late mutations.',
      ].join(' '),
    },
    {
      id: 'T4-FR3-could-not-check-fail-closed-is-prose-only-with-zero-instrument',
      severity: 'warning',
      note: [
        'CONFIRMING THE REFERRING BRIEF SUSPICION 3(c) -- yes, it is described in FR-3 prose and there is NO test.',
        'FR-3 description states: "COULD-NOT-CHECK BEHAVIOR: if the registry-function lookup itself errors (e.g. the function',
        'is missing, malformed, or throws), Postgres own default trigger semantics apply -- an error inside a BEFORE trigger',
        'aborts the entire UPDATE statement (fail-CLOSED), never silently permits it."',
        'MEASURED: the substrings "could-not-check", "malformed", and "registry-function lookup" each occur exactly ONCE in',
        'the entire PRD JSON -- all three in that one prose sentence. FR-3 has 8 acceptance criteria; none covers it. No',
        'test_scenario covers it. The guarantee has zero verification instrument anywhere in the plan.',
        'WHY IT MATTERS MORE THAN A TYPICAL PROSE GAP: this is the fail-safe direction of the SD single most load-bearing',
        'assumption. Everything else in FR-1..FR-6 assumes the registry lookup RETURNS. If it can ever error in a way that is',
        'caught and swallowed -- for instance if EXEC writes the validity check inside a BEGIN...EXCEPTION WHEN OTHERS THEN',
        'RETURN TRUE block, which is an entirely natural defensive-plpgsql reflex -- the guard silently degrades to permitting',
        'everything, and NO existing scenario would notice, because every expected-SUCCEED scenario still succeeds and every',
        'expected-REJECT scenario is rejected by the missing-stamp branch before the registry is ever consulted.',
        'The claim as written is also a claim about Postgres DEFAULT semantics rather than about code EXEC will write -- which',
        'is exactly the kind of assertion that is true of the language and false of the implementation, because the',
        'implementation is free to add an exception handler that overrides the default.',
        'REQUIRED NEW TEST SCENARIO (TS-27, "registry unavailable => fail closed"): in the scratch schema, CREATE OR REPLACE',
        'the registry function body to unconditionally RAISE (and, as a second variant, DROP it entirely), then attempt a',
        'protected-column UPDATE carrying a stamp that WOULD be valid against the intact registry. Assert the UPDATE is',
        'REJECTED and the row is unchanged -- never permitted, never silently passed. Then restore the registry and assert the',
        'same UPDATE now succeeds, proving the fixture actually toggled the condition under test.',
        'COST IS NEAR ZERO AND THE MECHANISM IS ALREADY SPECIFIED: TS-23 already establishes CREATE OR REPLACE of the registry',
        'function body inside the scratch schema, including a teardown-and-verify step. TS-27 is the same fixture with a',
        'different body. Reuse TS-23 teardown discipline verbatim -- restore, then run a verification query confirming the',
        'restore took, before any later scenario runs.',
      ].join(' '),
    },
    {
      id: 'T5-every-PLAN-phase-amendment-is-FR-AC-only-and-the-top-level-AC-enumerates-only-the-TS-array',
      severity: 'warning',
      note: [
        'THE STRUCTURAL FINDING BEHIND T1, T2, T3 AND T4 -- answering the referring brief question 1 directly. YES, the',
        'PLAN-phase-added requirements are systematically UNDER-TESTED relative to the original LEAD-phase requirements, and',
        'the asymmetry is total rather than partial.',
        'The 24 test_scenarios were authored at LEAD and describe the LEAD-phase design. Every subsequent amendment -- across',
        'two full database-agent passes and a three-round risk-agent second pass -- was written into FR acceptance_criteria.',
        'NOT ONE created, renumbered, or amended a test_scenario. The TS array still ends at TS-24 and its content is',
        'pre-amendment throughout.',
        'AMENDMENT-BY-AMENDMENT COVERAGE MAP (FR AC present? / TS present?):',
        '(1) zzz_ companion trigger exists and fires last -- FR-2 AC#6 yes / NO TS. TS-13 covers aaa_ ordering only, and only',
        'against the 4 named siblings; it predates zzz_ entirely.',
        '(2) zzz_ actually catches a late unstamped mutation -- NO AC / NO TS. See T3; this is the true-positive hole.',
        '(3) auto_transition_status self-stamps only when NULL -- FR-3 AC#8 yes / NO TS.',
        '(4) NULL-at-rest stamp + no inheritance by a later write -- FR-3 AC#7 yes (and well-specified: it correctly requires',
        'BOTH the SELECT-reads-NULL assertion AND the second-unstamped-write-rejected assertion) / NO TS.',
        '(5) dropped cascade exemption + 5 individually-stamped cascade functions -- FR-6 AC#1..#4 yes / TS-11 ASSERTS THE',
        'OPPOSITE. See T1.',
        '(6) rollback-swallow removal + rollback write lands -- FR-4 AC#5 yes (well-specified: requires forcing a mid-handoff',
        'failure AND asserting SDCW1 is not swallowed) / NO TS.',
        '(7) SQLSTATE SDCW1 round-trips to supabase-js as error.code -- FR-1 AC#5 yes / NO TS.',
        '(8) neither SDCW1 message text contains the substring "0 rows" -- FR-1 AC#4 yes / NO TS.',
        '(9) all 15 stamp-wiring points individually wired -- FR-4 AC#4 yes / NO TS. TS-18 exercises 3 transitions and names a',
        'dead one (T2).',
        '(10) the 7 fleet claim RPCs are not collaterally blocked by zzz_ -- FR-2 AC#5 yes / TS-10 covers claim_sd only, 1 of 7.',
        'WHY THE ASYMMETRY IS LOAD-BEARING RATHER THAN AN ORGANIZING PREFERENCE: PRD top-level acceptance_criteria[0] reads',
        '"TS-1 through TS-21, TS-23, and TS-24 (23 scenarios) each pass via an executed test run". It enumerates the TS ARRAY',
        'AND NOTHING ELSE. It does not reference FR-level acceptance criteria at all. So the PRD own definition of done is',
        'satisfiable by running exclusively the pre-amendment scenario set. An EXEC agent optimizing against the stated',
        'acceptance criterion would implement the LEAD-phase design, ship TS-11 depth exemption, wire the dead updateStatus(),',
        'and legitimately report full compliance.',
        'That is the highest-probability failure mode for this SD, and it is created entirely by field-level drift rather than',
        'by any error in reasoning -- the reasoning in the FRs is excellent.',
        'REQUIRED FIX -- PICK ONE, BOTH WORK: (a) promote the 8 uncovered amendments to explicit scenarios TS-25..TS-32 and',
        'extend acceptance_criteria[0] to cover the new range; or (b) amend acceptance_criteria[0] to read "...each pass via an',
        'executed test run, AND every acceptance_criterion of FR-1 through FR-8 has a named, executed verification". Option (a)',
        'is preferred -- it keeps a single enumerable executable list, which is the property that made this gap findable at all.',
        'Note that options (a) and (b) together would fully absorb T1, T3, T4, T6 and T7 as scenario edits.',
      ].join(' '),
    },
    {
      id: 'T6-FR2-AC6-live-pg_trigger-ordering-assertion-is-unsatisfiable-during-EXEC-under-TR1',
      severity: 'warning',
      note: [
        'ANSWERING THE REFERRING BRIEF QUESTION 2 (methodology-vs-TR-1 consistency). The 24 test_scenarios themselves are',
        'CONSISTENT with TR-1/TR-3: every scenario typed "ddl" is scoped to the scratch clone, TS-19/TS-20 are typed "unit"',
        'and touch no DB, TS-18 is typed "integration" but explicitly runs "against the scratch clone". I found NO scenario',
        'that would require live DDL against the production table. On that specific question the plan is clean.',
        'THE INCONSISTENCY IS IN AN FR AC, NOT IN THE TS ARRAY. FR-2 AC#6 requires: "A CI test asserts, via ORDER BY tgname',
        'COLLATE \\"C\\" on live pg_trigger, that aaa_enforce_canonical_lifecycle_write sorts FIRST and the new zzz_ guard sorts',
        'LAST among all BEFORE ROW triggers on strategic_directives_v2."',
        'TR-1 states no live DDL apply occurs during this SD EXEC phase -- the migration is staged and chairman-gated. So',
        'during EXEC, neither aaa_ nor zzz_ EXISTS on the live table. A CI test asserting their live sort POSITIONS cannot',
        'pass in the phase that is supposed to produce it. Written naively it will not fail loudly either -- a',
        '"SELECT ... WHERE tgname LIKE \'aaa%\'" returning zero rows makes a position assertion vacuously true or throws on an',
        'empty result depending on how it is written, and the zero-rows-reads-as-fine variant is the more likely accident.',
        'CLEAN SPLIT THAT SATISFIES BOTH TR-1 AND THE INTENT, PRE-APPLY: query live pg_trigger read-only (permitted -- a',
        'catalog SELECT is not DDL, and the evidence file Appendix already documents the exact query) for the CURRENT',
        'BEFORE-ROW trigger NAME LIST, then assert offline that the two PROPOSED names bound that list: no existing name sorts',
        'before \'aaa_enforce_canonical_lifecycle_write\' and none sorts after the chosen zzz_ name, under C collation. This is',
        'fully satisfiable during EXEC, is the assertion that actually carries the risk (a new sibling landing outside the',
        'bounds), and converts to the true position assertion automatically once the migration is applied.',
        'POST-APPLY: keep FR-2 AC#6 as written, tagged as a post-apply CI gate belonging to the chairman-gated apply ceremony,',
        'not to this SD EXEC phase.',
        'SUPPORTING MEASUREMENT, ALREADY IN THE EVIDENCE FILE: the aaa_-sorts-first half is confirmed at Section 2 firing-order',
        'verdict, including the byte-level argument and the check that no name begins with a digit or uppercase letter. The',
        'zzz_-sorts-last half is NOT stated as a verdict -- zzz_ was only proposed further down, at Section 5 option 1, after',
        'that verdict was written. The underlying DATA does support it (the 35-name table maxes out at',
        '\'validate_child_sd_sequence\'; nothing begins with w, x, y, or z), so this is a one-line evidence addendum rather than',
        'a re-measurement. Worth adding so the claim is measured rather than inferred by a later reader.',
      ].join(' '),
    },
    {
      id: 'T7-concurrency-entirely-unmeasured-and-the-brief-aaa-vs-zzz-race-premise-is-incorrect',
      severity: 'warning',
      note: [
        'ANSWERING THE REFERRING BRIEF QUESTION 3(a), INCLUDING A CORRECTION TO ITS PREMISE.',
        'CORRECTION FIRST: aaa_ and zzz_ CANNOT race each other, and no test should be written as though they can. Both are',
        'BEFORE ROW triggers on the same table. For any single UPDATE they execute sequentially inside one statement, one',
        'transaction, on one already-row-locked tuple, in tgname order. There is no interleaving point between them. A test',
        'framed as "two near-simultaneous UPDATEs to the same row, does aaa_ interleave with zzz_" would be measuring',
        'something that cannot happen, and would pass regardless of implementation quality.',
        'MEASURED GAP: the substring "concurren" appears EXACTLY ONCE in the entire PRD, and that occurrence is about the',
        'rollback sites lacking a concurrency PREDICATE (F8). "simultan" appears zero times. There is no concurrency test',
        'scenario and no concurrency acceptance criterion anywhere in the plan.',
        'THE REAL QUESTION, WHICH IS WORTH ONE SCENARIO: under READ COMMITTED, when an UPDATE finds a row concurrently updated',
        'by a transaction that then commits, Postgres re-fetches the row and re-evaluates via EvalPlanQual -- and BEFORE ROW',
        'triggers FIRE AGAIN for the retried attempt, against a DIFFERENT OLD tuple. Two consequences specific to this design:',
        '(i) zzz_ NEW.<stamp> := NULL runs twice. Harmless here because the assignment is idempotent and the client SET clause',
        're-supplies the stamp literal on the retry -- but it is idempotent BY LUCK OF THE CURRENT DESIGN, and nothing in the',
        'plan records that double-execution safety is a required property of zzz_. A future zzz_ that incremented a counter,',
        'wrote an audit row, or consumed a single-use token would be silently wrong.',
        '(ii) More interesting: aaa_ and zzz_ guard conditions are OLD-vs-NEW comparisons, so a RETRY CAN CHANGE THE VERDICT.',
        'Concretely -- writer A sets progress to 100; writer B concurrently issues an unstamped metadata-only UPDATE. On B',
        'first evaluation OLD.progress is below 100, auto_transition_status does not fire, no protected column changes, B',
        'passes. A commits. B re-evaluates against the new OLD where progress is 100 and current_phase is EXEC, so',
        'auto_transition_status NOW fires, assigns NEW.status, self-stamps, and B write is admitted as a status change',
        'authored by auto_transition_status. Same statement, same client, opposite classification, decided purely by',
        'concurrent timing.',
        'I BELIEVE THAT OUTCOME IS CORRECT BY DESIGN rather than a defect -- auto_transition_status genuinely is the author of',
        'that assignment, which is exactly FR-1 truthful-identity-claim rationale. But it is currently UNMEASURED, it is',
        'non-obvious, and it is the kind of timing-dependent behavior that becomes a mystifying production report',
        '("this SD status changed and no handoff ran") if nobody wrote it down before it happened.',
        'STATED HONESTLY: the EvalPlanQual BEFORE-trigger re-firing behavior above is documented Postgres READ COMMITTED',
        'semantics, not something I measured against this schema. It should be VERIFIED empirically in the scratch harness,',
        'not taken from this report -- which is cheap, since the harness already exists for other reasons.',
        'RECOMMENDED NEW TEST SCENARIO (TS-28, "concurrent update re-evaluation"), priority MEDIUM, not a blocker: two',
        'sessions against the scratch clone, session A commits a progress/current_phase write while session B unstamped',
        'metadata-only UPDATE is blocked on the row lock; assert B final outcome and record it as the DOCUMENTED expected',
        'behavior, whichever way it lands. The value here is pinning the behavior and proving BEFORE triggers re-fire, not',
        'asserting a predetermined verdict.',
      ].join(' '),
    },
    {
      id: 'T8-harness-feasibility-PASS-but-retarget-from-full-clone-to-the-existing-ephemeral-stub-tier',
      severity: 'info',
      note: [
        'ANSWERING THE REFERRING BRIEF QUESTION 4. VERDICT: YES, feasible, LOW flake risk, and the ONE-shared-harness call in',
        'implementation_approach is still CORRECT at full current scope -- but the harness should be retargeted, and the',
        '"clone" framing in TR-3 is the wrong shape and would cause real pain if taken literally.',
        'DIRECTLY APPLICABLE IN-REPO PRECEDENT, ALREADY ESTABLISHED: tests/ddl/*.db.test.js -- 11 files, 4292 lines. They use a',
        'raw pg Client against an EPHEMERAL vanilla PostgreSQL 16 with HAND-STUBBED dependencies, deliberately narrow',
        '("reproduced only as far as this migration own logic touches it"). They are fail-closed with no skip branch (if they',
        'cannot reach a database they fail loudly rather than silently passing). tests/ddl/venture-ingest-key-binding-ddl.db',
        '.test.js is the closest structural match and is the one to copy.',
        'WHY "CLONE strategic_directives_v2 with its 35 triggers and ~15 dependent functions" IS THE WRONG TARGET: those 35',
        'trigger functions have their own dependencies -- other tables, other functions, other triggers -- and reproducing',
        'them transitively is an unbounded excavation with an unbounded flake surface, on a table the PRD itself documents as',
        'trigger-dense. It also buys nothing: 34 of the 35 are irrelevant to this guard, because evidence Section 1c measures',
        'exactly ONE that mutates a protected column.',
        'WHAT THE HARNESS ACTUALLY NEEDS -- roughly 10 objects, not 50: (1) a stub strategic_directives_v2 carrying only the',
        'columns the guard and the fixtures touch (id, sd_key, status, current_phase, completion_date, progress, metadata,',
        'is_working_on, priority, claiming_session_id, updated_by, plus the new stamp column); (2) the registry function;',
        '(3) aaa_ and zzz_; (4) status_auto_transition, recreated verbatim from live pg_get_functiondef -- it is the only',
        'sibling whose BEHAVIOR matters, and it must be the real body, not a paraphrase, since FR-2 AC#4 and T3 both turn on',
        'its exact predicate; (5) instrumented no-op stand-ins for the other 3 named siblings, sufficient for ordering',
        'assertions only -- they need to record their firing order, not reproduce their logic; (6) the 5 cascade functions from',
        'FR-6 plus a minimal origin table (sd_phase_tracking-shaped) to drive a genuine depth>1 cascade; (7) the synthetic',
        'mid-sort mutator from T3.',
        'ONE HARNESS IS STILL THE RIGHT CALL, AND MORE SO NOW THAN AT LEAD: the amendments ADDED consumers rather than',
        'diversifying them. TS-13 ordering, the T3 zzz_ true-positive, FR-2 AC#4 false-reject, FR-6 cascade stamping, the T1',
        'depth>1 inversion, and the T4 registry-failure toggle all need the same three capabilities -- install/replace a',
        'trigger, install/replace the registry function, drive a cascade at depth>1. Splitting those across one-off fixtures',
        'would duplicate the stub table six ways and guarantee drift.',
        'ONE AMENDMENT TO THE HARNESS SPEC: implementation_approach describes the harness as clone-plus-install-siblings-plus-',
        'simulate-depth. Add a fourth capability -- REPLACE THE REGISTRY FUNCTION BODY AND RESTORE IT -- which TS-23 already',
        'requires ad hoc (including its own teardown-and-verify step) and which T4 TS-27 needs again. Making it a harness',
        'primitive with teardown built in is what prevents a leaked \'temp-probe-writer\' row from silently contaminating a',
        'later scenario, which is the single most likely flake source in this whole suite.',
        'RUNTIME AND FLAKE: comparable to the existing tests/ddl tier -- schema setup is a few hundred ms, the suite is',
        'single-connection and serial apart from the one T7 concurrency scenario, and there is no network, no browser, and no',
        'shared mutable state across runs. The only genuine flake risks are (a) registry-function state leaking between',
        'scenarios, addressed above, and (b) the T7 two-session scenario, which needs its own explicit lock-wait',
        'synchronization rather than a sleep.',
        'TIER BOUNDARY TO STATE EXPLICITLY, MATCHING THE PRECEDENT OWN CONVENTION: an ephemeral vanilla Postgres does NOT',
        'reproduce Supabase role inheritance, RLS posture, or the PostgREST layer. So FR-1 AC#5 (SQLSTATE SDCW1 round-trips to',
        'supabase-js as error.code) CANNOT be proven in this tier -- it needs a real PostgREST surface, and the repo already',
        'has that tier (scripts/anon-write-contract-probe.mjs is cited in the precedent header as the equivalent Tier C).',
        'Do not let a green ddl-tier run be read as satisfying FR-1 AC#5. The existing precedent files open with an explicit',
        '"WHAT A GREEN RUN OF THIS FILE DOES NOT MEAN" header block -- copy that convention verbatim here. It also happens to',
        'discharge TR-4 explicit-non-coverage-disclosure requirement at the test layer rather than only in the migration header.',
      ].join(' '),
    },
    {
      id: 'T9-what-the-plan-gets-right-recorded-so-it-is-not-lost-in-remediation',
      severity: 'info',
      note: [
        'Recorded deliberately: the findings above are concentrated in two stale FIELDS, not in the design. The design',
        'reasoning is strong and should not be disturbed while fixing the fields.',
        'SPECIFIC STRENGTHS WORTH PRESERVING: (1) FR-3 AC#7 (NULL-at-rest) is a MODEL acceptance criterion -- it requires both',
        'the direct observation (SELECT reads NULL) AND the consequence (a second unstamped write against the same row is',
        'rejected). Most authors would have written only the first, which a no-op implementation passes. (2) FR-6 AC#3',
        'requires the ABSENCE of pg_trigger_depth() in the migration text as a static check, correctly recognizing that no',
        'behavioral test can prove a code path is absent rather than merely unreached. (3) FR-4 AC#5 requires the rollback',
        'test to force a genuine mid-handoff failure and to assert the error is not swallowed -- it anticipates that a',
        'rollback test which never triggers the rollback is indistinguishable from one that catches nothing. (4) FR-5',
        'registry SSOT contract requires test fixtures to QUERY the registry rather than hardcode the writer list, and calls a',
        'hardcoded fixture list "a defect, not an acceptable shortcut" -- this prevents the standard second-representation',
        'drift between guard and test. (5) TS-4 (same-value UPDATE must NOT reject) correctly pins IS DISTINCT FROM semantics',
        'against the UPDATE OF misfire. (6) TS-23 already specifies teardown AND a post-teardown verification query.',
        'These are the properties that made the gaps in T1..T4 findable at all. Several of them are exactly the instrument',
        'diversity (behavioral + static + absence checks) that the remaining gaps lack.',
        'ALSO CONFIRMED SOUND, per referring brief question 2: no test scenario as written would require live DDL against the',
        'production table. TR-1/TR-2/TR-3 are mutually consistent and the ddl/integration/unit type assignments on the 24',
        'scenarios match their described methodology. The single exception is an FR AC rather than a TS, and is T6.',
      ].join(' '),
    },
  ],
  metadata: {
    assessment_type: 'pre_implementation_testing_strategy_review',
    exec_started: false,
    tests_executed: false,
    review_basis: 'PRD fields (8 FR / 24 TS / 4 TR / 5 top-level AC / implementation_approach) + writer-inventory evidence file (6 sections) + in-repo harness precedent survey',
    read_only: true,
    ddl_executed: false,
    blocking_findings: 3,
    warning_findings: 4,
    blocking_prd_amendments: [
      'TS-11: invert expected from "SUCCEEDS without a stamp -- cascade exemption via pg_trigger_depth()>1" to "REJECTED with SQLSTATE SDCW1"; fixture must assert it genuinely reached depth>1',
      'implementation_approach step 5: drop "TS-11 ... proves the exemption" framing; the harness proves the ABSENCE of an exemption',
      'TS-18 leg (2) + implementation_approach step 6: retarget from SDRepository.js updateStatus() (dead-by-unreachability) to plan-to-exec/state-transitions.js forward branch',
      'ADD TS-25: zzz_ true-positive via a synthetic mid-sort unaware mutator trigger -- without it zzz_ is unfalsifiable and zero-yield against the current estate',
      'ADD TS-27: registry-function-unavailable => fail-closed, covering FR-3 could-not-check prose which has no AC and no TS',
      'acceptance_criteria[0]: extend beyond the TS array so the ~8 amendment-only FR acceptance criteria are inside the definition of done',
    ],
    recommended_new_scenarios: {
      'TS-25': 'zzz_ TRUE-POSITIVE: synthetic BEFORE ROW UPDATE trigger sorting between aaa_ and zzz_ mutates NEW.status unstamped => zzz_ must REJECT (SDCW1); assert the mirror (mutator removed => same UPDATE succeeds)',
      'TS-26': 'zzz_ DISCRIMINATION: same synthetic mutator, self-stamping with a registry-valid identity => must SUCCEED; pins FR-1 self-stamp as the documented escape hatch',
      'TS-27': 'REGISTRY FAIL-CLOSED: CREATE OR REPLACE registry to RAISE (and variant: DROP it) => a would-be-valid stamped protected-column UPDATE must be REJECTED, row unchanged; restore and assert it succeeds again',
      'TS-28': 'CONCURRENT RE-EVALUATION (medium priority): two sessions, A commits progress/current_phase while B unstamped metadata-only UPDATE waits on the row lock; pin B outcome and prove BEFORE triggers re-fire on EvalPlanQual re-check',
      'TS-29': 'SQLSTATE ROUND-TRIP (FR-1 AC#5): assert error.code === SDCW1 through supabase-js -- MUST run in the PostgREST tier, not the ephemeral ddl tier',
      'TS-30': 'MESSAGE-TEXT CONSTRAINT (FR-1 AC#4): assert neither SDCW1 message contains the substring "0 rows" (skip-and-continue.js:146 string-matches it)',
      'TS-31': 'NULL-AT-REST (FR-3 AC#7): stamped write => SELECT reads NULL => second unstamped protected-column write on the same row REJECTED',
      'TS-32': 'ROLLBACK COMPENSATION (FR-4 AC#5): force a mid-handoff failure, assert the rollback write lands AND that an SDCW1 on that path propagates rather than being console.log-swallowed',
    },
    zzz_trigger_yield_analysis: {
      existing_triggers_mutating_a_protected_column: 1,
      which: 'status_auto_transition (BEFORE ROW position 6) -> auto_transition_status()',
      disposition_after_FR1: 'self-stamps, therefore ALLOWED by zzz_',
      net_yield_against_current_estate: 0,
      consequence: 'a broken/inert zzz_ passes all 24 existing scenarios AND FR-2 AC#4/AC#5/AC#6; only a synthetic unaware-sibling fixture (TS-25) can falsify it',
      is_this_a_design_defect: false,
      why_not: 'FR-2 is explicit that zzz_ value is prospective (future sibling trigger #36). The defect is the absent instrument, not the trigger.',
    },
    brief_premise_corrections: {
      'aaa_ vs zzz_ concurrent racing writes': 'CANNOT RACE. Both are BEFORE ROW triggers in the same statement/transaction/row-lock, executed sequentially in tgname order. The real concurrency question is EvalPlanQual re-firing of BEFORE triggers on a retried UPDATE -- see T7.',
      'FR-6 old-exemption-is-gone test': 'ALREADY WELL COVERED at FR level, and with good instrument diversity -- FR-6 AC#2 behavioral inversion PLUS AC#3 static-absence check. The problem is not missing coverage; it is that TS-11 asserts the contradiction.',
      'FR-3 registry malformed/missing test': 'CONFIRMED ABSENT. Prose-only in FR-3 description; not among FR-3 own 8 acceptance criteria; no TS. See T4.',
    },
    harness_verdict: {
      feasible: true,
      one_shared_harness_still_correct: true,
      flake_risk: 'low',
      retarget: 'from "clone strategic_directives_v2 + 35 triggers + ~15 functions" to the established tests/ddl/*.db.test.js tier: ephemeral vanilla PG16 + hand-stubbed narrow dependencies (~10 objects)',
      precedent_files: ['tests/ddl/venture-ingest-key-binding-ddl.db.test.js', 'tests/ddl/drive-reports-ddl.db.test.js'],
      precedent_scale: '11 files, 4292 lines, raw pg client, fail-closed with no skip branch',
      required_fourth_harness_primitive: 'replace-and-restore the registry function body with teardown built in (TS-23 needs it, TS-27 needs it again; leaked registry state is the top flake risk)',
      status_auto_transition_must_be_verbatim: 'recreate from live pg_get_functiondef, not paraphrased -- FR-2 AC#4 and TS-25 both turn on its exact predicate',
      tier_boundary: 'FR-1 AC#5 (SQLSTATE round-trip to supabase-js) CANNOT be proven in the ephemeral tier -- no PostgREST. Needs the Tier C surface (cf. scripts/anon-write-contract-probe.mjs).',
      copy_this_convention: 'the precedent "WHAT A GREEN RUN OF THIS FILE DOES NOT MEAN" header block -- also discharges TR-4 at the test layer',
    },
    evidence_file_addendum_recommended: 'Section 2 firing-order verdict states the aaa_-sorts-FIRST measurement but not the zzz_-sorts-LAST half (zzz_ was proposed later, at Section 5 option 1). The 35-name table supports it (max is validate_child_sd_sequence; no name begins with w/x/y/z) -- one-line addendum, not a re-measurement.',
    scenarios_reviewed: 24,
    frs_reviewed: 8,
    trs_reviewed: 4,
    ts_requiring_live_ddl_against_production: 0,
    methodology_consistent_with_TR1_TR2_TR3: true,
    method: 'full read of all 8 FRs, all 24 test_scenarios, all 4 TRs, top-level acceptance_criteria and implementation_approach; targeted reads of the writer-inventory evidence Sections 0-6; substring census across the PRD JSON for concurrency/fail-closed/registry-failure coverage; in-repo survey of existing DDL-test harness precedent. No code written, no tests executed, no DDL.',
  },
  execution_time_ms: 1320000,
};

const resolution = await resolveSubAgentRepo({
  sdId: SD_ID,
  subAgentCode: 'TESTING',
  targetApplication: 'EHG_Engineer',
});
applySubAgentRepoVerdict(results, resolution);

const stored = await storeSubAgentResults('TESTING', SD_ID, { name: 'QA Engineering Director' }, results, { phase: PHASE });
console.log('STORED_VERDICT=' + results.verdict);
console.log('STORED_ROW_ID=' + (stored?.id || stored?.data?.id || JSON.stringify(stored)));
console.log('BLOCKING_FINDINGS=' + results.metadata.blocking_findings);
console.log('BLOCKING_AMENDMENTS=' + results.metadata.blocking_prd_amendments.length);
console.log('NEW_SCENARIOS=' + Object.keys(results.metadata.recommended_new_scenarios).length);
console.log('REPO_PATH=' + results.metadata.repo_path);
