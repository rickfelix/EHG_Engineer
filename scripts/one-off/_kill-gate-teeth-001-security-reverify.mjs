import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../../lib/sub-agent-executor/results-storage.js';
import { getSupabaseClient } from '../../lib/sub-agent-executor/supabase-client.js';
import { isMainModule } from '../../lib/utils/is-main-module.js';

const SD_KEY = 'SD-LEO-INFRA-KILL-GATE-TEETH-001';

async function main() {
  const supabase = await getSupabaseClient();
  const resolution = await resolveSubAgentRepo({ sdId: SD_KEY, targetApplication: 'EHG_Engineer', subAgentCode: 'SECURITY', supabase });
  let results = {
    verdict: 'PASS',
    confidence_score: 88,
    findings: [],
    warnings: [
      'VERIFY-BLOCK IS SELECT-ONLY (non-blocking, narrower than house precedent). The DO $verify$ block asserts has_table_privilege(anon|authenticated, ..., \'SELECT\'). It would NOT catch a partial regression that re-granted only INSERT/UPDATE/DELETE/TRUNCATE to anon. The precedent file database/migrations/20260809_venture_demand_verdicts.sql uses the stronger form -- EXISTS (SELECT 1 FROM information_schema.role_table_grants WHERE grantee IN (\'anon\',\'authenticated\',\'PUBLIC\')) -- which catches ANY privilege verb. Recommend adopting the precedent form for both tables. Current form does correctly catch the exact regression that produced finding 30b707e0 (default-ACL by-name SELECT), so this is a hardening gap, not a hole.',
      'authenticated NOT ASSERTED on kill_gate_teeth_proof_records or on the function. The verify block checks anon+authenticated on kill_gate_sealed_predictions, but only anon on kill_gate_teeth_proof_records and only anon on kill_gate_teeth_discharged_predictions(). The REVOKE statements do name authenticated in all three places, so the achieved state is correct; the ASSERTION is asymmetric. A future revert that dropped `authenticated` from only the proof-records REVOKE would pass verification.',
      'TEST FRAGILITY: SAVEPOINT is taken AFTER `SET ROLE anon`, not before. In all three new regression tests the sequence is `SET ROLE anon` then `SAVEPOINT ...`, with SET ROLE outside the try block. If `SET ROLE anon` itself raised 42501 (Supabase postgres is not a true superuser -- the migration itself documents this for kill_gate_traversal_ro and grants membership explicitly; NO equivalent membership grant for anon exists or is asserted anywhere), the throw happens before any savepoint exists, aborting the outer transaction and cascading 25P02 into every later test -- precisely the trap the existing test comment names. Recommend SAVEPOINT before SET ROLE, and SET ROLE inside try. Applies to the pre-existing kill_gate_traversal_ro test too.',
      'RLS ADDITION IS UNEXERCISED BY ANY GREEN TEST AND INTRODUCES AN UNVERIFIED OWNER DEPENDENCY. Enabling RLS on kill_gate_sealed_predictions means the SECURITY DEFINER function kill_gate_teeth_discharged_predictions() now returns rows only because its owner is also the table owner (owners are RLS-exempt absent FORCE ROW LEVEL SECURITY) or carries BYPASSRLS. That holds when one role applies the whole migration, which is the only path today. It is proven by construction, not measured: the integration suite skips repo-wide (DESIGNATED_NON_PROD_REFS deliberately empty), so the pre-existing "ALLOWS the discharged-only RPC" test has never executed against the RLS-enabled shape. If the migration is ever applied with the function and table owned by different roles, that test flips from allow to empty-result.',
      'MIGRATION HEADER NOT RECONCILED WITH THE FIX. Header lines 20-30 argue at length that "RLS-based blindness was rejected" because service_role carries BYPASSRLS. The fix now enables RLS on both tables. The two are consistent (RLS here is defense-in-depth against a future non-BYPASSRLS grantee, NOT the traversal-blindness mechanism, which remains privilege-based) but the header never says so. A later reader could read the RLS as the blindness mechanism and then "simplify" the privilege revokes away -- reintroducing 30b707e0 exactly.',
      'ANON WRITE PATHS NOT TESTED. The three new regression tests assert SELECT/EXECUTE denial only. No test asserts anon INSERT denial on either table. REVOKE ALL covers it; nothing proves it.',
    ],
    recommendations: [
      'Adopt the precedent verify form from 20260809_venture_demand_verdicts.sql: EXISTS over information_schema.role_table_grants with grantee IN (\'anon\',\'authenticated\',\'PUBLIC\') for BOTH tables -- catches every privilege verb, not just SELECT, and is the already-house-blessed pattern this migration cites.',
      'Add has_function_privilege(\'authenticated\', ...) and has_table_privilege(\'authenticated\', \'kill_gate_teeth_proof_records\', ...) assertions so the verify block covers every principal the REVOKE statements name.',
      'Move SAVEPOINT above SET ROLE in all four role-assuming tests, and put SET ROLE inside the try, so a SET ROLE membership failure is recoverable rather than aborting the outer transaction.',
      'Add one sentence to the migration header stating that RLS is defense-in-depth only and that the privilege revokes are load-bearing and must not be removed, so the header no longer reads as arguing against a control the file now ships.',
      'Consider narrowing GRANT ALL to service_role (ALL includes TRUNCATE on a tamper-evident seal registry). NOT raised as a finding: it matches the precedent file verbatim, and service_role already holds these privileges via the same ALTER DEFAULT PRIVILEGES that caused the original finding, so the grant widens nothing in practice.',
    ],
    summary: 'RE-VERIFICATION of SECURITY finding 30b707e0-eb30-4e0c-9206-20fc142bc96b (which returned FAIL, confidence 90, on: REVOKE ALL ... FROM PUBLIC alone was insufficient because this project\'s ALTER DEFAULT PRIVILEGES grants anon/authenticated BY-NAME privileges on new public tables, leaving kill_gate_sealed_predictions, kill_gate_teeth_proof_records and kill_gate_teeth_discharged_predictions() fully anon-readable/writable with RLS disabled). VERDICT: PASS -- the blocking finding is CLOSED. All four fix elements are present in database/migrations/20260829_kill_gate_sealed_predictions.sql and correctly written: (1) ENABLE ROW LEVEL SECURITY + a service_role-only FOR ALL USING(true) WITH CHECK(true) policy on both tables, matching database/migrations/20260809_venture_demand_verdicts.sql verbatim in both statement shape and stated rationale; (2) REVOKE ALL ... FROM anon, authenticated, PUBLIC on both tables (lines 160, 287) AND on the function (line 212) -- the by-name revoke is the element that actually closes the finding, since the by-name default-ACL grant was never reachable through the PUBLIC pseudo-role; (3) explicit GRANT ALL TO service_role on both tables (lines 161, 288), matching precedent; (4) a post-apply DO $verify$ block (lines 296-316) asserting the ACHIEVED state via has_table_privilege / has_function_privilege / pg_class.relrowsecurity and RAISEing inside the transaction, so a migration that "ran clean" without achieving blindness aborts at apply time rather than being silently trusted -- this directly addresses the original root cause ("a REVOKE that ran clean while doing nothing"). Regression-catch confirmed: has_table_privilege includes privileges held by-name AND via PUBLIC, so reverting element 2 to a PUBLIC-only revoke leaves the default-ACL by-name SELECT in place, the assertion fires, and the migration aborts pre-COMMIT. Idempotence confirmed: ENABLE RLS, DROP POLICY IF EXISTS + CREATE POLICY, REVOKE and GRANT are all re-runnable against the pre-existing CREATE TABLE IF NOT EXISTS shape. NO NEW SECURITY ISSUE INTRODUCED: GRANT ALL TO service_role does include TRUNCATE, but it is verbatim the precedent file\'s posture and grants service_role nothing it did not already hold via the same ALTER DEFAULT PRIVILEGES that caused the original finding; USING(true) WITH CHECK(true) scoped TO service_role is appropriately permissive for exactly the reason the precedent gives -- the value of RLS here is that the table is no longer "one permissive policy away from exposure" with a live grant and nothing saying so, and every non-service principal now fails closed at BOTH the privilege layer and the row layer. TESTS: tests/integration/kill-gate-teeth/sealed-predictions-blindness.test.js was re-read in full; the three new regression tests (anon SELECT on base table, anon EXECUTE on the RPC, anon SELECT on kill_gate_teeth_proof_records) each use SET ROLE anon -> SAVEPOINT -> expect rejects 42501 -> finally ROLLBACK TO SAVEPOINT + RESET ROLE, structurally matching the existing kill_gate_traversal_ro test and correctly avoiding the 25P02 aborted-transaction cascade for the expected-failure statement. They close the specific green-suite/open-door gap the original finding named: the prior suite exercised only kill_gate_traversal_ro, which is NOLOGIN and not internet-reachable, while anon -- the Supabase client\'s default unauthenticated role -- was the actually-exposed principal and was untested. SUITES RUN: npx vitest run tests/integration/kill-gate-teeth/ -> 1 file skipped, 7 tests SKIPPED (not failed), correct and expected -- db-tier gate reports no_designated_target and refuses all network (DESIGNATED_NON_PROD_REFS deliberately empty repo-wide). npx vitest run tests/unit/eva/kill-gate-teeth/ -> 2 files, 16/16 PASS, unaffected by the migration change. CONFIDENCE 88, not higher: the fix is verified statically and by-construction, NOT measured against a live database -- the integration suite that would measure it skips everywhere by design, and the original finding was itself only discoverable by live measurement. What raises confidence back to 88 despite that is element 4: the migration is now self-verifying at apply time, so the failure mode that produced 30b707e0 (silent no-op REVOKE) can no longer reach COMMIT undetected. Six non-blocking warnings recorded, chiefly that the verify block is SELECT-only where the cited precedent checks every privilege verb via information_schema.role_table_grants, that the authenticated assertions are asymmetric across the two tables and the function, that SAVEPOINT is taken after SET ROLE (so a SET ROLE membership failure would abort the outer transaction), and that the migration header still argues "RLS-based blindness was rejected" without noting that the RLS it now ships is defense-in-depth rather than the blindness mechanism.',
    detailed_analysis: {
      sd_key: SD_KEY,
      phase: 'EXEC_TO_PLAN',
      reverifies: '30b707e0-eb30-4e0c-9206-20fc142bc96b',
      original_verdict: 'FAIL (confidence 90)',
      finding_status: 'CLOSED',
      artifacts_reviewed: [
        'database/migrations/20260829_kill_gate_sealed_predictions.sql (318 lines, full read)',
        'tests/integration/kill-gate-teeth/sealed-predictions-blindness.test.js (162 lines, full read)',
        'database/migrations/20260809_venture_demand_verdicts.sql (precedent posture + verify block)',
      ],
      fix_elements: {
        rls_enabled_both_tables: { present: true, lines: [150, 277] },
        service_role_policy_both_tables: { present: true, lines: [152, 279], shape: 'FOR ALL TO service_role USING (true) WITH CHECK (true)', matches_precedent: true },
        revoke_by_name_anon_authenticated_public: { present: true, tables: [160, 287], function: 212, note: 'this element is what actually closes 30b707e0' },
        grant_all_service_role: { present: true, lines: [161, 288], matches_precedent: true },
        post_apply_verify_block: { present: true, lines: [296, 316], mechanism: 'has_table_privilege + has_function_privilege + pg_class.relrowsecurity, RAISE EXCEPTION pre-COMMIT' },
      },
      regression_catch_analysis: 'has_table_privilege resolves privileges held by-name AND inherited via the PUBLIC pseudo-role. Reverting the REVOKE to PUBLIC-only leaves the ALTER DEFAULT PRIVILEGES by-name SELECT grant to anon intact -> has_table_privilege(anon, ..., SELECT) returns true -> RAISE EXCEPTION -> transaction aborts before COMMIT. The exact 30b707e0 regression IS caught. A partial regression re-granting only write verbs is NOT caught (see warnings).',
      new_issues_introduced: 'None blocking. GRANT ALL includes TRUNCATE on a tamper-evident seal registry, but is verbatim precedent and grants service_role nothing it did not already hold via the same default ACL. The RLS policy is a functional no-op against service_role (BYPASSRLS) by design; its value is fail-closed posture for any future non-BYPASSRLS grantee, which is the precedent file\'s own stated reasoning.',
      test_structure_analysis: 'All three new tests: SET ROLE anon -> SAVEPOINT <name> -> await expect(query).rejects.toMatchObject({code: 42501}) -> finally { ROLLBACK TO SAVEPOINT <name>; RESET ROLE }. Correct 25P02 avoidance for the expected-failure statement; RESET ROLE is explicit and not dependent on savepoint rollback restoring the role (SET ROLE precedes the SAVEPOINT, so rollback would not restore it -- the explicit RESET ROLE is load-bearing and is present). Distinct savepoint names per test, no reuse collision. Structurally sound and consistent with the pre-existing test.',
      suites_run: {
        'tests/integration/kill-gate-teeth/': '1 file skipped, 7 tests skipped (expected: db-tier reason=no_designated_target, all network refused)',
        'tests/unit/eva/kill-gate-teeth/': '2 files passed, 16/16 tests passed',
      },
      verification_limits: 'Static + by-construction review only. No live DB execution: the integration suite skips repo-wide by design and this reviewer did not point it at any project ref. The achieved ACL state is therefore asserted by the migration at apply time, not measured here.',
    },
    phase: 'EXEC_TO_PLAN',
  };
  results = applySubAgentRepoVerdict(results, resolution);
  const stored = await storeSubAgentResults('SECURITY', SD_KEY, { name: 'SECURITY' }, results, { sdKey: SD_KEY, phase: 'EXEC_TO_PLAN', source: 'manual' });
  console.log('SECURITY EVIDENCE WRITTEN:', stored.id, stored.verdict, stored.confidence);
  process.exit(0);
}
if (isMainModule(import.meta.url)) { main().catch((e) => { console.error('FAILED:', e.message, e.stack); process.exit(1); }); }
