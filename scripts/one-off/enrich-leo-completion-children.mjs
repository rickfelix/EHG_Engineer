import 'dotenv/config';
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

const updates = {
  'SD-LEO-INFRA-LEO-COMPLETION-001-B': {
    description: `Shared substrate that Children C/D/E all depend on. (1) Redesign lib/fleet/session-manifest.js from role-count drift math ({role,min}) into DESIRED-STATE SLOTS: name/color/role/account/worktree/model+effort per the checkpoint-1 REQUIRED amendment, as the frozen source-of-record interface for respawn (Child D) and relaunch-under-profile (Child E). Coordinate with SD-LEO-INFRA-FLEET-REGISTRY-MANIFEST-001 (already-merged sibling owning this file) to avoid the already-flagged naming collision. computeManifestDrift/normalizeDesiredManifest are pure and currently consumed only by their own tests, so extend additively rather than rewrite. (2) Build the canary/isolation harness that live drills in C and E require: a dedicated canary account/profile, canary-only callsign namespace, an assert-before-kill guard (target account_profile===canary), and env-gate discipline (FLEET_SPAWN_CONTROL_LIVE default-OFF, never set in a live-fleet session) — currently zero code references exist for this gate. (3) Originate the formal definition of "U4" (agent-browser cookie-non-leak guarantee referenced by Solomon checkpoint-3 but undefined anywhere in the codebase): a threat model plus an observable proof mechanism that Child E will implement against. Per Solomon pin: nothing here is accepted on assertion/unit-only.`,
    scope: `lib/fleet/session-manifest.js schema extension; new canary/isolation harness module; U4 threat-model + proof-mechanism spec document. Explicitly excludes: the supervisor process itself (Child C), the respawn runner (Child D), and cockpit UI wiring (Child E).`,
  },
  'SD-LEO-INFRA-LEO-COMPLETION-001-C': {
    description: `Closes G1a from Solomon checkpoint-3: "kill-supervisor survival is currently test-masked — mock spawnFn bypasses the detached-flag assertion, zero verb events, live flag default-OFF, no shell exists." Build a real, persistent, killable supervisor process (a launcher shell entrypoint) that owns/watches fleet child sessions, extending lib/fleet/spawn-control.js's existing verb-composition style (spawn/attach/stop/restart/relaunchUnderProfile/drainAndRestart) rather than replacing it — spawn-control.js today is spawn-and-forget (child.unref(), no PID tracking after spawn) with no supervisor concept at all. Acceptance is a LIVE DRILL only (Child B's canary harness): enable FLEET_SPAWN_CONTROL_LIVE on the canary account, kill -9 the real supervisor process, verify all fleet sessions continue uninterrupted and a real (non-mocked) fleet_verb_* event row is emitted. De-mask the existing spawn-control.test.js kill-path test that currently only asserts a mock spawnFn was called, never the real detached-process behavior.`,
    scope: `New launcher-shell supervisor process/module; de-masked kill-supervisor test exercising the real detached spawn path; live-drill runbook for Solomon's re-run. Depends on Child B's canary harness.`,
  },
  'SD-LEO-INFRA-LEO-COMPLETION-001-D': {
    description: `Closes G1b+G2 from Solomon checkpoint-3: "respawn is currently unbuildable — no runner, no scheduled task, no claude --resume consumer." Build a reboot-respawn runner registered as a Windows Scheduled Task (reusable buildSchtasksArgs pattern exists in scripts/setup-eva-watcher-task.mjs for an unrelated watcher, adapt for fleet respawn) that, on trigger, reads Child B's frozen desired-state-slot manifest and relaunches each slot via \`claude --resume\` with its captured session UUID — restoring context after a host reboot with zero live session running at trigger time. Acceptance is a LIVE DRILL: simulate a reboot with no live session, verify the runner fires, each slot relaunches correctly, and a drill record is persisted (events/SAER). Depends on Child C's supervisor existing as the process the scheduled task launches, and Child B's slot schema as its data source.`,
    scope: `New reboot-respawn runner; Windows Scheduled Task registration; claude --resume consumer wired to captured session UUIDs. Depends on Child B (schema) and Child C (supervisor).`,
  },
  'SD-LEO-INFRA-LEO-COMPLETION-001-E': {
    description: `Closes G3 (+ the U4/relaunch-under-profile live-drill leg of G1) from Solomon checkpoint-3: "no operator surface — wire the session-view/browser/attach blocks to real production callers (they currently have zero)." lib/fleet/browser-control.js and lib/fleet/session-detail-view.js (from the already-merged SD-LEO-INFRA-SESSION-VIEW-BROWSER-001 A/B) are fully built and tested but confirmed to have ZERO callers anywhere in the repo — wire a real backend consumer/route (this repo has no frontend shell; confirm whether the true UI lives in a separate EHG frontend repo or whether a backend CLI/route consumer satisfies "real production caller" here before scoping the render half). Render the chairman's named model/effort chips (data already captured via lib/checkin/steps/model-effort-merge.cjs into claude_sessions.metadata, but zero UI renders it — a mockup->PRD scope drop flagged in the sourcing proposal; may fold in here or split to a QF once the UI-shell question is resolved). Prove relaunch-under-profile (resolveSessionProfileDir/assertLocalhostBind already exist in browser-control.js) never leaks chairman cookies to the agent browser, against Child B's formal U4 definition — via a LIVE DRILL account-switch relaunch on the canary account, not an assertion. Depends on Child B's canary harness and U4 spec.`,
    scope: `Real production caller(s) for browser-control.js/session-detail-view.js; model/effort chip rendering (scope TBD pending UI-shell location question); U4 cookie-non-leak test against the Child B spec. Depends on Child B.`,
  },
};

for (const [key, fields] of Object.entries(updates)) {
  const { error } = await supabase.from('strategic_directives_v2').update(fields).eq('sd_key', key);
  console.log(key, error ? 'ERROR: ' + error.message : 'OK');
}
