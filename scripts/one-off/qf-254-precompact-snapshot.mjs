import { pathToFileURL } from 'node:url';
const { default: MemoryManager } = await import(pathToFileURL('C:/Users/rickf/Projects/_EHG/EHG_Engineer/lib/context/memory-manager.js').href);

const snapshot = [
  'Bravo fleet-worker, coordinator=3616c697. SD-LEO-INFRA-VENTURE-QUALITY-CAPA-001-F is FULLY COMPLETE (status=completed in DB) -- do not touch it again. Full LEO cycle done: LEAD-TO-PLAN, PLAN-TO-EXEC, EXEC-TO-PLAN, PLAN-TO-LEAD, LEAD-FINAL-APPROVAL all executed. Merged PRs: #8906 (main implementation), #8909 (post-merge docs). Post-completion tail complete: /document (PR #8909 merged), /heal (scored 89/100, corrective-SD generator deferred since only 1 below-threshold occurrence), /learn (created SD-LEARN-FIX-ADDRESS-PAT-LES-013), capture-completion-flags (routed 2 harness-backlog findings: CI predicate regex coverage gaps, and handoff.js LEAD-FINAL-APPROVAL exit-143-despite-success pattern).',
  'CURRENT WORK: coordinator-DIRECTED assignment QF-20260913-254 (never self-claimed -- came from /checkin action=claimed_assignment). Worktree: C:/Users/rickf/Projects/_EHG/EHG_Engineer/.worktrees/qf/QF-20260913-254, branch qf/QF-20260913-254. Task: wire scripts/gauge-findings/disposition-sweep.mjs (shipped by QF-20260911-425, had ZERO invokers anywhere) into a clock, mirroring how QF-20260913-812 wired batch-mint-sweep.mjs.',
  'IMPLEMENTATION DONE (2 commits): (1) d5990733c26 -- added STANDARD_LOOPS entry key=gauge-finding-disposition-sweep (cron 23 * * * *, gha_backed:true, session_arm:true) to scripts/coordinator-startup-check.mjs; new GHA workflow .github/workflows/gauge-finding-disposition-sweep-cron.yml; disposition-sweep.mjs now calls stampLastFired(supabase, PROCESS_KEY) with PROCESS_KEY=standard_loop:gauge-finding-disposition-sweep; added entry to scripts/hooks/recurring-tick-exemptions.json; new test tests/unit/coordinator/gauge-finding-disposition-sweep-standard-loop-qf254.test.js. (2) 91da58621be -- FIXED a CI failure this same QF caused: adding the STANDARD_LOOPS entry incremented total_loops 39->40 and gha_backed_count 18->19, breaking tests/unit/coordinator/coordinator-loop-governance-drift.test.js pinned snapshot (scripts/coordinator-loop-governance-snapshot.json) -- root-caused and fixed by updating the snapshot JSON, appending a paragraph to leo_protocol_sections DB row id=635 (Coordinator loop-registry governance) per that section\'s own instruction to update both together, and regenerating CLAUDE_COORDINATOR.md/_DIGEST.md via node scripts/generate-claude-md-from-db.js.',
  'VERIFIED LOCALLY: 26/26 tests pass across all affected suites, lint clean, live dry-run of disposition-sweep.mjs confirmed 5726 outstanding invariant_gauge_finding rows across 21 distinct fingerprints (close to ticket-cited 5687, drifted up slightly since), stampLastFired no-ops gracefully pre-registry-seed (expected).',
  'PR #8910 (https://github.com/rickfelix/EHG_Engineer/pull/8910) open on branch qf/QF-20260913-254. Both pushes (d5990733c26 then 91da58621be) have hit the same slow-CI pattern as PR #8906 earlier this session: every check green except "Run Unit Tier (quarantine-aware)" and "coverage", both confirmed genuinely in_progress (not stalled) each time checked -- these two jobs alone took ~25min on PR #8906. NEXT STEP: keep polling gh pr checks 8910 until both green (or investigate+fix if either fails, same root-cause discipline used twice already -- never skip/workaround). Once green + mergeStateStatus not CONFLICTING/BLOCKED: merge via lib/ship/auto-merge.mjs attemptAutoMerge({prNumber:8910, repoOwner:"rickfelix", repoName:"EHG_Engineer", branch:"qf/QF-20260913-254"}) -- EHG_Engineer is a platform repo, auto-passes with --admin.',
  'AFTER MERGE: node scripts/complete-quick-fix.js QF-20260913-254 --pr-url https://github.com/rickfelix/EHG_Engineer/pull/8910 (first pass records PR, prints "leaving IN_PROGRESS"), then re-run the SAME command WITH --scope-accepted "<attestation>" to actually complete it. Attestation should cite: wired disposition-sweep.mjs into STANDARD_LOOPS+GHA+stampLastFired+recurring-tick-exemptions mirroring QF-20260913-812; caught and fixed the governance-snapshot drift-test break (own CI failure, root-caused and fixed, not worked around); 26/26 tests passing; live dry-run confirmed 5726 outstanding rows/21 fingerprints; lint clean.',
  'THEN: run /checkin in the same turn (per standing same-turn-next-claim rule) and claim/build whatever the coordinator dispatches or the next self-claimable item is. Always end every pass with a ScheduleWakeup call unless explicitly told a turn is final.',
  'Standing directives still in force: never force-merge a venture/external-repo PR; never retry a Bash action the permission classifier denies (escalate/hold/re-route instead); use git merge origin/main --no-edit (never force-push) for conflicts; root-cause fixes only, never work around; measure premises against current origin/main/live state before acting; never self-claim a second item while holding one.',
].join('\n\n');

const memory = new MemoryManager();
await memory.updateSectionVerified('Pre-Compaction Snapshot', snapshot);
console.log('PREPARE: write verified');
