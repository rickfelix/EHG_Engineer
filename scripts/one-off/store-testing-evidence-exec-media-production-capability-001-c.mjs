/**
 * TESTING (QA Engineering Director) EXEC-TO-PLAN retrospective validation evidence for
 * SD-LEO-FEAT-MEDIA-PRODUCTION-CAPABILITY-001-C.
 * Post-implementation: tests actually executed, TS-1..TS-10 coverage independently read,
 * regression sweep over the three REUSED modules, per-story AC verification.
 */
import 'dotenv/config';
import { storeSubAgentResults } from '../../lib/sub-agent-executor/results-storage.js';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../../lib/sub-agents/resolve-repo.js';
import { getSupabaseClient } from '../../lib/sub-agent-executor/supabase-client.js';

const SD_KEY = 'SD-LEO-FEAT-MEDIA-PRODUCTION-CAPABILITY-001-C';
const supabase = await getSupabaseClient();

const results = {
  verdict: 'CONDITIONAL_PASS',
  confidence: 88,
  summary:
    'EXECUTED (not trusted): the 4 new unit files + the modified retention test = 44 passed / 0 failed. Full npm run test:unit = '
    + '43235 passed, 7 failed, all 7 in scripts/hooks/__tests__/post-completion-tail-enforcement.test.js — PRE-EXISTING, reproduced '
    + 'identically on a clean main worktree (7 failed | 6 passed), zero relationship to this SD. TS-1..TS-10 independently read at the '
    + 'assertion level: 9 of 10 are genuinely covered, including the two the handoff singled out — TS-1 is correctly invariant-based '
    + '(membership + arithmetic posteriorMean 41/202/11/202 + selectionReason enum, never equality against a second stochastic call, and '
    + 'no Math.random stub that could hang sampleGamma) and TS-9/TS-10 do BOTH pairwise-distinctness AND per-render token containment. '
    + 'CONDITIONAL on three real defects, none of which is a failing test: (D1) the new db-tier file is the ONLY 1 of 11 '
    + 'tests/integration/*.db.test.js that does not use the db-available.js helper, so it ERRORS (suite failure, exit 1) where its 10 '
    + 'siblings SKIP cleanly (exit 0) — and its own header comment claims it "self-skips ... matching every other *.db.test.js in this '
    + 'repo", which is measurably false; (D2) the no_outcome_data -> no_writer_yet normalization is a one-line wire at '
    + 'variant-scoring-cli.mjs:86 living INSIDE the unexported main(), so it is untested by construction — renderScoringState has no '
    + 'no_outcome_data case and would fall through to "UNRECOGNIZED status" if that wire broke, with no test failing (WIRE-not-ENDS: the '
    + 'bridge end and the render end are both green, the wire between them is unmeasured); (D3) AC-007-1 says the CLI "lists '
    + 'creative_asset_variant_scores rows" but it renders only the single winning selection, never a row listing.',
  findings: [
    'TEST EXECUTION, MEASURED NOT CLAIMED. npx vitest run over the 5 SD-relevant files: tests/unit/creative-asset-variant-scores-migration.test.js (5), tests/unit/marketing-ai-variant-outcome-derivation.test.js (7), tests/unit/creative-variant-scoring-bridge.test.js (8), tests/unit/eva-variant-scoring-cli.test.js (3), tests/unit/retention/retention-policy.test.js (21) = 44 passed, 0 failed, 1.60s. The 6th file, tests/integration/creative-asset-variant-scores-rls.db.test.js, reported "5 tests | 5 skipped" AND a FAILED SUITE simultaneously (see D1).',
    'BASELINE CONFIRMED PRE-EXISTING. Full npm run test:unit: Test Files 1 failed | 3496 passed | 15 skipped (3512); Tests 7 failed | 43235 passed | 1 expected fail | 209 skipped | 2 todo (43454), 188.65s. All 7 failures are in scripts/hooks/__tests__/post-completion-tail-enforcement.test.js (assertions of the form expect(r.stderr).toBe("") receiving a non-empty value). Re-ran that file alone on the CLEAN main worktree at C:/Users/rickf/Projects/_EHG/EHG_Engineer (branch main): 7 failed | 6 passed — byte-identical failure set. This SD did not touch that file, the hook it spawns, or anything it imports. NOT a regression from this SD.',
    'DEFECT D1 (MEDIUM) — THE DB-TIER FILE ERRORS WHERE ITS 10 SIBLINGS SKIP. Census of tests/integration/*.db.test.js: 11 files. 10 of 11 import { describeDb, itDb, HAS_REAL_DB } from ../helpers/db-available.js and wrap their suites, so under an undesignated DB_TARGET they report cleanly skipped with exit 0 (verified: venture-error-aggregation + pending-enablement-registry -> "Test Files 2 skipped (2)", exit 0). The 1 exception is the new tests/integration/creative-asset-variant-scores-rls.db.test.js:20-28, which uses a BARE vitest beforeAll calling createDatabaseClient() with no helper guard. The db-tier-gate refuses the socket, the error escapes beforeAll, and vitest records a FAILED SUITE: "Error: DB_TIER_BLOCKED: refused tcp://aws-1-us-east-1.pooler.supabase.com" thrown at tests/helpers/db-tier-gate.js:156 via the file line :27. Run in isolation the file exits 1. IMPORTANT SELF-CONTRADICTION: that same file lines 6-9 assert it "self-skips under an undesignated DB_TARGET via tests/setup.db.js runtime gate, matching every other *.db.test.js in this repo" — the comment is unverified prose describing behavior the file does not have. FIX: adopt the describeDb/itDb helper the other 10 use, and gate the beforeAll body on HAS_REAL_DB.',
    'D1 BLAST RADIUS, STATED HONESTLY AND NOT OVERSTATED. npm run test:integration = "vitest run --project db tests/integration/" already exits 1 in this environment for PRE-EXISTING reasons: 28 failed | 89 passed | 65 skipped (182 files), with failures like tests/integration/webhooks/webhook-routes-mounted.test.js (server did not become ready on the test port). So this file does NOT flip a previously-green project to red — it joins an already-red project as one more member. The defect still matters because it is red BY CONSTRUCTION rather than environmentally: when the sibling environmental failures are fixed, the other 10 db tests will go green-by-skipping and this one will remain red until a designated non-production ref exists. The db project is referenced by 6 CI workflows (can-auto-advance-tests, db-guards, drive-reports-ddl, migration-deploy-drift-guard, test-sd-creation, unit-tier).',
    'DEFECT D2 (MEDIUM) — THE ONE PIECE OF GLUE IN THE CLI IS UNTESTED BY CONSTRUCTION. The bridge returns status "no_outcome_data" (variant-scoring-bridge.js:74). The CLI render function switches on "no_writer_yet" (variant-scoring-cli.mjs:50) and has NO "no_outcome_data" case — an unmatched status hits default: at :59-60 and prints "UNRECOGNIZED status". The two vocabularies are reconciled by exactly one expression, variant-scoring-cli.mjs:86: `const normalized = result.status === "no_outcome_data" ? { ...result, status: "no_writer_yet" } : result;` — which sits inside the UNEXPORTED main(), below the import.meta.url guard, unreachable from any test. Repo-wide grep confirms :86 is the SOLE reconciliation site (no_outcome_data appears in exactly 5 places: bridge jsdoc :30, bridge return :74, cli :86, and 2 bridge-test lines). CONSEQUENCE: delete or typo line 86 and every one of the 44 tests still passes, while the chairman-facing CLI prints "UNRECOGNIZED status: {...}" for the single state FR-6 says production is permanently in today. The bridge end is tested (bridge test :69-80 asserts no_outcome_data) and the render end is tested (cli test :24 feeds a hand-constructed no_writer_yet) — but the wire between them is measured by nothing. FIX (choose one): move the normalization INTO renderScoringState as a case, or add a no_outcome_data case to the switch, or export the normalizer and test it.',
    'D2 IS ALSO A TS-9 COVERAGE QUALIFICATION. TS-9 case (d) is "bridged+approved rows with zero outcome data". The CLI test satisfies it by passing { status: "no_writer_yet", candidateCount: 2 } directly — a status value the bridge NEVER emits. So TS-9(d) is proven at the render layer against a synthetic status, not against the status the system actually produces. TS-9 cases (a) query_error, (b) no_bridged_rows and (c) gate_excluded do NOT have this problem: those three tokens are emitted verbatim by the bridge (:49, :53, :37) and consumed verbatim by the render switch (:40, :47, :42).',
    'DEFECT D3 (LOW) — AC-007-1 LITERAL TEXT NOT MET. AC-007-1 requires the CLI to "list creative_asset_variant_scores rows for a given venture_id with resolved selection reasoning". renderScoringState (:54-58) emits only "Candidates: N" plus ONE "Selected variant: X (creative_asset: Y)" line plus its posteriorMean/selectionReason — it never enumerates the bridged rows. The fleet-dashboard.cjs section-header idiom IS present (main() :71-75, the ==== banners and explicit-empty labels). This is a spec-vs-implementation divergence, arguably a defensible design (selectAssetVariant returns one selection by contract), but the AC as written is not satisfied and should be either met or amended rather than silently marked done.',
    'TS-1 VERIFIED GENUINELY INVARIANT-BASED (the specific thing the handoff asked me to check). tests/unit/creative-variant-scoring-bridge.test.js:82-110. It asserts (a) membership: expect([var1,var2]).toContain(result.selection.variantId); (b) selectionReason in the 3-value enum {single_variant, exploration_floor, thompson_sampling}; (c) posteriorMean via pure arithmetic — fixtures are var1 {impressions 200, conversions 40} and var2 {impressions 200, conversions 10}, so alpha/beta are 41/161 and 11/191, both summing to 202, and the test asserts toBeCloseTo(41/202 or 11/202, 10) selected by which variant actually came back. There is NO equality assertion against a second selectVariant() call and NO vi.spyOn(Math,"random") anywhere in the file — both G1 hazards avoided exactly as the PRD demanded. I re-derived the arithmetic against thompson-sampler.js:36-40/54-58/71 independently: alpha=successes+1, beta=failures+1, posteriorMean=alpha/(alpha+beta), correct on all three return paths, so the assertion holds regardless of which of the 3 branches fires. This is a correctly-constructed stochastic test.',
    'TS-10 VERIFIED TWO-DIMENSIONAL. tests/unit/eva-variant-scoring-cli.test.js:19-42 does BOTH halves the PRD demanded: per-render token containment via expect(text).toMatch(new RegExp(`\\[${token}\\]`)) for each of the 4 states, AND a genuine O(n^2) pairwise loop (i<j over all 4) asserting not.toBe. The PRD explicitly warned that "a bare 4-way string-inequality check is insufficient since it would pass on an incidental header difference alone" — the token-containment half closes exactly that hole. Render sites confirm the tokens are literally bracketed in the output (:41, :44, :48, :51). A third test (:44-49) separately asserts the error render contains ERROR and the empty render does not, closing the eva-idea-status.js "a failed read must never look identical to a genuinely empty table" precedent. TS-10 is well built.',
    'TS-2/TS-3/TS-5/TS-6/TS-7 VERIFIED COVERED. TS-2 (venture-uniform exclusion, both legs + the 3rd code): bridge test :38-58 covers product_review_not_approved, lifecycle_stage_gate_blocked AND missing_venture_id, each asserted verbatim; I confirmed against lib/creative/asset-view-gate.js:62/85/100 that those are exactly the 3 codes the real gate emits and that its signature is checkAssetViewAuthorized({supabase, ventureId}) matching the bridge call at :35. TS-3 (typed empty): :60-67 asserts the exact object { status: "no_bridged_rows" }, not a throw/null. TS-5 (zero-conversions): derivation test :30-33 asserts successes 0 / failures 10 — plus 3 bonus edges (multi-row aggregation, conversions>impressions floored at 0, null/undefined input). TS-6 (canonical-sampler isolation): bridge test :124-132 filters to IMPORT LINES ONLY before matching /experiment-assignment/, which is the right call — a whole-file grep would false-positive on the FR-5 header comment that deliberately NAMES the rejected sampler. TS-7 (TIER-1 AND non-delegatable AND no ON DELETE): migration test :54-63 asserts classifyMigration().tier===1 AND isDelegatableAdditive().delegatable===false with /policy_or_rls_chairman_only/, i.e. BOTH halves the PRD insisted on, and :44 asserts the comment-stripped DDL contains no ON DELETE. The stripSqlComments helper at :22-27 is necessary and correct — the migration header at :14-20 discusses ON DELETE as a hazard, so a naive whole-file regex would false-positive on documentation.',
    'TS-4 IS THE ONE SCENARIO NOT ACTUALLY EXERCISED — AND THAT IS PARTLY BY DESIGN, PARTLY D1. Leg A1 (static predicate) IS covered and DID run: migration test :47-52 asserts the non-tautological /SELECT company_id FROM user_company_access WHERE user_id = auth\\.uid\\(\\)/ predicate and FOR ALL TO authenticated, matching the tests/unit/creative-assets-migration.test.js:39 precedent. Legs A2 (pg_policies catalog) and A3 (weak anon control) are WRITTEN (db test :34-93, including a good FR-9 confdeltype==="a" NO-ACTION assertion) but have NEVER EXECUTED in any environment available to this session — the db tier fails closed with no designated non-production ref, and per D1 this file errors rather than skipping. So the live-RLS claim for this SD rests on (i) the static A1 assertion, which passed, and (ii) the separate DATABASE sub-agent live pass cited in the handoff — NOT on A2/A3, which are unexecuted code. The PRD is already honest that no authenticated-session behavioral test exists in this repo; I am recording that A2/A3 are additionally unrun, so nobody later mistakes their existence for their execution.',
    'REGRESSION SWEEP — thompson-sampler.js: NOT MODIFIED (git show confirms it is absent from the commit). Consumers: lib/marketing/ai/index.js:8 (re-export), lib/marketing/ai/optimization-loop.js:11, and the new lib/creative/variant-scoring-bridge.js:18. The new consumer only calls createSampler().selectVariant(outcomes) with the documented {id,successes,failures} contract and adds no scoring math of its own (AC-003-3 verified by reading the whole 86-line module — the only arithmetic is Math.max(0, impressions-conversions) in the DERIVATION module, which is outcome shaping, not Beta-posterior math). No regression.',
    'REGRESSION SWEEP — asset-view-gate.js: NOT MODIFIED. The bridge consumes it read-only at :19/:35 via the exact documented signature. NOTE the bridge unit test MOCKS this module (vi.mock at :7-9), so those 3 gate-code assertions prove pass-through, not that the real gate emits those codes — I closed that gap by reading lib/creative/asset-view-gate.js:62/85/100 directly and confirming all 3 literals match. No regression.',
    'REGRESSION SWEEP — lib/retention/policies.js: MODIFIED, but purely ADDITIVE (one object appended at :208-209 before the closing bracket; the diff adds 5 lines and removes 0). Full consumer census: lib/gates/operator-contract/harness-adapter.js:24,375 (spreads RETENTION_POLICIES into validateReaper — an additive entry can only help the CREATOR check), scripts/retention-enforce.js:40,189-191 (filters/iterates, order-independent), plus 2 test files. On the pinned-list question the handoff raised: tests/unit/retention/retention-policy.test.js:19 is the ONLY exhaustive pin (toEqual over the full table->timestampColumn map, correctly updated 20->21 and passing). The other consumer, tests/unit/retention/retention-enforce-table-scope.test.js:56-57, derives its expectations FROM RETENTION_POLICIES at runtime (toEqual(RETENTION_POLICIES.map(p=>p.table)) and toHaveLength(RETENTION_POLICIES.length)) so it self-adjusts and cannot break. The only other hard-coded count in that directory is toHaveLength(3) on SOAK_ENTRIES (:168), which this SD does not touch. Confirmed by execution: retention-policy.test.js 21/21 passed. NOTHING ELSE PINS THE LIST.',
    'PER-STORY AC VERIFICATION (9 stories, verified individually, NOT bulk-marked). US-001 PARTIAL: AC-001-1 PASS (migration :26-33 plain REFERENCES both sides, UNIQUE :32; no-ON-DELETE proven statically by migration test :44), AC-001-2 PASS (migration test :54-57 tier===1, executed), AC-001-3 NOT VERIFIED BY THIS SD TEST SUITE (requires the live authenticated insert; db legs unrun per D1 — relies on the external DATABASE sub-agent pass). US-002 PASS: AC-002-1 (variant-outcome-derivation.js:25 exports deriveVariantOutcomes returning {id,successes,failures}), AC-002-2 (:44-45 successes=conversions, failures=Math.max(0,impressions-conversions); derivation test :25-28 proves the floor), AC-002-3 (derivation test :46-56 asserts source has zero /supabase/ case-insensitively — and its comment correctly explains why it avoids naming client factory identifiers that would trip audit-db-test-guards.mjs). US-003 PASS: AC-003-1 (bridge :80-83 attaches creativeAssetId via the creativeAssetByVariant map :57; asserted bridge test :108), AC-003-2 SATISFIED-AS-AMENDED (the literal AC says "consistent with selectVariant() output for the same input", which TS-1/G1 deliberately SUPERSEDED with invariant checks because equality against a stochastic unseeded sampler is flaky by construction — the later hardening governs; flagging the AC text as stale, not the test), AC-003-3 PASS (no scoring math outside the sampler, read-verified). US-004 PASS: AC-004-1 (bridge test :38-58), AC-004-2 (direct import bridge :19, direct call :35 — code reference, not parity), AC-004-3 (git show --stat confirms zero S21/stage-21 paths in the commit).',
    'PER-STORY AC VERIFICATION, CONTINUED. US-005 PASS: AC-005-1 (bridge test :124-132, import-line scoped), AC-005-2 (variant-scoring-bridge.js:7-10 names BOTH samplers, declares thompson-sampler.js canonical, and states why the eva one is out of scope). US-006 PASS: AC-006-1 (variant-outcome-derivation.js:12-18 keeps the two diagnoses SEPARATE verbatim — daily_rollups "NO WRITER" vs marketing_attribution "writer exists ... never a conversion/success outcome" — and explicitly warns "Do not collapse these into one"; the retrospective leg is necessarily deferred to post-completion), AC-006-2 (all fixtures synthetic; zero live reads in the unit tier), AC-006-3 (queried sd_scope_deliverables, 9 rows, regex /production selection accuracy/i returns FALSE — no overclaim). US-007 PARTIAL: AC-007-2/3/4/6/7 PASS (4 non-collapsing states cli :40-53; posteriorMean+selectionReason :57; the RLS-bypass disclosure is printed unconditionally at :73 in the banner, not buried; renderScoringState exported :36 with main() gated by import.meta.url===pathToFileURL at :96, matching eva-idea-status.js; 4 bracketed tokens verified by TS-10), AC-007-5 PASS (package.json :263 "eva:variant-scoring:status" registered directly beside eva:events:status :260 / eva:services:status :261), AC-007-1 PARTIAL per D3 (renders one selection, not a row listing). US-008 PASS: AC-008-1 (policies.js :208-209 entry with timestampColumn created_at, mode archive, perRunCap DEFAULT_PER_RUN_CAP; created_at is DB-stamped DEFAULT now() per migration :31, so the retention key is not client-forgeable — the right property for a reaper), AC-008-2 NOT APPLICABLE by its own conditional ("If an armed cadence is judged unnecessary") — sd.metadata.operator_contract_waiver is null, which is CORRECT because the real RETENTION_POLICIES entry was shipped, so the waiver branch was never taken. US-009 PASS: AC-009-1 (migration :12-20 names the delete_venture NO-ACTION cascade gap, cites DATABASE evidence e4a15210-082f-49b1-a45e-2dbd3e121641 verbatim, and pre-empts the wrong fix with "Do not fix this by adding ON DELETE CASCADE"); AC-009-2/AC-009-3 are post-completion obligations (retrospective + capture-completion-flags needs_decision routing) that cannot be verified at EXEC-TO-PLAN and must be carried forward.',
    'MINOR / NON-BLOCKING — ONE UNASSERTED NARRATION. tests/unit/creative-variant-scoring-bridge.test.js:43 carries the trailing comment "never queries the bridge/outcome tables once gate-excluded". That claim is TRUE (the bridge returns at :36-38 before any .from() call, which I verified by reading the module) but NOTHING IN THE TEST ASSERTS IT — the mock supabase is not a spy and no call count is checked, so the early-return could be removed and the test would still pass on the returned object alone. This is the PAT-RATIONALE-WITHOUT-ASSERTION-001 shape (an explanatory sentence doing the work an assertion should do). Cheap fix if wanted: make makeMockSupabase record from() invocations and assert zero. Not blocking — the behavior is correct today.',
    'SCORECARD. Test files executed: 6 (5 pass, 1 suite-error by construction). Assertions passed: 44/44 in the SD-relevant unit files. Regressions introduced by this SD: 0 (7 full-suite failures all reproduce on clean main). test_scenarios genuinely covered: 9/10 fully (TS-1,2,3,5,6,7,8,9,10) + TS-4 covered at leg A1 only with A2/A3 written-but-unrun. User stories fully verified: 6/9 (US-002,003,004,005,006,008 — reading AC-003-2 as amended by TS-1/G1); PARTIAL: US-001 (AC-001-3 needs the live authenticated path), US-007 (AC-007-1 per D3), US-009 (AC-009-2/3 are post-completion). Defects found: 3 (D1 MEDIUM, D2 MEDIUM, D3 LOW) + 1 MINOR narration gap. No defect is a failing assertion; all three are gaps in what the suite can SEE.',
  ],
  warnings: [
    { severity: 'MEDIUM', issue: 'D1: tests/integration/creative-asset-variant-scores-rls.db.test.js:20-28 uses a bare beforeAll + createDatabaseClient() instead of the db-available.js describeDb/itDb helper that all 10 sibling db tests use, so it ERRORS (exit 1) where they SKIP cleanly (exit 0). Its own header comment lines 6-9 claim the opposite behavior.', recommendation: 'Import { describeDb, itDb, HAS_REAL_DB } from ../helpers/db-available.js, wrap both describe blocks, and guard the beforeAll body on HAS_REAL_DB. Then correct the header comment so it describes measured behavior.' },
    { severity: 'MEDIUM', issue: 'D2: the no_outcome_data -> no_writer_yet reconciliation is a single expression at scripts/eva/variant-scoring-cli.mjs:86 inside the unexported main(), unreachable from tests. renderScoringState has no no_outcome_data case, so breaking line 86 yields "UNRECOGNIZED status" output for the exact state FR-6 says production is permanently in — with all 44 tests still green.', recommendation: 'Move the normalization into renderScoringState (or add an explicit no_outcome_data case to the switch), then assert it: renderScoringState({status:"no_outcome_data",candidateCount:2}) must contain [no_writer_yet].' },
    { severity: 'LOW', issue: 'D3: AC-007-1 requires the CLI to "list creative_asset_variant_scores rows for a given venture_id"; renderScoringState emits only a Candidates count and the single winning selection, never a per-row listing.', recommendation: 'Either add a row listing to the selected-state render, or amend AC-007-1 to match the one-selection contract selectAssetVariant actually provides. Do not mark US-007 complete on the current text.' },
    { severity: 'LOW', issue: 'TS-4 legs A2/A3 are written but have never executed in any environment available to this session (db tier fails closed, plus D1). Their existence must not be read as their execution; the live-RLS claim currently rests on the static A1 assertion plus the separate DATABASE sub-agent pass.', recommendation: 'Record explicitly in the handoff/retrospective that A2/A3 are unrun pending a designated non-production ref, so a later reader does not treat the file as evidence of a live RLS test.' },
    { severity: 'LOW', issue: 'AC-003-2 as written ("selection consistent with thompson-sampler.js selectVariant() output for the same input") is stale — TS-1/G1 deliberately superseded it with invariant assertions because equality against an unseeded stochastic sampler is flaky by construction. The test is right and the AC text is wrong.', recommendation: 'Amend AC-003-2 to the invariant phrasing so a future reader does not "fix" the test back into flakiness.' },
    { severity: 'INFO', issue: 'tests/unit/creative-variant-scoring-bridge.test.js:43 asserts nothing for its claim that the bridge never queries once gate-excluded (PAT-RATIONALE-WITHOUT-ASSERTION-001 shape). The claim is true today by code reading.', recommendation: 'Optionally make the mock count from() calls and assert zero.' },
    { severity: 'INFO', issue: 'Pre-existing, NOT this SD: 7 failures in scripts/hooks/__tests__/post-completion-tail-enforcement.test.js reproduce identically on clean main, and npm run test:integration is already red with 28 failed files for environmental reasons (server port readiness).', recommendation: 'Track separately; do not attribute to this SD and do not let it mask D1.' },
  ],
  recommendations: [
    'Fix D1 first — it is 4 lines and it removes a red-by-construction test file from a project 6 CI workflows touch. Adopt the describeDb/itDb helper the other 10 db tests use, then delete the false self-skip claim in the header.',
    'Fix D2 by moving the normalization into the pure render seam. The current seam boundary was drawn one line too high: everything above line 86 is tested, everything below is tested, and the line itself is the only unmeasured link — which is precisely where a silent regression would land.',
    'Resolve D3 by decision, not by drift: either render the row listing AC-007-1 promises, or amend the AC. Leaving it as-is means US-007 gets marked done against text the code does not satisfy.',
    'Carry forward, in writing, that TS-4 A2/A3 are unrun. This is the single most likely thing for a later session to misread as "RLS was tested live here".',
    'Do NOT re-run the 7 hook-test failures as a retry — they are baseline. Retrying them burns budget and would falsely implicate this SD.',
    'US-009 AC-009-2/AC-009-3 remain open by design (retrospective wording + capture-completion-flags needs_decision routing for the delete_venture cascade gap). Ensure the completion tail actually routes it rather than letting it expire silently.',
  ],
  metadata: {
    phase_intent: 'EXEC_TO_PLAN retrospective (post-implementation) TESTING validation',
    commit_validated: '6edd174af04465b8b5f6f0ca93e10f9454336abe',
    branch: 'feat/SD-LEO-FEAT-MEDIA-PRODUCTION-CAPABILITY-001-C',
    tests_executed: true,
    sd_scoped_unit_results: { files: 5, passed: 44, failed: 0, duration_s: 1.6 },
    full_unit_suite: { files_passed: 3496, files_failed: 1, tests_passed: 43235, tests_failed: 7, skipped: 209, duration_s: 188.65 },
    baseline_failures_pre_existing: { file: 'scripts/hooks/__tests__/post-completion-tail-enforcement.test.js', count: 7, reproduced_on_clean_main: true, main_result: '7 failed | 6 passed' },
    regressions_introduced: 0,
    test_scenario_coverage: {
      'TS-1': 'COVERED — invariant-based (membership + arithmetic posteriorMean + reason enum); no Math.random stub; G1 satisfied',
      'TS-2': 'COVERED — all 3 gate codes incl. missing_venture_id, verbatim',
      'TS-3': 'COVERED — typed no_bridged_rows object, not throw/null',
      'TS-4': 'PARTIAL — leg A1 (static) covered and PASSED; legs A2/A3 written but NEVER EXECUTED (db tier fails closed + D1)',
      'TS-5': 'COVERED — zero-conversions plus 3 bonus edges',
      'TS-6': 'COVERED — import-line-scoped grep (correctly excludes the FR-5 prose comment)',
      'TS-7': 'COVERED — tier===1 AND delegatable===false AND no-ON-DELETE, all three asserted',
      'TS-8': 'COVERED — populated render path asserts variantId/creativeAssetId/posteriorMean/selectionReason',
      'TS-9': 'COVERED with qualification — 3 of 4 states use bridge-emitted tokens; the 4th (no_writer_yet) is hand-constructed and never emitted by the bridge (see D2)',
      'TS-10': 'COVERED — pairwise O(n^2) distinctness AND per-render token containment, both halves',
    },
    user_story_verdicts: {
      'US-001': 'PARTIAL — AC-001-1/2 PASS; AC-001-3 not verified by this suite (live authenticated insert unrun)',
      'US-002': 'PASS — all 3 ACs verified',
      'US-003': 'PASS — AC-003-2 read as amended by TS-1/G1 (AC text is stale, test is correct)',
      'US-004': 'PASS — all 3 ACs verified incl. S21 untouched via git show --stat',
      'US-005': 'PASS — both ACs verified',
      'US-006': 'PASS — AC-006-3 confirmed by querying sd_scope_deliverables (no overclaim); retrospective leg deferred',
      'US-007': 'PARTIAL — 6 of 7 ACs PASS; AC-007-1 not met (no row listing, D3)',
      'US-008': 'PASS — AC-008-1 shipped; AC-008-2 N/A by its own conditional (waiver branch not taken)',
      'US-009': 'PARTIAL — AC-009-1 PASS (migration :12-20 cites evidence verbatim); AC-009-2/3 are post-completion obligations',
    },
    defects: [
      { id: 'D1', severity: 'MEDIUM', location: 'tests/integration/creative-asset-variant-scores-rls.db.test.js:20-28', kind: 'db-tier file errors instead of skipping; only 1 of 11 not using db-available.js helper; header comment 6-9 falsely claims it self-skips' },
      { id: 'D2', severity: 'MEDIUM', location: 'scripts/eva/variant-scoring-cli.mjs:86', kind: 'sole no_outcome_data->no_writer_yet wire lives in unexported main(); untestable by construction; renderScoringState lacks a no_outcome_data case' },
      { id: 'D3', severity: 'LOW', location: 'scripts/eva/variant-scoring-cli.mjs:54-58 vs AC-007-1', kind: 'CLI renders one selection, does not list creative_asset_variant_scores rows as the AC requires' },
    ],
    regression_sweep: {
      'lib/marketing/ai/thompson-sampler.js': 'NOT MODIFIED; 3 consumers (index.js:8, optimization-loop.js:11, new bridge:18); contract-only use; no regression',
      'lib/creative/asset-view-gate.js': 'NOT MODIFIED; consumed read-only at bridge:19,35; 3 reason codes cross-checked against source :62/85/100 (test mocks the module, so source read was required)',
      'lib/retention/policies.js': 'MODIFIED additively (+5/-0 at :208-209); exhaustive pin at retention-policy.test.js:19 correctly updated 20->21 and passing; retention-enforce-table-scope.test.js:56-57 derives from RETENTION_POLICIES at runtime so it self-adjusts; SOAK_ENTRIES toHaveLength(3) untouched; NO OTHER PINNERS',
    },
    db_tier_status: 'fail-closed, no designated non-production ref (target ref dedlbzhpgkmetvhbkyzq); A2/A3 legs unrun',
    db_test_helper_census: { total_db_tests: 11, using_helper: 10, bare: 1, bare_file: 'tests/integration/creative-asset-variant-scores-rls.db.test.js' },
  },
};

results.detailed_analysis =
  'TESTING (QA Engineering Director) EXEC-TO-PLAN retrospective validation for ' + SD_KEY + '. '
  + results.findings.length + ' findings, persisted as text because results.findings is dropped by the storage layer.'
  + String.fromCharCode(10) + String.fromCharCode(10)
  + results.findings.map((f, i) => 'FINDING ' + (i + 1) + '/' + results.findings.length + ': ' + f).join(String.fromCharCode(10) + String.fromCharCode(10));

const resolution = await resolveSubAgentRepo({ sdId: SD_KEY, targetApplication: 'EHG_Engineer', subAgentCode: 'TESTING', supabase });
applySubAgentRepoVerdict(results, resolution);
const stored = await storeSubAgentResults('TESTING', SD_KEY, null, results, { phase: 'EXEC_TO_PLAN' });
console.log('Stored TESTING evidence id:', stored.id);
console.log('verdict:', stored.verdict, '| confidence:', stored.confidence);
