#!/usr/bin/env node
/**
 * SD-LEO-INFRA-ARCHITECTURE-PLANS-GET-001 — VALIDATION evidence at VERIFY (PLAN_VERIFICATION).
 *
 * Independent, code-first verification of all 6 FRs and 16 TS scenarios in the live PRD
 * (product_requirements_v2 / PRD-SD-LEO-INFRA-ARCHITECTURE-PLANS-GET-001) against the actual
 * worktree at HEAD 502abd7e100. Verdict CONDITIONAL_PASS: every FR is delivered and the
 * shipped behavior is correct, but three previously-unrecorded findings are disclosed --
 * a measurable widening of cascade-watcher Stage 2 onto never-approved plans resting on a
 * false in-code premise, a mutation-coverage hole on FR-2's dominant write path, and a
 * self-contradictory comment documenting the TS-6 NULL-created_by decision.
 */
import 'dotenv/config';
import { resolveSubAgentRepo, applySubAgentRepoVerdict } from '../../lib/sub-agents/resolve-repo.js';
import { storeSubAgentResults } from '../../lib/sub-agent-executor/results-storage.js';
import { createClient } from '@supabase/supabase-js';
import { isMainModule } from '../../lib/utils/is-main-module.js';

const SD_KEY = 'SD-LEO-INFRA-ARCHITECTURE-PLANS-GET-001';

async function main() {
  const supabase = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
  const { data: sdRow, error: sdErr } = await supabase.from('strategic_directives_v2').select('id').eq('sd_key', SD_KEY).single();
  if (sdErr) throw sdErr;

  const results = {
    verdict: 'CONDITIONAL_PASS',
    confidence: 88,
    phase: 'VERIFY',
    execution_time_ms: 0,
    summary: "Independent VALIDATION review at VERIFY. I read the live PRD from product_requirements_v2 (6 FRs, 16 TS) and verified every FR against the worktree source rather than against any prior claim. ALL SIX FRs ARE DELIVERED AND CORRECT IN CODE. FR-1: archplan-upsert.js:15 takes `approved = true`, and :117-129 resolves isApproved = (approved !== false) then writes status active|draft, chairman_approved, and chairman_approved_at now()|null in one coherent triple -- status and chairman_approved can never diverge from this writer. FR-2: archplan-command.mjs:220-231 delegates to the new pure resolver scripts/eva/archplan-approval-choice.mjs, whose ordering (rejectStringFlagValue on both flags FIRST, then both-flags, then neither-flags) and whose two error strings are verbatim-identical to vision-command.mjs:125-142, so the 'mirrors vision-command.mjs exactly' claim is literally true, not approximately true; the check sits above every DB touch in cmdUpsert, satisfying TS-3's before-any-write requirement. FR-3: both stage-17-doc-generation.js call sites pass approved:false (the primary write at :387-389 and the quality-retry write at :409-411). FR-4: both branches are unit-proven in the FR-1 describe block of lib/eva/__tests__/archplan-upsert.test.js. FR-5: archplan-promote.js is a genuine standalone UPDATE with the payload restricted to exactly status/chairman_approved/chairman_approved_at (never created_by/content/sections), a promotedBy===created_by refusal, explicit non-silent not_found and already_approved results, and no re-implementation of the quality gate. FR-6: no DDL -- git diff origin/main...HEAD --name-only returns zero files under database/migrations/, database/chairman-gated/, or supabase/ (TS-16 satisfied mechanically). I RAN the 8-file unit suite: 8 files, 112/112 passing, and I read the actual log output confirming the self-approval refusal, the quality-trigger rejection message, and all three CLI flag errors fired for real. TWO INDEPENDENT CONFIRMATIONS worth recording. (1) I verified against live Postgres that both triggers archplan-promote.js's docblock reasons about actually exist and are enabled on eva_architecture_plans -- trg_enforce_archplan_quality_advancement and trg_auto_validate_archplan_quality (plus update_eva_architecture_plans_updated_at, which is why the promotion UPDATE's omission of updated_at is correct rather than an oversight) -- so FR-5's decision to rely on the DB rather than duplicate the check rests on a real, verified mechanism. (2) FR-2's documentation follow-through is COMPLETE: I grepped every 'archplan-command.mjs upsert' occurrence repo-wide and classified each: every executable or copy-pasteable invocation now carries the mandatory flag (brainstorm.md:1622, eva-archplan.skill.md:172, all three cascade-watcher remediation_command strings, vision-evidence-scorer.js:261, pre-tool-enforce.cjs:1156, the CLI's own usage header); the remaining hits are prose in archived plan docs, .prd-payloads, and .rca scratch files, none of which is a runnable command. No caller was left behind that would now hard-error. SCOPE IS CLEAN: the five files outside the FR list are all necessary FR-2 follow-through (the four doc/emitted-command updates above) plus one .docmon/naming.exceptions.json entry disclosing a pre-existing naming-validator gap that blocked editing an existing .claude/skills/*.skill.md file -- no feature was delivered that the PRD did not ask for. THE THREE CONDITIONS are recorded as W1-W3 below. W1 is the substantive one and is NEW (it is not any of the three already-disclosed gaps): cascade-watcher Stage 2's removal of the chairman_approved filter is justified in-code by a premise I measured to be false, and it widens an SD-creating automation onto 23 never-approved legacy plans. W2 is a mutation hole that lets the SD's own headline fix be silently reverted on its dominant path while all 112 tests stay green. W3 is a comment that documents the opposite of the behavior it sits above. None of the three blocks merge: the shipped code is correct in every case, W1's blast radius is latent because no scheduler is wired, and W2/W3 are guard and documentation gaps rather than behavior defects.",
    critical_issues: [],
    warnings: [
      "W1 (NEW, MEDIUM -- measured, not inferred; distinct from the already-disclosed SECURITY W1/W2 which were about promoteArchPlan's reachability): scripts/cron/cascade-watcher.mjs:248-259 removes `.eq('chairman_approved', true)` from Stage 2's readiness query and justifies it in a comment asserting the filter 'was never actually gating anything' because 'archplan-upsert.js hardcoded it true on every row'. I measured the live table and that premise is FALSE for 23 of 255 rows (9%): eva_architecture_plans currently holds 211 active/approved, 20 draft/unapproved, 1 superseded/approved, AND 23 rows that are status='active' WITH chairman_approved=false. Those 23 did not come from upsertArchPlan's hardcoded path -- their created_by values are brainstorm-pipeline, batch-migration, NULL, 'LEO Orchestrator - SD-MAN-ORCH-STAGE-DESIGN-REFINEMENT-001', and one eva-archplan-command. Every one of them was EXCLUDED from Stage 2 before this SD and is ELIGIBLE after it. Quantifying the blast radius against Stage 2's remaining filters: 9 of the 23 already have an orchestrator SD (skipped by the existing-orchestrator check), leaving 14 that the next cron run would act on. Running the REAL parsePhases from scripts/create-orchestrator-from-plan.js against their content (not a heuristic), 4 of those clear the >=3-phase bar and WOULD HAVE AN ORCHESTRATOR SD PLUS CHILD SDs AUTO-GENERATED FROM A PLAN NO CHAIRMAN EVER APPROVED: ARCH-HEARTBEAT-INTEL-001 (4 phases), ARCH-VENTURE-FUNDAMENTALS-001 (4), ARCH-CHAIRMAN-WEB-UI-001 (5), ARCH-CHAIRMAN-WEB-UI-V2-001 (8); the other 10 would emit new INSUFFICIENT_PHASES rows into eva_cascade_errors. That is the opposite direction from this SD's stated purpose, inside the SD that exists to stop the system acting on approval it does not have. Compounding the premise error: the comment's stated goal -- avoiding 'silently strand[ing] every cascade-watcher-originated plan' -- is NOT ACHIEVED by the change anyway. Stage 1 now writes status='draft' (correctly), and Stage 2 still requires status='active', so cascade-watcher-originated plans remain stranded exactly as before; dropping the chairman_approved filter does nothing for them and only affects the 23 legacy rows. The Stage 1 comment at :208-213 states this plainly ('deliberately interrupts the previously-fully-automated Stage-1->Stage-2 cascade'), so the two comments contradict each other about whether stranding is the intent or the thing being avoided. MITIGATING, which is why this is a warning and not a critical issue: cascade-watcher appears dormant -- there is no .github/workflows entry invoking it, the only hook is the npm script cascade:watch:cron (--once, 'schedule externally' per README), the eva_cascade_heartbeat table does not exist in the schema cache, and eva_cascade_errors currently holds zero rows, so nothing indicates it has ever run in production. The exposure is latent until someone schedules it.",
      "W2 (NEW, MEDIUM -- mutation-coverage hole on FR-2's dominant write path): NOTHING asserts that cmdUpsert actually forwards the resolved `approved` value to upsertArchPlan. The forwarding is present and correct at scripts/eva/archplan-command.mjs:340 (`brainstormId, createdBy: 'eva-archplan-command', approved,`), so the shipped behavior is right -- but I traced a single-token mutant (delete `approved,` from that object literal) through all 8 test files and it ships GREEN on all 112 tests: archplan-approval-choice.test.js only exercises the pure resolver; archplan-command-approval.test.js's TS-11a/TS-11b assert only that the CLI gets PAST approval parsing and then fails at the DB layer (their own names say 'proceeds to the DB layer'), never the write outcome; archplan-upsert.test.js tests the helper in isolation. That mutant restores the exact bug this SD exists to fix, on the path responsible for 202 of 255 live rows (79%), because the helper's `approved = true` default would silently take over and `--draft` would write an active, chairman_approved plan. The PRD anticipated precisely this: TS-3 specified the helper-extraction option as 'a pure exported helper ... plus ONE WIRING ASSERTION', and TS-11's expected text is a WRITE-outcome claim ('--draft writes status=draft/chairman_approved=false; --approved writes status=active/chairman_approved=true with chairman_approved_at stamped') that no delivered test makes. The wiring assertion that did land covers rejectStringFlagValue (TS-4), not the approved-forwarding. Note ESLint's no-unused-vars would flag the orphaned `const approved` -- but npm run lint is not a blocking CI gate in this repo, so lint is not a substitute for the missing assertion. Cheapest closure, no production change: in archplan-command-approval.test.js, spy on or intercept the upsertArchPlan import and assert the received `approved` is false for --draft and true for --approved; alternatively assert on the record reaching the mocked eva_architecture_plans upsert, mirroring how stage-17-doc-generation.test.js's new FR-3 guard captures records at the DB boundary (that test is the strong pattern here and does catch its own mutants at both call sites).",
      "W3 (NEW, LOW -- documentation defect on the exact AC the PRD was amended to protect): lib/eva/__tests__/archplan-promote.test.js:68-70 documents the TS-6 NULL-created_by decision as 'refuse-on-null is the safer default' and then, in the same comment and in the test immediately below it, describes and asserts ALLOW-on-null: the test is named 'promotes a row with created_by=NULL when promotedBy is any real identity' and asserts result.promoted === true. The code agrees with the test, not the comment -- archplan-promote.js:86 is `promotedBy === row.created_by`, and since promotedBy is guarded non-null at :61, a NULL-created_by row is always promotable by any identity. TS-6 was amended specifically to require this case be 'explicitly decided and tested ... not left as an accidental pass-through', and behaviorally it IS decided and tested; but this comment is the ONLY place the decision is written down (archplan-promote.js's docblock mentions NULLs only as evidence the guard is weak, never stating the policy), and it names the opposite policy. A future reader auditing the guard reads 'refuse-on-null' and concludes the 18 of 255 live rows with created_by=NULL are protected, when in fact they are the least protected rows in the table -- promotable by anyone, including the same automated writer that created them. Fix is one comment line; no code change and no behavior change.",
    ],
    recommendations: [
      "W1 -- do not leave the false premise in the tree, because the next reader will reuse it. Two options, both small: (a) restore `.eq('chairman_approved', true)` on Stage 2 (this strands nothing new -- cascade-watcher-originated plans are already excluded by the retained status='active' filter, so the only effect is to keep the 23 legacy rows out), or (b) keep the removal and rewrite the comment to state what it actually does: 'chairman_approved is dropped here so that plans promoted via a future promoteArchPlan path are picked up; note this also admits 23 legacy active/unapproved rows that predate this SD.' Option (a) is the fail-closed choice and matches the SD's own purpose. Either way, correct the claim that the filter was a no-op -- it was not, for 9% of the table.",
      "W1 follow-on -- before anyone schedules cascade:watch:cron, triage the 4 plans that would immediately generate orchestrator SDs (ARCH-HEARTBEAT-INTEL-001, ARCH-VENTURE-FUNDAMENTALS-001, ARCH-CHAIRMAN-WEB-UI-001, ARCH-CHAIRMAN-WEB-UI-V2-001). Two of the four are chairman-facing by name. This is a data-state action, not a code action, and it is cheap to do now while the watcher is dormant and expensive to undo after it runs.",
      "W2 -- add the single missing wiring assertion (--draft => approved:false, --approved => approved:true actually reaching upsertArchPlan). This is the only FR in the SD whose headline behavior can be reverted by deleting one token without turning any test red, and it guards the 79% path. Recommend closing it in this SD rather than deferring: it is test-only, touches no production file, and the PRD's own TS-3/TS-11 already asked for it.",
      "W3 -- correct the three-line comment at archplan-promote.test.js:68-70 to say 'allow-on-null (decided: a NULL author cannot be self-approved against, so any real identity may promote)', and mirror one sentence of that decision into archplan-promote.js's docblock next to the existing guard-strength caveat, so the policy is discoverable from the source rather than only from a test comment.",
      "Advisory, no action needed in this SD: all four production callers of upsertArchPlan now pass `approved` explicitly, which means the `approved = true` default at archplan-upsert.js:15 is no longer exercised by any shipped path -- it exists solely for callers that do not exist yet, and a future caller that forgets the parameter silently gets chairman_approved=true. This matches FR-1 as written (default-true was mandated for backward-compat parity with vision-upsert.js) so it is not a defect here, and the prior SECURITY review already recorded the same residual as its W3. Flagging only to note that VERIFY-phase measurement now confirms the backward-compat rationale has fully expired in practice.",
      "Advisory on evidence completeness: TS-7 and TS-8 (the real-trigger promotion and rejection paths) are correctly placed at the integration tier in tests/integration/eva/archplan-promote-quality-trigger.test.js, and the file is well-formed -- it keeps all DB work inside it() bodies so the db-tier skip gate applies, and it cleans up its seeds. But that suite only runs under npm run test:integration with VITEST_DB_ALLOW_REF set, so it is skipped by the unit run and I found no record of it having been executed. FR-5's own acceptance criteria are satisfied by the unit tests regardless (all three required cases are covered), and I independently confirmed via live Postgres that the trigger it depends on exists and is enabled, so this is a gap in executed evidence rather than in delivered coverage.",
    ],
    detailed_analysis: {
      commands_run: [
        'Queried product_requirements_v2 for PRD-SD-LEO-INFRA-ARCHITECTURE-PLANS-GET-001 and read functional_requirements (FR-1..FR-6, all acceptance_criteria) and test_scenarios (TS-1..TS-16) in full from the DB -- not from any file or prior summary',
        'Read in full: lib/eva/archplan-upsert.js (147 lines), lib/eva/archplan-promote.js (111 lines), scripts/eva/archplan-approval-choice.mjs (22 lines), lib/eva/__tests__/archplan-promote.test.js (142 lines), scripts/eva/__tests__/archplan-approval-choice.test.js, scripts/eva/__tests__/archplan-command-approval.test.js, tests/integration/eva/archplan-promote-quality-trigger.test.js',
        'Read git diff origin/main...HEAD for scripts/eva/archplan-command.mjs, scripts/cron/cascade-watcher.mjs, lib/eva/stage-templates/analysis-steps/stage-17-doc-generation.js, lib/eva/__tests__/archplan-upsert.test.js, lib/eva/__tests__/stage-17-doc-generation.test.js, scripts/__tests__/cascade-watcher.test.js',
        'Read scripts/eva/archplan-command.mjs parseArgs (:64-75), cmdUpsert approval block (:220-231), the upsertArchPlan delegation (:337-341), and the tail dispatch -- confirmed the approval check precedes every DB touch and that --approved <value> captures the value as a string (so rejectStringFlagValue is the operative guard)',
        'Read lib/eva/vision-upsert.js:221-226 (rejectStringFlagValue) and scripts/eva/vision-command.mjs:121-144 -- compared error strings and check ORDER against archplan-approval-choice.mjs line by line; the mirror claim is verbatim-exact, not approximate',
        'RAN npx vitest run --project unit across all 8 designated files (archplan-upsert, stage-17-doc-generation, vision-upsert, archplan-promote, archplan-command-approval, archplan-approval-choice, cascade-watcher, cascade-status) -> 8 files passed, 112/112 tests passed, 15.42s; read the stdout to confirm the refusal/trigger/flag-error strings were genuinely emitted rather than merely asserted',
        "TS-16 / FR-6 executed directly: git diff origin/main...HEAD --name-only | grep -E '^(database/|supabase/)' -> zero matches across 28 changed files",
        "W1 measurement: queried all 255 eva_architecture_plans rows and cross-tabulated status x chairman_approved -> 211 active/true, 23 ACTIVE/FALSE, 20 draft/false, 1 superseded/true; isolated the 23 active/unapproved rows with vision_key NOT NULL as the set newly admitted by Stage 2's relaxed predicate",
        "W1 blast radius: for each of the 23, queried strategic_directives_v2 for sd_type='orchestrator' with metadata->>arch_key = plan_key -> 9 already have one (skipped), 14 do not; then imported the REAL parsePhases from scripts/create-orchestrator-from-plan.js and applied Stage 2's own phase logic (sections.implementation_phases first, parsePhases fallback, >=3 required) to those 14 -> 4 would create orchestrator SDs + children, 10 would emit INSUFFICIENT_PHASES rows",
        'W1 dormancy check: grepped .github/workflows for cascade-watcher/cascade:watch -> none; package.json exposes only cascade:watch:cron (--once); queried eva_cascade_heartbeat -> table absent from schema cache; queried eva_cascade_errors -> 0 rows. No evidence the watcher has ever run in production',
        'W1 created_by distribution measured to confirm the 23 rows did not originate from upsertArchPlan: eva-archplan-command 202, NULL 18, eager-synthesis 13, stage-17-doc-generation 7, brainstorm-pipeline 4, batch-migration 3, plus 8 singletons -- this also independently confirms the PRD FR-5 figures (202/255 = 79% single-value, 18 NULL) that the self-approval guard is described as a placeholder against',
        'W2 trace: grepped scripts/eva/__tests__/ for upsertArchPlan / _archUpsertCalls / chairman_approved -> zero hits, confirming no test observes the CLI write outcome; then walked each of the 8 test files against the delete-`approved,`-from-line-340 mutant to confirm all 112 tests remain green',
        'Live Postgres trigger check via scripts/lib/supabase-connection.js (engineer project): SELECT tgname, tgenabled FROM pg_trigger JOIN pg_class ... WHERE relname=\'eva_architecture_plans\' AND NOT tgisinternal -> trg_arch_creation_source_advisory, trg_auto_validate_archplan_quality, trg_enforce_archplan_quality_advancement, update_eva_architecture_plans_updated_at, all tgenabled=O (enabled). Confirms FR-5 relies on a real mechanism and that omitting updated_at from the promotion payload is correct',
        "FR-2 follow-through completeness: grepped 'archplan-command.mjs upsert' repo-wide (excluding node_modules/.git/.artifacts) and classified every hit as flag-bearing vs prose -> every runnable invocation carries --approved|--draft; remaining hits are archived plan docs, .prd-payloads, .rca scratch, README narrative, and one arrow-notation line in docs/reference/venture-pipeline-playbook.md:36. No executable caller left behind",
        "Scope-creep audit (GATE 4): reviewed the diff for all 5 files outside the FR list -- .claude/commands/brainstorm.md, .claude/skills/eva-archplan.skill.md, scripts/eva/vision-evidence-scorer.js:261, scripts/hooks/pre-tool-enforce.cjs:1156 are all mandatory FR-2 follow-through (each emits or documents an upsert invocation that would now hard-error without the flag); .docmon/naming.exceptions.json adds one documented entry for .claude/skills/*.skill.md disclosing a pre-existing validator gap. No unrequested feature found",
        "grep -rn 'upsertArchPlan(' excluding tests -> exactly 4 production call sites (archplan-command.mjs:338, stage-17:378 + :405, cascade-watcher:201); independently re-confirmed all 4 now pass an explicit approved value, so the write-path coverage claim is complete",
      ],
      fr_verification_matrix: {
        'FR-1': 'PASS -- archplan-upsert.js:15 (approved = true) + :117-129 (isApproved = approved !== false; status/chairman_approved/chairman_approved_at written as one coherent triple). All 4 acceptance criteria met; no existing caller needed a signature change.',
        'FR-2': 'PASS -- archplan-approval-choice.mjs resolveApprovalChoice + archplan-command.mjs:220-231 wiring; check precedes all DB work; error strings verbatim-match vision-command.mjs:136/140; help text corrected. All 5 acceptance criteria met in code. See W2: the resolver->helper forwarding is correct but unasserted.',
        'FR-3': 'PASS -- both call sites pass approved:false (stage-17:387-389 primary, :409-411 quality-retry). TS-5 guard is strong: the test captures records at the real eva_architecture_plans upsert boundary (so the real upsertArchPlan translation is exercised end to end) and drives the retry path via quality_checked:false + non-empty quality_issues, asserting every captured record. Mutating either call site turns it red.',
        'FR-4': 'PASS -- chairman_approved_at stamped on every approved write and null on every unapproved one; both branches proven in the FR-1 describe block (approved:true, approved omitted, approved:false).',
        'FR-5': 'PASS -- archplan-promote.js exists and is standalone; UPDATE payload restricted to exactly status/chairman_approved/chairman_approved_at (asserted by TS-10 with an exact Object.keys().sort() equality, the strongest form); self-approval refused; not_found and already_approved returned explicitly rather than as silent no-ops; the quality gate is delegated to the real trigger (verified enabled in live Postgres) rather than re-implemented. All 5 acceptance criteria met. Refusals return a structured {promoted:false, reason} plus a logger.warn rather than throwing -- non-silent and consistent, which satisfies the AC.',
        'FR-6': 'PASS -- zero files under database/migrations/, database/chairman-gated/, or supabase/ in the 28-file diff; the deferred column, table, and write-site are named in the PRD text and in archplan-promote.js\'s docblock. TS-16 satisfied by direct execution.',
        '4th write path (cascade-watcher, folded into FR-2/FR-3 spirit)': 'PARTIAL -- Stage 1 correctly passes approved:false and the new test asserts status/chairman_approved/chairman_approved_at on every captured record. Stage 2 correctly no longer filters on chairman_approved and the new test asserts that via captured eq() calls. But the JUSTIFICATION for the Stage 2 change is factually wrong and the change widens eligibility onto 23 legacy never-approved rows -- see W1.',
      },
      already_disclosed_not_re_reported: 'Per the review brief I did not re-litigate: (a) the SECURITY review\'s finding that archplan-upsert.js\'s onConflict upsert can also flip a draft to active and overwrites created_by, now accurately reflected in archplan-promote.js\'s docblock at :12-18; (b) promoteArchPlan having no caller (deferred to a follow-up); (c) FR-6\'s approved_by deferral; (d) the pre-existing eslint no-unused-vars in archplan-upsert.test.js from commit 6a81538f730. I independently confirmed each is genuinely disclosed in the tree rather than merely asserted, and W1 in particular is NOT a restatement of any of them -- no prior review measured the Stage 2 eligibility set.',
      test_execution: '8 files, 112/112 passed (npx vitest run --project unit, 15.42s). Note the earlier EXEC-phase runner artifact .artifacts/test-results/archplan-get-001-exec.json records 101 tests across 27 suites; the delta reflects tests added by the EXEC-TO-PLAN TESTING review follow-up commit 555e3fe4ed0, not a discrepancy.',
      no_merge_blockers: true,
      regression_check: 'One behavior widening found and recorded (W1: 23 legacy active/unapproved plans become eligible for cascade-watcher Stage 2 orchestrator generation, 4 of them actionable). It is latent because no scheduler is wired. Every other change in this diff is a strict narrowing of the false-approval surface relative to origin/main.',
    },
    metadata: {
      independent_verification: true,
      prd_source: 'product_requirements_v2 / PRD-SD-LEO-INFRA-ARCHITECTURE-PLANS-GET-001 (read live from DB)',
      head_commit: '502abd7e100',
      frs_verified: 6,
      frs_passing: 6,
      ts_scenarios_reviewed: 16,
      tests_passed: '112/112',
      new_findings: ['W1 cascade-watcher Stage 2 widening on a false premise (MEDIUM, measured)', 'W2 FR-2 forwarding mutation hole on the 79% path (MEDIUM)', 'W3 contradictory TS-6 NULL-decision comment (LOW)'],
      conditions: ['W1: correct the false no-op premise and decide (a) restore the filter or (b) restate the comment; triage the 4 actionable legacy plans before scheduling the watcher', 'W2: add the one missing wiring assertion so FR-2 cannot be reverted green'],
      ratifications_checked: ['6c263823', 'a588adba'],
    },
  };

  const resolution = await resolveSubAgentRepo({
    sdId: sdRow.id,
    targetApplication: 'EHG_Engineer',
    subAgentCode: 'VALIDATION',
    probeExistsRelative: 'scripts/one-off/archplan-verify-validation-evidence.mjs',
    supabase,
  });
  applySubAgentRepoVerdict(results, resolution, { skipVerdictAdjust: false });
  const stored = await storeSubAgentResults('VALIDATION', sdRow.id, { code: 'VALIDATION', name: 'Validation' }, results, { sdKey: SD_KEY, phase: 'VERIFY' });
  console.log('STORED:', 'VALIDATION', JSON.stringify({ id: stored?.id, verdict: stored?.verdict, phase: stored?.phase }));
}

if (isMainModule(import.meta.url)) {
  main().catch((err) => {
    console.error(err);
    process.exit(1);
  });
}
