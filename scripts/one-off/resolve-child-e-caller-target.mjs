import 'dotenv/config';
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(process.env.SUPABASE_URL || process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);

const description = `Closes G3 (+ the U4/relaunch-under-profile live-drill leg of G1) from Solomon checkpoint-3: "no operator surface — wire the session-view/browser/attach blocks to real production callers (they currently have zero)." lib/fleet/browser-control.js and lib/fleet/session-detail-view.js (from the already-merged SD-LEO-INFRA-SESSION-VIEW-BROWSER-001 A/B) are fully built and tested but confirmed to have ZERO callers anywhere in the repo. RESOLVED (LEAD-phase Explore pass): a sibling frontend repo (C:\\Users\\rickf\\Projects\\_EHG\\ehg\\, a real React/Vite SPA) exists but has zero session-view scaffolding — building there is unscoped greenfield work with cross-repo deploy risk. The concrete, lowest-risk integration target is scripts/fleet-dashboard.cjs — an actively-developed (commits same-day as this SD's sourcing), already-live CLI operator surface rendering per-session fleet state (v_active_sessions, claude_sessions, session_coordination), with an existing WORKERS section with per-session rows. Wire buildSessionDetailView() into a per-session detail render there, and expose browser-control.js's requestBrowserSession/signalTakeover as an interactive CLI action — this satisfies "real production caller" today without a net-new UI shell. Render the chairman's named model/effort chips in the same dashboard (data already captured via lib/checkin/steps/model-effort-merge.cjs into claude_sessions.metadata, but nothing renders it yet — the mockup->PRD scope drop flagged in the sourcing proposal folds into this CLI surface rather than splitting to a QF, since the target surface is now concrete). Prove relaunch-under-profile (resolveSessionProfileDir/assertLocalhostBind already exist in browser-control.js) never leaks chairman cookies to the agent browser, against Child B's formal U4 definition — via a LIVE DRILL account-switch relaunch on the canary account, not an assertion. Depends on Child B's canary harness and U4 spec.`;

const scope = `Wire buildSessionDetailView()/requestBrowserSession()/signalTakeover() into scripts/fleet-dashboard.cjs as the real production caller (confirmed concrete target, not the EHG frontend repo, which has no session-view scaffolding); render model/effort chips in the same dashboard; U4 cookie-non-leak live-drill test against the Child B spec. Depends on Child B.`;

const { error } = await supabase.from('strategic_directives_v2').update({ description, scope }).eq('sd_key', 'SD-LEO-INFRA-LEO-COMPLETION-001-E');
console.log(error ? 'ERROR: ' + error.message : 'OK');
