{
  "track": "STANDARD",
  "phase": "FINAL",
  "description": "STANDARD track FINAL phase with merged verification",
  "tasks": [
    {
      "id_template": "{{SD_ID}}-FINAL-READY",
      "subject": "FINAL Phase Initialized",
      "activeForm": "Initializing FINAL phase",
      "description": "Load FINAL context and EXEC handoff results.",
      "blockedBy": ["{{SD_ID}}-EXEC-WALL"],
      "metadata": {
        "category": "init",
        "phase": "FINAL",
        "loads_context": ["EXEC_HANDOFF"]
      }
    },
    {
      "id_template": "{{SD_ID}}-FINAL-VERIFY-LIGHT",
      "subject": "Light Verification",
      "activeForm": "Running light verification",
      "description": "Spot-check implementation against PRD. Quick fidelity check.",
      "blockedBy": ["{{SD_ID}}-FINAL-READY"],
      "metadata": {
        "category": "validation",
        "phase": "FINAL",
        "required": true
      }
    },
    {
      "id_template": "{{SD_ID}}-FINAL-PR",
      "subject": "PR Creation and Review",
      "activeForm": "Creating PR",
      "description": "Create pull request with proper formatting.",
      "blockedBy": ["{{SD_ID}}-FINAL-VERIFY-LIGHT"],
      "metadata": {
        "category": "core",
        "phase": "FINAL",
        "required": true
      }
    },
    {
      "id_template": "{{SD_ID}}-GATE-PR-MERGED",
      "subject": "Gate: PR Merged",
      "activeForm": "Verifying PR merge",
      "description": "Verify PR is merged to main branch.",
      "blockedBy": ["{{SD_ID}}-FINAL-PR"],
      "metadata": {
        "category": "gate",
        "phase": "FINAL",
        "gate_type": "PR_MERGED"
      }
    },
    {
      "id_template": "{{SD_ID}}-FINAL-LEARN",
      "subject": "Capture Learnings",
      "activeForm": "Capturing learnings",
      "description": "Run /learn command to capture patterns.",
      "blockedBy": ["{{SD_ID}}-GATE-PR-MERGED"],
      "metadata": {
        "category": "core",
        "phase": "FINAL",
        "required": true
      }
    },
    {
      "id_template": "{{SD_ID}}-FINAL-APPROVE",
      "subject": "SD APPROVED AND COMPLETED",
      "activeForm": "Completing SD",
      "description": "SD complete. Status updated to 'completed'.",
      "blockedBy": ["{{SD_ID}}-FINAL-LEARN"],
      "metadata": {
        "category": "completion",
        "phase": "FINAL",
        "sets_sd_status": "completed"
      }
    }
  ]
}
