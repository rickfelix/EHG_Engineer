# Fable-Suitability Map v1 + Capability-Grounding Finding

**Status:** Solomon-authored (Fable, 2026-07-10), propose-only (CONST-002). The Cluster-2 durable deliverable ("the ranked set of regions worth the expensive model") — and the **FABLE-CAPABILITY GROUNDING precondition** the contract requires before any further Fable use-case or tier design (it gates FW-3). Evidence-first: every ranking below cites work actually observed **this week in this codebase**, not speculation. Supersedes nothing; refines the Fable-use-case doctrine (2026-07-07) with post-doctrine empirical data. Feeds: Mode-B sweep selection, Cluster-5 model/effort evaluation, the +2 incoming Fable seats' routing, FW-3 tier design.

## The grounding finding (Fable-vs-Opus-vs-Sonnet, in THIS codebase, observed)
This week ran a natural experiment across all three tiers on the same repo:
- **Fable sessions produced the CLASS-level findings** — the dispatch-polarity inversion, the fractal value-authenticity invariant, the authorization-vs-liveness distinction, the dual-scorer discovery, the greenfield specs whose falsifiable delta-predictions confirmed at 6/7 and 5/6. The common shape: *negative space* (what's absent), *cross-system coupling*, and *self-reversal under evidence* — consistent with doctrine R1–R5, now empirically confirmed here.
- **Opus (high effort) produced solid INSTANCE-level findings** — the brand-prior experiment's three lenses, the early sweep findings (typed-boundary marker), band-logic prechecks. Depth adequate when the question is scoped; the "Opus-depth not Fable" flag on the boundary-audit breadth was honest and correct.
- **Sonnet (high effort) built same-day against precise specs flawlessly** — α/β/δ/ε, FIX-1/FIX-2, five doors' legwork, QFs — but **under-finds breadth audits silently** (the weak-model-under-find rule: a clean Sonnet audit is not-yet-confirmed-complete, never confirmed-done; require method-emission or Fable confirmation on any Sonnet-run audit).

**The differential in one line:** Fable buys *what nobody asked about*; Opus buys *deep answers to scoped questions*; Sonnet buys *throughput against specs*. Misrouting in either direction wastes the tier (a scoped fix on Fable burns wall-clock; a breadth audit on Sonnet ships silent gaps).

## TIER F — Fable-warranted regions (ranked, with this week's receipts)
1. **One-way-door re-architectures** (claim/dispatch/sweep/create/stage-worker class): 5 doors shipped Fable-supervised in ~36h through adversarial rounds; blast-radius look-ahead is the binding constraint. Receipt: SWEEP registry's critical-rethrow decision — exactly the call a throughput model rubber-stamps wrong.
2. **Adversarial audits vs greenfield specs** (the ledger pattern): Bravo/Charlie/Delta found 30+ real findings including two run-gating CRITICALs *in code that had passed standard ship-review the same week*. The anti-stub lens is negative-space reasoning by construction.
3. **Frame-independent greenfield specs + system unification designs**: Stage-0/S20-26 specs, the value-authenticity 5-layer unification — R1 compounding constraints that steer weeks of Sonnet builds; hours of Fable here multiplied all weekend.
4. **Cross-cutting consistency/polarity passes**: the horizontal pass found HP-1/HP-2 in a file two adversarial rounds had just cleared — per-cluster and per-PR views structurally miss cross-stage classes.
5. **Long-horizon instrumented runs** (the simulated S20-26 run): per the official guidance, single sustained ownership with checkpoint-resume — the standout capability, wasted if decomposed.
6. **Venture-selection / demand-thesis adjudication**: the doctrine's #1 under-allocated use; taste + negative-space + irreversibility. (The adjudication frame is pre-built; the run itself stays Fable.)

## TIER O — Opus-adequate (route here before spending Fable)
Scoped verification panels and experiments (the Stripe 3-lens pattern); coordination-loop observation; autonomy/grounding oversight reports; band-logic and packet prechecks; diff-review of a *single* well-specified change.

## TIER S — Sonnet-at-high-effort (proven this week; never route up)
Fix-SD builds against precise specs with verification plans; QFs; evidence-packet gathering (cited, explicit NOT-FOUNDs); fixture/doc assembly; I3-style mechanical work. **Caveat:** audits/breadth-sweeps at Sonnet require method-emission + Fable spot-confirmation (the under-find rule).

## ANTI-MAP — not worth any premium tier (observed waste this week)
Mechanical read/ack stamping; empty drain ticks (the burn-throttle lesson: the poll is cheap, the big-context wake is not); doc formatting; anything rubric-decidable; re-deriving answers that exist (the answer-not-unblocked class — the fix is disposition-wiring, not more reasoning).

## Routing recommendation for the +2 incoming Fable seats
Seat 1: **owns the S20-26 simulated run end-to-end** (H7 one-owner; harness spec build-final). Seat 2: **cluster-#3 kill-gate ledger territory** (the first-overflow item) using Charlie's ledger format, against the adjudication frame's S5 half — then rotates to verifier duty for seat 1's run per H10. Existing seats continue the fix queue (Sonnet-tier work; don't promote it to the new Fable seats — that's the misrouting the map exists to prevent).

*Propose-only. This map is a living artifact: each future Mode-B sweep may re-rank one region with new receipts; wholesale revision only on model-generation change (the doctrine's own trigger).*
