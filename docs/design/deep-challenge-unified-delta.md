# 26-Stage Deep Challenge — Unified Delta (cross-cluster synthesis)

**Status:** Solomon-authored (Fable 5 / high, 2026-07-12), propose-only (CONST-002). The deep-challenge commission's terminal deliverable (chairman, 07-10): observations → per-cluster ledgers → adjudication → **this unified delta** → prioritized residual map. Every in-window cluster is now adjudicated; this synthesizes what the whole challenge PROVED, REFUTED, FIXED, and LEFT. Committed at creation per the evidence-durability rule.

---

## 1. What the challenge proved (greenfield-vs-actual, by cluster)

**Cluster 1 — Stage-0 (selection):** the greenfield spec (R1–R10) **held completely — zero refuted requirements** across three independent audit ledgers. The as-built engine failed it in exactly the predicted classes: fabricated completeness (hardcoded 15/15), fail-toward-flattery scoring, machine-forged chairman approval, dead park path, dropped slates. **All six fix SDs shipped same-day** (fail-closed engine + seeded-defect canary, governed posture, E0–E3 evidence grading live on the ranking seam, thesis contract, traversability gate, chairman-decision authority) — flaw-ledger-to-fully-fixed in 2.5 hours, and the first Discovery run + chairman pick + thesis adjudication (RETURN-for-completion, correctly) ran on the fixed engine the same day. **Verdict: selection is now trustworthy; the greenfield-first method is validated end-to-end.**

**Cluster 2 — S20-26 / operations / marketing:** the spec (O1–O10) predicted the band's failure modes with one significant exception. **Refuted: O2's "thin gate-wiring"** — the artifact gate has HARD teeth (it blocked even the harness's sanctioned bypass; prediction revised spec-side, and the gate-correct/hatch-wrong ruling produced the fixture-seeder pattern). **Confirmed: O5 ops loops absent** (support/incident/health = CANNOT_DRIVE), **O6 attribution unproven** (the rail lives venture-side; platform cannot drive it), **O8 scale-or-exit total absence** (the likeliest-total-absence prediction, exactly right), **O3 artifact-theater** (distribution templates drove no channel action), and the **O4 honest-gauge class** manifested in the harness's own coverage gauge before any band gauge could be tested. The re-run (0711b) verified the fix wave live: seeder passes the gate honestly, journal survives teardown, coverage reports honest dispositions (7/9 positive). **Verdict: the band is DRIVABLE now but not OPERATING — its missing machinery is precisely the satellite layer the chairman has since commissioned.**

**Horizontal polarity pass:** one HIGH (exit-gate unknown-verifier→ALLOW — folded into the kill-gate seam work), one MED (missing-stage-row indistinguishable from no-gates), plus the structural observation that three polarities coexist in one file by accretion. Positive exemplars confirmed the doctrine is spreading (verifiers fail-closed, chairman-gate schism repaired).

**Kill-gates (demoted cluster, superseded by a stronger instrument):** rather than a static audit, the window produced the **teeth-proof regime** — Q5 three-mechanism map, the Tier-B `evaluateKillCriterion` seam wired (observe-only, binding rides Beta), and BOTH probes sealed with pre-registered predictions (ALPHA financial-fatal, BETA honest-pass + armed thesis kills + NO-DATA HOLD). **Verdict: kill-ability is designed and instrumented but NOT YET PROVEN — the probes have not run.**

## 2. The cross-cluster findings (what only the synthesis shows)

1. **One failure family dominated every cluster: fail-toward-flattery / fabricated-positive.** Stage-0's hardcoded 15/15 and forged approvals; the band's coverage-as-success gauge; the O4 fabricated-zero class; the polarity pass's unknown→ALLOW. The week's fixes all install the same invariant — **fail-closed, provenance-reached, never synthesize a positive** — and it is now doctrine in the spine spec (§"nothing fail-open, ungauged, or unowned"). The residual risk is not new instances being written; it is OLD instances not yet swept (the S6-14 band was deliberately out-of-window).
2. **The second family: dormant-completeness.** Built-never-invoked machinery appeared in every layer (CEO factory, ops collectors, APA children, the liveness watcher, the harness's own journal mirror). The P6 ownership closure + activation-evidence=block is the structural fix; it is in EXEC, not yet enforced everywhere.
3. **Evidence durability is a first-class requirement, learned three times in one day** (run journal, design docs, resurface ledger). Now codified (run-keyed teardown-exempt journals; commit-at-creation for deliverables; disposition-stamped feedback).
4. **The method itself is validated:** greenfield-cold → adversarial audit → adjudication → same-day fixes → re-run verification worked at every scale it was tried. The falsifiable delta-predictions cut both ways exactly as designed (one prediction refuted → spec revised, not code).

## 3. Prioritized residual map (SD-shaped; deduped against in-flight work)

**P0 — blocks the Phase-1 exit claim:**
- **Probe runs + Solomon grading** (post-ApexNiche-traversal; both sealed; the factory-can-kill proof is the last unproven Phase-1 leg).
- **S21 block-marker enum STILL violating** (re-run confirmed the interim migration did NOT admit the S21 marker — the F5 class persists live; one gated migration line, chairman-apply pre-flagged).
- **ApexNiche traversal completion** (the 30-SD sprint tree; the process-proof itself).

**P1 — the operating layer (already commissioned, listed for completeness):** spine core + satellites A–H (builders active; E–H design reviews are this window's item 2); relationship-engine/support + wind-down close O5/O8; venture-side attribution + first real demand-rail drive belong to venture-2's traversal (O6/O3 closure).

**P2 — mechanical residuals (QF-class, sourceable any time):** S20 verdict-enum contract mismatch (BLOCKED ∉ [PASS,FAIL,WARN] → no artifact); S24 cross-stage checklist contract (S23 omits what S24 reads); teardown table-list write-derived + fence-7 KPI zero-fixture assertion; the P1/P2/P3/P7 seven-principle closures (queued); enum-to-FK evaluation (verification-plan-first).

**Declared out-of-window (unchanged, honest):** the S6-14 band audit — most-trafficked, most-surfaced already; revisit only if venture-2's traversal trips it.

## 4. Phase-1 exit bearing (feeds the selection/exit packet directly)

Against Phase-1's ratified goal — *prove the process, not the revenue* — the challenge's ledger reads: **selection PROVEN** (fixed + exercised live), **traversal IN PROGRESS** (ApexNiche on the real build tree), **kill-ability INSTRUMENTED, NOT PROVEN** (probes sealed, unrun), **operating band DRIVABLE, NOT OPERATING** (satellite layer commissioned). The honest exit posture: Phase-1 completes when the traversal lands AND at least Probe-ALPHA grades PASS — everything else is Phase-2 build-out with designs already ratified. Counterfactual: if the chairman weights time-in-window over proof completeness, exit-on-traversal-alone is defensible ONLY with the probe runs pre-scheduled as Phase-2's first act — an unproven kill regime must not silently become background debt.

*Propose-only. Companion packet (Phase-1 exit + venture-2 selection) follows as the next increment.*
