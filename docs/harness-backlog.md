# Harness Backlog

**Purpose**: One-line deferred captures of harness-level bugs found during `[MODE: product]` sessions.

During product work, do NOT file SDs/QFs for harness issues (LEO-INFRA, gate bugs, session lifecycle drift, tooling gaps). Append them here and keep shipping product. A follow-up `[MODE: campaign]` session processes this backlog: triages, groups, and files the necessary `SD-LEO-INFRA-*` / `SD-LEARN-FIX-*` / `SD-MAN-INFRA-*` / `QF-*` against the real recurrence signal.

See CLAUDE.md → Session Mode Declaration for the full rule.

## Format

One line per item. Date, symptom, file or command where it surfaced, SD/QF from which session it was deferred.

```
2026-MM-DD | <symptom> | <file or command> | deferred from SD-...
```

## Items

<!-- Append below. Do not edit or remove existing lines — they are the signal for the next campaign session. -->
2026-04-26 | GATE5_GIT_COMMIT_ENFORCEMENT hardcodes appPath=EHG_ROOT (ehg frontend repo) — backend EHG_Engineer SDs see wrong branch, gate misfires; bypass needed every backend SD | scripts/verify-git-commit-status.js:33 + scripts/modules/handoff/executors/plan-to-lead/gates/git-commit-enforcement.js:18 | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001 (PR #3365)
2026-04-26 | sd_type gaming-detection trigger blocks LEAD-authority type changes when LLM detector + manual confirmation both agree the type is wrong (95% LLM confidence at PLAN-TO-EXEC SD_TYPE_VALIDATION); requires governance_metadata.bypass_reason that operator cannot self-grant from EXEC | DB trigger raising P0001 on strategic_directives_v2 sd_type updates | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | Chaining-end behavior: with chain_orchestrators=true and no next orchestrator queued, LEAD-FINAL-APPROVAL exits 1 attempting to claim a non-existent UUID (cosmetic, SD already completed correctly) | scripts/modules/handoff/executors/lead-final-approval/index.js + chain auto-continuation logic | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | model_usage_log CHECK constraint stale: rejects phase='EXEC-TO-PLAN' (handoff phase enum drifted from log enum) | model_usage_log.phase CHECK constraint | observed by database-agent during user-stories status update, deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | Concurrent npm install across sibling worktrees on shared C:\Users\rickf\Projects\_EHG\EHG_Engineer\node_modules causes ENOTEMPTY/ENOENT lock errors (recovery requires sequential install) | npm install on shared node_modules in worktree fleet | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001

2026-04-27 | splitPostgreSQLStatements (scripts/lib/supabase-connection.js) splits on semicolons inside single-line `--` comments, yielding malformed DDL fragments rejected as 42601 syntax error; workaround: send full migration as single client.query() call | scripts/lib/supabase-connection.js splitPostgreSQLStatements | observed during SD-LEO-INFRA-PR-TRACKING-BACKFILL-001 migration apply
2026-04-27 | leo-create-sd auto-generated SD metadata references vision_key/arch_key (e.g. VISION-EVA-SUPPORT-CLI-L2-001 + ARCH-EVA-SUPPORT-CLI-001 in SD-EVA-SUPPORT-CLI-SKILL-ORCH-001 metadata) but corresponding rows do NOT exist in vision_plans / sd_vision_plans / architecture_plans / sd_architecture_plans / brainstorm_sessions. Orphan FK-by-string. LEAD evaluation cannot trace strategic provenance | leo-create-sd vision/arch handoff path + table writes | deferred from SD-EVA-SUPPORT-CLI-SKILL-ORCH-001-A LEAD eval
- 2026-04-27: auto-trigger-stories.mjs Gemini timeout (3 retries) + priority-enum CHECK mismatch (script outputs MoSCoW must/should/could but DB CHECK is critical/high/medium/low). Forced manual-author fallback for SD-EHG-AI-GEN-GUARDRAILS-001 (US-1..10 inserted by general-purpose agent). 2-issue compound: LLM provider fragility + script/schema enum drift.
- 2026-04-27: PR #536 (SD-ACTIVATE-S18-MARKETING-COPY-ORCH-001) merged to main 2026-04-27T14:54Z but chairman_decisions.approval_type column NOT present in live DB (error: column does not exist). Migration apparently didn't run on merge. Discovered during SD-EHG-AI-GEN-GUARDRAILS-001 EXEC prerequisite check; FR-7 types.ts regen + FR-6 audit-trail integration both blocked. Possible cause: missing supabase migration deploy step in CI, or migration file not in PR scope. | live DB project dedlbzhpgkmetvhbkyzq + supabase/migrations/ | discovered EXEC pre-flight
- 2026-04-27: EHG repo has NO CI step that runs `supabase db push` on merge to main; supabase_migrations.schema_migrations has ZERO 20260427_* entries despite multiple 20260427 migrations existing in supabase/migrations/. PR #536 column went 5h unapplied as a result. Migrations land via out-of-band manual psql; ledger never updated. Systemic deployment hygiene gap. | .github/workflows/ + supabase/migrations/ | discovered during PR #536 unblock for SD-EHG-AI-GEN-GUARDRAILS-001
- 2026-04-27: chairman_unified_decisions view in PR #536 drops the `escalation` branch (live: 4 branches incl. agent_messages-driven escalation; PR #536: 3 branches with venture_decision shape + new approval_type). Frontend reading details->>'approval_type' from view gets NULL since live view's chairman_approval branch lacks it. Part 5 of 20260427_001 migration deferred during unblock. | ehg/supabase/migrations/20260427_001_add_approval_type_to_chairman_decisions.sql Part 5 + chairman_unified_decisions view | deferred during DB-agent unblock pass
- 2026-04-27: Migration 20260427000002_revert_decided_by_user_id never applied to live DB while sibling 000001/000003/000004 did apply. decided_by_user_id column still present + functions reference it. Out-of-band manual apply skipped the revert. Minor data-model inconsistency. | live DB chairman_decisions table + ehg/supabase/migrations/20260427000002* | discovered during sibling migration drift check
- 2026-04-27: husky pre-commit unconditionally runs `npm run test:smoke` regardless of branch type — fails for docs-only branches when network resolution is offline (DNS ENOTFOUND on test.invalid.local intentionally-invalid test target). Hook recognizes docs/ branches for LOC-threshold exemption (line 682) but not for smoke-test exemption. Forces `--no-verify` bypass for docs-only commits when environment is offline. Sibling pattern: same hook exempts docs/ from LOC-floor but not from runtime checks | .husky/pre-commit smoke-test stage | discovered while shipping docs/harness-backlog updates 2026-04-27
- 2026-04-28: SD-LEO-ENH-TREND-SCANNER-SCORING-001 has target_application='EHG' but primary code lives in EHG_Engineer (lib/eva/stage-zero/*, scripts/stage-zero-queue-processor.js, database/migrations/*). sd-start created worktree at ehg/.worktrees/ — wrong repo for the bulk of the diff. Manual workaround: switched DB strategic_directives_v2.worktree_path + claude_sessions.worktree_path to existing EHG_Engineer/.worktrees/SD-LEO-ENH-TREND-SCANNER-SCORING-001 worktree. Cross-repo SDs need PRD-aware target detection or per-FR repo routing | scripts/sd-start.js worktree creation + strategic_directives_v2.target_application | deferred from SD-LEO-ENH-TREND-SCANNER-SCORING-001 EXEC
- 2026-04-28: sd_type source-of-truth fragmentation across 4 surfaces — (1) leo-create-sd.js CLI --type flag accepts 'enhancement', (2) plan-parser inferSDType valid-list omits 'enhancement', (3) DB sd_type_check CHECK accepts {feature,bugfix,database,infrastructure,security,refactor,documentation,orchestrator,performance,enhancement,docs,discovery_spike,implementation,ux_debt,uat} but NOT 'fix', (4) vision-score gate SD_TYPE_THRESHOLDS lists 'fix' AND 'bugfix' as separate keys. Database-agent had to substitute 'fix'→'bugfix' on UPDATE during this SD's LEAD-TO-PLAN. Canonicalize the enum in one place (DB CHECK as source of truth) and fail loudly upstream | DB CHECK constraint sd_type_check + scripts/modules/handoff/executors/lead-to-plan/gates/vision-score.js SD_TYPE_THRESHOLDS + scripts/modules/plan-parser.js valid-list + scripts/leo-create-sd.js CLI flag list | discovered during SD-LEO-ENH-TREND-SCANNER-SCORING-001 LEAD-TO-PLAN sd_type correction
- 2026-04-28: leo-create-sd target_application autodetect ignores explicit `## Target Application` plan-content header. SD-LEO-ENH-TREND-SCANNER-SCORING-001 plan literally said "EHG_Engineer (Stage 0 venture engine; backend-only)" but SD landed with target_application='EHG'. detectFromKeyChanges (PR #3366) walks file paths in key_changes — but key_changes was a single boilerplate "Implement core changes for…" entry. Parser fell back to default 'EHG' instead of reading the explicit header. Detection priority should be: explicit header > file-path inference > default | scripts/lib/target-application.js detectFromKeyChanges + scripts/leo-create-sd.js header-parsing | deferred from SD-LEO-ENH-TREND-SCANNER-SCORING-001 creation 2026-04-28
- 2026-04-28: database-agent SendMessage resumption returned "completed" without persisting requested writes — first run claimed success on metadata patches (decomposition_judgment, cascade_flag_overridden, plan_advisories) but a fresh database-agent invocation found NONE of those keys present in governance_metadata; only orphan_check + risk_assessment existed. Root cause: prior agent wrote to wrong column (`metadata` vs `governance_metadata`) and reported success against the column it was asked to write — not the one it actually touched. Sibling pattern to feedback_sd_already_completed_when_claimed_via_sdnext (DB↔completion-signal drift class) | database-agent task harness + SendMessage resumption path | deferred from SD-LEO-ENH-TREND-SCANNER-SCORING-001 LEAD prep 2026-04-28
- 2026-04-28: GATE_TEST_COVERAGE_QUALITY spawns Playwright with 60s timeout for backend-only SDs that have no UI tests — gate always times out and forces a documented bypass at every backend EXEC-TO-PLAN. Closed memory `feedback_playwright_coverage_gate_timeout_backend_sds.md` flagged this 2026-04-XX with proposed fix: short-circuit when sd_type IN ('bugfix','infrastructure','documentation') AND diff doesn't touch *.tsx/pages/components. Bypass is so routine it's effectively a no-op gate for an entire SD class | scripts/modules/handoff/executors/exec-to-plan/gates/test-coverage-quality.js:328 (npx playwright test spawn) | deferred from SD-LEO-ENH-TREND-SCANNER-SCORING-001 EXEC-TO-PLAN; QF-20260428-PLAYWRIGHT-BACKEND filed
- 2026-04-28: After /ship Step 6.7 auto-cleans worktree, LEAD-FINAL-APPROVAL runs from main repo and needs that repo's node_modules intact. If a parallel session has trashed it during the SD's lifetime (npm install collision per existing 2026-04-26 entry), the handoff fails post-merge with `Cannot find package '@supabase/supabase-js'` — leaves SD in "merged but not LEAD-FINAL'd" limbo. Recovery is `npm install` then re-run handoff, but the failure mode is non-obvious because the worktree (where work happened) is gone, while main repo deps are what the handoff needs. Pre-handoff health check + auto-recovery would surface the issue before the destructive merge step rather than after | scripts/handoff.js LEAD-FINAL-APPROVAL preflight + lib/supabase-client.js import surface | deferred from SD-LEO-ENH-TREND-SCANNER-SCORING-001 LEAD-FINAL recovery
- 2026-04-28: Retros routinely list "lessons learned" that are actually symptoms of tooling gaps — would never need to be re-learned if the underlying system gap were fixed. Examples from SD-LEO-ENH-TREND-SCANNER-SCORING-001's retro: cross-repo target_app misclassification (→ filed SD-LEO-INFRA-START-WORKTREE-BRANCH-001), Playwright bypass for backend SDs (→ this QF), schema drift between memory and live DB (→ multiple sibling backlog entries 2026-04-26/28), node_modules wipe + handoff health check (→ this entry). Need: retro-agent triage step that classifies each candidate lesson as `domain-specific` (keep as lesson) vs `tooling gap` (file as backlog item / SD / QF instead of memorializing prose). Otherwise lessons accumulate as buried prose nobody queries while the systemic gaps recur | retro-agent prompt + retrospectives table content quality | meta-defect surfaced during SD-LEO-ENH-TREND-SCANNER-SCORING-001 closeout
- 2026-04-28: Worktree cleanup EPERM on Windows (file handle still open) leaves orphan worktree at `.worktrees/SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001/` after /ship pipeline completes — recovery is `git worktree remove --force` later. Likely race between Node fs.rmdir and Windows AV/indexer. Sibling pattern to other Windows-EPERM entries; needs retry-with-backoff or platform-specific cleanup path | scripts/worktree-cleanup.* + scripts/ship.* | deferred from SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001 ship pipeline
- 2026-04-28: target_application auto-flipped EHG_Engineer → EHG between SD creation and LEAD-TO-PLAN handoff for SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001 — sibling of 2026-04-28 entry "leo-create-sd target_application autodetect ignores explicit `## Target Application` plan-content header" but flip happened POST-creation, suggesting a second autodetect pass during LEAD-TO-PLAN that overrides the originally-set value. Two recurrences in 24h confirms the autodetect needs an explicit-header-respect rule | scripts/lib/target-application.js + LEAD-TO-PLAN handoff target validation | deferred from SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001
- 2026-04-28: stories-agent referenced in CLAUDE_LEAD.md/CLAUDE_PLAN.md but not registered as a valid subagent_type — sibling of memory `reference_subagent_inventory_no_risk_agent.md` (risk-agent same defect). Phase context files reference agents that don't exist; sub-agent invocations route to general-purpose by default. Generalize: audit all phase docs for agents not in the subagent_type enum and either register or remove | CLAUDE_LEAD.md + CLAUDE_PLAN.md + Task tool subagent_type allowlist | deferred from SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001
- 2026-04-28: model_usage_log_phase_check CHECK constraint rejects phase='PLAN-TO-LEAD' — sibling of 2026-04-26 entry rejecting 'EXEC-TO-PLAN'. Confirms enum drift between handoff phase enum (LEAD-TO-PLAN, PLAN-TO-EXEC, EXEC-TO-PLAN, PLAN-TO-LEAD, LEAD-FINAL) and log table CHECK. Two sibling rejections in 2 days. Fix: ALTER CHECK constraint to match the canonical handoff phase enum | model_usage_log.phase CHECK constraint | deferred from SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001 PLAN-TO-LEAD
- 2026-04-28: 7 pre-existing test failures on origin/main in tests/unit/eva/stage-templates/stage-14.test.js — `computeDerived()` returns undefined for fields the test expects. Not introduced by SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001 (verified clean). Means main branch test coverage on Stage 14 is broken; CI is either green-on-failure or the file is excluded from CI. Investigate root cause + close gap (Stage 14 is now load-bearing for the house-stack constraint, can't have its tests rotting) | tests/unit/eva/stage-templates/stage-14.test.js + lib/eva/stage-templates/analysis-steps/stage-14-technical-architecture.js computeDerived | discovered during SD-LEO-ENH-CONSTRAIN-STAGE-EHG-001 EXEC test pass
- 2026-04-29: replit-repo-seeder.js::seedRepo(ventureId, repoUrl) accepts the GitHub URL as a parameter and uses it to clone/commit/push the venture docs, but NEVER persists the URL back to any DB column. As a result, every venture that ships through the seeder loses provenance of its repo URL — Stage 20 then can't find it. LexiGuard hit this 2026-04-29: URL was used for push but absent from venture_resources, ventures.repo_url, venture_provisioning_state, and all stage-19 artifacts. Manual backfill required to unblock S20. Fix: have seedRepo() call registerVentureResource(ventureId, 'github_repo', repoUrl, 'github') after a successful push, and also UPDATE ventures.repo_url. Idempotent under the existing UNIQUE (venture_id, resource_type, resource_identifier) constraint. | lib/eva/bridge/replit-repo-seeder.js seedRepo() success path | discovered during LexiGuard S19→S20 pre-approval review
- 2026-04-29: Stage 20 Code Quality Gate analyzer (lib/eva/stage-templates/analysis-steps/stage-20-code-quality.js) had a code-vs-schema mismatch — read `select('github_repo')` from venture_resources, but the migration that shipped (supabase/migrations/20260330_create_venture_resources.sql, SD-LEO-INFRA-UNIFIED-VENTURE-CREATION-001-B) is row-per-resource (resource_type='github_repo' + resource_identifier=URL). Flat-column query always returned "column does not exist" → silent fallthrough → S20 returned 'FAIL' with "No GitHub repo URL found" for every venture since PR #3209 (2026-04-22). 7 days of latent failure for any venture that reached S20. Fix shipped on branch fix/stage-20-venture-resources-query-shape 2026-04-29; root cause is divergent design between analyzer author (assumed flat columns) and migration author (canonical row-per-resource model). Sibling pattern: vision-vs-implementation drift, same class as S19 marketing-copy binding gap (Rule 7). | lib/eva/stage-templates/analysis-steps/stage-20-code-quality.js:177 + supabase/migrations/20260330_create_venture_resources.sql | discovered during LexiGuard S19→S20 pre-approval review; fix shipped same day
- 2026-04-29: database-agent created 3 ad-hoc search scripts during the LexiGuard GitHub URL hunt — scripts/one-off/lexiguard-gh-search{,-2,-3}.mjs — violating CLAUDE_CORE.md "Script Creation Anti-Patterns" (one-off creation scripts prohibited). Sub-agent created when sequential refinement of one script would have sufficed. Pattern: agent's tool harness encourages "create new script" rather than "edit existing", because edits require Read-before-Edit while writes don't. Fix: update database-agent prompt to prefer in-place SQL via Bash one-liner OR a single iterated script when ad-hoc queries are needed. | sub-agent persona prompts (database-agent) + scripts/one-off/lexiguard-gh-search*.mjs leftovers | discovered during LexiGuard S19→S20 pre-approval review
- 2026-04-29: lib/eva/stage-templates/analysis-steps/stage-20-code-quality.js has NO test coverage. Existing files at tests/unit/eva/stage-templates/stage-20.test.js + stage-20-build-execution.test.js test legacy stage-19.js + stage-20-build-execution.js, NOT the canonical post-redesign analyzer. Means a 7-day-latent code-vs-schema bug shipped to main without any test catching it. CI is green-on-no-tests for this file. Add unit tests covering: (a) primary venture_resources path with row-per-resource shape, (b) ventures.repo_url fallback, (c) stage19Data fallback, (d) all 4 check_types (npm_audit, secret_detection, lint, test_suite), (e) verdict computation logic. | tests/unit/eva/stage-templates/analysis-steps/stage-20-code-quality.test.js (missing) | discovered during LexiGuard S19→S20 pre-approval review
