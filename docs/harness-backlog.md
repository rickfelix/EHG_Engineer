# Harness Backlog

**Purpose**: One-line deferred captures of harness-level bugs found during `[MODE: product]` sessions.

During product work, do NOT file SDs/QFs for harness issues (LEO-INFRA, gate bugs, session lifecycle drift, tooling gaps). Append them here and keep shipping product. A follow-up `[MODE: campaign]` session processes this backlog: triages, groups, and files the necessary `SD-LEO-INFRA-*` / `SD-LEARN-FIX-*` / `SD-MAN-INFRA-*` / `QF-*` against the real recurrence signal.

See CLAUDE.md → Session Mode Declaration for the full rule.

## Format

One line per item. Date, symptom, file or command where it surfaced, SD/QF from which session it was deferred.

```
2026-MM-DD | <symptom> | <file or command> | deferred from SD-...
```

## Items

<!-- Append below. Do not edit or remove existing lines — they are the signal for the next campaign session. -->
2026-04-26 | GATE5_GIT_COMMIT_ENFORCEMENT hardcodes appPath=EHG_ROOT (ehg frontend repo) — backend EHG_Engineer SDs see wrong branch, gate misfires; bypass needed every backend SD | scripts/verify-git-commit-status.js:33 + scripts/modules/handoff/executors/plan-to-lead/gates/git-commit-enforcement.js:18 | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001 (PR #3365)
2026-04-26 | sd_type gaming-detection trigger blocks LEAD-authority type changes when LLM detector + manual confirmation both agree the type is wrong (95% LLM confidence at PLAN-TO-EXEC SD_TYPE_VALIDATION); requires governance_metadata.bypass_reason that operator cannot self-grant from EXEC | DB trigger raising P0001 on strategic_directives_v2 sd_type updates | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | Chaining-end behavior: with chain_orchestrators=true and no next orchestrator queued, LEAD-FINAL-APPROVAL exits 1 attempting to claim a non-existent UUID (cosmetic, SD already completed correctly) | scripts/modules/handoff/executors/lead-final-approval/index.js + chain auto-continuation logic | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | model_usage_log CHECK constraint stale: rejects phase='EXEC-TO-PLAN' (handoff phase enum drifted from log enum) | model_usage_log.phase CHECK constraint | observed by database-agent during user-stories status update, deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | Concurrent npm install across sibling worktrees on shared C:\Users\rickf\Projects\_EHG\EHG_Engineer\node_modules causes ENOTEMPTY/ENOENT lock errors (recovery requires sequential install) | npm install on shared node_modules in worktree fleet | deferred from SD-LEO-INFRA-VISION-SCORER-L2-FLAGS-001
2026-04-26 | Stale pre-emptive chairman_decisions row at S18 created 18h before venture actually entered S18 (created 02:04:48 UTC, venture transitioned ~19:53 UTC); row sat pending awaiting current stage to catch up — possible pre-emptive gate creation or rollback artifact | chairman_decisions table; venture 94856fc6-9ba9-4f56-9a5c-85041031a0fc | deferred from S18 dead-gate campaign 2026-04-26
2026-04-26 | Stage progress denominator mismatch in venture detail UI: header shows "Stage N/26" while gate banner shows "Stage N/25" (observed at S17 and S18); cosmetic but inconsistent | EHG/src/components/stages and parent venture header components | deferred from S18 dead-gate campaign 2026-04-26
2026-04-26 | screen-0 (uploaded design) missing s17_design_system extraction row while screen-1..5 (generated archetypes) all have one — verify intent vs gap (uploaded designs may legitimately skip extraction, OR worker bug) | venture_artifacts table for venture 94856fc6 | deferred from S18 dead-gate campaign 2026-04-26
2026-04-26 | strategic_directives_v2.metadata.pr_url is unpopulated on 100% of 2528 completed SDs (incl. SDs shipped today) — /ship Step 4 returns gh pr URL but never persists it back to SD; CAPA 1 PRD's FR-1/FR-2 keyed on this field are dead code (real fix used branch state instead). Three follow-up SDs implied: pr_url populator in /ship, backfill from ship_review_findings + gh pr lookup, periodic reconciler | scripts/skills/ship.md Step 4 + completion path in handoff.js LEAD-FINAL-APPROVAL | discovered during reconciliation audit follow-up to SD-LEO-INFRA-HANDOFF-MERGE-MAIN-001
2026-04-26 | ship_review_findings has only 21 distinct sd_keys vs 2528 completed SDs (98.7% coverage gap); table is recent so most gaps are pre-existence not bad-merge — but the cross-check needed to distinguish requires gh pr lookup against 484 last-30-day untracked SDs. CSV artifact saved | docs/audits/sd-completion-without-pr-tracking-2026-04-26.csv | deferred from reconciliation audit 2026-04-26
2026-04-26 | Fleet dashboard has no QF visibility: fleet-dashboard.cjs renders only SDs (no QF section); stale-session-sweep.cjs lacks branch→quick_fixes cross-reference for qf/* and quick-fix/* branches; ghost-detector path (file location TBD — predictive-signals.cjs cited in original recommendation does not exist) may classify QF workers as ghosts. Estimated 100-200 LOC if filed; verify ghost-detector location before scoping | scripts/fleet-dashboard.cjs + scripts/stale-session-sweep.cjs | deferred from SD-LEO-INFRA-S17-PLAYWRIGHT-COVERAGE-001
2026-04-26 | LEAD-FINAL handoff appears auto-skipped on infrastructure SDs: SD-LEO-INFRA-HANDOFF-MERGE-MAIN-001 went status=completed via PLAN-TO-LEAD only (no LEAD-FINAL row in sd_phase_handoffs), so PR_MERGE_VERIFICATION gate (lives at lead-final-approval/gates.js) never ran for the SD's own ship — exactly the gate this SD hardens. Observed in 5-row handoff trail: LEAD-TO-PLAN→PLAN-TO-EXEC→EXEC-TO-PLAN→PLAN-TO-LEAD(reject)→PLAN-TO-LEAD(accept 97), then completion at ~21:32Z driven by autonomous loop on still-alive orphan session 796b83dc | scripts/modules/handoff/executors/lead-final-approval/* + status-flip path that bypasses LEAD-FINAL | deferred from SD-LEO-INFRA-HANDOFF-MERGE-MAIN-001 (PR #3378)
2026-04-26 | StageContentRouter.tsx:159-171 universal pre-processing placeholder shell renders "Approve to begin processing this stage" with NO Approve button — intercepts ALL gate stages (S3,S5,S13,S17,S18,S23) before per-stage renderer can show its own action. CAPA 2's Generate button at Stage18MarketingCopy:237 is unreachable because the router gates the renderer on advisoryData existing, which only happens after generation runs — chicken-and-egg. Filed as SD-MAN-FIX-S18-DEAD-GATE-001 | EHG/src/components/ventures/stages/StageContentRouter.tsx | discovered during CAPA 2 verification 2026-04-26
2026-04-27 | HARNESS-CLAIM-CROSS-TABLE-DRIFT — claim_sd() does not consult strategic_directives_v2.claiming_session_id when evaluating ownership; only checks heartbeat-window of OTHER claude_sessions rows holding same sd_key. Direct UPDATE to SD's claiming_session_id is silently overridden the next time peer's session-bound code calls claim_sd. Two surfaces (claude_sessions.sd_key + strategic_directives_v2.claiming_session_id) aren't joined. Witnessed thrice during SD-LEO-INFRA-TEST-COVERAGE-HYGIENE-001 EXEC-to-LEAD-FINAL transitions | DB function claim_sd() + strategic_directives_v2.claiming_session_id pointer | deferred from SD-LEO-INFRA-TEST-COVERAGE-HYGIENE-001 (PR #3384)
2026-04-27 | HARNESS-PEER-CLAIM-NEVER-RELEASED — when a peer claude.exe is launched into an existing worktree that already holds a claim, the new session legitimately re-claims (or appears to) but release_sd() is never called against the prior owner; cleanup-stale-sessions cron only marks status='stale' after heartbeat_at < NOW() - 900s (15min); during that window two sessions both think they own the SD. A stronger model would invalidate the prior claude_sessions.sd_key whenever a new session inserts/updates with the same worktree_path AND same sd_key (two simultaneous owners on one worktree is structurally impossible) | session lifecycle: SessionStart auto-worktree hook + claim_sd() | deferred from SD-LEO-INFRA-TEST-COVERAGE-HYGIENE-001 (PR #3384)
2026-04-27 | Worktree reaper removed .git file + lib/ directory from active worktree mid-session (SD-LEO-INFRA-TEST-COVERAGE-HYGIENE-001 worktree at .worktrees/SD-LEO-INFRA-.../) AFTER PR #3384 merged + branch auto-deleted; subsequent handoff.js calls from that worktree fail with ERR_MODULE_NOT_FOUND for lib/supabase-client.js. Recovery: run handoff from sibling worktree. Reaper should not strip required content while session still active on the directory | scripts/worktree-reaper or auto-cleanup hook | deferred from SD-LEO-INFRA-TEST-COVERAGE-HYGIENE-001 (PR #3384)
2026-04-27 | quick_fixes table missing pr_number column AND loc_changed column — PR linkage stored via free-form pr_url string (no integer key for joins to ship_review_findings/PR-tracking dashboards); LOC stored only as actual_loc/estimated_loc with no canonical changed-LOC field. Inconsistent with strategic_directives_v2 patterns | quick_fixes table DDL | deferred from QF-20260427-411 (PR #537)
2026-04-27 | quick_fixes table missing updated_at column — inconsistent with most LEO tables (strategic_directives_v2, claude_sessions, retrospectives all have updated_at); blocks generic UPDATE-touch triggers + audit timeline reconstruction. Surfaced when database-agent close-out script rejected updated_at via PostgREST schema cache | quick_fixes table DDL | deferred from QF-20260427-411 (PR #537)
2026-04-27 | complete-quick-fix.js interactive yes/no prompts incompatible with non-TTY stdin: piping printf/echo answers does not advance through the loop; script appears to re-prompt indefinitely after compliance refinement loop completes. Forces operators to either (a) run interactively in a real TTY or (b) bypass the script entirely via database-agent. Recurrence: scope-creep + linting false positives compound the friction (script reads stale local main + lints unrelated detritus, raises 3-of-5 warnings every QF) | scripts/complete-quick-fix.js compliance + completion prompts | deferred from QF-20260427-411 (PR #537)
2026-04-27 | complete-quick-fix.js compliance gate compares against local main (stale) instead of origin/main: if local main is behind origin (peer's PR merged after our worktree was created off origin/main), the script reports unrelated files as "scope creep" and "files don't match issue description". Misleads operators into thinking their QF is wrong; the actual PR diff on GitHub is correct. Fix: rev-parse origin/main and `git diff origin/main..HEAD` instead of bare `main` | scripts/complete-quick-fix.js diff comparison logic | deferred from QF-20260427-411 (PR #537)
2026-04-27 | QF auto-completion hook misfire: a DIFFERENT PR (#538, for SD-ACTIVATE-S18) mentioning the QF ID in its commit message was sufficient to flip QF-20260427-411 from open→completed with fake verified_by='ship-step-6.3-bypass'. Hook's QF-ID extraction needs an authoritative-PR check (the QF row should only complete via its own assigned pr_url/branch). Sibling pattern to feedback_bad_merge_silent_regression_pattern + project_qf_081_completed (autoDetectGitInfo authoritative-source fix) | /ship Step 6.3 + auto-learning hook + QF detector | deferred from QF-20260427-411 actual fix (PR #539)
2026-04-27 | quick_fixes table missing metadata JSONB column for structured provenance (auto_completion_misfire flag, misfire_origin, etc.). The compliance_details JSONB exists but is owned by the rubric scorer — not safe to overload. Misfire context currently captured only as free-text in verification_notes (not machine-queryable) | quick_fixes table DDL | deferred from QF-20260427-411 close-out (PR #539)
2026-04-27 | Brainstorm "Suggested Next Steps" pre-file declarations don't auto-execute: brainstorm 3fa31151 listed three companion SDs as "pre-filed" (SD-MAN-FIX-S22-S26-ROUTER-PARITY-001, SD-EHG-AI-GEN-GUARDRAILS-001, SD-LEO-INFRA-PROP-CONTRACT-AUDIT-001) but DB query returned zero matches — they were never actually inserted. The eva-archplan-command pipeline outputs the recommendation but doesn't ship the SD rows | scripts/eva/archplan-command.mjs + brainstorm-to-SD pipeline | discovered during SD-ACTIVATE-S18 follow-up sequencing 2026-04-27
2026-04-27 | /brainstorm Board of Directors deliberation does not load project-specific calibration data into context — board sized EVA Support build cost as "3-5 days" / "5-7 days" without referencing docs/sd-cycle-time-reference.md (p50/p75 by sd_type × target_application). Day-budget guesses surface as authoritative-sounding output. Board prompts (per /brainstorm skill 6D.1a) should auto-load relevant calibration docs when domain=architecture and target involves sized buckets. Affects every cost-estimation step in board-mode brainstorms | scripts/brainstorm-deliberate.js + .claude/commands/brainstorm.md Step 6D.1a board prompts | discovered during EVA Support brainstorm 2026-04-27
2026-04-27 | sd-start.js claim resets strategic_directives_v2.status + current_phase: claiming an SD that is already in_progress/EXEC (3 accepted handoffs in sd_phase_handoffs, PRD with populated exec_checklist, retrospective DEEPLY in EXEC) ROLLS BACK status→draft, current_phase→LEAD on UPDATE. is_working_on + claiming_session_id flip correctly; only the phase markers regress. Authority to detect: handoff history + PRD state. Recovery: database-agent UPDATE to restore status+phase. Witnessed during /leo continue resume of SD-LEO-INFRA-VISION-FIDELITY-GATE-001 | scripts/sd-start.js claim path | deferred from SD-LEO-INFRA-VISION-FIDELITY-GATE-001 EXEC PR-1 resume
- 2026-04-27: auto-trigger-stories.mjs Gemini timeout (3 retries) + priority-enum CHECK mismatch (script outputs MoSCoW must/should/could but DB CHECK is critical/high/medium/low). Forced manual-author fallback for SD-EHG-AI-GEN-GUARDRAILS-001 (US-1..10 inserted by general-purpose agent). 2-issue compound: LLM provider fragility + script/schema enum drift.
- 2026-04-27: PR #536 (SD-ACTIVATE-S18-MARKETING-COPY-ORCH-001) merged to main 2026-04-27T14:54Z but chairman_decisions.approval_type column NOT present in live DB (error: column does not exist). Migration apparently didn't run on merge. Discovered during SD-EHG-AI-GEN-GUARDRAILS-001 EXEC prerequisite check; FR-7 types.ts regen + FR-6 audit-trail integration both blocked. Possible cause: missing supabase migration deploy step in CI, or migration file not in PR scope. | live DB project dedlbzhpgkmetvhbkyzq + supabase/migrations/ | discovered EXEC pre-flight
- 2026-04-27: EHG repo has NO CI step that runs `supabase db push` on merge to main; supabase_migrations.schema_migrations has ZERO 20260427_* entries despite multiple 20260427 migrations existing in supabase/migrations/. PR #536 column went 5h unapplied as a result. Migrations land via out-of-band manual psql; ledger never updated. Systemic deployment hygiene gap. | .github/workflows/ + supabase/migrations/ | discovered during PR #536 unblock for SD-EHG-AI-GEN-GUARDRAILS-001
- 2026-04-27: chairman_unified_decisions view in PR #536 drops the `escalation` branch (live: 4 branches incl. agent_messages-driven escalation; PR #536: 3 branches with venture_decision shape + new approval_type). Frontend reading details->>'approval_type' from view gets NULL since live view's chairman_approval branch lacks it. Part 5 of 20260427_001 migration deferred during unblock. | ehg/supabase/migrations/20260427_001_add_approval_type_to_chairman_decisions.sql Part 5 + chairman_unified_decisions view | deferred during DB-agent unblock pass
- 2026-04-27: Migration 20260427000002_revert_decided_by_user_id never applied to live DB while sibling 000001/000003/000004 did apply. decided_by_user_id column still present + functions reference it. Out-of-band manual apply skipped the revert. Minor data-model inconsistency. | live DB chairman_decisions table + ehg/supabase/migrations/20260427000002* | discovered during sibling migration drift check
