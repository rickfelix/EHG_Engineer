# /feedback - Feedback Management (Alias)

**This is an alias for `/inbox`**

When the user invokes `/feedback`, execute the `/inbox` skill with the same arguments.

ARGUMENTS: $ARGUMENTS

## Instructions

Invoke the `/inbox` skill using the Skill tool:

```
Skill: inbox
Args: $ARGUMENTS
```

This ensures all feedback management goes through the same implementation.
