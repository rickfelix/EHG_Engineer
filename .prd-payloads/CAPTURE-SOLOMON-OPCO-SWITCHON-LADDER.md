# Solomon verdict — op-co switch-on automation (OBSERVE-FIRST ladder)

Source: Solomon consult reply id=fc99426b (ref 948c6a6d), 2026-07-16. Confidence high. escalate_to_chairman=false (rides Adam's packet); systemic_flag=null.
**Disposition: design captured; HOME it in SD-LEO-INFRA-HOLD-STATE-CONTRACT-001's surface (Solomon: "one mechanism, not two"). Source the switch-on-ladder as an EXTENSION of that SD once it lands — do NOT fork a parallel policy.**

## Verdict
(a) **PRIMARY AXIS = OBSERVE-FIRST vs ACT-DIRECT** (not notify-vs-decide — all switch-ons are acts). Two-step ladder: **dark → OBSERVE-ONLY** (precheck green = near-always auto-proceed; actuators unbound; generates the evidence the closure verifier + eval need) **→ BINDING** (consequence routing governs THIS flip, fed by observe-window evidence). The reversible/bounded/no-live-money conjunction is the SECONDARY gate, step 2 only. Observe→binding flips of VENTURE GATES are already chairman-only (only-the-chairman row 4) — inherit, don't re-litigate.

(b) **Chairman-ratified CLASS LIST, never per-instance self-classification** (identical to SMS: design-time criteria admit a class; runtime = enumerated membership, fail-closed, unknown→chairman). Strengtheners: (i) home the class registry IN the hold-state-contract surface — a switch-on class row IS a hold row with a machine-verifiable release_condition; (ii) reversible = reversible-BY-MECHANISM: each class declares revert verb + TTL + invoker, and FIRST activation of any class requires a **REVERT REHEARSAL** in precheck (prove the off-switch before the on-switch; untested revert = not reversible).

(c) **MISSING PRECHECKS (7):** (1) revert-path verified; (2) observe-clean WINDOWED + fresh-at-flip (trailing N days, re-evaluated at flip — closed-once decays, same rule as collectors); (3) dependency blast-radius via loop_registry dependency_edges (no flip while a dependent is mid-incident); (4) gauge+alarm ARMED AND VERIFIED FIRING before flip, named owner (else recreates built-but-dark); (5) rate/soak: one switch-on per soak window, respect CONST-007's 3-AUTO-per-24h cap (no new counter); (6) hard stop while FREEZE (CONST-009) or an active incident is open; (7) CONST-003 audit stamp (actor, policy version, precheck evidence snapshot).

(d) **NEVER-AUTO regardless of green** (bind to doctrine): venture-gate binding flips (row 4); ANY live-money enablement — Stripe livemode/payouts/spend (row 7); public-launch / FIRST external-send of any customer-facing channel (row 9); venture selection/kill/promote (rows 1-3); gate config/thresholds/skip-lists (CONST-013); ANY agent-authority or credential/grant expansion (grants lowest layer, never auto); FREEZE/kill-switch machinery + no-auto-override (row 8); AND THE POLICY/RATCHET ITSELF (never widens itself — same self-reference guard as the SMS whitelist).

**CORRECTION (Solomon):** this is NOT the shadow-trial sandbox application (that deferral covers CONTRACT MUTATION); observe-mode already IS the shadow trial for ACTIVATION, so nothing here depends on the later sandbox item.

**COUNTERFACTUAL:** if most op-co capabilities lack an observe mode, the ladder degrades to the original design — then make observe-mode itself part of the bar (no observe mode = not auto-eligible, chairman packet only).

**VERIFICATION:** pilot ONE capability with a real revert rehearsal + one deliberately-failed precheck (prove fail-closed) before the policy binds anything else.
