# Solomon systemic hand-off — DISTILL-PIPELINE INTEGRATION SEAMS

Source: Solomon oracle advisory id=b9f3fc0e-ec53-48a4-b5fd-5d70a5309dc4, 2026-07-17. Confidence: high.
Provenance: **chairman-ratified 2026-07-17** (in-session verbatim agreement; chairman commissioned the integration review of distill vs PM-plan vs Adam-relationship; Solomon diagnosed from a live YouTube-focused distill run; chairman ratified all four seams). Division: Solomon diagnoses, **Adam sources**. escalate_to_chairman=false; systemic_flag=distill-integration-seams.

## The four seams (chairman-ratified)

**SEAM 1 (highest) — RECONCILE CHECKS SDs BUT NOT INSTITUTIONS.** Refine Phase D matches intake items against `strategic_directives_v2` only; anything institutionalized as a ROLE DUTY / protocol section / contract clause is invisible — which is why 3 Todoist Fable-era items (deep-arch-review, flaky-RCA, dedup-sweep — now CLAUDE_SOLOMON.md Cluster 1/3 duties) resurfaced in the chairman review queue today. **Fix:** reconcile against `leo_protocol_sections` too; add `already_institutionalized` as a first-class disposition alongside `already_done`, with a pointer to the absorbing section.

**SEAM 2 — DISTILL MUTATES THE PLAN YOU MANAGE, WITHOUT YOU IN THE LOOP.** The intake pipeline creates waves (historically whole parallel roadmaps; an archived one sits beside the active one) with no Adam disposition step — yet the roadmap is Adam's plan-of-record (chairman 07-16), and both PLAN CHECK and the new roadmap-retro derive from it. **Fix:** distill wave output becomes a PROPOSAL Adam reconciles into the canonical roadmap, never a direct write — one plan surface. **SEQUENCING:** land this before/with the roadmap-retro SD, so the retro grades a single-writer plan from cycle 1.

**SEAM 3 — REFINE IS NOT STANDING.** Wave counts (213-item wave 5, hundreds pending) show clustering outruns dedup/reconcile/score. **Fix:** fold reconcile into the default pipeline path so nothing reaches the chairman review queue or a wave without a done-state check (honors dedup-vs-DONE-STATE doctrine).

**SEAM 4 (a DON'T-build, ratified) — keep Solomon OUT of per-item distill scoring.** 400+-item 4-persona scoring is breadth (Adam's altitude / automation); Solomon touchpoints stay as designed (retro output-review first cycles, then exception-only on amendments). **Encode as an explicit NON-GOAL** in whatever SD is sourced so a future sourcing pass does not helpfully wire the oracle in.

## Addendum (Solomon-proposed, NOT yet chairman-ratified — Adam discretion)
Sync-leg hard failures fail QUIET — the YouTube leg ran `deleted_client` for a MONTH (OAuth client deleted in Google Cloud ~07-09 credential restructuring; newest YT row 06-19; chairman repairing now via new OAuth client + `scripts/eva-youtube-auth.js`). **Recommend:** any sync leg with errors>=1 emits a loud durable signal (feedback row or Adam advisory), never just a log line.

## Verification plan (Solomon-specified)
- Seam 1: re-run refine on the current queue, confirm the 3 Fable-era items reconcile to CLAUDE_SOLOMON.md instead of resurfacing.
- Seam 2: one distill run end-to-end showing proposal → Adam-disposition → single canonical roadmap write, PLAN CHECK unchanged.
- Seam 3: pipeline run shows reconcile executed on the default path.
- COUNTERFACTUAL: if distill is retired/absorbed into the PM loop entirely, seams 2-3 collapse into that redesign — check belt for anything already covering it before sourcing.
